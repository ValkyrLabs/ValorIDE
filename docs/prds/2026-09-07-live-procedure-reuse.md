# Live procedure reuse and execution evidence

The full local consumer test/build gate is clean. The next requirement is a real, authorized procedure that agents can discover and invoke with an inspectable outcome. Initial live reads show four legacy learned Procedure rows but no active immutable workflow binding on the authorized active-candidate page. The SkillOptics Lab is live and already contains workflow experience capsules and observation-stage procedure candidates. Null lazy collection fields in a generated read are not proof that underlying evidence is absent.

## Plan

1. Preserve current read-only discovery and Lab evidence. Inspect existing candidates through the canonical SkillOptics services; do not manually manufacture a Procedure binding or populate server-owned lineage fields.
2. Stage one eligible, evidence-backed candidate through the existing shadow-experiment endpoint. Staging must produce an immutable, ABI-valid WorkflowVersion and mechanization receipt or a precise rejection. It must not activate a procedure or execute external effects.
3. Use the existing holdout/replay/canary lifecycle to establish correctness and eligibility. Any new fixture data must be clearly synthetic and limited to controlled, non-mutating workflows. Actual human approval remains a distinct canonical receipt; never synthesize or bypass it.
4. Validate native agent discovery, dispatch and exact execution readback against the resulting binding when its gates permit. Connect inspection to the existing authenticated Workflow Studio rather than create a second workflow UI or execution runtime. Preserve server-owned identity, ACL, immutable version and fresh session checks.
5. Record exact evidence, coverage and remaining blockers in GrayMatter with full-text readback. Report installation, runtime execution, output correctness and measured token/cost improvement separately.

Ownership: ValkyrAI backend/frontend source and publishing remain frozen to the coordinated release owner. GrayMatter's reviewer source/index/archive freeze remains intact. This lane may perform scoped authenticated acceptance operations and edit ValorIDE-owned source, tests, documentation and evidence. No Maven/generation or production publishing runs here.

## Workflow Studio handoff increment

Project the validated workflowId from the server's exact WorkflowExecution alongside its existing reference/state projection. Keep raw execution context and credentials out of the card. Carry the dispatch client's backend into result history and offer Open workflow only for a verified execution state with valid execution/workflow references and backend.

Use a narrow webview message and the existing valoride.workflows.openStudio command. Validate the workflow UUID and backend in the extension; bind ticket creation and retries to that backend, rechecking configuration around asynchronous authentication and ticket responses. The fresh server session and its ACL remain authoritative. Do not claim that opening the workflow selects the exact execution; keep the execution reference visible.

Test the result projection, request/result provenance, malformed or stale navigation targets, configuration changes during authentication and handoff, and the user's card-to-host action before implementing. Run the production build and complete host/webview matrix after focused tests pass.

## Accepted local increment

Procedure dispatch and exact status reads now retain a validated workflowId without exposing raw execution context. Result cards preserve their backend and open that workflow through the existing authenticated Studio command. The host rejects invalid navigation data and backend changes. Ticket creation rechecks configuration after authentication and checks the current session/backend after the response; retries keep the original backend. The UI explicitly opens the workflow, while the exact execution reference stays visible. Malformed history cannot treat inherited JavaScript property names as known workflow states.

The tests first reproduced missing workflow/backend projection and missing navigation; a later malformed-state regression also failed before its correction. A provider test exercises the registered Studio command and its retry callback. The first production build found a test-fixture TypeScript error; it was corrected before the final passing gates.

Final validation: production extension build, root type checks, lint, final webview type/build/sync, 107 Jest files / 825 tests, 10 host Vitest files / 91 tests, 2 Node files / 6 tests, actual VS Code 1.136.1 / 231 tests plus 4 external-service skips, 64 webview files / 472 tests plus 34 existing skips, and 5 package-audit tests. Total: 1,630 passed, 38 skipped. The final webview build includes the malformed-state correction. No install, marketplace publication, production activation or token/cost improvement is claimed.

Evidence: /Users/johnmcmahon/workspace/2025/valkyr/ValkyrAI/work/deployment/20260907-valoride-studio. The exact extension SHA-256 is c4835b74d3f8e762ec9086574dce25453f4a36fde60ddf65ed6bef5e644f2157. All seven shipped webview files match the final webview build.

Live mechanization still requires coordinated backend investigation. The normal authenticated main-tenant Lab shows a candidate that its canonical shadow staging operation reports as not found. Raw tenant JPA selection versus generated READ ACL filtering is a suspected visibility mismatch, not a confirmed cause. Source/Maven/publishing remains with the backend release owner. This evidence is durably recorded in GrayMatter artifact ab83d6a7-2608-4c93-a2ac-801e9b4738a8 with exact ID/full-text verification; the Studio provenance invariant is 084bd409-d051-4d99-b531-a20f5d2ee132, also verified by readback.

## Historical acceptance replay plan

The Lab's expanded bounded request returns 27 candidates, none owned by the current account. An owner-filtered generated WorkflowExecution page returns two successful historical remote-runtime acceptance executions that share immutable version 95040360-fcf7-40a8-b4ad-1b20979ea077 and definition hash 2889c970f51301882a293f7f2575857566affc27a4d21f3bda3f3538cc8a338d. These are attributable prior acceptance runs, not new customer production outcomes.

Inspect their exact authorized version through the existing engineering contract. If it remains valid and safely reusable, normalize each as replay evidence through the existing SkillObservation API, with stable trace/event identity and metadata derived exclusively from the persisted execution. Assemble/evaluate the resulting capsules and stage only the resulting authorized candidate. Keep historical occurrence times, explicit replay classification, source references and outcome evidence. Never supply principal/tenant overrides, fabricate immutable bindings or approvals, or activate a procedure. Record each response before dependent calls and stop on an unknown outcome or failed precondition. This establishes the supported recurring-workflow branch; arbitrary Codex trace compilation and measured savings remain separate requirements.

## Historical replay result

The live normalized observations are skillobs-b5ba751654de8113ce56cae3cc92f9dd6e908461 and skillobs-e3b4d9ddfcf4235c7aea2149eb96c5c4a35f8123, both explicitly replay-class and bound to the two distinct owned executions. Their capsules, skillcap-84242b56d96647d518386b59a308a6d71b34e8de and skillcap-9d0b87dc97f93eefd2a7ee5957d6a69ab154a9b3, were assembled and evaluated through canonical APIs; both support candidate skillcand-d2bfe969be81fb5ba864126eb140e434f322b35c.

Shadow staging succeeded. Exact generated Procedure readback verifies dd7d111c-99b6-44d0-9733-6aa18c51296a / proc-f7ebb0c5-e48b-3501-a607-ede4e8256c68-r1 is deterministic, disabled, and shadow-bound. The new Workflow is 045afbba-c420-46c4-8d3d-ce849505801f; its immutable version 6a5bdc00-5969-4be9-a7aa-40e67cd21125 is VALID. Engineering readback matches the procedure's content hash 4c687788d7788519edae5beb706d90339eab944651d96a93ea190b3896062dc8 and ABI hash 08b2a573cf866c7c61e1a266e09b81cf8e7feb8839bdcf47e7f5604766fa172a. The procedure carries mechanization receipt reference skillmech-4d56744d1a5e151a583e9f9fdcc337ac7ab6807e; the receipt body was not independently retrieved.

The canonical experiment assessment remains non-promotable: baseline/candidate sample counts are both zero against minimum20. Reported zero latency/credit aggregates therefore mean no samples, not measured free execution. The workflow has zero recent executions and active deployments, approvals are absent, and the active procedure page excludes this shadow binding. No promotion, canary, execution or activation was attempted.

This is a structural acceptance of the existing recurring-workflow compiler branch using a constant fixture MapInject module. It does not establish parameter generalization, arbitrary Codex session compilation, a useful business operation, comparative economics, or model-call savings. The next acceptance must use meaningful held-out inputs, actual exact-version candidate executions, matched baseline fixtures and independent output checks before any promotion request.

All request/response evidence and the bounded replay harness are under /Users/johnmcmahon/workspace/2025/valkyr/ValkyrAI/work/deployment/20260907-live-procedure-reuse/historical-replay. The candidate authorization/visibility repair is separately proposed in /Users/johnmcmahon/workspace/2025/valkyr/ValkyrAI/work-docs/product-planning/2026-09-07-skillopt-candidate-authority-repair.md; backend source and release ownership are unchanged.

Readback coverage: exact capsule GET verifies identity, status and workflow execution reference, but its lazy observationRefs collection serializes as null. The canonical evaluation responses carry the observation lineage; those responses and stage's supporting capsule references are the lineage evidence in acceptance.json. This serialization limitation is included in the backend regression proposal. It is not proof of missing persisted observations.

The final historical replay artifact is durably recorded in GrayMatter as fc724587-40f8-4abf-9aff-55fb4390d462 with exact ID, sourceMessageId and full-text verification. The first write overlapped the coordinated api-0 deployment and returned an uncertain failure; a bounded full authorized scan and an exact-ID recovery probe found no match before a single unchanged retry. After the release owner reported restart/readiness completion, another exact Procedure GET verified the same version/content/ABI binding, disabled state and shadow status. The replay evidence therefore survives that observed service transition; it still contains no newly executed candidate workflow.

## Distribution increment: preserve the tested runtime

Read-only inspection found that installed version 3.20.975 and the existing 3.20.976 VSIX both omit the native procedure endpoint and Workflow Studio target present in the tested workspace bundle. Preserve both existing artifacts. Build a separately named candidate VSIX from the exact tested production bytes with cached VSCE 3.9.2's `pack` function, which performs package collection, validation and archive creation without `packageCommand`'s prepublish rebuild. Keep the manifest and runtime files unchanged and retain the current version; do not install or publish this candidate during preparation.

Before packaging, snapshot the extension, all runtime files, manifest and existing distribution hashes. Use the repository's existing runtime allowlist and no dependency inclusion. Run the existing VSIX audit against the explicit new output path, then extract the archive and verify all packaged runtime files against their workspace hashes, including the extension entry and all seven webview assets. Confirm the package manifest identity/version and preserve exact bundle hashes through packaging. Record the packager version, archive checksum, audit and parity results. The already-passed full source test matrix need not be repeated unless these bytes or source change; archive validation is the new acceptance boundary. Installation, marketplace publication and a real installed-host procedure run remain separate steps.

Completed: the fresh candidate VSIX is `ValkyrAI/work/deployment/20260907-valoride-procedure-distribution/valoride-dev-3.20.976-procedure-candidate.vsix`, SHA-256 `9f607a31ad1de72ec66826f2dde09a87b0e6fdc1cab502d13ef4643a5ba77499`. The existing package audit passes: 11,699,761 bytes, 39 archive entries. All 37 source files match byte-for-byte, using VSCE's standard `README.md` to `readme.md` and `LICENSE` to `LICENSE.txt` archive names. All seven webview assets match the tested build. The manifest is unchanged, the workspace extension retains its tested hash, and the old VSIX remains intact. Initial filename verification stopped on the standard README rename; the corrected explicit mapping and final content checks are recorded in the evidence directory. No installation or marketplace publication occurred.

## Installed package acceptance

Install the audited VSIX with the existing VS Code 1.136.1 CLI into a fresh extensions directory and user-data profile. Preserve the user's current profile, installed extension and workspace. A small test observer extension will activate the installed target by its real extension ID, verify the loaded path and bundle checksum, and check that its native Workflow Studio commands register. The observer must be the only development extension so the target genuinely loads from the VSIX installation. Use a disposable empty workspace and local backend fixture for this unauthenticated gate; do not supply real credentials or start an agent task.

Exercise the installed command's invalid-reference, backend-mismatch and missing-session responses, verifying readable user guidance and absence of a Studio handoff request. Review activation and extension-host logs for missing packaged assets, module failures and unhandled errors. Where available, inspect the rendered sidebar in the isolated window. Keep installed-package activation, unauthenticated boundary checks, authenticated backend handoff and full procedure execution as separate acceptance claims. If a real packaging or startup defect appears, preserve the failing artifact and add a targeted regression before changing the responsible existing source path.

### First-install findings and repair plan

The actual installed target activates in VS Code 1.136.1 with production extension mode, the exact tested bundle hash and native Workflow Studio commands. Its three invalid-reference/backend/session guidance messages are present in the real output log and the isolated backend records zero handoff requests. The first host launch hit macOS's Unix socket path limit before extension startup; a shorter `/tmp` profile succeeded while preserving the original failed receipt. Native visual inspection is unverified because the Mac is locked.

Runtime review found optional startup configuration defects: `PromptService` aborts an ordinary workspace without `.valoride/prompts/system.json`; `LLMPromptService` reports no prompt despite the real Task engine retaining its canonical built-in `SYSTEM_PROMPT`; `LLMContextInjector` captures services before LLM prompt initialization and misses the extension context; the SWARM orchestrator initializes before its broadcaster, so initial worker registration is skipped.

Repair the existing paths with focused regressions first. Treat absent workspace prompt/catalog/SWARM files as absent optional layers, without writing defaults into customer workspaces or inventing available APIs or workers. Preserve explicit configured content and reject malformed files. Keep the canonical Task system prompt and manual/ThorAPI override precedence; an absent optional LLM selection should clearly leave the built-in prompt active. Initialize the injector after its dependencies settle and supply the current extension context so its existing GrayMatter session gate is usable. Initialize the broadcaster before registering orchestrator workers. Tests must cover an empty real temporary workspace, partial configuration, malformed content, explicit overrides, and missing-layer composition. Rebuild, run the applicable full source checks, package a new distinct candidate and repeat installed-host log acceptance before replacing the first artifact. Backend source and generation remain with the release owner.

Five focused startup regressions failed before implementation; all 41 focused tests subsequently passed. The production package command then failed immediately because it referenced nonexistent `build:webview:prod`. Route `package:prod` through the existing complete `package` command, which already performs the standard production Vite build, type checks, lint and production esbuild. Preserve the missing-script log and validate the corrected public command by actually running it; do not add a parallel build path.

The repaired production build and full matrix passed: 825 Jest, 97 host Vitest, 6 Node, 231 VS Code host and 472 webview tests, plus 5 package-audit tests (1,636 passed, 38 existing skips). The new package passes the archive audit and all 37 source-file byte comparisons. Its installed production-mode startup passes on the user's VS Code 1.133.0, including dependency readiness, all three canonical worker registration IDs, the built-in prompt default and three Studio validation/session messages, with zero handoff requests. The first 1.136.1 probe expected enum names instead of the canonical lowercase registration IDs; preserve that failed probe and its correction record.

Next delivery step: update the existing local ValorIDE installation using the official VS Code CLI and the verified `startup-candidate` VSIX, retaining the existing 3.20.975 VSIX as rollback. Its extension bundle was verified to match the currently installed version exactly. Read back the installed version, bundle and webview hashes. Do not force-reload active user windows; installed files, current-window activation, authenticated Studio handoff and visual acceptance remain separate claims. The locked Mac prevents native visual QA at this time.

Delivered locally at 2026-09-07 13:19 UTC: official VS Code CLI installation succeeded, and the default extension registry now selects `valkyrlabsinc.valoride-dev` 3.20.976. All 37 installed files were verified against the audited package inputs, with only VS Code's own manifest metadata excluded from the manifest comparison; all seven webview assets match. Installed extension SHA-256 is `118c44463f834d35a6e8d190cf69cfd6a62ace019f66c246ea665f563e2bd702`; VSIX SHA-256 is `9227d0d74b14959a152a98279899a00accacb289fa55891697234659f7e880e0`. The previous 3.20.975 VSIX remains the exact-bundle rollback. The original user windows were not force-reloaded, so current-window activation, authenticated Studio handoff, visual QA and real procedure execution remain unverified. Final evidence is under `ValkyrAI/work/deployment/20260907-valoride-startup`, including `local-installed-readback.json`, `test-summary.json`, `package-parity.json` and the installed-host receipts.

## Staging packaging command repair

The public `npm run package:staging` command was executed and failed before building because `build:webview:staging` is missing. Preserve that failing command log and the original dirty manifest. Add only the missing staging-mode webview entry point, forwarding `--mode staging` into the existing webview `build` script and then using the existing asset sync. Retain the existing extension type checks, lint and production esbuild stages.

Validate by running the actual corrected public command, checking that Vite reports staging mode, comparing all synced webview file hashes, and recording the resulting bundle hashes. This changes a local build entry point; it does not install, publish, change version, or establish a staging backend session. Existing authenticated backend selection remains authoritative. The root backend release owner retains source/generation/Maven/publication ownership. Evidence is under `ValkyrAI/work/deployment/20260907-valoride-staging-build`.

Accepted: `npm run package:staging` completed in 89.044 seconds with exit 0. Its actual log shows the existing webview TypeScript build, Vite staging mode, asset sync, extension type check, lint and production esbuild. All seven webview files match the synced distribution bytes, with no extra files. The manifest diff adds only the missing script and preserves version 3.20.976 and all pre-existing dirty fields. The accepted startup VSIX and local installation remain unchanged. This is a build-command repair; no new staging deployment, backend session, installation or full application test run is claimed.

## Complete discovery continuation and procedure review

Repair the native one-page Procedure search contract: every nonempty authorized page may have a later page, including a short page or one whose records are all excluded from the candidate projection. Return the next page while within the existing 10000-page index bound; mark an explicit empty page and the page limit separately. Retain complete:false and one_authorized_page so a single call never claims that earlier pages were read or that the collection is atomic. Extend the existing agent prompt to follow this continuation when more candidates are needed.

Carry an optional valid workflowId in candidate summaries, alongside the existing exact version and input declarations. In the current chat card, offer a compact required-input/version disclosure and Open workflow through the existing backend-bound WorkflowStudioTarget/host command. Opening a workflow does not execute or promote a Procedure and does not select an exact execution. Keep unsupported history non-actionable and never infer nested object fields from a top-level type.

First reproduce short-page and all-excluded continuation failures; test explicit empty end, the maximum page index, optional malformed workflow references and unchanged auth/dispatch rules. Cover result-card input review and validated navigation, including missing/stale provenance. Run focused host/webview tests, then the existing full consumer matrix and production package command. Inspect responsive rendered cards where possible and preserve source/build/package/install boundaries. No ValkyrAI backend/frontend, Maven, live Workflow writes, GrayMatter public surfaces or marketplace publishing in this lane.

Delivery for this increment: after the corrected production build, create a distinct 3.20.976 Procedure discovery candidate archive using the installed VSCE packer and existing runtime allowlist, preserving the manifest/version and prior startup VSIX. Audit the archive and verify all runtime bytes. Reuse the existing isolated production-mode VS Code observer against the user's installed VS Code application with no real credentials, then update the default local extension using its official CLI and verify the installed hashes. Retain the startup VSIX as rollback and do not force-reload active user windows. Isolated rendered-card QA, packaged-host startup, default installed bytes and authenticated current-window behavior remain distinct evidence boundaries.

Accepted and installed locally: two host regressions and three result-card regressions first failed, then passed. The complete consumer matrix passed 1,636 tests with 38 existing skips; five package-audit tests also passed (1,641 total). The first production build caught the webview ES5 target rejecting Set spread; Array.from fixed that compatibility issue, all 11 card tests were rerun, and the final production build passed. The final component renders at 360px and 760px without horizontal overflow or browser errors and sends the expected Workflow Studio message. A new 39-entry VSIX passes the runtime allowlist audit and all 37 source-file comparisons. Isolated production-mode startup passed on the installed VS Code 1.133.0 with zero authenticated handoff requests. The official local install succeeded, and all 37 installed source files match after accounting for VSCE's standard readme.md/LICENSE.txt names and VS Code manifest metadata. The initial checker filename error is preserved separately. Version remains 3.20.976; extension SHA-256 is e2ae04d1d397b29580479530da24190e56e351479ba3b1ddaf44b14dad0a0ab2 and VSIX SHA-256 is 393f20270b262730b59ce264a6df5cd759766acc093d975d4c0b28cf2062816f. The startup VSIX remains rollback; no active window was force-reloaded. Current-window activation, authenticated handoff, real procedure execution, public publication and cost savings are still unverified. Evidence: ValkyrAI/work/deployment/20260907-valoride-procedure-discovery/acceptance.json.
