// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  ApplicationDataPlaneBinding,
  ApplicationDataPlaneBindingIsolationModeEnum,
  ApplicationDataPlaneBindingStatusEnum,
} from '@thorapi/model';

import { useAddApplicationDataPlaneBindingMutation } from '../../services/ApplicationDataPlaneBindingService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Authoritative binding from one generated customer application to its active data-plane placement.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const IsolationModeValidation = () => {
  return [
    'org_schema',
    'app_schema',
    'app_database',
    'byoc_database',
  ];
};
const StatusValidation = () => {
  return [
    'requested',
    'binding',
    'active',
    'validating',
    'suspended',
    'failed',
    'retired',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
      isolationMode: Yup.mixed()
        .oneOf(IsolationModeValidation(), "Invalid value for isolationMode")
        .required("isolationMode is required."),
        schemaName: Yup.string().required("schemaName is required.").matches(/^org_[0-9a-f]{32}(_app_[0-9a-f]{8})?$/, "schemaName must match pattern Canonical schema name for org_schema/app_schema modes; never supplied directly by an untrusted client."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        databaseName: Yup.string(),
        databaseHost: Yup.string(),
        databaseRegion: Yup.string(),
        credentialRef: Yup.string(),
        boundAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("boundAt must be a valid date"),
        lastValidatedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("lastValidatedAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const ApplicationDataPlaneBindingForm: React.FC = () => {
  const [addApplicationDataPlaneBinding, addApplicationDataPlaneBindingResult] = useAddApplicationDataPlaneBindingMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<ApplicationDataPlaneBinding> = {
        isolationMode: undefined,
          schemaName: '',
        status: undefined,
          databaseName: '',
          databaseHost: '',
          databaseRegion: '',
          credentialRef: '',
          boundAt: new Date(),
          lastValidatedAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new ApplicationDataPlaneBinding:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<ApplicationDataPlaneBinding>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("ApplicationDataPlaneBinding form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addApplicationDataPlaneBinding(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `ApplicationDataPlaneBinding created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create ApplicationDataPlaneBinding:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as ApplicationDataPlaneBinding}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addApplicationDataPlaneBindingResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New ApplicationDataPlaneBinding
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="isolationMode" className="nice-form-control">
                      <b>
                        Isolation Mode:
                        {touched.isolationMode &&
                         !errors.isolationMode && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="isolationMode"
                          value={values.isolationMode || ''}
                          className={
                            errors.isolationMode
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('isolationMode', true);
                            setFieldValue('isolationMode', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Isolation Mode" />
                          <IsolationModeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="isolationMode"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="schemaName" className="nice-form-control">
                      <b>
                        Schema Name:
                        {touched.schemaName &&
                         !errors.schemaName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="schemaName"
                            value={values?.schemaName}
                            placeholder="Schema Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="schemaName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="databaseName" className="nice-form-control">
                      <b>
                        Database Name:
                        {touched.databaseName &&
                         !errors.databaseName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="databaseName"
                            value={values?.databaseName}
                            placeholder="Database Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="databaseName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="databaseHost" className="nice-form-control">
                      <b>
                        Database Host:
                        {touched.databaseHost &&
                         !errors.databaseHost && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="databaseHost"
                            value={values?.databaseHost}
                            placeholder="Database Host"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="databaseHost"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="databaseRegion" className="nice-form-control">
                      <b>
                        Database Region:
                        {touched.databaseRegion &&
                         !errors.databaseRegion && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="databaseRegion"
                            value={values?.databaseRegion}
                            placeholder="Database Region"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="databaseRegion"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="credentialRef" className="nice-form-control">
                      <b>
                        Credential Ref:
                        {touched.credentialRef &&
                         !errors.credentialRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="credentialRef"
                            value={values?.credentialRef}
                            placeholder="Credential Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="credentialRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="boundAt" className="nice-form-control">
                      <b>
                        Bound At:
                        {touched.boundAt &&
                         !errors.boundAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="boundAt"
                            type="datetime-local"
                            value={values.boundAt ? 
                              new Date(values.boundAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('boundAt', true);
                              const v = e.target.value;
                              setFieldValue('boundAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.boundAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="boundAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastValidatedAt" className="nice-form-control">
                      <b>
                        Last Validated At:
                        {touched.lastValidatedAt &&
                         !errors.lastValidatedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="lastValidatedAt"
                            type="datetime-local"
                            value={values.lastValidatedAt ? 
                              new Date(values.lastValidatedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('lastValidatedAt', true);
                              const v = e.target.value;
                              setFieldValue('lastValidatedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.lastValidatedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="lastValidatedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New ApplicationDataPlaneBinding
                  </CoolButton>

                  {(addApplicationDataPlaneBindingResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addApplicationDataPlaneBindingResult as any).error ? (addApplicationDataPlaneBindingResult as any).error.data : (addApplicationDataPlaneBindingResult as any).error)}
                    </Alert>
                  )}

                  {(addApplicationDataPlaneBindingResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addApplicationDataPlaneBindingResult: {JSON.stringify(addApplicationDataPlaneBindingResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.ApplicationDataPlaneBinding"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase isolationmodelookup
uppercase ISOLATIONMODELOOKUP
snakecase isolation_mode_lookup
pascalcase IsolationModeLookup
camelcase isolationModeLookup
kebabcase isolation-mode-lookup
*/

const IsolationModeLookup = () => {
  return (
    <>
      <option value='org_schema' label="Org Schema" />
      <option value='app_schema' label="App Schema" />
      <option value='app_database' label="App Database" />
      <option value='byoc_database' label="Byoc Database" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='requested' label="Requested" />
      <option value='binding' label="Binding" />
      <option value='active' label="Active" />
      <option value='validating' label="Validating" />
      <option value='suspended' label="Suspended" />
      <option value='failed' label="Failed" />
      <option value='retired' label="Retired" />
    </>
  );
};



/* Export the generated form */
export default ApplicationDataPlaneBindingForm;

