// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  Procedure,
  ProcedureRiskLevelEnum,
} from '@thorapi/model';

import { useAddProcedureMutation } from '../../services/ProcedureService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Durable deterministic procedure candidate considered by SkillOpt before model/tool calls.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const RiskLevelValidation = () => {
  return [
    'low',
    'medium',
    'high',
    'critical',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        procedureRef: Yup.string().required("procedureRef is required."),
        name: Yup.string().required("name is required."),
        enabled: Yup.boolean().required("enabled is required."),
        confidence: asNumber(Yup.number().typeError("confidence must be a number")).required("confidence is required."),
        tenantId: Yup.string(),
        description: Yup.string(),
        deterministic: Yup.boolean(),
        successCount: asNumber(Yup.number().integer().typeError("successCount must be a number")),
        failureCount: asNumber(Yup.number().integer().typeError("failureCount must be a number")),
        ratingAverage: asNumber(Yup.number().typeError("ratingAverage must be a number")),
        estimatedCreditCost: asNumber(Yup.number().typeError("estimatedCreditCost must be a number")),
        latencyMs: asNumber(Yup.number().integer().typeError("latencyMs must be a number")),
      riskLevel: Yup.mixed()
        .oneOf(RiskLevelValidation(), "Invalid value for riskLevel")
        ,
        lastOutcomeAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("lastOutcomeAt must be a valid date"),
        metadataJson: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const ProcedureForm: React.FC = () => {
  const [addProcedure, addProcedureResult] = useAddProcedureMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<Procedure> = {
          procedureRef: '',
          name: '',
          enabled: false,
          confidence: 0,
          tenantId: '',
          description: '',
          deterministic: false,
          successCount: 0,
          failureCount: 0,
          ratingAverage: 0,
          estimatedCreditCost: 0,
          latencyMs: 0,
        riskLevel: undefined,
          lastOutcomeAt: new Date(),
          metadataJson: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new Procedure:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<Procedure>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("Procedure form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addProcedure(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `Procedure created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create Procedure:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as Procedure}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addProcedureResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New Procedure
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="procedureRef" className="nice-form-control">
                      <b>
                        Procedure Ref:
                        {touched.procedureRef &&
                         !errors.procedureRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="procedureRef"
                            value={values?.procedureRef}
                            placeholder="Procedure Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="procedureRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="name" className="nice-form-control">
                      <b>
                        Name:
                        {touched.name &&
                         !errors.name && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="name"
                            value={values?.name}
                            placeholder="Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="name"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="enabled" className="nice-form-control">
                      <b>
                        Enabled:
                        {touched.enabled &&
                         !errors.enabled && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="enabled"
                            name="enabled"
                            checked={values.enabled || false}
                            onChange={(e) => {
                              setFieldTouched('enabled', true);
                              setFieldValue('enabled', e.target.checked);
                            }}
                            isInvalid={!!errors.enabled}
                            className={errors.enabled ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="enabled"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="confidence" className="nice-form-control">
                      <b>
                        Confidence:
                        {touched.confidence &&
                         !errors.confidence && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="confidence"
                            type="number"
                            step="any"
                            value={values.confidence || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('confidence', true);
                              const v = e.target.value;
                              setFieldValue('confidence', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.confidence
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="confidence"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="description" className="nice-form-control">
                      <b>
                        Description:
                        {touched.description &&
                         !errors.description && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="description"
                            value={values?.description}
                            placeholder="Description"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="description"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="deterministic" className="nice-form-control">
                      <b>
                        Deterministic:
                        {touched.deterministic &&
                         !errors.deterministic && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="deterministic"
                            name="deterministic"
                            checked={values.deterministic || false}
                            onChange={(e) => {
                              setFieldTouched('deterministic', true);
                              setFieldValue('deterministic', e.target.checked);
                            }}
                            isInvalid={!!errors.deterministic}
                            className={errors.deterministic ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="deterministic"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="successCount" className="nice-form-control">
                      <b>
                        Success Count:
                        {touched.successCount &&
                         !errors.successCount && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>







                          {/* LONG FIELD */}
                          <Field
                            name="successCount"
                            type="number"
                            value={values.successCount || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('successCount', true);
                              const v = e.target.value;
                              setFieldValue('successCount', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.successCount
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />



                      <ErrorMessage
                        className="error"
                        name="successCount"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="failureCount" className="nice-form-control">
                      <b>
                        Failure Count:
                        {touched.failureCount &&
                         !errors.failureCount && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>







                          {/* LONG FIELD */}
                          <Field
                            name="failureCount"
                            type="number"
                            value={values.failureCount || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('failureCount', true);
                              const v = e.target.value;
                              setFieldValue('failureCount', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.failureCount
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />



                      <ErrorMessage
                        className="error"
                        name="failureCount"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="ratingAverage" className="nice-form-control">
                      <b>
                        Rating Average:
                        {touched.ratingAverage &&
                         !errors.ratingAverage && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="ratingAverage"
                            type="number"
                            step="any"
                            value={values.ratingAverage || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('ratingAverage', true);
                              const v = e.target.value;
                              setFieldValue('ratingAverage', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.ratingAverage
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="ratingAverage"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="estimatedCreditCost" className="nice-form-control">
                      <b>
                        Estimated Credit Cost:
                        {touched.estimatedCreditCost &&
                         !errors.estimatedCreditCost && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="estimatedCreditCost"
                            type="number"
                            step="any"
                            value={values.estimatedCreditCost || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('estimatedCreditCost', true);
                              const v = e.target.value;
                              setFieldValue('estimatedCreditCost', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.estimatedCreditCost
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="estimatedCreditCost"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="latencyMs" className="nice-form-control">
                      <b>
                        Latency Ms:
                        {touched.latencyMs &&
                         !errors.latencyMs && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="latencyMs"
                            type="number"
                            value={values.latencyMs || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('latencyMs', true);
                              const v = e.target.value;
                              setFieldValue('latencyMs', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.latencyMs
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="latencyMs"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="riskLevel" className="nice-form-control">
                      <b>
                        Risk Level:
                        {touched.riskLevel &&
                         !errors.riskLevel && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="riskLevel"
                          value={values.riskLevel || ''}
                          className={
                            errors.riskLevel
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('riskLevel', true);
                            setFieldValue('riskLevel', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Risk Level" />
                          <RiskLevelLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="riskLevel"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastOutcomeAt" className="nice-form-control">
                      <b>
                        Last Outcome At:
                        {touched.lastOutcomeAt &&
                         !errors.lastOutcomeAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="lastOutcomeAt"
                            type="datetime-local"
                            value={values.lastOutcomeAt ? 
                              new Date(values.lastOutcomeAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('lastOutcomeAt', true);
                              const v = e.target.value;
                              setFieldValue('lastOutcomeAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.lastOutcomeAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="lastOutcomeAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="metadataJson" className="nice-form-control">
                      <b>
                        Metadata Json:
                        {touched.metadataJson &&
                         !errors.metadataJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="metadataJson"
                            value={values?.metadataJson}
                            placeholder="Metadata Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="metadataJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New Procedure
                  </CoolButton>

                  {(addProcedureResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addProcedureResult as any).error ? (addProcedureResult as any).error.data : (addProcedureResult as any).error)}
                    </Alert>
                  )}

                  {(addProcedureResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addProcedureResult: {JSON.stringify(addProcedureResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.Procedure"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase risklevellookup
uppercase RISKLEVELLOOKUP
snakecase risk_level_lookup
pascalcase RiskLevelLookup
camelcase riskLevelLookup
kebabcase risk-level-lookup
*/

const RiskLevelLookup = () => {
  return (
    <>
      <option value='low' label="Low" />
      <option value='medium' label="Medium" />
      <option value='high' label="High" />
      <option value='critical' label="Critical" />
    </>
  );
};



/* Export the generated form */
export default ProcedureForm;

