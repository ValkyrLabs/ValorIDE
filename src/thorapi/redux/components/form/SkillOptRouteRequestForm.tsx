// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  SkillOptRouteRequest,
  SkillOptRouteRequestRiskLevelEnum,
  SkillOptRouteRequestPrivacyPolicyEnum,
} from '@thorapi/model';

import { useAddSkillOptRouteRequestMutation } from '../../services/SkillOptRouteRequestService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Request for deterministic SkillOpt route selection.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const RiskLevelValidation = () => {
  return [
    'low',
    'medium',
    'high',
    'critical',
  ];
};
const PrivacyPolicyValidation = () => {
  return [
    'public',
    'internal',
    'confidential',
    'restricted',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        tenantId: Yup.string(),
        accountId: Yup.string(),
        traceId: Yup.string(),
        estimatedCreditCost: asNumber(Yup.number().typeError("estimatedCreditCost must be a number")),
        creditBalance: asNumber(Yup.number().typeError("creditBalance must be a number")),
        contextSufficiency: asNumber(Yup.number().typeError("contextSufficiency must be a number")),
        confidence: asNumber(Yup.number().typeError("confidence must be a number")),
        latencyBudgetMs: asNumber(Yup.number().integer().typeError("latencyBudgetMs must be a number")),
      riskLevel: Yup.mixed()
        .oneOf(RiskLevelValidation(), "Invalid value for riskLevel")
        ,
      privacyPolicy: Yup.mixed()
        .oneOf(PrivacyPolicyValidation(), "Invalid value for privacyPolicy")
        ,
        tenantPolicy: Yup.string(),
        localAvailable: Yup.boolean(),
        billable: Yup.boolean(),
        requiresAudit: Yup.boolean(),
        humanApprovalAvailable: Yup.boolean(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const SkillOptRouteRequestForm: React.FC = () => {
  const [addSkillOptRouteRequest, addSkillOptRouteRequestResult] = useAddSkillOptRouteRequestMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<SkillOptRouteRequest> = {
          tenantId: '',
          accountId: '',
          traceId: '',
          estimatedCreditCost: 0,
          creditBalance: 0,
          contextSufficiency: 0,
          confidence: 0,
          latencyBudgetMs: 0,
        riskLevel: undefined,
        privacyPolicy: undefined,
          tenantPolicy: '',
          localAvailable: false,
          billable: false,
          requiresAudit: false,
          humanApprovalAvailable: false,
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new SkillOptRouteRequest:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<SkillOptRouteRequest>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("SkillOptRouteRequest form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addSkillOptRouteRequest(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `SkillOptRouteRequest created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create SkillOptRouteRequest:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as SkillOptRouteRequest}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addSkillOptRouteRequestResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New SkillOptRouteRequest
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="accountId" className="nice-form-control">
                      <b>
                        Account Id:
                        {touched.accountId &&
                         !errors.accountId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="accountId"
                            value={values?.accountId}
                            placeholder="Account Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="accountId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="traceId" className="nice-form-control">
                      <b>
                        Trace Id:
                        {touched.traceId &&
                         !errors.traceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="traceId"
                            value={values?.traceId}
                            placeholder="Trace Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="traceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="estimatedCreditCost" className="nice-form-control">
                      <b>
                        Estimated Credit Cost:
                        {touched.estimatedCreditCost &&
                         !errors.estimatedCreditCost && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="estimatedCreditCost"
                            type="number"
                            step="any"
                            value={values.estimatedCreditCost || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('estimatedCreditCost', true);
                              const v = e.target.value;
                              setFieldValue('estimatedCreditCost', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.estimatedCreditCost
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="estimatedCreditCost"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="creditBalance" className="nice-form-control">
                      <b>
                        Credit Balance:
                        {touched.creditBalance &&
                         !errors.creditBalance && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="creditBalance"
                            type="number"
                            step="any"
                            value={values.creditBalance || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('creditBalance', true);
                              const v = e.target.value;
                              setFieldValue('creditBalance', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.creditBalance
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="creditBalance"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="contextSufficiency" className="nice-form-control">
                      <b>
                        Context Sufficiency:
                        {touched.contextSufficiency &&
                         !errors.contextSufficiency && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="contextSufficiency"
                            type="number"
                            step="any"
                            value={values.contextSufficiency || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('contextSufficiency', true);
                              const v = e.target.value;
                              setFieldValue('contextSufficiency', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.contextSufficiency
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="contextSufficiency"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="confidence" className="nice-form-control">
                      <b>
                        Confidence:
                        {touched.confidence &&
                         !errors.confidence && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="confidence"
                            type="number"
                            step="any"
                            value={values.confidence || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('confidence', true);
                              const v = e.target.value;
                              setFieldValue('confidence', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.confidence
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="confidence"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="latencyBudgetMs" className="nice-form-control">
                      <b>
                        Latency Budget Ms:
                        {touched.latencyBudgetMs &&
                         !errors.latencyBudgetMs && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="latencyBudgetMs"
                            type="number"
                            value={values.latencyBudgetMs || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('latencyBudgetMs', true);
                              const v = e.target.value;
                              setFieldValue('latencyBudgetMs', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.latencyBudgetMs
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="latencyBudgetMs"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="riskLevel" className="nice-form-control">
                      <b>
                        Risk Level:
                        {touched.riskLevel &&
                         !errors.riskLevel && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="riskLevel"
                          value={values.riskLevel || ''}
                          className={
                            errors.riskLevel
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('riskLevel', true);
                            setFieldValue('riskLevel', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Risk Level" />
                          <RiskLevelLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="riskLevel"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="privacyPolicy" className="nice-form-control">
                      <b>
                        Privacy Policy:
                        {touched.privacyPolicy &&
                         !errors.privacyPolicy && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="privacyPolicy"
                          value={values.privacyPolicy || ''}
                          className={
                            errors.privacyPolicy
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('privacyPolicy', true);
                            setFieldValue('privacyPolicy', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Privacy Policy" />
                          <PrivacyPolicyLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="privacyPolicy"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantPolicy" className="nice-form-control">
                      <b>
                        Tenant Policy:
                        {touched.tenantPolicy &&
                         !errors.tenantPolicy && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantPolicy"
                            value={values?.tenantPolicy}
                            placeholder="Tenant Policy"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantPolicy"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="localAvailable" className="nice-form-control">
                      <b>
                        Local Available:
                        {touched.localAvailable &&
                         !errors.localAvailable && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="localAvailable"
                            name="localAvailable"
                            checked={values.localAvailable || false}
                            onChange={(e) => {
                              setFieldTouched('localAvailable', true);
                              setFieldValue('localAvailable', e.target.checked);
                            }}
                            isInvalid={!!errors.localAvailable}
                            className={errors.localAvailable ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="localAvailable"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="billable" className="nice-form-control">
                      <b>
                        Billable:
                        {touched.billable &&
                         !errors.billable && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="billable"
                            name="billable"
                            checked={values.billable || false}
                            onChange={(e) => {
                              setFieldTouched('billable', true);
                              setFieldValue('billable', e.target.checked);
                            }}
                            isInvalid={!!errors.billable}
                            className={errors.billable ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="billable"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="requiresAudit" className="nice-form-control">
                      <b>
                        Requires Audit:
                        {touched.requiresAudit &&
                         !errors.requiresAudit && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="requiresAudit"
                            name="requiresAudit"
                            checked={values.requiresAudit || false}
                            onChange={(e) => {
                              setFieldTouched('requiresAudit', true);
                              setFieldValue('requiresAudit', e.target.checked);
                            }}
                            isInvalid={!!errors.requiresAudit}
                            className={errors.requiresAudit ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="requiresAudit"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="humanApprovalAvailable" className="nice-form-control">
                      <b>
                        Human Approval Available:
                        {touched.humanApprovalAvailable &&
                         !errors.humanApprovalAvailable && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="humanApprovalAvailable"
                            name="humanApprovalAvailable"
                            checked={values.humanApprovalAvailable || false}
                            onChange={(e) => {
                              setFieldTouched('humanApprovalAvailable', true);
                              setFieldValue('humanApprovalAvailable', e.target.checked);
                            }}
                            isInvalid={!!errors.humanApprovalAvailable}
                            className={errors.humanApprovalAvailable ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="humanApprovalAvailable"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New SkillOptRouteRequest
                  </CoolButton>

                  {(addSkillOptRouteRequestResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addSkillOptRouteRequestResult as any).error ? (addSkillOptRouteRequestResult as any).error.data : (addSkillOptRouteRequestResult as any).error)}
                    </Alert>
                  )}

                  {(addSkillOptRouteRequestResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addSkillOptRouteRequestResult: {JSON.stringify(addSkillOptRouteRequestResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.SkillOptRouteRequest"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase risklevellookup
uppercase RISKLEVELLOOKUP
snakecase risk_level_lookup
pascalcase RiskLevelLookup
camelcase riskLevelLookup
kebabcase risk-level-lookup
*/

const RiskLevelLookup = () => {
  return (
    <>
      <option value='low' label="Low" />
      <option value='medium' label="Medium" />
      <option value='high' label="High" />
      <option value='critical' label="Critical" />
    </>
  );
};

/*
lowercase privacypolicylookup
uppercase PRIVACYPOLICYLOOKUP
snakecase privacy_policy_lookup
pascalcase PrivacyPolicyLookup
camelcase privacyPolicyLookup
kebabcase privacy-policy-lookup
*/

const PrivacyPolicyLookup = () => {
  return (
    <>
      <option value='public' label="Public" />
      <option value='internal' label="Internal" />
      <option value='confidential' label="Confidential" />
      <option value='restricted' label="Restricted" />
    </>
  );
};



/* Export the generated form */
export default SkillOptRouteRequestForm;

