// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  TrustProof,
  TrustProofProofStatusEnum,
  TrustProofProofFormatEnum,
} from '@thorapi/model';

import { useAddTrustProofMutation } from '../../services/TrustProofService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Exportable proof for a runtime execution, policy manifest, key version set, redaction scope, and event chain.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const ProofStatusValidation = () => {
  return [
    'GENERATED',
    'VERIFIED',
    'FAILED',
    'REVOKED',
  ];
};
const ProofFormatValidation = () => {
  return [
    'JCS_JSON',
    'CANONICAL_CBOR',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        proofHash: Yup.string(),
      proofStatus: Yup.mixed()
        .oneOf(ProofStatusValidation(), "Invalid value for proofStatus")
        ,
      proofFormat: Yup.mixed()
        .oneOf(ProofFormatValidation(), "Invalid value for proofFormat")
        ,
        proofBundleRef: Yup.string(),
        ownerId: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const TrustProofForm: React.FC = () => {
  const [addTrustProof, addTrustProofResult] = useAddTrustProofMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<TrustProof> = {
          proofHash: '',
        proofStatus: undefined,
        proofFormat: undefined,
          proofBundleRef: '',
          ownerId: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new TrustProof:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<TrustProof>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("TrustProof form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addTrustProof(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `TrustProof created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create TrustProof:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as TrustProof}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addTrustProofResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New TrustProof
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="proofHash" className="nice-form-control">
                      <b>
                        Proof Hash:
                        {touched.proofHash &&
                         !errors.proofHash && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="proofHash"
                            value={values?.proofHash}
                            placeholder="Proof Hash"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="proofHash"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="proofStatus" className="nice-form-control">
                      <b>
                        Proof Status:
                        {touched.proofStatus &&
                         !errors.proofStatus && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="proofStatus"
                          value={values.proofStatus || ''}
                          className={
                            errors.proofStatus
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('proofStatus', true);
                            setFieldValue('proofStatus', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Proof Status" />
                          <ProofStatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="proofStatus"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="proofFormat" className="nice-form-control">
                      <b>
                        Proof Format:
                        {touched.proofFormat &&
                         !errors.proofFormat && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="proofFormat"
                          value={values.proofFormat || ''}
                          className={
                            errors.proofFormat
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('proofFormat', true);
                            setFieldValue('proofFormat', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Proof Format" />
                          <ProofFormatLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="proofFormat"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="proofBundleRef" className="nice-form-control">
                      <b>
                        Proof Bundle Ref:
                        {touched.proofBundleRef &&
                         !errors.proofBundleRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="proofBundleRef"
                            value={values?.proofBundleRef}
                            placeholder="Proof Bundle Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="proofBundleRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="ownerId" className="nice-form-control">
                      <b>
                        Owner Id:
                        {touched.ownerId &&
                         !errors.ownerId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="ownerId"
                            value={values?.ownerId}
                            placeholder="Owner Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="ownerId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New TrustProof
                  </CoolButton>

                  {(addTrustProofResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addTrustProofResult as any).error ? (addTrustProofResult as any).error.data : (addTrustProofResult as any).error)}
                    </Alert>
                  )}

                  {(addTrustProofResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addTrustProofResult: {JSON.stringify(addTrustProofResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.TrustProof"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase proofstatuslookup
uppercase PROOFSTATUSLOOKUP
snakecase proof_status_lookup
pascalcase ProofStatusLookup
camelcase proofStatusLookup
kebabcase proof-status-lookup
*/

const ProofStatusLookup = () => {
  return (
    <>
      <option value='GENERATED' label="Generated" />
      <option value='VERIFIED' label="Verified" />
      <option value='FAILED' label="Failed" />
      <option value='REVOKED' label="Revoked" />
    </>
  );
};

/*
lowercase proofformatlookup
uppercase PROOFFORMATLOOKUP
snakecase proof_format_lookup
pascalcase ProofFormatLookup
camelcase proofFormatLookup
kebabcase proof-format-lookup
*/

const ProofFormatLookup = () => {
  return (
    <>
      <option value='JCS_JSON' label="Jcs Json" />
      <option value='CANONICAL_CBOR' label="Canonical Cbor" />
    </>
  );
};



/* Export the generated form */
export default TrustProofForm;

