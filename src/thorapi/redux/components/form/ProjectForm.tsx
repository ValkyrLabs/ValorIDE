// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  Project,
  ProjectProjectTypeEnum,
  ProjectStatusEnum,
  ProjectCurrentStageEnum,
  ProjectSourceSurfaceEnum,
} from '@thorapi/model';

import { useAddProjectMutation } from '../../services/ProjectService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
A strategy-backed project workspace. Projects bind the high-level strategy, goals, reusable tasks, generated applications, deployments, MCP publications, workflow runs, and RBAC-visible object links that represent what has been achieved and what remains to be done. 
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const ProjectTypeValidation = () => {
  return [
    'app-generation',
    'api-builder',
    'workflow-mcp',
    'graymatter-memory',
    'valoride-coding',
    'deployment',
    'content-publishing',
    'business-in-a-box',
  ];
};
const StatusValidation = () => {
  return [
    'planned',
    'active',
    'blocked',
    'review',
    'completed',
    'archived',
  ];
};
const CurrentStageValidation = () => {
  return [
    'define-strategy',
    'define-goals',
    'plan-tasks',
    'generate-api',
    'generate-app',
    'vibe-code',
    'deploy',
    'publish-mcp',
    'operate',
    'complete',
  ];
};
const SourceSurfaceValidation = () => {
  return [
    'dashboard',
    'graymatter',
    'valor',
    'valoride',
    'api',
    'workflow',
    'swarm',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        name: Yup.string(),
        description: Yup.string(),
      projectType: Yup.mixed()
        .oneOf(ProjectTypeValidation(), "Invalid value for projectType")
        ,
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        ,
      currentStage: Yup.mixed()
        .oneOf(CurrentStageValidation(), "Invalid value for currentStage")
        ,
        progressPercent: asNumber(Yup.number().integer().typeError("progressPercent must be a number")),
      sourceSurface: Yup.mixed()
        .oneOf(SourceSurfaceValidation(), "Invalid value for sourceSurface")
        ,
        sourceAgentId: Yup.string(),
        repositoryUrl: Yup.string(),
        workspacePath: Yup.string(),
        notes: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const ProjectForm: React.FC = () => {
  const [addProject, addProjectResult] = useAddProjectMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<Project> = {
          name: '',
          description: '',
        projectType: undefined,
        status: undefined,
        currentStage: undefined,
          progressPercent: 0,
        sourceSurface: undefined,
          sourceAgentId: '',
          repositoryUrl: '',
          workspacePath: '',
          notes: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new Project:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<Project>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("Project form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addProject(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `Project created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create Project:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as Project}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addProjectResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New Project
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="name" className="nice-form-control">
                      <b>
                        Name:
                        {touched.name &&
                         !errors.name && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="name"
                            value={values?.name}
                            placeholder="Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="name"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="description" className="nice-form-control">
                      <b>
                        Description:
                        {touched.description &&
                         !errors.description && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="description"
                            value={values?.description}
                            placeholder="Description"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="description"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="projectType" className="nice-form-control">
                      <b>
                        Project Type:
                        {touched.projectType &&
                         !errors.projectType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="projectType"
                          value={values.projectType || ''}
                          className={
                            errors.projectType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('projectType', true);
                            setFieldValue('projectType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Project Type" />
                          <ProjectTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="projectType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentStage" className="nice-form-control">
                      <b>
                        Current Stage:
                        {touched.currentStage &&
                         !errors.currentStage && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="currentStage"
                          value={values.currentStage || ''}
                          className={
                            errors.currentStage
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('currentStage', true);
                            setFieldValue('currentStage', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Current Stage" />
                          <CurrentStageLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="currentStage"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="progressPercent" className="nice-form-control">
                      <b>
                        Progress Percent:
                        {touched.progressPercent &&
                         !errors.progressPercent && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="progressPercent"
                            type="number"
                            value={values.progressPercent || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('progressPercent', true);
                              const v = e.target.value;
                              setFieldValue('progressPercent', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.progressPercent
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="progressPercent"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceSurface" className="nice-form-control">
                      <b>
                        Source Surface:
                        {touched.sourceSurface &&
                         !errors.sourceSurface && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="sourceSurface"
                          value={values.sourceSurface || ''}
                          className={
                            errors.sourceSurface
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('sourceSurface', true);
                            setFieldValue('sourceSurface', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Source Surface" />
                          <SourceSurfaceLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="sourceSurface"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceAgentId" className="nice-form-control">
                      <b>
                        Source Agent Id:
                        {touched.sourceAgentId &&
                         !errors.sourceAgentId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="sourceAgentId"
                            value={values?.sourceAgentId}
                            placeholder="Source Agent Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="sourceAgentId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="repositoryUrl" className="nice-form-control">
                      <b>
                        Repository Url:
                        {touched.repositoryUrl &&
                         !errors.repositoryUrl && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="repositoryUrl"
                            value={values?.repositoryUrl}
                            placeholder="Repository Url"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="repositoryUrl"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="workspacePath" className="nice-form-control">
                      <b>
                        Workspace Path:
                        {touched.workspacePath &&
                         !errors.workspacePath && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="workspacePath"
                            value={values?.workspacePath}
                            placeholder="Workspace Path"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="workspacePath"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="notes" className="nice-form-control">
                      <b>
                        Notes:
                        {touched.notes &&
                         !errors.notes && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="notes"
                            value={values?.notes}
                            placeholder="Notes"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="notes"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New Project
                  </CoolButton>

                  {(addProjectResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addProjectResult as any).error ? (addProjectResult as any).error.data : (addProjectResult as any).error)}
                    </Alert>
                  )}

                  {(addProjectResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addProjectResult: {JSON.stringify(addProjectResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.Project"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase projecttypelookup
uppercase PROJECTTYPELOOKUP
snakecase project_type_lookup
pascalcase ProjectTypeLookup
camelcase projectTypeLookup
kebabcase project-type-lookup
*/

const ProjectTypeLookup = () => {
  return (
    <>
      <option value='app-generation' label="App Generation" />
      <option value='api-builder' label="Api Builder" />
      <option value='workflow-mcp' label="Workflow Mcp" />
      <option value='graymatter-memory' label="Graymatter Memory" />
      <option value='valoride-coding' label="Valoride Coding" />
      <option value='deployment' label="Deployment" />
      <option value='content-publishing' label="Content Publishing" />
      <option value='business-in-a-box' label="Business In A Box" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='planned' label="Planned" />
      <option value='active' label="Active" />
      <option value='blocked' label="Blocked" />
      <option value='review' label="Review" />
      <option value='completed' label="Completed" />
      <option value='archived' label="Archived" />
    </>
  );
};

/*
lowercase currentstagelookup
uppercase CURRENTSTAGELOOKUP
snakecase current_stage_lookup
pascalcase CurrentStageLookup
camelcase currentStageLookup
kebabcase current-stage-lookup
*/

const CurrentStageLookup = () => {
  return (
    <>
      <option value='define-strategy' label="Define Strategy" />
      <option value='define-goals' label="Define Goals" />
      <option value='plan-tasks' label="Plan Tasks" />
      <option value='generate-api' label="Generate Api" />
      <option value='generate-app' label="Generate App" />
      <option value='vibe-code' label="Vibe Code" />
      <option value='deploy' label="Deploy" />
      <option value='publish-mcp' label="Publish Mcp" />
      <option value='operate' label="Operate" />
      <option value='complete' label="Complete" />
    </>
  );
};

/*
lowercase sourcesurfacelookup
uppercase SOURCESURFACELOOKUP
snakecase source_surface_lookup
pascalcase SourceSurfaceLookup
camelcase sourceSurfaceLookup
kebabcase source-surface-lookup
*/

const SourceSurfaceLookup = () => {
  return (
    <>
      <option value='dashboard' label="Dashboard" />
      <option value='graymatter' label="Graymatter" />
      <option value='valor' label="Valor" />
      <option value='valoride' label="Valoride" />
      <option value='api' label="Api" />
      <option value='workflow' label="Workflow" />
      <option value='swarm' label="Swarm" />
    </>
  );
};



/* Export the generated form */
export default ProjectForm;

