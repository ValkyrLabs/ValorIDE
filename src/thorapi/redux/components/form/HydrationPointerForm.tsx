// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  HydrationPointer,
  HydrationPointerTargetTypeEnum,
  HydrationPointerStatusEnum,
} from '@thorapi/model';

import { useAddHydrationPointerMutation } from '../../services/HydrationPointerService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Lazy source expansion pointer for ContextPage hydration.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const TargetTypeValidation = () => {
  return [
    'file',
    'model',
    'schema',
    'receipt',
    'memory',
    'procedure',
    'workflow',
    'generated_artifact',
    'tenant_app_record',
    'external_source',
    'other',
  ];
};
const StatusValidation = () => {
  return [
    'available',
    'hydrated',
    'denied',
    'stale',
    'missing',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        pointerRef: Yup.string().required("pointerRef is required."),
      targetType: Yup.mixed()
        .oneOf(TargetTypeValidation(), "Invalid value for targetType")
        .required("targetType is required."),
        targetId: Yup.string().required("targetId is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        targetUri: Yup.string(),
        summary: Yup.string(),
        estimatedTokens: asNumber(Yup.number().integer().typeError("estimatedTokens must be a number")),
        policy: Yup.string(),
        lastHydratedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("lastHydratedAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const HydrationPointerForm: React.FC = () => {
  const [addHydrationPointer, addHydrationPointerResult] = useAddHydrationPointerMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<HydrationPointer> = {
          pointerRef: '',
        targetType: undefined,
          targetId: '',
        status: undefined,
          targetUri: '',
          summary: '',
          estimatedTokens: 0,
          policy: '',
          lastHydratedAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new HydrationPointer:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<HydrationPointer>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("HydrationPointer form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addHydrationPointer(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `HydrationPointer created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create HydrationPointer:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as HydrationPointer}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addHydrationPointerResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New HydrationPointer
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="pointerRef" className="nice-form-control">
                      <b>
                        Pointer Ref:
                        {touched.pointerRef &&
                         !errors.pointerRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="pointerRef"
                            value={values?.pointerRef}
                            placeholder="Pointer Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="pointerRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="targetType" className="nice-form-control">
                      <b>
                        Target Type:
                        {touched.targetType &&
                         !errors.targetType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="targetType"
                          value={values.targetType || ''}
                          className={
                            errors.targetType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('targetType', true);
                            setFieldValue('targetType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Target Type" />
                          <TargetTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="targetType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="targetId" className="nice-form-control">
                      <b>
                        Target Id:
                        {touched.targetId &&
                         !errors.targetId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="targetId"
                            value={values?.targetId}
                            placeholder="Target Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="targetId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="targetUri" className="nice-form-control">
                      <b>
                        Target Uri:
                        {touched.targetUri &&
                         !errors.targetUri && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="targetUri"
                            value={values?.targetUri}
                            placeholder="Target Uri"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="targetUri"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="summary" className="nice-form-control">
                      <b>
                        Summary:
                        {touched.summary &&
                         !errors.summary && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="summary"
                            value={values?.summary}
                            placeholder="Summary"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="summary"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="estimatedTokens" className="nice-form-control">
                      <b>
                        Estimated Tokens:
                        {touched.estimatedTokens &&
                         !errors.estimatedTokens && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="estimatedTokens"
                            type="number"
                            value={values.estimatedTokens || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('estimatedTokens', true);
                              const v = e.target.value;
                              setFieldValue('estimatedTokens', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.estimatedTokens
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="estimatedTokens"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="policy" className="nice-form-control">
                      <b>
                        Policy:
                        {touched.policy &&
                         !errors.policy && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="policy"
                            value={values?.policy}
                            placeholder="Policy"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="policy"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastHydratedAt" className="nice-form-control">
                      <b>
                        Last Hydrated At:
                        {touched.lastHydratedAt &&
                         !errors.lastHydratedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="lastHydratedAt"
                            type="datetime-local"
                            value={values.lastHydratedAt ? 
                              new Date(values.lastHydratedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('lastHydratedAt', true);
                              const v = e.target.value;
                              setFieldValue('lastHydratedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.lastHydratedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="lastHydratedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New HydrationPointer
                  </CoolButton>

                  {(addHydrationPointerResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addHydrationPointerResult as any).error ? (addHydrationPointerResult as any).error.data : (addHydrationPointerResult as any).error)}
                    </Alert>
                  )}

                  {(addHydrationPointerResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addHydrationPointerResult: {JSON.stringify(addHydrationPointerResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.HydrationPointer"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase targettypelookup
uppercase TARGETTYPELOOKUP
snakecase target_type_lookup
pascalcase TargetTypeLookup
camelcase targetTypeLookup
kebabcase target-type-lookup
*/

const TargetTypeLookup = () => {
  return (
    <>
      <option value='file' label="File" />
      <option value='model' label="Model" />
      <option value='schema' label="Schema" />
      <option value='receipt' label="Receipt" />
      <option value='memory' label="Memory" />
      <option value='procedure' label="Procedure" />
      <option value='workflow' label="Workflow" />
      <option value='generated_artifact' label="Generated Artifact" />
      <option value='tenant_app_record' label="Tenant App Record" />
      <option value='external_source' label="External Source" />
      <option value='other' label="Other" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='available' label="Available" />
      <option value='hydrated' label="Hydrated" />
      <option value='denied' label="Denied" />
      <option value='stale' label="Stale" />
      <option value='missing' label="Missing" />
    </>
  );
};



/* Export the generated form */
export default HydrationPointerForm;

