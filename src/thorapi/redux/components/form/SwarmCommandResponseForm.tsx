// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  SwarmCommandResponse,
} from '@thorapi/model';

import { useAddSwarmCommandResponseMutation } from '../../services/SwarmCommandResponseService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Acknowledgement of a forwarded swarm command.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        status: Yup.string(),
        targetInstanceId: Yup.string(),
        issuedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("issuedAt must be a valid date"),
        commandId: Yup.string(),
        rejectionCode: Yup.string(),
        receiptRef: Yup.string(),
        traceId: Yup.string(),
        contextPageRef: Yup.string(),
        skillOptReceiptRef: Yup.string(),
        workflowExecutionRef: Yup.string(),
        workflowDispatchJson: Yup.string(),
        commandPayloadJson: Yup.string(),
        retryCount: asNumber(Yup.number().integer().typeError("retryCount must be a number")),
        nextRetryAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("nextRetryAt must be a valid date"),
        leaseOwner: Yup.string(),
        leaseUntil: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("leaseUntil must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const SwarmCommandResponseForm: React.FC = () => {
  const [addSwarmCommandResponse, addSwarmCommandResponseResult] = useAddSwarmCommandResponseMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<SwarmCommandResponse> = {
          status: '',
          targetInstanceId: '',
          issuedAt: new Date(),
          commandId: '',
          rejectionCode: '',
          receiptRef: '',
          traceId: '',
          contextPageRef: '',
          skillOptReceiptRef: '',
          workflowExecutionRef: '',
          workflowDispatchJson: '',
          commandPayloadJson: '',
          retryCount: 0,
          nextRetryAt: new Date(),
          leaseOwner: '',
          leaseUntil: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new SwarmCommandResponse:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<SwarmCommandResponse>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("SwarmCommandResponse form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addSwarmCommandResponse(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `SwarmCommandResponse created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create SwarmCommandResponse:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as SwarmCommandResponse}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addSwarmCommandResponseResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New SwarmCommandResponse
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="status"
                            value={values?.status}
                            placeholder="Status"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="targetInstanceId" className="nice-form-control">
                      <b>
                        Target Instance Id:
                        {touched.targetInstanceId &&
                         !errors.targetInstanceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="targetInstanceId"
                            value={values?.targetInstanceId}
                            placeholder="Target Instance Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="targetInstanceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="issuedAt" className="nice-form-control">
                      <b>
                        Issued At:
                        {touched.issuedAt &&
                         !errors.issuedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="issuedAt"
                            type="datetime-local"
                            value={values.issuedAt ? 
                              new Date(values.issuedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('issuedAt', true);
                              const v = e.target.value;
                              setFieldValue('issuedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.issuedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="issuedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="commandId" className="nice-form-control">
                      <b>
                        Command Id:
                        {touched.commandId &&
                         !errors.commandId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="commandId"
                            value={values?.commandId}
                            placeholder="Command Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="commandId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="rejectionCode" className="nice-form-control">
                      <b>
                        Rejection Code:
                        {touched.rejectionCode &&
                         !errors.rejectionCode && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="rejectionCode"
                            value={values?.rejectionCode}
                            placeholder="Rejection Code"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="rejectionCode"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="receiptRef" className="nice-form-control">
                      <b>
                        Receipt Ref:
                        {touched.receiptRef &&
                         !errors.receiptRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="receiptRef"
                            value={values?.receiptRef}
                            placeholder="Receipt Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="receiptRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="traceId" className="nice-form-control">
                      <b>
                        Trace Id:
                        {touched.traceId &&
                         !errors.traceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="traceId"
                            value={values?.traceId}
                            placeholder="Trace Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="traceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="contextPageRef" className="nice-form-control">
                      <b>
                        Context Page Ref:
                        {touched.contextPageRef &&
                         !errors.contextPageRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="contextPageRef"
                            value={values?.contextPageRef}
                            placeholder="Context Page Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="contextPageRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="skillOptReceiptRef" className="nice-form-control">
                      <b>
                        Skill Opt Receipt Ref:
                        {touched.skillOptReceiptRef &&
                         !errors.skillOptReceiptRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="skillOptReceiptRef"
                            value={values?.skillOptReceiptRef}
                            placeholder="Skill Opt Receipt Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="skillOptReceiptRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="workflowExecutionRef" className="nice-form-control">
                      <b>
                        Workflow Execution Ref:
                        {touched.workflowExecutionRef &&
                         !errors.workflowExecutionRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="workflowExecutionRef"
                            value={values?.workflowExecutionRef}
                            placeholder="Workflow Execution Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="workflowExecutionRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="workflowDispatchJson" className="nice-form-control">
                      <b>
                        Workflow Dispatch Json:
                        {touched.workflowDispatchJson &&
                         !errors.workflowDispatchJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="workflowDispatchJson"
                            value={values?.workflowDispatchJson}
                            placeholder="Workflow Dispatch Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="workflowDispatchJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="commandPayloadJson" className="nice-form-control">
                      <b>
                        Command Payload Json:
                        {touched.commandPayloadJson &&
                         !errors.commandPayloadJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="commandPayloadJson"
                            value={values?.commandPayloadJson}
                            placeholder="Command Payload Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="commandPayloadJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="retryCount" className="nice-form-control">
                      <b>
                        Retry Count:
                        {touched.retryCount &&
                         !errors.retryCount && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="retryCount"
                            type="number"
                            value={values.retryCount || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('retryCount', true);
                              const v = e.target.value;
                              setFieldValue('retryCount', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.retryCount
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="retryCount"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="nextRetryAt" className="nice-form-control">
                      <b>
                        Next Retry At:
                        {touched.nextRetryAt &&
                         !errors.nextRetryAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="nextRetryAt"
                            type="datetime-local"
                            value={values.nextRetryAt ? 
                              new Date(values.nextRetryAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('nextRetryAt', true);
                              const v = e.target.value;
                              setFieldValue('nextRetryAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.nextRetryAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="nextRetryAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="leaseOwner" className="nice-form-control">
                      <b>
                        Lease Owner:
                        {touched.leaseOwner &&
                         !errors.leaseOwner && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="leaseOwner"
                            value={values?.leaseOwner}
                            placeholder="Lease Owner"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="leaseOwner"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="leaseUntil" className="nice-form-control">
                      <b>
                        Lease Until:
                        {touched.leaseUntil &&
                         !errors.leaseUntil && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="leaseUntil"
                            type="datetime-local"
                            value={values.leaseUntil ? 
                              new Date(values.leaseUntil).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('leaseUntil', true);
                              const v = e.target.value;
                              setFieldValue('leaseUntil', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.leaseUntil
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="leaseUntil"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New SwarmCommandResponse
                  </CoolButton>

                  {(addSwarmCommandResponseResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addSwarmCommandResponseResult as any).error ? (addSwarmCommandResponseResult as any).error.data : (addSwarmCommandResponseResult as any).error)}
                    </Alert>
                  )}

                  {(addSwarmCommandResponseResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addSwarmCommandResponseResult: {JSON.stringify(addSwarmCommandResponseResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.SwarmCommandResponse"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};



/* Export the generated form */
export default SwarmCommandResponseForm;

