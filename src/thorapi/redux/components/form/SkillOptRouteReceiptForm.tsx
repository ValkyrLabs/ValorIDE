// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  SkillOptRouteReceipt,
  SkillOptRouteReceiptOutcomeEnum,
} from '@thorapi/model';

import { useAddSkillOptRouteReceiptMutation } from '../../services/SkillOptRouteReceiptService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Receipt explaining why SkillOpt selected a route.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const OutcomeValidation = () => {
  return [
    'pending',
    'success',
    'failure',
    'partial',
    'canceled',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        receiptRef: Yup.string().required("receiptRef is required."),
        traceId: Yup.string().required("traceId is required."),
        routeExplanation: Yup.string().required("routeExplanation is required."),
        estimatedCreditCost: asNumber(Yup.number().typeError("estimatedCreditCost must be a number")).required("estimatedCreditCost is required."),
        confidence: asNumber(Yup.number().typeError("confidence must be a number")).required("confidence is required."),
        tenantId: Yup.string(),
        accountId: Yup.string(),
        creditBalance: asNumber(Yup.number().typeError("creditBalance must be a number")),
        contextSufficiency: asNumber(Yup.number().typeError("contextSufficiency must be a number")),
        requiredHydration: Yup.boolean(),
        requiredApproval: Yup.boolean(),
        billable: Yup.boolean(),
        stopReason: Yup.string(),
        policyDecision: Yup.string(),
        routeScoresJson: Yup.string(),
        createdAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("createdAt must be a valid date"),
      outcome: Yup.mixed()
        .oneOf(OutcomeValidation(), "Invalid value for outcome")
        ,
        outcomeAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("outcomeAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const SkillOptRouteReceiptForm: React.FC = () => {
  const [addSkillOptRouteReceipt, addSkillOptRouteReceiptResult] = useAddSkillOptRouteReceiptMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<SkillOptRouteReceipt> = {
          receiptRef: '',
          traceId: '',
          routeExplanation: '',
          estimatedCreditCost: 0,
          confidence: 0,
          tenantId: '',
          accountId: '',
          creditBalance: 0,
          contextSufficiency: 0,
          requiredHydration: false,
          requiredApproval: false,
          billable: false,
          stopReason: '',
          policyDecision: '',
          routeScoresJson: '',
          createdAt: new Date(),
        outcome: undefined,
          outcomeAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new SkillOptRouteReceipt:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<SkillOptRouteReceipt>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("SkillOptRouteReceipt form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addSkillOptRouteReceipt(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `SkillOptRouteReceipt created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create SkillOptRouteReceipt:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as SkillOptRouteReceipt}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addSkillOptRouteReceiptResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New SkillOptRouteReceipt
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="receiptRef" className="nice-form-control">
                      <b>
                        Receipt Ref:
                        {touched.receiptRef &&
                         !errors.receiptRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="receiptRef"
                            value={values?.receiptRef}
                            placeholder="Receipt Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="receiptRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="traceId" className="nice-form-control">
                      <b>
                        Trace Id:
                        {touched.traceId &&
                         !errors.traceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="traceId"
                            value={values?.traceId}
                            placeholder="Trace Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="traceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="routeExplanation" className="nice-form-control">
                      <b>
                        Route Explanation:
                        {touched.routeExplanation &&
                         !errors.routeExplanation && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="routeExplanation"
                            value={values?.routeExplanation}
                            placeholder="Route Explanation"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="routeExplanation"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="estimatedCreditCost" className="nice-form-control">
                      <b>
                        Estimated Credit Cost:
                        {touched.estimatedCreditCost &&
                         !errors.estimatedCreditCost && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="estimatedCreditCost"
                            type="number"
                            step="any"
                            value={values.estimatedCreditCost || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('estimatedCreditCost', true);
                              const v = e.target.value;
                              setFieldValue('estimatedCreditCost', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.estimatedCreditCost
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="estimatedCreditCost"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="confidence" className="nice-form-control">
                      <b>
                        Confidence:
                        {touched.confidence &&
                         !errors.confidence && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="confidence"
                            type="number"
                            step="any"
                            value={values.confidence || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('confidence', true);
                              const v = e.target.value;
                              setFieldValue('confidence', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.confidence
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="confidence"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="accountId" className="nice-form-control">
                      <b>
                        Account Id:
                        {touched.accountId &&
                         !errors.accountId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="accountId"
                            value={values?.accountId}
                            placeholder="Account Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="accountId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="creditBalance" className="nice-form-control">
                      <b>
                        Credit Balance:
                        {touched.creditBalance &&
                         !errors.creditBalance && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="creditBalance"
                            type="number"
                            step="any"
                            value={values.creditBalance || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('creditBalance', true);
                              const v = e.target.value;
                              setFieldValue('creditBalance', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.creditBalance
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="creditBalance"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="contextSufficiency" className="nice-form-control">
                      <b>
                        Context Sufficiency:
                        {touched.contextSufficiency &&
                         !errors.contextSufficiency && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="contextSufficiency"
                            type="number"
                            step="any"
                            value={values.contextSufficiency || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('contextSufficiency', true);
                              const v = e.target.value;
                              setFieldValue('contextSufficiency', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.contextSufficiency
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="contextSufficiency"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="requiredHydration" className="nice-form-control">
                      <b>
                        Required Hydration:
                        {touched.requiredHydration &&
                         !errors.requiredHydration && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="requiredHydration"
                            name="requiredHydration"
                            checked={values.requiredHydration || false}
                            onChange={(e) => {
                              setFieldTouched('requiredHydration', true);
                              setFieldValue('requiredHydration', e.target.checked);
                            }}
                            isInvalid={!!errors.requiredHydration}
                            className={errors.requiredHydration ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="requiredHydration"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="requiredApproval" className="nice-form-control">
                      <b>
                        Required Approval:
                        {touched.requiredApproval &&
                         !errors.requiredApproval && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="requiredApproval"
                            name="requiredApproval"
                            checked={values.requiredApproval || false}
                            onChange={(e) => {
                              setFieldTouched('requiredApproval', true);
                              setFieldValue('requiredApproval', e.target.checked);
                            }}
                            isInvalid={!!errors.requiredApproval}
                            className={errors.requiredApproval ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="requiredApproval"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="billable" className="nice-form-control">
                      <b>
                        Billable:
                        {touched.billable &&
                         !errors.billable && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="billable"
                            name="billable"
                            checked={values.billable || false}
                            onChange={(e) => {
                              setFieldTouched('billable', true);
                              setFieldValue('billable', e.target.checked);
                            }}
                            isInvalid={!!errors.billable}
                            className={errors.billable ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="billable"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stopReason" className="nice-form-control">
                      <b>
                        Stop Reason:
                        {touched.stopReason &&
                         !errors.stopReason && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stopReason"
                            value={values?.stopReason}
                            placeholder="Stop Reason"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stopReason"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="policyDecision" className="nice-form-control">
                      <b>
                        Policy Decision:
                        {touched.policyDecision &&
                         !errors.policyDecision && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="policyDecision"
                            value={values?.policyDecision}
                            placeholder="Policy Decision"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="policyDecision"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="routeScoresJson" className="nice-form-control">
                      <b>
                        Route Scores Json:
                        {touched.routeScoresJson &&
                         !errors.routeScoresJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="routeScoresJson"
                            value={values?.routeScoresJson}
                            placeholder="Route Scores Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="routeScoresJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="createdAt" className="nice-form-control">
                      <b>
                        Created At:
                        {touched.createdAt &&
                         !errors.createdAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="createdAt"
                            type="datetime-local"
                            value={values.createdAt ? 
                              new Date(values.createdAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('createdAt', true);
                              const v = e.target.value;
                              setFieldValue('createdAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.createdAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="createdAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="outcome" className="nice-form-control">
                      <b>
                        Outcome:
                        {touched.outcome &&
                         !errors.outcome && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="outcome"
                          value={values.outcome || ''}
                          className={
                            errors.outcome
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('outcome', true);
                            setFieldValue('outcome', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Outcome" />
                          <OutcomeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="outcome"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="outcomeAt" className="nice-form-control">
                      <b>
                        Outcome At:
                        {touched.outcomeAt &&
                         !errors.outcomeAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="outcomeAt"
                            type="datetime-local"
                            value={values.outcomeAt ? 
                              new Date(values.outcomeAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('outcomeAt', true);
                              const v = e.target.value;
                              setFieldValue('outcomeAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.outcomeAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="outcomeAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New SkillOptRouteReceipt
                  </CoolButton>

                  {(addSkillOptRouteReceiptResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addSkillOptRouteReceiptResult as any).error ? (addSkillOptRouteReceiptResult as any).error.data : (addSkillOptRouteReceiptResult as any).error)}
                    </Alert>
                  )}

                  {(addSkillOptRouteReceiptResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addSkillOptRouteReceiptResult: {JSON.stringify(addSkillOptRouteReceiptResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.SkillOptRouteReceipt"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase outcomelookup
uppercase OUTCOMELOOKUP
snakecase outcome_lookup
pascalcase OutcomeLookup
camelcase outcomeLookup
kebabcase outcome-lookup
*/

const OutcomeLookup = () => {
  return (
    <>
      <option value='pending' label="Pending" />
      <option value='success' label="Success" />
      <option value='failure' label="Failure" />
      <option value='partial' label="Partial" />
      <option value='canceled' label="Canceled" />
    </>
  );
};



/* Export the generated form */
export default SkillOptRouteReceiptForm;

