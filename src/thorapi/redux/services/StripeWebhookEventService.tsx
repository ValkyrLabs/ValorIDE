// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { StripeWebhookEvent } from '@thorapi/model/StripeWebhookEvent'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type StripeWebhookEventResponse = StripeWebhookEvent[]
type StripeWebhookEventPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<StripeWebhookEvent>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type StripeWebhookEventListQueryArg = {
  example?: Partial<StripeWebhookEvent>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toStripeWebhookEventList = (result: unknown): StripeWebhookEventResponse => {
  if (Array.isArray(result)) {
    return result as StripeWebhookEventResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as StripeWebhookEventResponse) : []
}

export const StripeWebhookEventService = createApi({
  reducerPath: 'StripeWebhookEvent', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['StripeWebhookEvent'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getStripeWebhookEventsPaged: build.query<StripeWebhookEventResponse, StripeWebhookEventPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `StripeWebhookEvent?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toStripeWebhookEventList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeWebhookEvent' as const, id })),
          { type: 'StripeWebhookEvent', id: `PAGE_${page}` },
          { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getStripeWebhookEvents: build.query<StripeWebhookEventResponse, StripeWebhookEventListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `StripeWebhookEvent?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `StripeWebhookEvent`;
      },
      providesTags: (result) => {
        const rows = toStripeWebhookEventList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeWebhookEvent' as const, id })),
          { type: 'StripeWebhookEvent', id: 'LIST' },
          { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addStripeWebhookEvent: build.mutation<StripeWebhookEvent, Partial<StripeWebhookEvent>>({
      query: (body) => ({
        url: `StripeWebhookEvent`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'StripeWebhookEvent', id: 'LIST' },
        { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getStripeWebhookEvent: build.query<StripeWebhookEvent, string>({
      query: (id) => `StripeWebhookEvent/${id}`,
      providesTags: (result, error, id) => [{ type: 'StripeWebhookEvent', id }],
    }),

    // 5) Update
    updateStripeWebhookEvent: build.mutation<StripeWebhookEvent, Pick<StripeWebhookEvent, 'id'> & Partial<StripeWebhookEvent>>({
      query: ({ id, ...patch }) => ({
        url: `StripeWebhookEvent/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<StripeWebhookEvent, 'id'>) => [
        { type: 'StripeWebhookEvent', id },
        { type: 'StripeWebhookEvent', id: 'LIST' },
        { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteStripeWebhookEvent: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `StripeWebhookEvent/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'StripeWebhookEvent', id },
        { type: 'StripeWebhookEvent', id: 'LIST' },
        { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteStripeWebhookEventCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `StripeWebhookEvent/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'StripeWebhookEvent', id },
        { type: 'StripeWebhookEvent', id: 'LIST' },
        { type: 'StripeWebhookEvent', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetStripeWebhookEventsPagedQuery`
export const {
  useGetStripeWebhookEventsPagedQuery,     // immediate fetch
  useLazyGetStripeWebhookEventsPagedQuery, // lazy fetch
  useGetStripeWebhookEventQuery,
  useGetStripeWebhookEventsQuery,
  useAddStripeWebhookEventMutation,
  useUpdateStripeWebhookEventMutation,
  useDeleteStripeWebhookEventMutation,
  useDeleteStripeWebhookEventCascadeMutation,
} = StripeWebhookEventService
