// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { OpenClawLightsailLaunchRequest } from '@thorapi/model/OpenClawLightsailLaunchRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type OpenClawLightsailLaunchRequestResponse = OpenClawLightsailLaunchRequest[]
type OpenClawLightsailLaunchRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<OpenClawLightsailLaunchRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type OpenClawLightsailLaunchRequestListQueryArg = {
  example?: Partial<OpenClawLightsailLaunchRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toOpenClawLightsailLaunchRequestList = (result: unknown): OpenClawLightsailLaunchRequestResponse => {
  if (Array.isArray(result)) {
    return result as OpenClawLightsailLaunchRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as OpenClawLightsailLaunchRequestResponse) : []
}

export const OpenClawLightsailLaunchRequestService = createApi({
  reducerPath: 'OpenClawLightsailLaunchRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['OpenClawLightsailLaunchRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getOpenClawLightsailLaunchRequestsPaged: build.query<OpenClawLightsailLaunchRequestResponse, OpenClawLightsailLaunchRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `OpenClawLightsailLaunchRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toOpenClawLightsailLaunchRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchRequest' as const, id })),
          { type: 'OpenClawLightsailLaunchRequest', id: `PAGE_${page}` },
          { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getOpenClawLightsailLaunchRequests: build.query<OpenClawLightsailLaunchRequestResponse, OpenClawLightsailLaunchRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `OpenClawLightsailLaunchRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `OpenClawLightsailLaunchRequest`;
      },
      providesTags: (result) => {
        const rows = toOpenClawLightsailLaunchRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchRequest' as const, id })),
          { type: 'OpenClawLightsailLaunchRequest', id: 'LIST' },
          { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addOpenClawLightsailLaunchRequest: build.mutation<OpenClawLightsailLaunchRequest, Partial<OpenClawLightsailLaunchRequest>>({
      query: (body) => ({
        url: `OpenClawLightsailLaunchRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'OpenClawLightsailLaunchRequest', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getOpenClawLightsailLaunchRequest: build.query<OpenClawLightsailLaunchRequest, string>({
      query: (id) => `OpenClawLightsailLaunchRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'OpenClawLightsailLaunchRequest', id }],
    }),

    // 5) Update
    updateOpenClawLightsailLaunchRequest: build.mutation<OpenClawLightsailLaunchRequest, Pick<OpenClawLightsailLaunchRequest, 'id'> & Partial<OpenClawLightsailLaunchRequest>>({
      query: ({ id, ...patch }) => ({
        url: `OpenClawLightsailLaunchRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<OpenClawLightsailLaunchRequest, 'id'>) => [
        { type: 'OpenClawLightsailLaunchRequest', id },
        { type: 'OpenClawLightsailLaunchRequest', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteOpenClawLightsailLaunchRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `OpenClawLightsailLaunchRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'OpenClawLightsailLaunchRequest', id },
        { type: 'OpenClawLightsailLaunchRequest', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteOpenClawLightsailLaunchRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `OpenClawLightsailLaunchRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'OpenClawLightsailLaunchRequest', id },
        { type: 'OpenClawLightsailLaunchRequest', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetOpenClawLightsailLaunchRequestsPagedQuery`
export const {
  useGetOpenClawLightsailLaunchRequestsPagedQuery,     // immediate fetch
  useLazyGetOpenClawLightsailLaunchRequestsPagedQuery, // lazy fetch
  useGetOpenClawLightsailLaunchRequestQuery,
  useGetOpenClawLightsailLaunchRequestsQuery,
  useAddOpenClawLightsailLaunchRequestMutation,
  useUpdateOpenClawLightsailLaunchRequestMutation,
  useDeleteOpenClawLightsailLaunchRequestMutation,
  useDeleteOpenClawLightsailLaunchRequestCascadeMutation,
} = OpenClawLightsailLaunchRequestService
