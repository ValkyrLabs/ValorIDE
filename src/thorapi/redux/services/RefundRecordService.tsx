// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RefundRecord } from '@thorapi/model/RefundRecord'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RefundRecordResponse = RefundRecord[]
type RefundRecordPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RefundRecord>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RefundRecordListQueryArg = {
  example?: Partial<RefundRecord>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRefundRecordList = (result: unknown): RefundRecordResponse => {
  if (Array.isArray(result)) {
    return result as RefundRecordResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RefundRecordResponse) : []
}

export const RefundRecordService = createApi({
  reducerPath: 'RefundRecord', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RefundRecord'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRefundRecordsPaged: build.query<RefundRecordResponse, RefundRecordPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RefundRecord?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRefundRecordList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RefundRecord' as const, id })),
          { type: 'RefundRecord', id: `PAGE_${page}` },
          { type: 'RefundRecord', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRefundRecords: build.query<RefundRecordResponse, RefundRecordListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RefundRecord?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RefundRecord`;
      },
      providesTags: (result) => {
        const rows = toRefundRecordList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RefundRecord' as const, id })),
          { type: 'RefundRecord', id: 'LIST' },
          { type: 'RefundRecord', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRefundRecord: build.mutation<RefundRecord, Partial<RefundRecord>>({
      query: (body) => ({
        url: `RefundRecord`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RefundRecord', id: 'LIST' },
        { type: 'RefundRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRefundRecord: build.query<RefundRecord, string>({
      query: (id) => `RefundRecord/${id}`,
      providesTags: (result, error, id) => [{ type: 'RefundRecord', id }],
    }),

    // 5) Update
    updateRefundRecord: build.mutation<RefundRecord, Pick<RefundRecord, 'id'> & Partial<RefundRecord>>({
      query: ({ id, ...patch }) => ({
        url: `RefundRecord/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RefundRecord, 'id'>) => [
        { type: 'RefundRecord', id },
        { type: 'RefundRecord', id: 'LIST' },
        { type: 'RefundRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRefundRecord: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RefundRecord/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RefundRecord', id },
        { type: 'RefundRecord', id: 'LIST' },
        { type: 'RefundRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRefundRecordCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RefundRecord/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RefundRecord', id },
        { type: 'RefundRecord', id: 'LIST' },
        { type: 'RefundRecord', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRefundRecordsPagedQuery`
export const {
  useGetRefundRecordsPagedQuery,     // immediate fetch
  useLazyGetRefundRecordsPagedQuery, // lazy fetch
  useGetRefundRecordQuery,
  useGetRefundRecordsQuery,
  useAddRefundRecordMutation,
  useUpdateRefundRecordMutation,
  useDeleteRefundRecordMutation,
  useDeleteRefundRecordCascadeMutation,
} = RefundRecordService
