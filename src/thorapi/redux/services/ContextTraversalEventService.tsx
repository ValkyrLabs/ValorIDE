// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextTraversalEvent } from '@thorapi/model/ContextTraversalEvent'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextTraversalEventResponse = ContextTraversalEvent[]
type ContextTraversalEventPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextTraversalEvent>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextTraversalEventListQueryArg = {
  example?: Partial<ContextTraversalEvent>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextTraversalEventList = (result: unknown): ContextTraversalEventResponse => {
  if (Array.isArray(result)) {
    return result as ContextTraversalEventResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextTraversalEventResponse) : []
}

export const ContextTraversalEventService = createApi({
  reducerPath: 'ContextTraversalEvent', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextTraversalEvent'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextTraversalEventsPaged: build.query<ContextTraversalEventResponse, ContextTraversalEventPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextTraversalEvent?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextTraversalEventList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextTraversalEvent' as const, id })),
          { type: 'ContextTraversalEvent', id: `PAGE_${page}` },
          { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextTraversalEvents: build.query<ContextTraversalEventResponse, ContextTraversalEventListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextTraversalEvent?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextTraversalEvent`;
      },
      providesTags: (result) => {
        const rows = toContextTraversalEventList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextTraversalEvent' as const, id })),
          { type: 'ContextTraversalEvent', id: 'LIST' },
          { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextTraversalEvent: build.mutation<ContextTraversalEvent, Partial<ContextTraversalEvent>>({
      query: (body) => ({
        url: `ContextTraversalEvent`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextTraversalEvent', id: 'LIST' },
        { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextTraversalEvent: build.query<ContextTraversalEvent, string>({
      query: (id) => `ContextTraversalEvent/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextTraversalEvent', id }],
    }),

    // 5) Update
    updateContextTraversalEvent: build.mutation<ContextTraversalEvent, Pick<ContextTraversalEvent, 'id'> & Partial<ContextTraversalEvent>>({
      query: ({ id, ...patch }) => ({
        url: `ContextTraversalEvent/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextTraversalEvent, 'id'>) => [
        { type: 'ContextTraversalEvent', id },
        { type: 'ContextTraversalEvent', id: 'LIST' },
        { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextTraversalEvent: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextTraversalEvent/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextTraversalEvent', id },
        { type: 'ContextTraversalEvent', id: 'LIST' },
        { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextTraversalEventCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextTraversalEvent/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextTraversalEvent', id },
        { type: 'ContextTraversalEvent', id: 'LIST' },
        { type: 'ContextTraversalEvent', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextTraversalEventsPagedQuery`
export const {
  useGetContextTraversalEventsPagedQuery,     // immediate fetch
  useLazyGetContextTraversalEventsPagedQuery, // lazy fetch
  useGetContextTraversalEventQuery,
  useGetContextTraversalEventsQuery,
  useAddContextTraversalEventMutation,
  useUpdateContextTraversalEventMutation,
  useDeleteContextTraversalEventMutation,
  useDeleteContextTraversalEventCascadeMutation,
} = ContextTraversalEventService
