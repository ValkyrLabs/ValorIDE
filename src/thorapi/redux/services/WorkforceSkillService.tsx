// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { WorkforceSkill } from '@thorapi/model/WorkforceSkill'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type WorkforceSkillResponse = WorkforceSkill[]
type WorkforceSkillPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<WorkforceSkill>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type WorkforceSkillListQueryArg = {
  example?: Partial<WorkforceSkill>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toWorkforceSkillList = (result: unknown): WorkforceSkillResponse => {
  if (Array.isArray(result)) {
    return result as WorkforceSkillResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as WorkforceSkillResponse) : []
}

export const WorkforceSkillService = createApi({
  reducerPath: 'WorkforceSkill', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['WorkforceSkill'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getWorkforceSkillsPaged: build.query<WorkforceSkillResponse, WorkforceSkillPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `WorkforceSkill?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toWorkforceSkillList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'WorkforceSkill' as const, id })),
          { type: 'WorkforceSkill', id: `PAGE_${page}` },
          { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getWorkforceSkills: build.query<WorkforceSkillResponse, WorkforceSkillListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `WorkforceSkill?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `WorkforceSkill`;
      },
      providesTags: (result) => {
        const rows = toWorkforceSkillList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'WorkforceSkill' as const, id })),
          { type: 'WorkforceSkill', id: 'LIST' },
          { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addWorkforceSkill: build.mutation<WorkforceSkill, Partial<WorkforceSkill>>({
      query: (body) => ({
        url: `WorkforceSkill`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'WorkforceSkill', id: 'LIST' },
        { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getWorkforceSkill: build.query<WorkforceSkill, string>({
      query: (id) => `WorkforceSkill/${id}`,
      providesTags: (result, error, id) => [{ type: 'WorkforceSkill', id }],
    }),

    // 5) Update
    updateWorkforceSkill: build.mutation<WorkforceSkill, Pick<WorkforceSkill, 'id'> & Partial<WorkforceSkill>>({
      query: ({ id, ...patch }) => ({
        url: `WorkforceSkill/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<WorkforceSkill, 'id'>) => [
        { type: 'WorkforceSkill', id },
        { type: 'WorkforceSkill', id: 'LIST' },
        { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteWorkforceSkill: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `WorkforceSkill/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'WorkforceSkill', id },
        { type: 'WorkforceSkill', id: 'LIST' },
        { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteWorkforceSkillCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `WorkforceSkill/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'WorkforceSkill', id },
        { type: 'WorkforceSkill', id: 'LIST' },
        { type: 'WorkforceSkill', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetWorkforceSkillsPagedQuery`
export const {
  useGetWorkforceSkillsPagedQuery,     // immediate fetch
  useLazyGetWorkforceSkillsPagedQuery, // lazy fetch
  useGetWorkforceSkillQuery,
  useGetWorkforceSkillsQuery,
  useAddWorkforceSkillMutation,
  useUpdateWorkforceSkillMutation,
  useDeleteWorkforceSkillMutation,
  useDeleteWorkforceSkillCascadeMutation,
} = WorkforceSkillService
