// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Project } from '@thorapi/model/Project'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ProjectResponse = Project[]
type ProjectPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Project>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ProjectListQueryArg = {
  example?: Partial<Project>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toProjectList = (result: unknown): ProjectResponse => {
  if (Array.isArray(result)) {
    return result as ProjectResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ProjectResponse) : []
}

export const ProjectService = createApi({
  reducerPath: 'Project', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Project'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getProjectsPaged: build.query<ProjectResponse, ProjectPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Project?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toProjectList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Project' as const, id })),
          { type: 'Project', id: `PAGE_${page}` },
          { type: 'Project', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getProjects: build.query<ProjectResponse, ProjectListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Project?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Project`;
      },
      providesTags: (result) => {
        const rows = toProjectList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Project' as const, id })),
          { type: 'Project', id: 'LIST' },
          { type: 'Project', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addProject: build.mutation<Project, Partial<Project>>({
      query: (body) => ({
        url: `Project`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Project', id: 'LIST' },
        { type: 'Project', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getProject: build.query<Project, string>({
      query: (id) => `Project/${id}`,
      providesTags: (result, error, id) => [{ type: 'Project', id }],
    }),

    // 5) Update
    updateProject: build.mutation<Project, Pick<Project, 'id'> & Partial<Project>>({
      query: ({ id, ...patch }) => ({
        url: `Project/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Project, 'id'>) => [
        { type: 'Project', id },
        { type: 'Project', id: 'LIST' },
        { type: 'Project', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteProject: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Project/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Project', id },
        { type: 'Project', id: 'LIST' },
        { type: 'Project', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteProjectCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Project/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Project', id },
        { type: 'Project', id: 'LIST' },
        { type: 'Project', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetProjectsPagedQuery`
export const {
  useGetProjectsPagedQuery,     // immediate fetch
  useLazyGetProjectsPagedQuery, // lazy fetch
  useGetProjectQuery,
  useGetProjectsQuery,
  useAddProjectMutation,
  useUpdateProjectMutation,
  useDeleteProjectMutation,
  useDeleteProjectCascadeMutation,
} = ProjectService
