// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import { AppendTrustEventRequest } from "@thorapi/model/AppendTrustEventRequest";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query

type AppendTrustEventRequestResponse = AppendTrustEventRequest[];

const toAppendTrustEventRequestList = (
  result: unknown,
): AppendTrustEventRequestResponse => {
  if (Array.isArray(result)) {
    return result as AppendTrustEventRequestResponse;
  }

  const candidate =
    (result as any)?.content ??
    (result as any)?.items ??
    (result as any)?.results ??
    (result as any)?.data;
  return Array.isArray(candidate)
    ? (candidate as AppendTrustEventRequestResponse)
    : [];
};

export const AppendTrustEventRequestService = createApi({
  reducerPath: "AppendTrustEventRequest", // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ["AppendTrustEventRequest"],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAppendTrustEventRequestsPaged: build.query<
      AppendTrustEventRequestResponse,
      {
        page: number;
        size?: number;
        example?: Partial<AppendTrustEventRequest>;
      }
    >({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example)
          q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `AppendTrustEventRequest?${q.join("&")}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAppendTrustEventRequestList(result);
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({
              type: "AppendTrustEventRequest" as const,
              id,
            })),
          { type: "AppendTrustEventRequest", id: `PAGE_${page}` },
        ];
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAppendTrustEventRequests: build.query<
      AppendTrustEventRequestResponse,
      { example?: Partial<AppendTrustEventRequest> } | void
    >({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `AppendTrustEventRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `AppendTrustEventRequest`;
      },
      providesTags: (result) => {
        const rows = toAppendTrustEventRequestList(result);
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({
              type: "AppendTrustEventRequest" as const,
              id,
            })),
          { type: "AppendTrustEventRequest", id: "LIST" },
        ];
      },
    }),

    // 3) Create
    addAppendTrustEventRequest: build.mutation<
      AppendTrustEventRequest,
      Partial<AppendTrustEventRequest>
    >({
      query: (body) => ({
        url: `AppendTrustEventRequest`,
        method: "POST",
        body,
      }),
      invalidatesTags: [{ type: "AppendTrustEventRequest", id: "LIST" }],
    }),

    // 4) Get single by ID
    getAppendTrustEventRequest: build.query<AppendTrustEventRequest, string>({
      query: (id) => `AppendTrustEventRequest/${id}`,
      providesTags: (result, error, id) => [
        { type: "AppendTrustEventRequest", id },
      ],
    }),

    // 5) Update
    updateAppendTrustEventRequest: build.mutation<
      void,
      Pick<AppendTrustEventRequest, "id"> & Partial<AppendTrustEventRequest>
    >({
      query: ({ id, ...patch }) => ({
        url: `AppendTrustEventRequest/${id}`,
        method: "PUT",
        body: patch,
      }),
      async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
        if (id) {
          const patchResult = dispatch(
            AppendTrustEventRequestService.util.updateQueryData(
              "getAppendTrustEventRequest",
              id,
              (draft) => {
                Object.assign(draft, patch);
              },
            ),
          );
          try {
            await queryFulfilled;
          } catch {
            patchResult.undo();
          }
        }
      },
      invalidatesTags: (
        result,
        error,
        { id }: Pick<AppendTrustEventRequest, "id">,
      ) => [
        { type: "AppendTrustEventRequest", id },
        { type: "AppendTrustEventRequest", id: "LIST" },
      ],
    }),

    // 6) Delete
    deleteAppendTrustEventRequest: build.mutation<
      { success: boolean; id: string },
      number
    >({
      query(id) {
        return {
          url: `AppendTrustEventRequest/${id}`,
          method: "DELETE",
        };
      },
      invalidatesTags: (result, error, id) => [
        { type: "AppendTrustEventRequest", id },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAppendTrustEventRequestCascade: build.mutation<
      { success: boolean; id: string },
      { id: string; cascade?: boolean; trash?: boolean }
    >({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
        return {
          url: `AppendTrustEventRequest/${id}?${params}`,
          method: "DELETE",
        };
      },
      invalidatesTags: (result, error, { id }) => [
        { type: "AppendTrustEventRequest", id },
        { type: "AppendTrustEventRequest", id: "LIST" },
      ],
    }),
  }),
});

// Notice we now also export `useLazyGetAppendTrustEventRequestsPagedQuery`
export const {
  useGetAppendTrustEventRequestsPagedQuery, // immediate fetch
  useLazyGetAppendTrustEventRequestsPagedQuery, // lazy fetch
  useGetAppendTrustEventRequestQuery,
  useGetAppendTrustEventRequestsQuery,
  useAddAppendTrustEventRequestMutation,
  useUpdateAppendTrustEventRequestMutation,
  useDeleteAppendTrustEventRequestMutation,
  useDeleteAppendTrustEventRequestCascadeMutation,
} = AppendTrustEventRequestService;
