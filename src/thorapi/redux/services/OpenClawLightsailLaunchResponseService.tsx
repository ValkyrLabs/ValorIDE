// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { OpenClawLightsailLaunchResponse } from '@thorapi/model/OpenClawLightsailLaunchResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type OpenClawLightsailLaunchResponseResponse = OpenClawLightsailLaunchResponse[]
type OpenClawLightsailLaunchResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<OpenClawLightsailLaunchResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type OpenClawLightsailLaunchResponseListQueryArg = {
  example?: Partial<OpenClawLightsailLaunchResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toOpenClawLightsailLaunchResponseList = (result: unknown): OpenClawLightsailLaunchResponseResponse => {
  if (Array.isArray(result)) {
    return result as OpenClawLightsailLaunchResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as OpenClawLightsailLaunchResponseResponse) : []
}

export const OpenClawLightsailLaunchResponseService = createApi({
  reducerPath: 'OpenClawLightsailLaunchResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['OpenClawLightsailLaunchResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getOpenClawLightsailLaunchResponsesPaged: build.query<OpenClawLightsailLaunchResponseResponse, OpenClawLightsailLaunchResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `OpenClawLightsailLaunchResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toOpenClawLightsailLaunchResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchResponse' as const, id })),
          { type: 'OpenClawLightsailLaunchResponse', id: `PAGE_${page}` },
          { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getOpenClawLightsailLaunchResponses: build.query<OpenClawLightsailLaunchResponseResponse, OpenClawLightsailLaunchResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `OpenClawLightsailLaunchResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `OpenClawLightsailLaunchResponse`;
      },
      providesTags: (result) => {
        const rows = toOpenClawLightsailLaunchResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchResponse' as const, id })),
          { type: 'OpenClawLightsailLaunchResponse', id: 'LIST' },
          { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addOpenClawLightsailLaunchResponse: build.mutation<OpenClawLightsailLaunchResponse, Partial<OpenClawLightsailLaunchResponse>>({
      query: (body) => ({
        url: `OpenClawLightsailLaunchResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'OpenClawLightsailLaunchResponse', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getOpenClawLightsailLaunchResponse: build.query<OpenClawLightsailLaunchResponse, string>({
      query: (id) => `OpenClawLightsailLaunchResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'OpenClawLightsailLaunchResponse', id }],
    }),

    // 5) Update
    updateOpenClawLightsailLaunchResponse: build.mutation<OpenClawLightsailLaunchResponse, Pick<OpenClawLightsailLaunchResponse, 'id'> & Partial<OpenClawLightsailLaunchResponse>>({
      query: ({ id, ...patch }) => ({
        url: `OpenClawLightsailLaunchResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<OpenClawLightsailLaunchResponse, 'id'>) => [
        { type: 'OpenClawLightsailLaunchResponse', id },
        { type: 'OpenClawLightsailLaunchResponse', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteOpenClawLightsailLaunchResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `OpenClawLightsailLaunchResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'OpenClawLightsailLaunchResponse', id },
        { type: 'OpenClawLightsailLaunchResponse', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteOpenClawLightsailLaunchResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `OpenClawLightsailLaunchResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'OpenClawLightsailLaunchResponse', id },
        { type: 'OpenClawLightsailLaunchResponse', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetOpenClawLightsailLaunchResponsesPagedQuery`
export const {
  useGetOpenClawLightsailLaunchResponsesPagedQuery,     // immediate fetch
  useLazyGetOpenClawLightsailLaunchResponsesPagedQuery, // lazy fetch
  useGetOpenClawLightsailLaunchResponseQuery,
  useGetOpenClawLightsailLaunchResponsesQuery,
  useAddOpenClawLightsailLaunchResponseMutation,
  useUpdateOpenClawLightsailLaunchResponseMutation,
  useDeleteOpenClawLightsailLaunchResponseMutation,
  useDeleteOpenClawLightsailLaunchResponseCascadeMutation,
} = OpenClawLightsailLaunchResponseService
