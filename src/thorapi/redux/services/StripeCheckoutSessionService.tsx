// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { StripeCheckoutSession } from '@thorapi/model/StripeCheckoutSession'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type StripeCheckoutSessionResponse = StripeCheckoutSession[]
type StripeCheckoutSessionPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<StripeCheckoutSession>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type StripeCheckoutSessionListQueryArg = {
  example?: Partial<StripeCheckoutSession>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toStripeCheckoutSessionList = (result: unknown): StripeCheckoutSessionResponse => {
  if (Array.isArray(result)) {
    return result as StripeCheckoutSessionResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as StripeCheckoutSessionResponse) : []
}

export const StripeCheckoutSessionService = createApi({
  reducerPath: 'StripeCheckoutSession', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['StripeCheckoutSession'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getStripeCheckoutSessionsPaged: build.query<StripeCheckoutSessionResponse, StripeCheckoutSessionPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `StripeCheckoutSession?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toStripeCheckoutSessionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeCheckoutSession' as const, id })),
          { type: 'StripeCheckoutSession', id: `PAGE_${page}` },
          { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getStripeCheckoutSessions: build.query<StripeCheckoutSessionResponse, StripeCheckoutSessionListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `StripeCheckoutSession?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `StripeCheckoutSession`;
      },
      providesTags: (result) => {
        const rows = toStripeCheckoutSessionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeCheckoutSession' as const, id })),
          { type: 'StripeCheckoutSession', id: 'LIST' },
          { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addStripeCheckoutSession: build.mutation<StripeCheckoutSession, Partial<StripeCheckoutSession>>({
      query: (body) => ({
        url: `StripeCheckoutSession`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'StripeCheckoutSession', id: 'LIST' },
        { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getStripeCheckoutSession: build.query<StripeCheckoutSession, string>({
      query: (id) => `StripeCheckoutSession/${id}`,
      providesTags: (result, error, id) => [{ type: 'StripeCheckoutSession', id }],
    }),

    // 5) Update
    updateStripeCheckoutSession: build.mutation<StripeCheckoutSession, Pick<StripeCheckoutSession, 'id'> & Partial<StripeCheckoutSession>>({
      query: ({ id, ...patch }) => ({
        url: `StripeCheckoutSession/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<StripeCheckoutSession, 'id'>) => [
        { type: 'StripeCheckoutSession', id },
        { type: 'StripeCheckoutSession', id: 'LIST' },
        { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteStripeCheckoutSession: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `StripeCheckoutSession/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'StripeCheckoutSession', id },
        { type: 'StripeCheckoutSession', id: 'LIST' },
        { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteStripeCheckoutSessionCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `StripeCheckoutSession/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'StripeCheckoutSession', id },
        { type: 'StripeCheckoutSession', id: 'LIST' },
        { type: 'StripeCheckoutSession', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetStripeCheckoutSessionsPagedQuery`
export const {
  useGetStripeCheckoutSessionsPagedQuery,     // immediate fetch
  useLazyGetStripeCheckoutSessionsPagedQuery, // lazy fetch
  useGetStripeCheckoutSessionQuery,
  useGetStripeCheckoutSessionsQuery,
  useAddStripeCheckoutSessionMutation,
  useUpdateStripeCheckoutSessionMutation,
  useDeleteStripeCheckoutSessionMutation,
  useDeleteStripeCheckoutSessionCascadeMutation,
} = StripeCheckoutSessionService
