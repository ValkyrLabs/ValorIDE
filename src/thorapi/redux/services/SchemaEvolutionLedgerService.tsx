// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SchemaEvolutionLedger } from '@thorapi/model/SchemaEvolutionLedger'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SchemaEvolutionLedgerResponse = SchemaEvolutionLedger[]
type SchemaEvolutionLedgerPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SchemaEvolutionLedger>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SchemaEvolutionLedgerListQueryArg = {
  example?: Partial<SchemaEvolutionLedger>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSchemaEvolutionLedgerList = (result: unknown): SchemaEvolutionLedgerResponse => {
  if (Array.isArray(result)) {
    return result as SchemaEvolutionLedgerResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SchemaEvolutionLedgerResponse) : []
}

export const SchemaEvolutionLedgerService = createApi({
  reducerPath: 'SchemaEvolutionLedger', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SchemaEvolutionLedger'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSchemaEvolutionLedgersPaged: build.query<SchemaEvolutionLedgerResponse, SchemaEvolutionLedgerPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SchemaEvolutionLedger?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSchemaEvolutionLedgerList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SchemaEvolutionLedger' as const, id })),
          { type: 'SchemaEvolutionLedger', id: `PAGE_${page}` },
          { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSchemaEvolutionLedgers: build.query<SchemaEvolutionLedgerResponse, SchemaEvolutionLedgerListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SchemaEvolutionLedger?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SchemaEvolutionLedger`;
      },
      providesTags: (result) => {
        const rows = toSchemaEvolutionLedgerList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SchemaEvolutionLedger' as const, id })),
          { type: 'SchemaEvolutionLedger', id: 'LIST' },
          { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSchemaEvolutionLedger: build.mutation<SchemaEvolutionLedger, Partial<SchemaEvolutionLedger>>({
      query: (body) => ({
        url: `SchemaEvolutionLedger`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SchemaEvolutionLedger', id: 'LIST' },
        { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSchemaEvolutionLedger: build.query<SchemaEvolutionLedger, string>({
      query: (id) => `SchemaEvolutionLedger/${id}`,
      providesTags: (result, error, id) => [{ type: 'SchemaEvolutionLedger', id }],
    }),

    // 5) Update
    updateSchemaEvolutionLedger: build.mutation<SchemaEvolutionLedger, Pick<SchemaEvolutionLedger, 'id'> & Partial<SchemaEvolutionLedger>>({
      query: ({ id, ...patch }) => ({
        url: `SchemaEvolutionLedger/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SchemaEvolutionLedger, 'id'>) => [
        { type: 'SchemaEvolutionLedger', id },
        { type: 'SchemaEvolutionLedger', id: 'LIST' },
        { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSchemaEvolutionLedger: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SchemaEvolutionLedger/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SchemaEvolutionLedger', id },
        { type: 'SchemaEvolutionLedger', id: 'LIST' },
        { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSchemaEvolutionLedgerCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SchemaEvolutionLedger/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SchemaEvolutionLedger', id },
        { type: 'SchemaEvolutionLedger', id: 'LIST' },
        { type: 'SchemaEvolutionLedger', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSchemaEvolutionLedgersPagedQuery`
export const {
  useGetSchemaEvolutionLedgersPagedQuery,     // immediate fetch
  useLazyGetSchemaEvolutionLedgersPagedQuery, // lazy fetch
  useGetSchemaEvolutionLedgerQuery,
  useGetSchemaEvolutionLedgersQuery,
  useAddSchemaEvolutionLedgerMutation,
  useUpdateSchemaEvolutionLedgerMutation,
  useDeleteSchemaEvolutionLedgerMutation,
  useDeleteSchemaEvolutionLedgerCascadeMutation,
} = SchemaEvolutionLedgerService
