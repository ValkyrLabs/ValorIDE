// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import { ArrayIntegerItem } from "@thorapi/model/ArrayIntegerItem";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query

type ArrayIntegerItemResponse = ArrayIntegerItem[];

const toArrayIntegerItemList = (result: unknown): ArrayIntegerItemResponse => {
  if (Array.isArray(result)) {
    return result as ArrayIntegerItemResponse;
  }

  const candidate =
    (result as any)?.content ??
    (result as any)?.items ??
    (result as any)?.results ??
    (result as any)?.data;
  return Array.isArray(candidate)
    ? (candidate as ArrayIntegerItemResponse)
    : [];
};

export const ArrayIntegerItemService = createApi({
  reducerPath: "ArrayIntegerItem", // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ["ArrayIntegerItem"],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getArrayIntegerItemsPaged: build.query<
      ArrayIntegerItemResponse,
      { page: number; size?: number; example?: Partial<ArrayIntegerItem> }
    >({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example)
          q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ArrayIntegerItem?${q.join("&")}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toArrayIntegerItemList(result);
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: "ArrayIntegerItem" as const, id })),
          { type: "ArrayIntegerItem", id: `PAGE_${page}` },
        ];
      },
    }),

    // 2) Simple "get all" Query (optional)
    getArrayIntegerItems: build.query<
      ArrayIntegerItemResponse,
      { example?: Partial<ArrayIntegerItem> } | void
    >({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ArrayIntegerItem?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ArrayIntegerItem`;
      },
      providesTags: (result) => {
        const rows = toArrayIntegerItemList(result);
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: "ArrayIntegerItem" as const, id })),
          { type: "ArrayIntegerItem", id: "LIST" },
        ];
      },
    }),

    // 3) Create
    addArrayIntegerItem: build.mutation<
      ArrayIntegerItem,
      Partial<ArrayIntegerItem>
    >({
      query: (body) => ({
        url: `ArrayIntegerItem`,
        method: "POST",
        body,
      }),
      invalidatesTags: [{ type: "ArrayIntegerItem", id: "LIST" }],
    }),

    // 4) Get single by ID
    getArrayIntegerItem: build.query<ArrayIntegerItem, string>({
      query: (id) => `ArrayIntegerItem/${id}`,
      providesTags: (result, error, id) => [{ type: "ArrayIntegerItem", id }],
    }),

    // 5) Update
    updateArrayIntegerItem: build.mutation<
      void,
      Pick<ArrayIntegerItem, "id"> & Partial<ArrayIntegerItem>
    >({
      query: ({ id, ...patch }) => ({
        url: `ArrayIntegerItem/${id}`,
        method: "PUT",
        body: patch,
      }),
      async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
        if (id) {
          const patchResult = dispatch(
            ArrayIntegerItemService.util.updateQueryData(
              "getArrayIntegerItem",
              id,
              (draft) => {
                Object.assign(draft, patch);
              },
            ),
          );
          try {
            await queryFulfilled;
          } catch {
            patchResult.undo();
          }
        }
      },
      invalidatesTags: (
        result,
        error,
        { id }: Pick<ArrayIntegerItem, "id">,
      ) => [
        { type: "ArrayIntegerItem", id },
        { type: "ArrayIntegerItem", id: "LIST" },
      ],
    }),

    // 6) Delete
    deleteArrayIntegerItem: build.mutation<
      { success: boolean; id: string },
      number
    >({
      query(id) {
        return {
          url: `ArrayIntegerItem/${id}`,
          method: "DELETE",
        };
      },
      invalidatesTags: (result, error, id) => [
        { type: "ArrayIntegerItem", id },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteArrayIntegerItemCascade: build.mutation<
      { success: boolean; id: string },
      { id: string; cascade?: boolean; trash?: boolean }
    >({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
        return {
          url: `ArrayIntegerItem/${id}?${params}`,
          method: "DELETE",
        };
      },
      invalidatesTags: (result, error, { id }) => [
        { type: "ArrayIntegerItem", id },
        { type: "ArrayIntegerItem", id: "LIST" },
      ],
    }),
  }),
});

// Notice we now also export `useLazyGetArrayIntegerItemsPagedQuery`
export const {
  useGetArrayIntegerItemsPagedQuery, // immediate fetch
  useLazyGetArrayIntegerItemsPagedQuery, // lazy fetch
  useGetArrayIntegerItemQuery,
  useGetArrayIntegerItemsQuery,
  useAddArrayIntegerItemMutation,
  useUpdateArrayIntegerItemMutation,
  useDeleteArrayIntegerItemMutation,
  useDeleteArrayIntegerItemCascadeMutation,
} = ArrayIntegerItemService;
