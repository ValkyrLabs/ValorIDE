// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-09T22:07:20.612811-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { JobSeeker } from '@thorapi/model/JobSeeker'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type JobSeekerResponse = JobSeeker[]

export const JobSeekerService = createApi({
  reducerPath: 'JobSeeker', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['JobSeeker'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getJobSeekersPaged: build.query<JobSeekerResponse, { page: number; size?: number; example?: Partial<JobSeeker> }>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `JobSeeker?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) =>
        result
          ? [
              ...result.map(({ id }) => ({ type: 'JobSeeker' as const, id })),
              { type: 'JobSeeker', id: `PAGE_${page}` },
            ]
          : [],
    }),

    // 2) Simple "get all" Query (optional)
    getJobSeekers: build.query<JobSeekerResponse, { example?: Partial<JobSeeker> } | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `JobSeeker?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `JobSeeker`;
      },
      providesTags: (result) =>
        result
          ? [
              ...result.map(({ id }) => ({ type: 'JobSeeker' as const, id })),
              { type: 'JobSeeker', id: 'LIST' },
            ]
          : [{ type: 'JobSeeker', id: 'LIST' }],
    }),

    // 3) Create
    addJobSeeker: build.mutation<JobSeeker, Partial<JobSeeker>>({
      query: (body) => ({
        url: `JobSeeker`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [{ type: 'JobSeeker', id: 'LIST' }],
    }),

    // 4) Get single by ID
    getJobSeeker: build.query<JobSeeker, string>({
      query: (id) => `JobSeeker/${id}`,
      providesTags: (result, error, id) => [{ type: 'JobSeeker', id }],
    }),

    // 5) Update
    updateJobSeeker: build.mutation<void, Pick<JobSeeker, 'id'> & Partial<JobSeeker>>({
      query: ({ id, ...patch }) => ({
        url: `JobSeeker/${id}`,
        method: 'PUT',
        body: patch,
      }),
      async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
        if (id) {
          const patchResult = dispatch(
            JobSeekerService.util.updateQueryData('getJobSeeker', id, (draft) => {
              Object.assign(draft, patch)
            })
          )
          try {
            await queryFulfilled
          } catch {
            patchResult.undo()
          }
        }
      },
      invalidatesTags: (result, error, { id }: Pick<JobSeeker, 'id'>) => [
        { type: 'JobSeeker', id },
        { type: 'JobSeeker', id: 'LIST' },
      ],
    }),

    // 6) Delete
    deleteJobSeeker: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `JobSeeker/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [{ type: 'JobSeeker', id }],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteJobSeekerCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `JobSeeker/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [{ type: 'JobSeeker', id }, { type: 'JobSeeker', id: 'LIST' }],
    }),
  }),
})

// Notice we now also export `useLazyGetJobSeekersPagedQuery`
export const {
  useGetJobSeekersPagedQuery,     // immediate fetch
  useLazyGetJobSeekersPagedQuery, // lazy fetch
  useGetJobSeekerQuery,
  useGetJobSeekersQuery,
  useAddJobSeekerMutation,
  useUpdateJobSeekerMutation,
  useDeleteJobSeekerMutation,
  useDeleteJobSeekerCascadeMutation,
} = JobSeekerService
