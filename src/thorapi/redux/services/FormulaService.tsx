// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Formula } from '@thorapi/model/Formula'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type FormulaResponse = Formula[]
type FormulaPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Formula>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type FormulaListQueryArg = {
  example?: Partial<Formula>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toFormulaList = (result: unknown): FormulaResponse => {
  if (Array.isArray(result)) {
    return result as FormulaResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as FormulaResponse) : []
}

export const FormulaService = createApi({
  reducerPath: 'Formula', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Formula'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getFormulasPaged: build.query<FormulaResponse, FormulaPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Formula?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toFormulaList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Formula' as const, id })),
          { type: 'Formula', id: `PAGE_${page}` },
          { type: 'Formula', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getFormulas: build.query<FormulaResponse, FormulaListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Formula?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Formula`;
      },
      providesTags: (result) => {
        const rows = toFormulaList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Formula' as const, id })),
          { type: 'Formula', id: 'LIST' },
          { type: 'Formula', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addFormula: build.mutation<Formula, Partial<Formula>>({
      query: (body) => ({
        url: `Formula`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Formula', id: 'LIST' },
        { type: 'Formula', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getFormula: build.query<Formula, string>({
      query: (id) => `Formula/${id}`,
      providesTags: (result, error, id) => [{ type: 'Formula', id }],
    }),

    // 5) Update
    updateFormula: build.mutation<Formula, Pick<Formula, 'id'> & Partial<Formula>>({
      query: ({ id, ...patch }) => ({
        url: `Formula/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Formula, 'id'>) => [
        { type: 'Formula', id },
        { type: 'Formula', id: 'LIST' },
        { type: 'Formula', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteFormula: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Formula/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Formula', id },
        { type: 'Formula', id: 'LIST' },
        { type: 'Formula', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteFormulaCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Formula/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Formula', id },
        { type: 'Formula', id: 'LIST' },
        { type: 'Formula', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetFormulasPagedQuery`
export const {
  useGetFormulasPagedQuery,     // immediate fetch
  useLazyGetFormulasPagedQuery, // lazy fetch
  useGetFormulaQuery,
  useGetFormulasQuery,
  useAddFormulaMutation,
  useUpdateFormulaMutation,
  useDeleteFormulaMutation,
  useDeleteFormulaCascadeMutation,
} = FormulaService
