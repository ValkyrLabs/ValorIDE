// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { HydrationPointer } from '@thorapi/model/HydrationPointer'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type HydrationPointerResponse = HydrationPointer[]
type HydrationPointerPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<HydrationPointer>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type HydrationPointerListQueryArg = {
  example?: Partial<HydrationPointer>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toHydrationPointerList = (result: unknown): HydrationPointerResponse => {
  if (Array.isArray(result)) {
    return result as HydrationPointerResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as HydrationPointerResponse) : []
}

export const HydrationPointerService = createApi({
  reducerPath: 'HydrationPointer', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['HydrationPointer'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getHydrationPointersPaged: build.query<HydrationPointerResponse, HydrationPointerPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `HydrationPointer?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toHydrationPointerList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'HydrationPointer' as const, id })),
          { type: 'HydrationPointer', id: `PAGE_${page}` },
          { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getHydrationPointers: build.query<HydrationPointerResponse, HydrationPointerListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `HydrationPointer?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `HydrationPointer`;
      },
      providesTags: (result) => {
        const rows = toHydrationPointerList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'HydrationPointer' as const, id })),
          { type: 'HydrationPointer', id: 'LIST' },
          { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addHydrationPointer: build.mutation<HydrationPointer, Partial<HydrationPointer>>({
      query: (body) => ({
        url: `HydrationPointer`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'HydrationPointer', id: 'LIST' },
        { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getHydrationPointer: build.query<HydrationPointer, string>({
      query: (id) => `HydrationPointer/${id}`,
      providesTags: (result, error, id) => [{ type: 'HydrationPointer', id }],
    }),

    // 5) Update
    updateHydrationPointer: build.mutation<HydrationPointer, Pick<HydrationPointer, 'id'> & Partial<HydrationPointer>>({
      query: ({ id, ...patch }) => ({
        url: `HydrationPointer/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<HydrationPointer, 'id'>) => [
        { type: 'HydrationPointer', id },
        { type: 'HydrationPointer', id: 'LIST' },
        { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteHydrationPointer: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `HydrationPointer/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'HydrationPointer', id },
        { type: 'HydrationPointer', id: 'LIST' },
        { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteHydrationPointerCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `HydrationPointer/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'HydrationPointer', id },
        { type: 'HydrationPointer', id: 'LIST' },
        { type: 'HydrationPointer', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetHydrationPointersPagedQuery`
export const {
  useGetHydrationPointersPagedQuery,     // immediate fetch
  useLazyGetHydrationPointersPagedQuery, // lazy fetch
  useGetHydrationPointerQuery,
  useGetHydrationPointersQuery,
  useAddHydrationPointerMutation,
  useUpdateHydrationPointerMutation,
  useDeleteHydrationPointerMutation,
  useDeleteHydrationPointerCascadeMutation,
} = HydrationPointerService
