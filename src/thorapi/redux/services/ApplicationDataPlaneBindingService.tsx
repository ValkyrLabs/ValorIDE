// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ApplicationDataPlaneBinding } from '@thorapi/model/ApplicationDataPlaneBinding'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ApplicationDataPlaneBindingResponse = ApplicationDataPlaneBinding[]
type ApplicationDataPlaneBindingPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ApplicationDataPlaneBinding>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ApplicationDataPlaneBindingListQueryArg = {
  example?: Partial<ApplicationDataPlaneBinding>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toApplicationDataPlaneBindingList = (result: unknown): ApplicationDataPlaneBindingResponse => {
  if (Array.isArray(result)) {
    return result as ApplicationDataPlaneBindingResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ApplicationDataPlaneBindingResponse) : []
}

export const ApplicationDataPlaneBindingService = createApi({
  reducerPath: 'ApplicationDataPlaneBinding', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ApplicationDataPlaneBinding'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getApplicationDataPlaneBindingsPaged: build.query<ApplicationDataPlaneBindingResponse, ApplicationDataPlaneBindingPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ApplicationDataPlaneBinding?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toApplicationDataPlaneBindingList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ApplicationDataPlaneBinding' as const, id })),
          { type: 'ApplicationDataPlaneBinding', id: `PAGE_${page}` },
          { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getApplicationDataPlaneBindings: build.query<ApplicationDataPlaneBindingResponse, ApplicationDataPlaneBindingListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ApplicationDataPlaneBinding?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ApplicationDataPlaneBinding`;
      },
      providesTags: (result) => {
        const rows = toApplicationDataPlaneBindingList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ApplicationDataPlaneBinding' as const, id })),
          { type: 'ApplicationDataPlaneBinding', id: 'LIST' },
          { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addApplicationDataPlaneBinding: build.mutation<ApplicationDataPlaneBinding, Partial<ApplicationDataPlaneBinding>>({
      query: (body) => ({
        url: `ApplicationDataPlaneBinding`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ApplicationDataPlaneBinding', id: 'LIST' },
        { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getApplicationDataPlaneBinding: build.query<ApplicationDataPlaneBinding, string>({
      query: (id) => `ApplicationDataPlaneBinding/${id}`,
      providesTags: (result, error, id) => [{ type: 'ApplicationDataPlaneBinding', id }],
    }),

    // 5) Update
    updateApplicationDataPlaneBinding: build.mutation<ApplicationDataPlaneBinding, Pick<ApplicationDataPlaneBinding, 'id'> & Partial<ApplicationDataPlaneBinding>>({
      query: ({ id, ...patch }) => ({
        url: `ApplicationDataPlaneBinding/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ApplicationDataPlaneBinding, 'id'>) => [
        { type: 'ApplicationDataPlaneBinding', id },
        { type: 'ApplicationDataPlaneBinding', id: 'LIST' },
        { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteApplicationDataPlaneBinding: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ApplicationDataPlaneBinding/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ApplicationDataPlaneBinding', id },
        { type: 'ApplicationDataPlaneBinding', id: 'LIST' },
        { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteApplicationDataPlaneBindingCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ApplicationDataPlaneBinding/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ApplicationDataPlaneBinding', id },
        { type: 'ApplicationDataPlaneBinding', id: 'LIST' },
        { type: 'ApplicationDataPlaneBinding', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetApplicationDataPlaneBindingsPagedQuery`
export const {
  useGetApplicationDataPlaneBindingsPagedQuery,     // immediate fetch
  useLazyGetApplicationDataPlaneBindingsPagedQuery, // lazy fetch
  useGetApplicationDataPlaneBindingQuery,
  useGetApplicationDataPlaneBindingsQuery,
  useAddApplicationDataPlaneBindingMutation,
  useUpdateApplicationDataPlaneBindingMutation,
  useDeleteApplicationDataPlaneBindingMutation,
  useDeleteApplicationDataPlaneBindingCascadeMutation,
} = ApplicationDataPlaneBindingService
