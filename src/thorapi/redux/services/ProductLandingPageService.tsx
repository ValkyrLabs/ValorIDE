// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ProductLandingPage } from '@thorapi/model/ProductLandingPage'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ProductLandingPageResponse = ProductLandingPage[]
type ProductLandingPagePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ProductLandingPage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ProductLandingPageListQueryArg = {
  example?: Partial<ProductLandingPage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toProductLandingPageList = (result: unknown): ProductLandingPageResponse => {
  if (Array.isArray(result)) {
    return result as ProductLandingPageResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ProductLandingPageResponse) : []
}

export const ProductLandingPageService = createApi({
  reducerPath: 'ProductLandingPage', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ProductLandingPage'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getProductLandingPagesPaged: build.query<ProductLandingPageResponse, ProductLandingPagePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ProductLandingPage?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toProductLandingPageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ProductLandingPage' as const, id })),
          { type: 'ProductLandingPage', id: `PAGE_${page}` },
          { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getProductLandingPages: build.query<ProductLandingPageResponse, ProductLandingPageListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ProductLandingPage?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ProductLandingPage`;
      },
      providesTags: (result) => {
        const rows = toProductLandingPageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ProductLandingPage' as const, id })),
          { type: 'ProductLandingPage', id: 'LIST' },
          { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addProductLandingPage: build.mutation<ProductLandingPage, Partial<ProductLandingPage>>({
      query: (body) => ({
        url: `ProductLandingPage`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ProductLandingPage', id: 'LIST' },
        { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getProductLandingPage: build.query<ProductLandingPage, string>({
      query: (id) => `ProductLandingPage/${id}`,
      providesTags: (result, error, id) => [{ type: 'ProductLandingPage', id }],
    }),

    // 5) Update
    updateProductLandingPage: build.mutation<ProductLandingPage, Pick<ProductLandingPage, 'id'> & Partial<ProductLandingPage>>({
      query: ({ id, ...patch }) => ({
        url: `ProductLandingPage/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ProductLandingPage, 'id'>) => [
        { type: 'ProductLandingPage', id },
        { type: 'ProductLandingPage', id: 'LIST' },
        { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteProductLandingPage: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ProductLandingPage/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ProductLandingPage', id },
        { type: 'ProductLandingPage', id: 'LIST' },
        { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteProductLandingPageCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ProductLandingPage/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ProductLandingPage', id },
        { type: 'ProductLandingPage', id: 'LIST' },
        { type: 'ProductLandingPage', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetProductLandingPagesPagedQuery`
export const {
  useGetProductLandingPagesPagedQuery,     // immediate fetch
  useLazyGetProductLandingPagesPagedQuery, // lazy fetch
  useGetProductLandingPageQuery,
  useGetProductLandingPagesQuery,
  useAddProductLandingPageMutation,
  useUpdateProductLandingPageMutation,
  useDeleteProductLandingPageMutation,
  useDeleteProductLandingPageCascadeMutation,
} = ProductLandingPageService
