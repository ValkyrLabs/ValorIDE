// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { WorkforceTransitionRecord } from '@thorapi/model/WorkforceTransitionRecord'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type WorkforceTransitionRecordResponse = WorkforceTransitionRecord[]
type WorkforceTransitionRecordPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<WorkforceTransitionRecord>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type WorkforceTransitionRecordListQueryArg = {
  example?: Partial<WorkforceTransitionRecord>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toWorkforceTransitionRecordList = (result: unknown): WorkforceTransitionRecordResponse => {
  if (Array.isArray(result)) {
    return result as WorkforceTransitionRecordResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as WorkforceTransitionRecordResponse) : []
}

export const WorkforceTransitionRecordService = createApi({
  reducerPath: 'WorkforceTransitionRecord', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['WorkforceTransitionRecord'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getWorkforceTransitionRecordsPaged: build.query<WorkforceTransitionRecordResponse, WorkforceTransitionRecordPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `WorkforceTransitionRecord?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toWorkforceTransitionRecordList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'WorkforceTransitionRecord' as const, id })),
          { type: 'WorkforceTransitionRecord', id: `PAGE_${page}` },
          { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getWorkforceTransitionRecords: build.query<WorkforceTransitionRecordResponse, WorkforceTransitionRecordListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `WorkforceTransitionRecord?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `WorkforceTransitionRecord`;
      },
      providesTags: (result) => {
        const rows = toWorkforceTransitionRecordList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'WorkforceTransitionRecord' as const, id })),
          { type: 'WorkforceTransitionRecord', id: 'LIST' },
          { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addWorkforceTransitionRecord: build.mutation<WorkforceTransitionRecord, Partial<WorkforceTransitionRecord>>({
      query: (body) => ({
        url: `WorkforceTransitionRecord`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'WorkforceTransitionRecord', id: 'LIST' },
        { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getWorkforceTransitionRecord: build.query<WorkforceTransitionRecord, string>({
      query: (id) => `WorkforceTransitionRecord/${id}`,
      providesTags: (result, error, id) => [{ type: 'WorkforceTransitionRecord', id }],
    }),

    // 5) Update
    updateWorkforceTransitionRecord: build.mutation<WorkforceTransitionRecord, Pick<WorkforceTransitionRecord, 'id'> & Partial<WorkforceTransitionRecord>>({
      query: ({ id, ...patch }) => ({
        url: `WorkforceTransitionRecord/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<WorkforceTransitionRecord, 'id'>) => [
        { type: 'WorkforceTransitionRecord', id },
        { type: 'WorkforceTransitionRecord', id: 'LIST' },
        { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteWorkforceTransitionRecord: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `WorkforceTransitionRecord/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'WorkforceTransitionRecord', id },
        { type: 'WorkforceTransitionRecord', id: 'LIST' },
        { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteWorkforceTransitionRecordCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `WorkforceTransitionRecord/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'WorkforceTransitionRecord', id },
        { type: 'WorkforceTransitionRecord', id: 'LIST' },
        { type: 'WorkforceTransitionRecord', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetWorkforceTransitionRecordsPagedQuery`
export const {
  useGetWorkforceTransitionRecordsPagedQuery,     // immediate fetch
  useLazyGetWorkforceTransitionRecordsPagedQuery, // lazy fetch
  useGetWorkforceTransitionRecordQuery,
  useGetWorkforceTransitionRecordsQuery,
  useAddWorkforceTransitionRecordMutation,
  useUpdateWorkforceTransitionRecordMutation,
  useDeleteWorkforceTransitionRecordMutation,
  useDeleteWorkforceTransitionRecordCascadeMutation,
} = WorkforceTransitionRecordService
