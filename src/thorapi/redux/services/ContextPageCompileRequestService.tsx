// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageCompileRequest } from '@thorapi/model/ContextPageCompileRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageCompileRequestResponse = ContextPageCompileRequest[]
type ContextPageCompileRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageCompileRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageCompileRequestListQueryArg = {
  example?: Partial<ContextPageCompileRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageCompileRequestList = (result: unknown): ContextPageCompileRequestResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageCompileRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageCompileRequestResponse) : []
}

export const ContextPageCompileRequestService = createApi({
  reducerPath: 'ContextPageCompileRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageCompileRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageCompileRequestsPaged: build.query<ContextPageCompileRequestResponse, ContextPageCompileRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageCompileRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageCompileRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageCompileRequest' as const, id })),
          { type: 'ContextPageCompileRequest', id: `PAGE_${page}` },
          { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageCompileRequests: build.query<ContextPageCompileRequestResponse, ContextPageCompileRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageCompileRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageCompileRequest`;
      },
      providesTags: (result) => {
        const rows = toContextPageCompileRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageCompileRequest' as const, id })),
          { type: 'ContextPageCompileRequest', id: 'LIST' },
          { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageCompileRequest: build.mutation<ContextPageCompileRequest, Partial<ContextPageCompileRequest>>({
      query: (body) => ({
        url: `ContextPageCompileRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageCompileRequest', id: 'LIST' },
        { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageCompileRequest: build.query<ContextPageCompileRequest, string>({
      query: (id) => `ContextPageCompileRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageCompileRequest', id }],
    }),

    // 5) Update
    updateContextPageCompileRequest: build.mutation<ContextPageCompileRequest, Pick<ContextPageCompileRequest, 'id'> & Partial<ContextPageCompileRequest>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageCompileRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageCompileRequest, 'id'>) => [
        { type: 'ContextPageCompileRequest', id },
        { type: 'ContextPageCompileRequest', id: 'LIST' },
        { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageCompileRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageCompileRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageCompileRequest', id },
        { type: 'ContextPageCompileRequest', id: 'LIST' },
        { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageCompileRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageCompileRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageCompileRequest', id },
        { type: 'ContextPageCompileRequest', id: 'LIST' },
        { type: 'ContextPageCompileRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageCompileRequestsPagedQuery`
export const {
  useGetContextPageCompileRequestsPagedQuery,     // immediate fetch
  useLazyGetContextPageCompileRequestsPagedQuery, // lazy fetch
  useGetContextPageCompileRequestQuery,
  useGetContextPageCompileRequestsQuery,
  useAddContextPageCompileRequestMutation,
  useUpdateContextPageCompileRequestMutation,
  useDeleteContextPageCompileRequestMutation,
  useDeleteContextPageCompileRequestCascadeMutation,
} = ContextPageCompileRequestService
