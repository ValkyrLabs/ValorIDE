// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SkillOptRouteResponse } from '@thorapi/model/SkillOptRouteResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SkillOptRouteResponseResponse = SkillOptRouteResponse[]
type SkillOptRouteResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SkillOptRouteResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SkillOptRouteResponseListQueryArg = {
  example?: Partial<SkillOptRouteResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSkillOptRouteResponseList = (result: unknown): SkillOptRouteResponseResponse => {
  if (Array.isArray(result)) {
    return result as SkillOptRouteResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SkillOptRouteResponseResponse) : []
}

export const SkillOptRouteResponseService = createApi({
  reducerPath: 'SkillOptRouteResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SkillOptRouteResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSkillOptRouteResponsesPaged: build.query<SkillOptRouteResponseResponse, SkillOptRouteResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SkillOptRouteResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSkillOptRouteResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteResponse' as const, id })),
          { type: 'SkillOptRouteResponse', id: `PAGE_${page}` },
          { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSkillOptRouteResponses: build.query<SkillOptRouteResponseResponse, SkillOptRouteResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SkillOptRouteResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SkillOptRouteResponse`;
      },
      providesTags: (result) => {
        const rows = toSkillOptRouteResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteResponse' as const, id })),
          { type: 'SkillOptRouteResponse', id: 'LIST' },
          { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSkillOptRouteResponse: build.mutation<SkillOptRouteResponse, Partial<SkillOptRouteResponse>>({
      query: (body) => ({
        url: `SkillOptRouteResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SkillOptRouteResponse', id: 'LIST' },
        { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSkillOptRouteResponse: build.query<SkillOptRouteResponse, string>({
      query: (id) => `SkillOptRouteResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'SkillOptRouteResponse', id }],
    }),

    // 5) Update
    updateSkillOptRouteResponse: build.mutation<SkillOptRouteResponse, Pick<SkillOptRouteResponse, 'id'> & Partial<SkillOptRouteResponse>>({
      query: ({ id, ...patch }) => ({
        url: `SkillOptRouteResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SkillOptRouteResponse, 'id'>) => [
        { type: 'SkillOptRouteResponse', id },
        { type: 'SkillOptRouteResponse', id: 'LIST' },
        { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSkillOptRouteResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SkillOptRouteResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SkillOptRouteResponse', id },
        { type: 'SkillOptRouteResponse', id: 'LIST' },
        { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSkillOptRouteResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SkillOptRouteResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SkillOptRouteResponse', id },
        { type: 'SkillOptRouteResponse', id: 'LIST' },
        { type: 'SkillOptRouteResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSkillOptRouteResponsesPagedQuery`
export const {
  useGetSkillOptRouteResponsesPagedQuery,     // immediate fetch
  useLazyGetSkillOptRouteResponsesPagedQuery, // lazy fetch
  useGetSkillOptRouteResponseQuery,
  useGetSkillOptRouteResponsesQuery,
  useAddSkillOptRouteResponseMutation,
  useUpdateSkillOptRouteResponseMutation,
  useDeleteSkillOptRouteResponseMutation,
  useDeleteSkillOptRouteResponseCascadeMutation,
} = SkillOptRouteResponseService
