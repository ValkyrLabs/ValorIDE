// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalProvenance } from '@thorapi/model/RetrievalProvenance'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalProvenanceResponse = RetrievalProvenance[]
type RetrievalProvenancePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalProvenance>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalProvenanceListQueryArg = {
  example?: Partial<RetrievalProvenance>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalProvenanceList = (result: unknown): RetrievalProvenanceResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalProvenanceResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalProvenanceResponse) : []
}

export const RetrievalProvenanceService = createApi({
  reducerPath: 'RetrievalProvenance', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalProvenance'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalProvenancesPaged: build.query<RetrievalProvenanceResponse, RetrievalProvenancePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalProvenance?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalProvenanceList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalProvenance' as const, id })),
          { type: 'RetrievalProvenance', id: `PAGE_${page}` },
          { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalProvenances: build.query<RetrievalProvenanceResponse, RetrievalProvenanceListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalProvenance?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalProvenance`;
      },
      providesTags: (result) => {
        const rows = toRetrievalProvenanceList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalProvenance' as const, id })),
          { type: 'RetrievalProvenance', id: 'LIST' },
          { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalProvenance: build.mutation<RetrievalProvenance, Partial<RetrievalProvenance>>({
      query: (body) => ({
        url: `RetrievalProvenance`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalProvenance', id: 'LIST' },
        { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalProvenance: build.query<RetrievalProvenance, string>({
      query: (id) => `RetrievalProvenance/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalProvenance', id }],
    }),

    // 5) Update
    updateRetrievalProvenance: build.mutation<RetrievalProvenance, Pick<RetrievalProvenance, 'id'> & Partial<RetrievalProvenance>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalProvenance/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalProvenance, 'id'>) => [
        { type: 'RetrievalProvenance', id },
        { type: 'RetrievalProvenance', id: 'LIST' },
        { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalProvenance: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalProvenance/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalProvenance', id },
        { type: 'RetrievalProvenance', id: 'LIST' },
        { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalProvenanceCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalProvenance/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalProvenance', id },
        { type: 'RetrievalProvenance', id: 'LIST' },
        { type: 'RetrievalProvenance', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalProvenancesPagedQuery`
export const {
  useGetRetrievalProvenancesPagedQuery,     // immediate fetch
  useLazyGetRetrievalProvenancesPagedQuery, // lazy fetch
  useGetRetrievalProvenanceQuery,
  useGetRetrievalProvenancesQuery,
  useAddRetrievalProvenanceMutation,
  useUpdateRetrievalProvenanceMutation,
  useDeleteRetrievalProvenanceMutation,
  useDeleteRetrievalProvenanceCascadeMutation,
} = RetrievalProvenanceService
