// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { TenantSchemaBundle } from '@thorapi/model/TenantSchemaBundle'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type TenantSchemaBundleResponse = TenantSchemaBundle[]
type TenantSchemaBundlePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<TenantSchemaBundle>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type TenantSchemaBundleListQueryArg = {
  example?: Partial<TenantSchemaBundle>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toTenantSchemaBundleList = (result: unknown): TenantSchemaBundleResponse => {
  if (Array.isArray(result)) {
    return result as TenantSchemaBundleResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as TenantSchemaBundleResponse) : []
}

export const TenantSchemaBundleService = createApi({
  reducerPath: 'TenantSchemaBundle', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['TenantSchemaBundle'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getTenantSchemaBundlesPaged: build.query<TenantSchemaBundleResponse, TenantSchemaBundlePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `TenantSchemaBundle?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toTenantSchemaBundleList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaBundle' as const, id })),
          { type: 'TenantSchemaBundle', id: `PAGE_${page}` },
          { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getTenantSchemaBundles: build.query<TenantSchemaBundleResponse, TenantSchemaBundleListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `TenantSchemaBundle?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `TenantSchemaBundle`;
      },
      providesTags: (result) => {
        const rows = toTenantSchemaBundleList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaBundle' as const, id })),
          { type: 'TenantSchemaBundle', id: 'LIST' },
          { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addTenantSchemaBundle: build.mutation<TenantSchemaBundle, Partial<TenantSchemaBundle>>({
      query: (body) => ({
        url: `TenantSchemaBundle`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'TenantSchemaBundle', id: 'LIST' },
        { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getTenantSchemaBundle: build.query<TenantSchemaBundle, string>({
      query: (id) => `TenantSchemaBundle/${id}`,
      providesTags: (result, error, id) => [{ type: 'TenantSchemaBundle', id }],
    }),

    // 5) Update
    updateTenantSchemaBundle: build.mutation<TenantSchemaBundle, Pick<TenantSchemaBundle, 'id'> & Partial<TenantSchemaBundle>>({
      query: ({ id, ...patch }) => ({
        url: `TenantSchemaBundle/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<TenantSchemaBundle, 'id'>) => [
        { type: 'TenantSchemaBundle', id },
        { type: 'TenantSchemaBundle', id: 'LIST' },
        { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteTenantSchemaBundle: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `TenantSchemaBundle/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'TenantSchemaBundle', id },
        { type: 'TenantSchemaBundle', id: 'LIST' },
        { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteTenantSchemaBundleCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `TenantSchemaBundle/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'TenantSchemaBundle', id },
        { type: 'TenantSchemaBundle', id: 'LIST' },
        { type: 'TenantSchemaBundle', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetTenantSchemaBundlesPagedQuery`
export const {
  useGetTenantSchemaBundlesPagedQuery,     // immediate fetch
  useLazyGetTenantSchemaBundlesPagedQuery, // lazy fetch
  useGetTenantSchemaBundleQuery,
  useGetTenantSchemaBundlesQuery,
  useAddTenantSchemaBundleMutation,
  useUpdateTenantSchemaBundleMutation,
  useDeleteTenantSchemaBundleMutation,
  useDeleteTenantSchemaBundleCascadeMutation,
} = TenantSchemaBundleService
