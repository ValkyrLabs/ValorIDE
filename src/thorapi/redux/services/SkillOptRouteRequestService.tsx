// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SkillOptRouteRequest } from '@thorapi/model/SkillOptRouteRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SkillOptRouteRequestResponse = SkillOptRouteRequest[]
type SkillOptRouteRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SkillOptRouteRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SkillOptRouteRequestListQueryArg = {
  example?: Partial<SkillOptRouteRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSkillOptRouteRequestList = (result: unknown): SkillOptRouteRequestResponse => {
  if (Array.isArray(result)) {
    return result as SkillOptRouteRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SkillOptRouteRequestResponse) : []
}

export const SkillOptRouteRequestService = createApi({
  reducerPath: 'SkillOptRouteRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SkillOptRouteRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSkillOptRouteRequestsPaged: build.query<SkillOptRouteRequestResponse, SkillOptRouteRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SkillOptRouteRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSkillOptRouteRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteRequest' as const, id })),
          { type: 'SkillOptRouteRequest', id: `PAGE_${page}` },
          { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSkillOptRouteRequests: build.query<SkillOptRouteRequestResponse, SkillOptRouteRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SkillOptRouteRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SkillOptRouteRequest`;
      },
      providesTags: (result) => {
        const rows = toSkillOptRouteRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteRequest' as const, id })),
          { type: 'SkillOptRouteRequest', id: 'LIST' },
          { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSkillOptRouteRequest: build.mutation<SkillOptRouteRequest, Partial<SkillOptRouteRequest>>({
      query: (body) => ({
        url: `SkillOptRouteRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SkillOptRouteRequest', id: 'LIST' },
        { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSkillOptRouteRequest: build.query<SkillOptRouteRequest, string>({
      query: (id) => `SkillOptRouteRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'SkillOptRouteRequest', id }],
    }),

    // 5) Update
    updateSkillOptRouteRequest: build.mutation<SkillOptRouteRequest, Pick<SkillOptRouteRequest, 'id'> & Partial<SkillOptRouteRequest>>({
      query: ({ id, ...patch }) => ({
        url: `SkillOptRouteRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SkillOptRouteRequest, 'id'>) => [
        { type: 'SkillOptRouteRequest', id },
        { type: 'SkillOptRouteRequest', id: 'LIST' },
        { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSkillOptRouteRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SkillOptRouteRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SkillOptRouteRequest', id },
        { type: 'SkillOptRouteRequest', id: 'LIST' },
        { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSkillOptRouteRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SkillOptRouteRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SkillOptRouteRequest', id },
        { type: 'SkillOptRouteRequest', id: 'LIST' },
        { type: 'SkillOptRouteRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSkillOptRouteRequestsPagedQuery`
export const {
  useGetSkillOptRouteRequestsPagedQuery,     // immediate fetch
  useLazyGetSkillOptRouteRequestsPagedQuery, // lazy fetch
  useGetSkillOptRouteRequestQuery,
  useGetSkillOptRouteRequestsQuery,
  useAddSkillOptRouteRequestMutation,
  useUpdateSkillOptRouteRequestMutation,
  useDeleteSkillOptRouteRequestMutation,
  useDeleteSkillOptRouteRequestCascadeMutation,
} = SkillOptRouteRequestService
