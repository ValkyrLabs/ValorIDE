// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ServiceSubscription } from '@thorapi/model/ServiceSubscription'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ServiceSubscriptionResponse = ServiceSubscription[]

const toServiceSubscriptionList = (result: unknown): ServiceSubscriptionResponse => {
  if (Array.isArray(result)) {
    return result as ServiceSubscriptionResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ServiceSubscriptionResponse) : []
}

export const ServiceSubscriptionService = createApi({
  reducerPath: 'ServiceSubscription', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ServiceSubscription'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getServiceSubscriptionsPaged: build.query<ServiceSubscriptionResponse, { page: number; size?: number; example?: Partial<ServiceSubscription> }>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ServiceSubscription?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toServiceSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ServiceSubscription' as const, id })),
          { type: 'ServiceSubscription', id: `PAGE_${page}` },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getServiceSubscriptions: build.query<ServiceSubscriptionResponse, { example?: Partial<ServiceSubscription> } | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ServiceSubscription?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ServiceSubscription`;
      },
      providesTags: (result) => {
        const rows = toServiceSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ServiceSubscription' as const, id })),
          { type: 'ServiceSubscription', id: 'LIST' },
        ]
      },
    }),

    // 3) Create
    addServiceSubscription: build.mutation<ServiceSubscription, Partial<ServiceSubscription>>({
      query: (body) => ({
        url: `ServiceSubscription`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [{ type: 'ServiceSubscription', id: 'LIST' }],
    }),

    // 4) Get single by ID
    getServiceSubscription: build.query<ServiceSubscription, string>({
      query: (id) => `ServiceSubscription/${id}`,
      providesTags: (result, error, id) => [{ type: 'ServiceSubscription', id }],
    }),

    // 5) Update
    updateServiceSubscription: build.mutation<void, Pick<ServiceSubscription, 'id'> & Partial<ServiceSubscription>>({
      query: ({ id, ...patch }) => ({
        url: `ServiceSubscription/${id}`,
        method: 'PUT',
        body: patch,
      }),
      async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
        if (id) {
          const patchResult = dispatch(
            ServiceSubscriptionService.util.updateQueryData('getServiceSubscription', id, (draft) => {
              Object.assign(draft, patch)
            })
          )
          try {
            await queryFulfilled
          } catch {
            patchResult.undo()
          }
        }
      },
      invalidatesTags: (result, error, { id }: Pick<ServiceSubscription, 'id'>) => [
        { type: 'ServiceSubscription', id },
        { type: 'ServiceSubscription', id: 'LIST' },
      ],
    }),

    // 6) Delete
    deleteServiceSubscription: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ServiceSubscription/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [{ type: 'ServiceSubscription', id }],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteServiceSubscriptionCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ServiceSubscription/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [{ type: 'ServiceSubscription', id }, { type: 'ServiceSubscription', id: 'LIST' }],
    }),
  }),
})

// Notice we now also export `useLazyGetServiceSubscriptionsPagedQuery`
export const {
  useGetServiceSubscriptionsPagedQuery,     // immediate fetch
  useLazyGetServiceSubscriptionsPagedQuery, // lazy fetch
  useGetServiceSubscriptionQuery,
  useGetServiceSubscriptionsQuery,
  useAddServiceSubscriptionMutation,
  useUpdateServiceSubscriptionMutation,
  useDeleteServiceSubscriptionMutation,
  useDeleteServiceSubscriptionCascadeMutation,
} = ServiceSubscriptionService
