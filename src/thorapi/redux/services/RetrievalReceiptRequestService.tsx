// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalReceiptRequest } from '@thorapi/model/RetrievalReceiptRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalReceiptRequestResponse = RetrievalReceiptRequest[]
type RetrievalReceiptRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalReceiptRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalReceiptRequestListQueryArg = {
  example?: Partial<RetrievalReceiptRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalReceiptRequestList = (result: unknown): RetrievalReceiptRequestResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalReceiptRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalReceiptRequestResponse) : []
}

export const RetrievalReceiptRequestService = createApi({
  reducerPath: 'RetrievalReceiptRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalReceiptRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalReceiptRequestsPaged: build.query<RetrievalReceiptRequestResponse, RetrievalReceiptRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalReceiptRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalReceiptRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptRequest' as const, id })),
          { type: 'RetrievalReceiptRequest', id: `PAGE_${page}` },
          { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalReceiptRequests: build.query<RetrievalReceiptRequestResponse, RetrievalReceiptRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalReceiptRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalReceiptRequest`;
      },
      providesTags: (result) => {
        const rows = toRetrievalReceiptRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptRequest' as const, id })),
          { type: 'RetrievalReceiptRequest', id: 'LIST' },
          { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalReceiptRequest: build.mutation<RetrievalReceiptRequest, Partial<RetrievalReceiptRequest>>({
      query: (body) => ({
        url: `RetrievalReceiptRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalReceiptRequest', id: 'LIST' },
        { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalReceiptRequest: build.query<RetrievalReceiptRequest, string>({
      query: (id) => `RetrievalReceiptRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalReceiptRequest', id }],
    }),

    // 5) Update
    updateRetrievalReceiptRequest: build.mutation<RetrievalReceiptRequest, Pick<RetrievalReceiptRequest, 'id'> & Partial<RetrievalReceiptRequest>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalReceiptRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalReceiptRequest, 'id'>) => [
        { type: 'RetrievalReceiptRequest', id },
        { type: 'RetrievalReceiptRequest', id: 'LIST' },
        { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalReceiptRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalReceiptRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalReceiptRequest', id },
        { type: 'RetrievalReceiptRequest', id: 'LIST' },
        { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalReceiptRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalReceiptRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalReceiptRequest', id },
        { type: 'RetrievalReceiptRequest', id: 'LIST' },
        { type: 'RetrievalReceiptRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalReceiptRequestsPagedQuery`
export const {
  useGetRetrievalReceiptRequestsPagedQuery,     // immediate fetch
  useLazyGetRetrievalReceiptRequestsPagedQuery, // lazy fetch
  useGetRetrievalReceiptRequestQuery,
  useGetRetrievalReceiptRequestsQuery,
  useAddRetrievalReceiptRequestMutation,
  useUpdateRetrievalReceiptRequestMutation,
  useDeleteRetrievalReceiptRequestMutation,
  useDeleteRetrievalReceiptRequestCascadeMutation,
} = RetrievalReceiptRequestService
