// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { TicketComment } from '@thorapi/model/TicketComment'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type TicketCommentResponse = TicketComment[]
type TicketCommentPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<TicketComment>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type TicketCommentListQueryArg = {
  example?: Partial<TicketComment>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toTicketCommentList = (result: unknown): TicketCommentResponse => {
  if (Array.isArray(result)) {
    return result as TicketCommentResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as TicketCommentResponse) : []
}

export const TicketCommentService = createApi({
  reducerPath: 'TicketComment', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['TicketComment'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getTicketCommentsPaged: build.query<TicketCommentResponse, TicketCommentPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `TicketComment?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toTicketCommentList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TicketComment' as const, id })),
          { type: 'TicketComment', id: `PAGE_${page}` },
          { type: 'TicketComment', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getTicketComments: build.query<TicketCommentResponse, TicketCommentListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `TicketComment?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `TicketComment`;
      },
      providesTags: (result) => {
        const rows = toTicketCommentList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TicketComment' as const, id })),
          { type: 'TicketComment', id: 'LIST' },
          { type: 'TicketComment', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addTicketComment: build.mutation<TicketComment, Partial<TicketComment>>({
      query: (body) => ({
        url: `TicketComment`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'TicketComment', id: 'LIST' },
        { type: 'TicketComment', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getTicketComment: build.query<TicketComment, string>({
      query: (id) => `TicketComment/${id}`,
      providesTags: (result, error, id) => [{ type: 'TicketComment', id }],
    }),

    // 5) Update
    updateTicketComment: build.mutation<TicketComment, Pick<TicketComment, 'id'> & Partial<TicketComment>>({
      query: ({ id, ...patch }) => ({
        url: `TicketComment/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<TicketComment, 'id'>) => [
        { type: 'TicketComment', id },
        { type: 'TicketComment', id: 'LIST' },
        { type: 'TicketComment', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteTicketComment: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `TicketComment/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'TicketComment', id },
        { type: 'TicketComment', id: 'LIST' },
        { type: 'TicketComment', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteTicketCommentCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `TicketComment/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'TicketComment', id },
        { type: 'TicketComment', id: 'LIST' },
        { type: 'TicketComment', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetTicketCommentsPagedQuery`
export const {
  useGetTicketCommentsPagedQuery,     // immediate fetch
  useLazyGetTicketCommentsPagedQuery, // lazy fetch
  useGetTicketCommentQuery,
  useGetTicketCommentsQuery,
  useAddTicketCommentMutation,
  useUpdateTicketCommentMutation,
  useDeleteTicketCommentMutation,
  useDeleteTicketCommentCascadeMutation,
} = TicketCommentService
