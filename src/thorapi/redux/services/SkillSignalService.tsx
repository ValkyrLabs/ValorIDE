// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SkillSignal } from '@thorapi/model/SkillSignal'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SkillSignalResponse = SkillSignal[]
type SkillSignalPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SkillSignal>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SkillSignalListQueryArg = {
  example?: Partial<SkillSignal>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSkillSignalList = (result: unknown): SkillSignalResponse => {
  if (Array.isArray(result)) {
    return result as SkillSignalResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SkillSignalResponse) : []
}

export const SkillSignalService = createApi({
  reducerPath: 'SkillSignal', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SkillSignal'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSkillSignalsPaged: build.query<SkillSignalResponse, SkillSignalPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SkillSignal?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSkillSignalList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillSignal' as const, id })),
          { type: 'SkillSignal', id: `PAGE_${page}` },
          { type: 'SkillSignal', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSkillSignals: build.query<SkillSignalResponse, SkillSignalListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SkillSignal?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SkillSignal`;
      },
      providesTags: (result) => {
        const rows = toSkillSignalList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillSignal' as const, id })),
          { type: 'SkillSignal', id: 'LIST' },
          { type: 'SkillSignal', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSkillSignal: build.mutation<SkillSignal, Partial<SkillSignal>>({
      query: (body) => ({
        url: `SkillSignal`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SkillSignal', id: 'LIST' },
        { type: 'SkillSignal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSkillSignal: build.query<SkillSignal, string>({
      query: (id) => `SkillSignal/${id}`,
      providesTags: (result, error, id) => [{ type: 'SkillSignal', id }],
    }),

    // 5) Update
    updateSkillSignal: build.mutation<SkillSignal, Pick<SkillSignal, 'id'> & Partial<SkillSignal>>({
      query: ({ id, ...patch }) => ({
        url: `SkillSignal/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SkillSignal, 'id'>) => [
        { type: 'SkillSignal', id },
        { type: 'SkillSignal', id: 'LIST' },
        { type: 'SkillSignal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSkillSignal: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SkillSignal/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SkillSignal', id },
        { type: 'SkillSignal', id: 'LIST' },
        { type: 'SkillSignal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSkillSignalCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SkillSignal/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SkillSignal', id },
        { type: 'SkillSignal', id: 'LIST' },
        { type: 'SkillSignal', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSkillSignalsPagedQuery`
export const {
  useGetSkillSignalsPagedQuery,     // immediate fetch
  useLazyGetSkillSignalsPagedQuery, // lazy fetch
  useGetSkillSignalQuery,
  useGetSkillSignalsQuery,
  useAddSkillSignalMutation,
  useUpdateSkillSignalMutation,
  useDeleteSkillSignalMutation,
  useDeleteSkillSignalCascadeMutation,
} = SkillSignalService
