// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageRecompressRequest } from '@thorapi/model/ContextPageRecompressRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageRecompressRequestResponse = ContextPageRecompressRequest[]
type ContextPageRecompressRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageRecompressRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageRecompressRequestListQueryArg = {
  example?: Partial<ContextPageRecompressRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageRecompressRequestList = (result: unknown): ContextPageRecompressRequestResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageRecompressRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageRecompressRequestResponse) : []
}

export const ContextPageRecompressRequestService = createApi({
  reducerPath: 'ContextPageRecompressRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageRecompressRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageRecompressRequestsPaged: build.query<ContextPageRecompressRequestResponse, ContextPageRecompressRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageRecompressRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageRecompressRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageRecompressRequest' as const, id })),
          { type: 'ContextPageRecompressRequest', id: `PAGE_${page}` },
          { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageRecompressRequests: build.query<ContextPageRecompressRequestResponse, ContextPageRecompressRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageRecompressRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageRecompressRequest`;
      },
      providesTags: (result) => {
        const rows = toContextPageRecompressRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageRecompressRequest' as const, id })),
          { type: 'ContextPageRecompressRequest', id: 'LIST' },
          { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageRecompressRequest: build.mutation<ContextPageRecompressRequest, Partial<ContextPageRecompressRequest>>({
      query: (body) => ({
        url: `ContextPageRecompressRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageRecompressRequest', id: 'LIST' },
        { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageRecompressRequest: build.query<ContextPageRecompressRequest, string>({
      query: (id) => `ContextPageRecompressRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageRecompressRequest', id }],
    }),

    // 5) Update
    updateContextPageRecompressRequest: build.mutation<ContextPageRecompressRequest, Pick<ContextPageRecompressRequest, 'id'> & Partial<ContextPageRecompressRequest>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageRecompressRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageRecompressRequest, 'id'>) => [
        { type: 'ContextPageRecompressRequest', id },
        { type: 'ContextPageRecompressRequest', id: 'LIST' },
        { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageRecompressRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageRecompressRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageRecompressRequest', id },
        { type: 'ContextPageRecompressRequest', id: 'LIST' },
        { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageRecompressRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageRecompressRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageRecompressRequest', id },
        { type: 'ContextPageRecompressRequest', id: 'LIST' },
        { type: 'ContextPageRecompressRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageRecompressRequestsPagedQuery`
export const {
  useGetContextPageRecompressRequestsPagedQuery,     // immediate fetch
  useLazyGetContextPageRecompressRequestsPagedQuery, // lazy fetch
  useGetContextPageRecompressRequestQuery,
  useGetContextPageRecompressRequestsQuery,
  useAddContextPageRecompressRequestMutation,
  useUpdateContextPageRecompressRequestMutation,
  useDeleteContextPageRecompressRequestMutation,
  useDeleteContextPageRecompressRequestCascadeMutation,
} = ContextPageRecompressRequestService
