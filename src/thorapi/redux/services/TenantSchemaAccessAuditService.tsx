// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { TenantSchemaAccessAudit } from '@thorapi/model/TenantSchemaAccessAudit'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type TenantSchemaAccessAuditResponse = TenantSchemaAccessAudit[]
type TenantSchemaAccessAuditPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<TenantSchemaAccessAudit>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type TenantSchemaAccessAuditListQueryArg = {
  example?: Partial<TenantSchemaAccessAudit>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toTenantSchemaAccessAuditList = (result: unknown): TenantSchemaAccessAuditResponse => {
  if (Array.isArray(result)) {
    return result as TenantSchemaAccessAuditResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as TenantSchemaAccessAuditResponse) : []
}

export const TenantSchemaAccessAuditService = createApi({
  reducerPath: 'TenantSchemaAccessAudit', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['TenantSchemaAccessAudit'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getTenantSchemaAccessAuditsPaged: build.query<TenantSchemaAccessAuditResponse, TenantSchemaAccessAuditPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `TenantSchemaAccessAudit?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toTenantSchemaAccessAuditList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaAccessAudit' as const, id })),
          { type: 'TenantSchemaAccessAudit', id: `PAGE_${page}` },
          { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getTenantSchemaAccessAudits: build.query<TenantSchemaAccessAuditResponse, TenantSchemaAccessAuditListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `TenantSchemaAccessAudit?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `TenantSchemaAccessAudit`;
      },
      providesTags: (result) => {
        const rows = toTenantSchemaAccessAuditList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaAccessAudit' as const, id })),
          { type: 'TenantSchemaAccessAudit', id: 'LIST' },
          { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addTenantSchemaAccessAudit: build.mutation<TenantSchemaAccessAudit, Partial<TenantSchemaAccessAudit>>({
      query: (body) => ({
        url: `TenantSchemaAccessAudit`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'TenantSchemaAccessAudit', id: 'LIST' },
        { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getTenantSchemaAccessAudit: build.query<TenantSchemaAccessAudit, string>({
      query: (id) => `TenantSchemaAccessAudit/${id}`,
      providesTags: (result, error, id) => [{ type: 'TenantSchemaAccessAudit', id }],
    }),

    // 5) Update
    updateTenantSchemaAccessAudit: build.mutation<TenantSchemaAccessAudit, Pick<TenantSchemaAccessAudit, 'id'> & Partial<TenantSchemaAccessAudit>>({
      query: ({ id, ...patch }) => ({
        url: `TenantSchemaAccessAudit/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<TenantSchemaAccessAudit, 'id'>) => [
        { type: 'TenantSchemaAccessAudit', id },
        { type: 'TenantSchemaAccessAudit', id: 'LIST' },
        { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteTenantSchemaAccessAudit: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `TenantSchemaAccessAudit/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'TenantSchemaAccessAudit', id },
        { type: 'TenantSchemaAccessAudit', id: 'LIST' },
        { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteTenantSchemaAccessAuditCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `TenantSchemaAccessAudit/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'TenantSchemaAccessAudit', id },
        { type: 'TenantSchemaAccessAudit', id: 'LIST' },
        { type: 'TenantSchemaAccessAudit', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetTenantSchemaAccessAuditsPagedQuery`
export const {
  useGetTenantSchemaAccessAuditsPagedQuery,     // immediate fetch
  useLazyGetTenantSchemaAccessAuditsPagedQuery, // lazy fetch
  useGetTenantSchemaAccessAuditQuery,
  useGetTenantSchemaAccessAuditsQuery,
  useAddTenantSchemaAccessAuditMutation,
  useUpdateTenantSchemaAccessAuditMutation,
  useDeleteTenantSchemaAccessAuditMutation,
  useDeleteTenantSchemaAccessAuditCascadeMutation,
} = TenantSchemaAccessAuditService
