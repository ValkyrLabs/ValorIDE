// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { StripeCustomerLink } from '@thorapi/model/StripeCustomerLink'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type StripeCustomerLinkResponse = StripeCustomerLink[]
type StripeCustomerLinkPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<StripeCustomerLink>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type StripeCustomerLinkListQueryArg = {
  example?: Partial<StripeCustomerLink>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toStripeCustomerLinkList = (result: unknown): StripeCustomerLinkResponse => {
  if (Array.isArray(result)) {
    return result as StripeCustomerLinkResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as StripeCustomerLinkResponse) : []
}

export const StripeCustomerLinkService = createApi({
  reducerPath: 'StripeCustomerLink', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['StripeCustomerLink'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getStripeCustomerLinksPaged: build.query<StripeCustomerLinkResponse, StripeCustomerLinkPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `StripeCustomerLink?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toStripeCustomerLinkList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeCustomerLink' as const, id })),
          { type: 'StripeCustomerLink', id: `PAGE_${page}` },
          { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getStripeCustomerLinks: build.query<StripeCustomerLinkResponse, StripeCustomerLinkListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `StripeCustomerLink?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `StripeCustomerLink`;
      },
      providesTags: (result) => {
        const rows = toStripeCustomerLinkList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeCustomerLink' as const, id })),
          { type: 'StripeCustomerLink', id: 'LIST' },
          { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addStripeCustomerLink: build.mutation<StripeCustomerLink, Partial<StripeCustomerLink>>({
      query: (body) => ({
        url: `StripeCustomerLink`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'StripeCustomerLink', id: 'LIST' },
        { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getStripeCustomerLink: build.query<StripeCustomerLink, string>({
      query: (id) => `StripeCustomerLink/${id}`,
      providesTags: (result, error, id) => [{ type: 'StripeCustomerLink', id }],
    }),

    // 5) Update
    updateStripeCustomerLink: build.mutation<StripeCustomerLink, Pick<StripeCustomerLink, 'id'> & Partial<StripeCustomerLink>>({
      query: ({ id, ...patch }) => ({
        url: `StripeCustomerLink/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<StripeCustomerLink, 'id'>) => [
        { type: 'StripeCustomerLink', id },
        { type: 'StripeCustomerLink', id: 'LIST' },
        { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteStripeCustomerLink: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `StripeCustomerLink/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'StripeCustomerLink', id },
        { type: 'StripeCustomerLink', id: 'LIST' },
        { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteStripeCustomerLinkCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `StripeCustomerLink/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'StripeCustomerLink', id },
        { type: 'StripeCustomerLink', id: 'LIST' },
        { type: 'StripeCustomerLink', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetStripeCustomerLinksPagedQuery`
export const {
  useGetStripeCustomerLinksPagedQuery,     // immediate fetch
  useLazyGetStripeCustomerLinksPagedQuery, // lazy fetch
  useGetStripeCustomerLinkQuery,
  useGetStripeCustomerLinksQuery,
  useAddStripeCustomerLinkMutation,
  useUpdateStripeCustomerLinkMutation,
  useDeleteStripeCustomerLinkMutation,
  useDeleteStripeCustomerLinkCascadeMutation,
} = StripeCustomerLinkService
