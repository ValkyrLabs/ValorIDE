// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { LlmCommand } from '@thorapi/model/LlmCommand'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type LlmCommandResponse = LlmCommand[]
type LlmCommandPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<LlmCommand>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type LlmCommandListQueryArg = {
  example?: Partial<LlmCommand>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toLlmCommandList = (result: unknown): LlmCommandResponse => {
  if (Array.isArray(result)) {
    return result as LlmCommandResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as LlmCommandResponse) : []
}

export const LlmCommandService = createApi({
  reducerPath: 'LlmCommand', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['LlmCommand'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getLlmCommandsPaged: build.query<LlmCommandResponse, LlmCommandPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `LlmCommand?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toLlmCommandList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'LlmCommand' as const, id })),
          { type: 'LlmCommand', id: `PAGE_${page}` },
          { type: 'LlmCommand', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getLlmCommands: build.query<LlmCommandResponse, LlmCommandListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `LlmCommand?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `LlmCommand`;
      },
      providesTags: (result) => {
        const rows = toLlmCommandList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'LlmCommand' as const, id })),
          { type: 'LlmCommand', id: 'LIST' },
          { type: 'LlmCommand', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addLlmCommand: build.mutation<LlmCommand, Partial<LlmCommand>>({
      query: (body) => ({
        url: `LlmCommand`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'LlmCommand', id: 'LIST' },
        { type: 'LlmCommand', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getLlmCommand: build.query<LlmCommand, string>({
      query: (id) => `LlmCommand/${id}`,
      providesTags: (result, error, id) => [{ type: 'LlmCommand', id }],
    }),

    // 5) Update
    updateLlmCommand: build.mutation<LlmCommand, Pick<LlmCommand, 'id'> & Partial<LlmCommand>>({
      query: ({ id, ...patch }) => ({
        url: `LlmCommand/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<LlmCommand, 'id'>) => [
        { type: 'LlmCommand', id },
        { type: 'LlmCommand', id: 'LIST' },
        { type: 'LlmCommand', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteLlmCommand: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `LlmCommand/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'LlmCommand', id },
        { type: 'LlmCommand', id: 'LIST' },
        { type: 'LlmCommand', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteLlmCommandCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `LlmCommand/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'LlmCommand', id },
        { type: 'LlmCommand', id: 'LIST' },
        { type: 'LlmCommand', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetLlmCommandsPagedQuery`
export const {
  useGetLlmCommandsPagedQuery,     // immediate fetch
  useLazyGetLlmCommandsPagedQuery, // lazy fetch
  useGetLlmCommandQuery,
  useGetLlmCommandsQuery,
  useAddLlmCommandMutation,
  useUpdateLlmCommandMutation,
  useDeleteLlmCommandMutation,
  useDeleteLlmCommandCascadeMutation,
} = LlmCommandService
