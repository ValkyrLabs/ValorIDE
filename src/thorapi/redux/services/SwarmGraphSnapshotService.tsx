// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SwarmGraphSnapshot } from '@thorapi/model/SwarmGraphSnapshot'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SwarmGraphSnapshotResponse = SwarmGraphSnapshot[]
type SwarmGraphSnapshotPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SwarmGraphSnapshot>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SwarmGraphSnapshotListQueryArg = {
  example?: Partial<SwarmGraphSnapshot>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSwarmGraphSnapshotList = (result: unknown): SwarmGraphSnapshotResponse => {
  if (Array.isArray(result)) {
    return result as SwarmGraphSnapshotResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SwarmGraphSnapshotResponse) : []
}

export const SwarmGraphSnapshotService = createApi({
  reducerPath: 'SwarmGraphSnapshot', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SwarmGraphSnapshot'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSwarmGraphSnapshotsPaged: build.query<SwarmGraphSnapshotResponse, SwarmGraphSnapshotPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SwarmGraphSnapshot?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSwarmGraphSnapshotList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SwarmGraphSnapshot' as const, id })),
          { type: 'SwarmGraphSnapshot', id: `PAGE_${page}` },
          { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSwarmGraphSnapshots: build.query<SwarmGraphSnapshotResponse, SwarmGraphSnapshotListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SwarmGraphSnapshot?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SwarmGraphSnapshot`;
      },
      providesTags: (result) => {
        const rows = toSwarmGraphSnapshotList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SwarmGraphSnapshot' as const, id })),
          { type: 'SwarmGraphSnapshot', id: 'LIST' },
          { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSwarmGraphSnapshot: build.mutation<SwarmGraphSnapshot, Partial<SwarmGraphSnapshot>>({
      query: (body) => ({
        url: `SwarmGraphSnapshot`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SwarmGraphSnapshot', id: 'LIST' },
        { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSwarmGraphSnapshot: build.query<SwarmGraphSnapshot, string>({
      query: (id) => `SwarmGraphSnapshot/${id}`,
      providesTags: (result, error, id) => [{ type: 'SwarmGraphSnapshot', id }],
    }),

    // 5) Update
    updateSwarmGraphSnapshot: build.mutation<SwarmGraphSnapshot, Pick<SwarmGraphSnapshot, 'id'> & Partial<SwarmGraphSnapshot>>({
      query: ({ id, ...patch }) => ({
        url: `SwarmGraphSnapshot/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SwarmGraphSnapshot, 'id'>) => [
        { type: 'SwarmGraphSnapshot', id },
        { type: 'SwarmGraphSnapshot', id: 'LIST' },
        { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSwarmGraphSnapshot: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SwarmGraphSnapshot/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SwarmGraphSnapshot', id },
        { type: 'SwarmGraphSnapshot', id: 'LIST' },
        { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSwarmGraphSnapshotCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SwarmGraphSnapshot/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SwarmGraphSnapshot', id },
        { type: 'SwarmGraphSnapshot', id: 'LIST' },
        { type: 'SwarmGraphSnapshot', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSwarmGraphSnapshotsPagedQuery`
export const {
  useGetSwarmGraphSnapshotsPagedQuery,     // immediate fetch
  useLazyGetSwarmGraphSnapshotsPagedQuery, // lazy fetch
  useGetSwarmGraphSnapshotQuery,
  useGetSwarmGraphSnapshotsQuery,
  useAddSwarmGraphSnapshotMutation,
  useUpdateSwarmGraphSnapshotMutation,
  useDeleteSwarmGraphSnapshotMutation,
  useDeleteSwarmGraphSnapshotCascadeMutation,
} = SwarmGraphSnapshotService
