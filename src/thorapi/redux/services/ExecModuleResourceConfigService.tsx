// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ExecModuleResourceConfig } from '@thorapi/model/ExecModuleResourceConfig'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ExecModuleResourceConfigResponse = ExecModuleResourceConfig[]
type ExecModuleResourceConfigPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ExecModuleResourceConfig>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ExecModuleResourceConfigListQueryArg = {
  example?: Partial<ExecModuleResourceConfig>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toExecModuleResourceConfigList = (result: unknown): ExecModuleResourceConfigResponse => {
  if (Array.isArray(result)) {
    return result as ExecModuleResourceConfigResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ExecModuleResourceConfigResponse) : []
}

export const ExecModuleResourceConfigService = createApi({
  reducerPath: 'ExecModuleResourceConfig', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ExecModuleResourceConfig'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getExecModuleResourceConfigsPaged: build.query<ExecModuleResourceConfigResponse, ExecModuleResourceConfigPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ExecModuleResourceConfig?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toExecModuleResourceConfigList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ExecModuleResourceConfig' as const, id })),
          { type: 'ExecModuleResourceConfig', id: `PAGE_${page}` },
          { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getExecModuleResourceConfigs: build.query<ExecModuleResourceConfigResponse, ExecModuleResourceConfigListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ExecModuleResourceConfig?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ExecModuleResourceConfig`;
      },
      providesTags: (result) => {
        const rows = toExecModuleResourceConfigList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ExecModuleResourceConfig' as const, id })),
          { type: 'ExecModuleResourceConfig', id: 'LIST' },
          { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addExecModuleResourceConfig: build.mutation<ExecModuleResourceConfig, Partial<ExecModuleResourceConfig>>({
      query: (body) => ({
        url: `ExecModuleResourceConfig`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ExecModuleResourceConfig', id: 'LIST' },
        { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getExecModuleResourceConfig: build.query<ExecModuleResourceConfig, string>({
      query: (id) => `ExecModuleResourceConfig/${id}`,
      providesTags: (result, error, id) => [{ type: 'ExecModuleResourceConfig', id }],
    }),

    // 5) Update
    updateExecModuleResourceConfig: build.mutation<ExecModuleResourceConfig, Pick<ExecModuleResourceConfig, 'id'> & Partial<ExecModuleResourceConfig>>({
      query: ({ id, ...patch }) => ({
        url: `ExecModuleResourceConfig/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ExecModuleResourceConfig, 'id'>) => [
        { type: 'ExecModuleResourceConfig', id },
        { type: 'ExecModuleResourceConfig', id: 'LIST' },
        { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteExecModuleResourceConfig: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ExecModuleResourceConfig/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ExecModuleResourceConfig', id },
        { type: 'ExecModuleResourceConfig', id: 'LIST' },
        { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteExecModuleResourceConfigCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ExecModuleResourceConfig/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ExecModuleResourceConfig', id },
        { type: 'ExecModuleResourceConfig', id: 'LIST' },
        { type: 'ExecModuleResourceConfig', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetExecModuleResourceConfigsPagedQuery`
export const {
  useGetExecModuleResourceConfigsPagedQuery,     // immediate fetch
  useLazyGetExecModuleResourceConfigsPagedQuery, // lazy fetch
  useGetExecModuleResourceConfigQuery,
  useGetExecModuleResourceConfigsQuery,
  useAddExecModuleResourceConfigMutation,
  useUpdateExecModuleResourceConfigMutation,
  useDeleteExecModuleResourceConfigMutation,
  useDeleteExecModuleResourceConfigCascadeMutation,
} = ExecModuleResourceConfigService
