{
  // This separate tsconfig is necessary because VS Code's test runner still expects CommonJS output,
  // while our main project uses ES Modules (ESM). It inherits most settings from the base tsconfig.json
  // but overrides a few compiler options so the emitted JavaScript stays compatible with the test runner.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "types": [
      "node",
      "jest",
      "mocha",
      "should",
      "vscode",
      "chai"
    ],
    "typeRoots": [
      "./node_modules/@types",
      "./src/test/types"
    ],
    "outDir": "out",
    "rootDir": "."
  },
  "include": [
    "src/**/*.test.ts"
  ],
  "exclude": [
    "src/test/**/*.js",
    "src/**/__tests__/*"
  ]
}
