// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query
export const PublishWorkflowRequestService = createApi({
    reducerPath: "PublishWorkflowRequest", // This should remain unique
    baseQuery: customBaseQuery,
    tagTypes: ["PublishWorkflowRequest"],
    endpoints: (build) => ({
        // 1) Paged Query Endpoint
        // Standardized pagination: page (0-based), size (page size)
        getPublishWorkflowRequestsPaged: build.query({
            query: ({ page, size = 20, example }) => {
                const q = [`page=${page}`, `size=${size}`];
                if (example)
                    q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
                return `PublishWorkflowRequest?${q.join("&")}`;
            },
            providesTags: (result, error, { page }) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "PublishWorkflowRequest",
                        id,
                    })),
                    { type: "PublishWorkflowRequest", id: `PAGE_${page}` },
                ]
                : [],
        }),
        // 2) Simple "get all" Query (optional)
        getPublishWorkflowRequests: build.query({
            query: (arg) => {
                if (arg && arg.example) {
                    const ex = arg.example;
                    return `PublishWorkflowRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
                }
                return `PublishWorkflowRequest`;
            },
            providesTags: (result) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "PublishWorkflowRequest",
                        id,
                    })),
                    { type: "PublishWorkflowRequest", id: "LIST" },
                ]
                : [{ type: "PublishWorkflowRequest", id: "LIST" }],
        }),
        // 3) Create
        addPublishWorkflowRequest: build.mutation({
            query: (body) => ({
                url: `PublishWorkflowRequest`,
                method: "POST",
                body,
            }),
            invalidatesTags: [{ type: "PublishWorkflowRequest", id: "LIST" }],
        }),
        // 4) Get single by ID
        getPublishWorkflowRequest: build.query({
            query: (id) => `PublishWorkflowRequest/${id}`,
            providesTags: (result, error, id) => [
                { type: "PublishWorkflowRequest", id },
            ],
        }),
        // 5) Update
        updatePublishWorkflowRequest: build.mutation({
            query: ({ id, ...patch }) => ({
                url: `PublishWorkflowRequest/${id}`,
                method: "PUT",
                body: patch,
            }),
            async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
                if (id) {
                    const patchResult = dispatch(PublishWorkflowRequestService.util.updateQueryData("getPublishWorkflowRequest", id, (draft) => {
                        Object.assign(draft, patch);
                    }));
                    try {
                        await queryFulfilled;
                    }
                    catch {
                        patchResult.undo();
                    }
                }
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "PublishWorkflowRequest", id },
                { type: "PublishWorkflowRequest", id: "LIST" },
            ],
        }),
        // 6) Delete
        deletePublishWorkflowRequest: build.mutation({
            query(id) {
                return {
                    url: `PublishWorkflowRequest/${id}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, id) => [
                { type: "PublishWorkflowRequest", id },
            ],
        }),
        // 7) Cascade / soft-delete (marks trashed, cascades children)
        deletePublishWorkflowRequestCascade: build.mutation({
            query({ id, cascade = true, trash = true }) {
                const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
                return {
                    url: `PublishWorkflowRequest/${id}?${params}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "PublishWorkflowRequest", id },
                { type: "PublishWorkflowRequest", id: "LIST" },
            ],
        }),
    }),
});
// Notice we now also export `useLazyGetPublishWorkflowRequestsPagedQuery`
export const { useGetPublishWorkflowRequestsPagedQuery, // immediate fetch
useLazyGetPublishWorkflowRequestsPagedQuery, // lazy fetch
useGetPublishWorkflowRequestQuery, useGetPublishWorkflowRequestsQuery, useAddPublishWorkflowRequestMutation, useUpdatePublishWorkflowRequestMutation, useDeletePublishWorkflowRequestMutation, useDeletePublishWorkflowRequestCascadeMutation, } = PublishWorkflowRequestService;
//# sourceMappingURL=PublishWorkflowRequestService.js.map