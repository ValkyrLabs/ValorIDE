// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query
export const JobApplicationService = createApi({
    reducerPath: "JobApplication", // This should remain unique
    baseQuery: customBaseQuery,
    tagTypes: ["JobApplication"],
    endpoints: (build) => ({
        // 1) Paged Query Endpoint
        // Standardized pagination: page (0-based), size (page size)
        getJobApplicationsPaged: build.query({
            query: ({ page, size = 20, example }) => {
                const q = [`page=${page}`, `size=${size}`];
                if (example)
                    q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
                return `JobApplication?${q.join("&")}`;
            },
            providesTags: (result, error, { page }) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "JobApplication",
                        id,
                    })),
                    { type: "JobApplication", id: `PAGE_${page}` },
                ]
                : [],
        }),
        // 2) Simple "get all" Query (optional)
        getJobApplications: build.query({
            query: (arg) => {
                if (arg && arg.example) {
                    const ex = arg.example;
                    return `JobApplication?example=${encodeURIComponent(JSON.stringify(ex))}`;
                }
                return `JobApplication`;
            },
            providesTags: (result) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "JobApplication",
                        id,
                    })),
                    { type: "JobApplication", id: "LIST" },
                ]
                : [{ type: "JobApplication", id: "LIST" }],
        }),
        // 3) Create
        addJobApplication: build.mutation({
            query: (body) => ({
                url: `JobApplication`,
                method: "POST",
                body,
            }),
            invalidatesTags: [{ type: "JobApplication", id: "LIST" }],
        }),
        // 4) Get single by ID
        getJobApplication: build.query({
            query: (id) => `JobApplication/${id}`,
            providesTags: (result, error, id) => [{ type: "JobApplication", id }],
        }),
        // 5) Update
        updateJobApplication: build.mutation({
            query: ({ id, ...patch }) => ({
                url: `JobApplication/${id}`,
                method: "PUT",
                body: patch,
            }),
            async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
                if (id) {
                    const patchResult = dispatch(JobApplicationService.util.updateQueryData("getJobApplication", id, (draft) => {
                        Object.assign(draft, patch);
                    }));
                    try {
                        await queryFulfilled;
                    }
                    catch {
                        patchResult.undo();
                    }
                }
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "JobApplication", id },
                { type: "JobApplication", id: "LIST" },
            ],
        }),
        // 6) Delete
        deleteJobApplication: build.mutation({
            query(id) {
                return {
                    url: `JobApplication/${id}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, id) => [{ type: "JobApplication", id }],
        }),
        // 7) Cascade / soft-delete (marks trashed, cascades children)
        deleteJobApplicationCascade: build.mutation({
            query({ id, cascade = true, trash = true }) {
                const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
                return {
                    url: `JobApplication/${id}?${params}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "JobApplication", id },
                { type: "JobApplication", id: "LIST" },
            ],
        }),
    }),
});
// Notice we now also export `useLazyGetJobApplicationsPagedQuery`
export const { useGetJobApplicationsPagedQuery, // immediate fetch
useLazyGetJobApplicationsPagedQuery, // lazy fetch
useGetJobApplicationQuery, useGetJobApplicationsQuery, useAddJobApplicationMutation, useUpdateJobApplicationMutation, useDeleteJobApplicationMutation, useDeleteJobApplicationCascadeMutation, } = JobApplicationService;
//# sourceMappingURL=JobApplicationService.js.map