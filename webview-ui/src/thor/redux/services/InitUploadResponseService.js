// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query
export const InitUploadResponseService = createApi({
    reducerPath: "InitUploadResponse", // This should remain unique
    baseQuery: customBaseQuery,
    tagTypes: ["InitUploadResponse"],
    endpoints: (build) => ({
        // 1) Paged Query Endpoint
        // Standardized pagination: page (0-based), size (page size)
        getInitUploadResponsesPaged: build.query({
            query: ({ page, size = 20, example }) => {
                const q = [`page=${page}`, `size=${size}`];
                if (example)
                    q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
                return `InitUploadResponse?${q.join("&")}`;
            },
            providesTags: (result, error, { page }) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "InitUploadResponse",
                        id,
                    })),
                    { type: "InitUploadResponse", id: `PAGE_${page}` },
                ]
                : [],
        }),
        // 2) Simple "get all" Query (optional)
        getInitUploadResponses: build.query({
            query: (arg) => {
                if (arg && arg.example) {
                    const ex = arg.example;
                    return `InitUploadResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
                }
                return `InitUploadResponse`;
            },
            providesTags: (result) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "InitUploadResponse",
                        id,
                    })),
                    { type: "InitUploadResponse", id: "LIST" },
                ]
                : [{ type: "InitUploadResponse", id: "LIST" }],
        }),
        // 3) Create
        addInitUploadResponse: build.mutation({
            query: (body) => ({
                url: `InitUploadResponse`,
                method: "POST",
                body,
            }),
            invalidatesTags: [{ type: "InitUploadResponse", id: "LIST" }],
        }),
        // 4) Get single by ID
        getInitUploadResponse: build.query({
            query: (id) => `InitUploadResponse/${id}`,
            providesTags: (result, error, id) => [{ type: "InitUploadResponse", id }],
        }),
        // 5) Update
        updateInitUploadResponse: build.mutation({
            query: ({ id, ...patch }) => ({
                url: `InitUploadResponse/${id}`,
                method: "PUT",
                body: patch,
            }),
            async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
                if (id) {
                    const patchResult = dispatch(InitUploadResponseService.util.updateQueryData("getInitUploadResponse", id, (draft) => {
                        Object.assign(draft, patch);
                    }));
                    try {
                        await queryFulfilled;
                    }
                    catch {
                        patchResult.undo();
                    }
                }
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "InitUploadResponse", id },
                { type: "InitUploadResponse", id: "LIST" },
            ],
        }),
        // 6) Delete
        deleteInitUploadResponse: build.mutation({
            query(id) {
                return {
                    url: `InitUploadResponse/${id}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, id) => [
                { type: "InitUploadResponse", id },
            ],
        }),
        // 7) Cascade / soft-delete (marks trashed, cascades children)
        deleteInitUploadResponseCascade: build.mutation({
            query({ id, cascade = true, trash = true }) {
                const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
                return {
                    url: `InitUploadResponse/${id}?${params}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "InitUploadResponse", id },
                { type: "InitUploadResponse", id: "LIST" },
            ],
        }),
    }),
});
// Notice we now also export `useLazyGetInitUploadResponsesPagedQuery`
export const { useGetInitUploadResponsesPagedQuery, // immediate fetch
useLazyGetInitUploadResponsesPagedQuery, // lazy fetch
useGetInitUploadResponseQuery, useGetInitUploadResponsesQuery, useAddInitUploadResponseMutation, useUpdateInitUploadResponseMutation, useDeleteInitUploadResponseMutation, useDeleteInitUploadResponseCascadeMutation, } = InitUploadResponseService;
//# sourceMappingURL=InitUploadResponseService.js.map