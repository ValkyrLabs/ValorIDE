// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query
export const SubscriberListService = createApi({
    reducerPath: "SubscriberList", // This should remain unique
    baseQuery: customBaseQuery,
    tagTypes: ["SubscriberList"],
    endpoints: (build) => ({
        // 1) Paged Query Endpoint
        // Standardized pagination: page (0-based), size (page size)
        getSubscriberListsPaged: build.query({
            query: ({ page, size = 20, example }) => {
                const q = [`page=${page}`, `size=${size}`];
                if (example)
                    q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
                return `SubscriberList?${q.join("&")}`;
            },
            providesTags: (result, error, { page }) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "SubscriberList",
                        id,
                    })),
                    { type: "SubscriberList", id: `PAGE_${page}` },
                ]
                : [],
        }),
        // 2) Simple "get all" Query (optional)
        getSubscriberLists: build.query({
            query: (arg) => {
                if (arg && arg.example) {
                    const ex = arg.example;
                    return `SubscriberList?example=${encodeURIComponent(JSON.stringify(ex))}`;
                }
                return `SubscriberList`;
            },
            providesTags: (result) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "SubscriberList",
                        id,
                    })),
                    { type: "SubscriberList", id: "LIST" },
                ]
                : [{ type: "SubscriberList", id: "LIST" }],
        }),
        // 3) Create
        addSubscriberList: build.mutation({
            query: (body) => ({
                url: `SubscriberList`,
                method: "POST",
                body,
            }),
            invalidatesTags: [{ type: "SubscriberList", id: "LIST" }],
        }),
        // 4) Get single by ID
        getSubscriberList: build.query({
            query: (id) => `SubscriberList/${id}`,
            providesTags: (result, error, id) => [{ type: "SubscriberList", id }],
        }),
        // 5) Update
        updateSubscriberList: build.mutation({
            query: ({ id, ...patch }) => ({
                url: `SubscriberList/${id}`,
                method: "PUT",
                body: patch,
            }),
            async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
                if (id) {
                    const patchResult = dispatch(SubscriberListService.util.updateQueryData("getSubscriberList", id, (draft) => {
                        Object.assign(draft, patch);
                    }));
                    try {
                        await queryFulfilled;
                    }
                    catch {
                        patchResult.undo();
                    }
                }
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "SubscriberList", id },
                { type: "SubscriberList", id: "LIST" },
            ],
        }),
        // 6) Delete
        deleteSubscriberList: build.mutation({
            query(id) {
                return {
                    url: `SubscriberList/${id}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, id) => [{ type: "SubscriberList", id }],
        }),
        // 7) Cascade / soft-delete (marks trashed, cascades children)
        deleteSubscriberListCascade: build.mutation({
            query({ id, cascade = true, trash = true }) {
                const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
                return {
                    url: `SubscriberList/${id}?${params}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "SubscriberList", id },
                { type: "SubscriberList", id: "LIST" },
            ],
        }),
    }),
});
// Notice we now also export `useLazyGetSubscriberListsPagedQuery`
export const { useGetSubscriberListsPagedQuery, // immediate fetch
useLazyGetSubscriberListsPagedQuery, // lazy fetch
useGetSubscriberListQuery, useGetSubscriberListsQuery, useAddSubscriberListMutation, useUpdateSubscriberListMutation, useDeleteSubscriberListMutation, useDeleteSubscriberListCascadeMutation, } = SubscriberListService;
//# sourceMappingURL=SubscriberListService.js.map