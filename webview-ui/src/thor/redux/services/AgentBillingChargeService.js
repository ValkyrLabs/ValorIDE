// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from "@reduxjs/toolkit/query/react";
import customBaseQuery from "../customBaseQuery"; // Import the custom base query
export const AgentBillingChargeService = createApi({
    reducerPath: "AgentBillingCharge", // This should remain unique
    baseQuery: customBaseQuery,
    tagTypes: ["AgentBillingCharge"],
    endpoints: (build) => ({
        // 1) Paged Query Endpoint
        // Standardized pagination: page (0-based), size (page size)
        getAgentBillingChargesPaged: build.query({
            query: ({ page, size = 20, example }) => {
                const q = [`page=${page}`, `size=${size}`];
                if (example)
                    q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
                return `AgentBillingCharge?${q.join("&")}`;
            },
            providesTags: (result, error, { page }) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "AgentBillingCharge",
                        id,
                    })),
                    { type: "AgentBillingCharge", id: `PAGE_${page}` },
                ]
                : [],
        }),
        // 2) Simple "get all" Query (optional)
        getAgentBillingCharges: build.query({
            query: (arg) => {
                if (arg && arg.example) {
                    const ex = arg.example;
                    return `AgentBillingCharge?example=${encodeURIComponent(JSON.stringify(ex))}`;
                }
                return `AgentBillingCharge`;
            },
            providesTags: (result) => result
                ? [
                    ...result.map(({ id }) => ({
                        type: "AgentBillingCharge",
                        id,
                    })),
                    { type: "AgentBillingCharge", id: "LIST" },
                ]
                : [{ type: "AgentBillingCharge", id: "LIST" }],
        }),
        // 3) Create
        addAgentBillingCharge: build.mutation({
            query: (body) => ({
                url: `AgentBillingCharge`,
                method: "POST",
                body,
            }),
            invalidatesTags: [{ type: "AgentBillingCharge", id: "LIST" }],
        }),
        // 4) Get single by ID
        getAgentBillingCharge: build.query({
            query: (id) => `AgentBillingCharge/${id}`,
            providesTags: (result, error, id) => [{ type: "AgentBillingCharge", id }],
        }),
        // 5) Update
        updateAgentBillingCharge: build.mutation({
            query: ({ id, ...patch }) => ({
                url: `AgentBillingCharge/${id}`,
                method: "PUT",
                body: patch,
            }),
            async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
                if (id) {
                    const patchResult = dispatch(AgentBillingChargeService.util.updateQueryData("getAgentBillingCharge", id, (draft) => {
                        Object.assign(draft, patch);
                    }));
                    try {
                        await queryFulfilled;
                    }
                    catch {
                        patchResult.undo();
                    }
                }
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "AgentBillingCharge", id },
                { type: "AgentBillingCharge", id: "LIST" },
            ],
        }),
        // 6) Delete
        deleteAgentBillingCharge: build.mutation({
            query(id) {
                return {
                    url: `AgentBillingCharge/${id}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, id) => [
                { type: "AgentBillingCharge", id },
            ],
        }),
        // 7) Cascade / soft-delete (marks trashed, cascades children)
        deleteAgentBillingChargeCascade: build.mutation({
            query({ id, cascade = true, trash = true }) {
                const params = [`cascade=${cascade}`, `trash=${trash}`].join("&");
                return {
                    url: `AgentBillingCharge/${id}?${params}`,
                    method: "DELETE",
                };
            },
            invalidatesTags: (result, error, { id }) => [
                { type: "AgentBillingCharge", id },
                { type: "AgentBillingCharge", id: "LIST" },
            ],
        }),
    }),
});
// Notice we now also export `useLazyGetAgentBillingChargesPagedQuery`
export const { useGetAgentBillingChargesPagedQuery, // immediate fetch
useLazyGetAgentBillingChargesPagedQuery, // lazy fetch
useGetAgentBillingChargeQuery, useGetAgentBillingChargesQuery, useAddAgentBillingChargeMutation, useUpdateAgentBillingChargeMutation, useDeleteAgentBillingChargeMutation, useDeleteAgentBillingChargeCascadeMutation, } = AgentBillingChargeService;
//# sourceMappingURL=AgentBillingChargeService.js.map