// tslint:disable

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATED DATE:** 2025-12-07T16:29:11.456024-08:00[America/Los_Angeles]
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen


Template file: typescript-redux-query/apis.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description: IdempotencyKeyApi
*/

import {
  HttpMethods,
  QueryConfig,
  ResponseBody,
  ResponseText,
} from "redux-query";
import * as runtime from "../src/runtime";
import {
  IdempotencyKey,
  IdempotencyKeyFromJSON,
  IdempotencyKeyToJSON,
} from "../model";

export interface DeleteIdempotencyKeyApiRequest {
  id: string;
}

export interface GetIdempotencyKeyApiRequest {
  id: string;
}

export interface GetIdempotencyKeyListApiRequest {
  page?: number;
  size?: number;
  sort?: Array<string>;
}

export interface PostIdempotencyKeyApiRequest {
  idempotencyKey: IdempotencyKey;
}

export interface UpdateIdempotencyKeyApiRequest {
  id: string;
  idempotencyKey: IdempotencyKey;
}

/**
 * Deletes a specific IdempotencyKey.
 * Delete a IdempotencyKey.
 */
function deleteIdempotencyKeyRaw<T>(
  requestParameters: DeleteIdempotencyKeyApiRequest,
  requestConfig: runtime.TypedQueryConfig<T, void> = {},
): QueryConfig<T> {
  if (requestParameters.id === null || requestParameters.id === undefined) {
    throw new runtime.RequiredError(
      "id",
      "Required parameter requestParameters.id was null or undefined when calling deleteIdempotencyKey.",
    );
  }

  let queryParameters = null;

  const headerParameters: runtime.HttpHeaders = {};

  const { meta = {} } = requestConfig;

  const config: QueryConfig<T> = {
    url: `${runtime.Configuration.basePath}/IdempotencyKey/{id}`.replace(
      `{${"id"}}`,
      encodeURIComponent(String(requestParameters.id)),
    ),
    meta,
    update: requestConfig.update,
    queryKey: requestConfig.queryKey,
    optimisticUpdate: requestConfig.optimisticUpdate,
    force: requestConfig.force,
    rollback: requestConfig.rollback,
    options: {
      method: "DELETE",
      headers: headerParameters,
    },
    body: queryParameters,
  };

  const { transform: requestTransform } = requestConfig;
  if (requestTransform) {
  }

  return config;
}

/**
 * Deletes a specific IdempotencyKey.
 * Delete a IdempotencyKey.
 */
export function deleteIdempotencyKey<T>(
  requestParameters: DeleteIdempotencyKeyApiRequest,
  requestConfig?: runtime.TypedQueryConfig<T, void>,
): QueryConfig<T> {
  return deleteIdempotencyKeyRaw(requestParameters, requestConfig);
}

/**
 * Retrieves a single IdempotencyKey for a specific uid.
 * Retrieve a single IdempotencyKey
 */
function getIdempotencyKeyRaw<T>(
  requestParameters: GetIdempotencyKeyApiRequest,
  requestConfig: runtime.TypedQueryConfig<T, IdempotencyKey> = {},
): QueryConfig<T> {
  if (requestParameters.id === null || requestParameters.id === undefined) {
    throw new runtime.RequiredError(
      "id",
      "Required parameter requestParameters.id was null or undefined when calling getIdempotencyKey.",
    );
  }

  let queryParameters = null;

  const headerParameters: runtime.HttpHeaders = {};

  const { meta = {} } = requestConfig;

  const config: QueryConfig<T> = {
    url: `${runtime.Configuration.basePath}/IdempotencyKey/{id}`.replace(
      `{${"id"}}`,
      encodeURIComponent(String(requestParameters.id)),
    ),
    meta,
    update: requestConfig.update,
    queryKey: requestConfig.queryKey,
    optimisticUpdate: requestConfig.optimisticUpdate,
    force: requestConfig.force,
    rollback: requestConfig.rollback,
    options: {
      method: "GET",
      headers: headerParameters,
    },
    body: queryParameters,
  };

  const { transform: requestTransform } = requestConfig;
  if (requestTransform) {
    config.transform = (body: ResponseBody, text: ResponseBody) =>
      requestTransform(IdempotencyKeyFromJSON(body), text);
  }

  return config;
}

/**
 * Retrieves a single IdempotencyKey for a specific uid.
 * Retrieve a single IdempotencyKey
 */
export function getIdempotencyKey<T>(
  requestParameters: GetIdempotencyKeyApiRequest,
  requestConfig?: runtime.TypedQueryConfig<T, IdempotencyKey>,
): QueryConfig<T> {
  return getIdempotencyKeyRaw(requestParameters, requestConfig);
}

/**
 * Retrieves a list of IdempotencyKeys.
 * Retrieve a list of IdempotencyKeys
 */
function getIdempotencyKeyListRaw<T>(
  requestParameters: GetIdempotencyKeyListApiRequest,
  requestConfig: runtime.TypedQueryConfig<T, Array<IdempotencyKey>> = {},
): QueryConfig<T> {
  let queryParameters = null;

  queryParameters = {};

  if (requestParameters.page !== undefined) {
    queryParameters["page"] = requestParameters.page;
  }

  if (requestParameters.size !== undefined) {
    queryParameters["size"] = requestParameters.size;
  }

  if (requestParameters.sort) {
    queryParameters["sort"] = requestParameters.sort;
  }

  const headerParameters: runtime.HttpHeaders = {};

  const { meta = {} } = requestConfig;

  const config: QueryConfig<T> = {
    url: `${runtime.Configuration.basePath}/IdempotencyKey`,
    meta,
    update: requestConfig.update,
    queryKey: requestConfig.queryKey,
    optimisticUpdate: requestConfig.optimisticUpdate,
    force: requestConfig.force,
    rollback: requestConfig.rollback,
    options: {
      method: "GET",
      headers: headerParameters,
    },
    body: queryParameters,
  };

  const { transform: requestTransform } = requestConfig;
  if (requestTransform) {
    config.transform = (body: ResponseBody, text: ResponseBody) =>
      requestTransform(body.map(IdempotencyKeyFromJSON), text);
  }

  return config;
}

/**
 * Retrieves a list of IdempotencyKeys.
 * Retrieve a list of IdempotencyKeys
 */
export function getIdempotencyKeyList<T>(
  requestParameters: GetIdempotencyKeyListApiRequest,
  requestConfig?: runtime.TypedQueryConfig<T, Array<IdempotencyKey>>,
): QueryConfig<T> {
  return getIdempotencyKeyListRaw(requestParameters, requestConfig);
}

/**
 * Creates a new IdempotencyKey.
 * Create a new IdempotencyKey
 */
function postIdempotencyKeyRaw<T>(
  requestParameters: PostIdempotencyKeyApiRequest,
  requestConfig: runtime.TypedQueryConfig<T, IdempotencyKey> = {},
): QueryConfig<T> {
  if (
    requestParameters.idempotencyKey === null ||
    requestParameters.idempotencyKey === undefined
  ) {
    throw new runtime.RequiredError(
      "idempotencyKey",
      "Required parameter requestParameters.idempotencyKey was null or undefined when calling postIdempotencyKey.",
    );
  }

  let queryParameters = null;

  const headerParameters: runtime.HttpHeaders = {};

  headerParameters["Content-Type"] = "application/json";

  const { meta = {} } = requestConfig;

  const config: QueryConfig<T> = {
    url: `${runtime.Configuration.basePath}/IdempotencyKey`,
    meta,
    update: requestConfig.update,
    queryKey: requestConfig.queryKey,
    optimisticUpdate: requestConfig.optimisticUpdate,
    force: requestConfig.force,
    rollback: requestConfig.rollback,
    options: {
      method: "POST",
      headers: headerParameters,
    },
    body:
      queryParameters || IdempotencyKeyToJSON(requestParameters.idempotencyKey),
  };

  const { transform: requestTransform } = requestConfig;
  if (requestTransform) {
    config.transform = (body: ResponseBody, text: ResponseBody) =>
      requestTransform(IdempotencyKeyFromJSON(body), text);
  }

  return config;
}

/**
 * Creates a new IdempotencyKey.
 * Create a new IdempotencyKey
 */
export function postIdempotencyKey<T>(
  requestParameters: PostIdempotencyKeyApiRequest,
  requestConfig?: runtime.TypedQueryConfig<T, IdempotencyKey>,
): QueryConfig<T> {
  return postIdempotencyKeyRaw(requestParameters, requestConfig);
}

/**
 * Updates an existing IdempotencyKey.
 * Update an existing IdempotencyKey
 */
function updateIdempotencyKeyRaw<T>(
  requestParameters: UpdateIdempotencyKeyApiRequest,
  requestConfig: runtime.TypedQueryConfig<T, IdempotencyKey> = {},
): QueryConfig<T> {
  if (requestParameters.id === null || requestParameters.id === undefined) {
    throw new runtime.RequiredError(
      "id",
      "Required parameter requestParameters.id was null or undefined when calling updateIdempotencyKey.",
    );
  }

  if (
    requestParameters.idempotencyKey === null ||
    requestParameters.idempotencyKey === undefined
  ) {
    throw new runtime.RequiredError(
      "idempotencyKey",
      "Required parameter requestParameters.idempotencyKey was null or undefined when calling updateIdempotencyKey.",
    );
  }

  let queryParameters = null;

  const headerParameters: runtime.HttpHeaders = {};

  headerParameters["Content-Type"] = "application/json";

  const { meta = {} } = requestConfig;

  const config: QueryConfig<T> = {
    url: `${runtime.Configuration.basePath}/IdempotencyKey/{id}`.replace(
      `{${"id"}}`,
      encodeURIComponent(String(requestParameters.id)),
    ),
    meta,
    update: requestConfig.update,
    queryKey: requestConfig.queryKey,
    optimisticUpdate: requestConfig.optimisticUpdate,
    force: requestConfig.force,
    rollback: requestConfig.rollback,
    options: {
      method: "PUT",
      headers: headerParameters,
    },
    body:
      queryParameters || IdempotencyKeyToJSON(requestParameters.idempotencyKey),
  };

  const { transform: requestTransform } = requestConfig;
  if (requestTransform) {
    config.transform = (body: ResponseBody, text: ResponseBody) =>
      requestTransform(IdempotencyKeyFromJSON(body), text);
  }

  return config;
}

/**
 * Updates an existing IdempotencyKey.
 * Update an existing IdempotencyKey
 */
export function updateIdempotencyKey<T>(
  requestParameters: UpdateIdempotencyKeyApiRequest,
  requestConfig?: runtime.TypedQueryConfig<T, IdempotencyKey>,
): QueryConfig<T> {
  return updateIdempotencyKeyRaw(requestParameters, requestConfig);
}
