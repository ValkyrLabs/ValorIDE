// tslint:disable

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen


Template file: typescript-redux-query/apis.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description: SkilloptOpsApi
*/

import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../src/runtime';
import {
    SkillOptRouteOutcomeRequest,
    SkillOptRouteOutcomeRequestFromJSON,
    SkillOptRouteOutcomeRequestToJSON,
    SkillOptRouteReceipt,
    SkillOptRouteReceiptFromJSON,
    SkillOptRouteReceiptToJSON,
    SkillOptRouteRequest,
    SkillOptRouteRequestFromJSON,
    SkillOptRouteRequestToJSON,
    SkillOptRouteResponse,
    SkillOptRouteResponseFromJSON,
    SkillOptRouteResponseToJSON,
    SkillSignal,
    SkillSignalFromJSON,
    SkillSignalToJSON,
} from '../model';

export interface GetSkilloptRouteReceiptApiRequest {
    accountId: string;
    receiptRef: string;
}

export interface ListSkilloptRouteReceiptsApiRequest {
    accountId: string;
}

export interface RecordSkilloptRouteOutcomeApiRequest {
    skillOptRouteOutcomeRequest: SkillOptRouteOutcomeRequest;
}

export interface RouteSkilloptApiRequest {
    skillOptRouteRequest: SkillOptRouteRequest;
}


/**
 * Returns one SkillOpt route receipt only when it belongs to the requested account.
 * Get an account-scoped SkillOpt route receipt
 */
function getSkilloptRouteReceiptRaw<T>(requestParameters: GetSkilloptRouteReceiptApiRequest, requestConfig: runtime.TypedQueryConfig<T, SkillOptRouteReceipt> = {}): QueryConfig<T> {
    if (requestParameters.accountId === null || requestParameters.accountId === undefined) {
        throw new runtime.RequiredError('accountId','Required parameter requestParameters.accountId was null or undefined when calling getSkilloptRouteReceipt.');
    }

    if (requestParameters.receiptRef === null || requestParameters.receiptRef === undefined) {
        throw new runtime.RequiredError('receiptRef','Required parameter requestParameters.receiptRef was null or undefined when calling getSkilloptRouteReceipt.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/skillopt_ops/{accountId}/route_receipts/{receiptRef}`.replace(`{${"accountId"}}`, encodeURIComponent(String(requestParameters.accountId))).replace(`{${"receiptRef"}}`, encodeURIComponent(String(requestParameters.receiptRef))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SkillOptRouteReceiptFromJSON(body), text);
    }

    return config;
}

/**
* Returns one SkillOpt route receipt only when it belongs to the requested account.
* Get an account-scoped SkillOpt route receipt
*/
export function getSkilloptRouteReceipt<T>(requestParameters: GetSkilloptRouteReceiptApiRequest, requestConfig?: runtime.TypedQueryConfig<T, SkillOptRouteReceipt>): QueryConfig<T> {
    return getSkilloptRouteReceiptRaw(requestParameters, requestConfig);
}

/**
 * Returns account-scoped SkillOpt route receipts for route explanation and audit inspection.
 * List SkillOpt route receipts for an account
 */
function listSkilloptRouteReceiptsRaw<T>(requestParameters: ListSkilloptRouteReceiptsApiRequest, requestConfig: runtime.TypedQueryConfig<T, Array<SkillOptRouteReceipt>> = {}): QueryConfig<T> {
    if (requestParameters.accountId === null || requestParameters.accountId === undefined) {
        throw new runtime.RequiredError('accountId','Required parameter requestParameters.accountId was null or undefined when calling listSkilloptRouteReceipts.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/skillopt_ops/{accountId}/route_receipts`.replace(`{${"accountId"}}`, encodeURIComponent(String(requestParameters.accountId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(SkillOptRouteReceiptFromJSON), text);
    }

    return config;
}

/**
* Returns account-scoped SkillOpt route receipts for route explanation and audit inspection.
* List SkillOpt route receipts for an account
*/
export function listSkilloptRouteReceipts<T>(requestParameters: ListSkilloptRouteReceiptsApiRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<SkillOptRouteReceipt>>): QueryConfig<T> {
    return listSkilloptRouteReceiptsRaw(requestParameters, requestConfig);
}

/**
 * Converts route outcomes and ratings into SkillSignal records and updates linked procedure confidence.
 * Record SkillOpt route outcome
 */
function recordSkilloptRouteOutcomeRaw<T>(requestParameters: RecordSkilloptRouteOutcomeApiRequest, requestConfig: runtime.TypedQueryConfig<T, SkillSignal> = {}): QueryConfig<T> {
    if (requestParameters.skillOptRouteOutcomeRequest === null || requestParameters.skillOptRouteOutcomeRequest === undefined) {
        throw new runtime.RequiredError('skillOptRouteOutcomeRequest','Required parameter requestParameters.skillOptRouteOutcomeRequest was null or undefined when calling recordSkilloptRouteOutcome.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/skillopt_ops/route_outcome`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || SkillOptRouteOutcomeRequestToJSON(requestParameters.skillOptRouteOutcomeRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SkillSignalFromJSON(body), text);
    }

    return config;
}

/**
* Converts route outcomes and ratings into SkillSignal records and updates linked procedure confidence.
* Record SkillOpt route outcome
*/
export function recordSkilloptRouteOutcome<T>(requestParameters: RecordSkilloptRouteOutcomeApiRequest, requestConfig?: runtime.TypedQueryConfig<T, SkillSignal>): QueryConfig<T> {
    return recordSkilloptRouteOutcomeRaw(requestParameters, requestConfig);
}

/**
 * Scores context, credits, policy, procedures, risk, and model/tool availability, then writes a route receipt.
 * Select a deterministic SkillOpt route
 */
function routeSkilloptRaw<T>(requestParameters: RouteSkilloptApiRequest, requestConfig: runtime.TypedQueryConfig<T, SkillOptRouteResponse> = {}): QueryConfig<T> {
    if (requestParameters.skillOptRouteRequest === null || requestParameters.skillOptRouteRequest === undefined) {
        throw new runtime.RequiredError('skillOptRouteRequest','Required parameter requestParameters.skillOptRouteRequest was null or undefined when calling routeSkillopt.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/skillopt_ops/route`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || SkillOptRouteRequestToJSON(requestParameters.skillOptRouteRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SkillOptRouteResponseFromJSON(body), text);
    }

    return config;
}

/**
* Scores context, credits, policy, procedures, risk, and model/tool availability, then writes a route receipt.
* Select a deterministic SkillOpt route
*/
export function routeSkillopt<T>(requestParameters: RouteSkilloptApiRequest, requestConfig?: runtime.TypedQueryConfig<T, SkillOptRouteResponse>): QueryConfig<T> {
    return routeSkilloptRaw(requestParameters, requestConfig);
}

