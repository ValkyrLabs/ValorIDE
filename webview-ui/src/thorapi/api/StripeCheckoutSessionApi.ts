// tslint:disable

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen


Template file: typescript-redux-query/apis.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description: StripeCheckoutSessionApi
*/

import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../src/runtime';
import {
    StripeCheckoutSession,
    StripeCheckoutSessionFromJSON,
    StripeCheckoutSessionToJSON,
} from '../model';

export interface DeleteStripeCheckoutSessionApiRequest {
    id: string;
}

export interface GetStripeCheckoutSessionApiRequest {
    id: string;
}

export interface GetStripeCheckoutSessionListApiRequest {
    page?: number;
    size?: number;
    sort?: Array<string>;
    example?: string;
}

export interface PatchStripeCheckoutSessionByIdApiRequest {
    id: string;
    stripeCheckoutSession: StripeCheckoutSession;
}

export interface PostStripeCheckoutSessionApiRequest {
    stripeCheckoutSession: StripeCheckoutSession;
}

export interface UpdateStripeCheckoutSessionApiRequest {
    id: string;
    stripeCheckoutSession: StripeCheckoutSession;
}


/**
 * Deletes a specific StripeCheckoutSession.
 * Delete a StripeCheckoutSession.
 */
function deleteStripeCheckoutSessionRaw<T>(requestParameters: DeleteStripeCheckoutSessionApiRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling deleteStripeCheckoutSession.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Deletes a specific StripeCheckoutSession.
* Delete a StripeCheckoutSession.
*/
export function deleteStripeCheckoutSession<T>(requestParameters: DeleteStripeCheckoutSessionApiRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return deleteStripeCheckoutSessionRaw(requestParameters, requestConfig);
}

/**
 * Retrieves a single StripeCheckoutSession for a specific uid.
 * Retrieve a single StripeCheckoutSession
 */
function getStripeCheckoutSessionRaw<T>(requestParameters: GetStripeCheckoutSessionApiRequest, requestConfig: runtime.TypedQueryConfig<T, StripeCheckoutSession> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getStripeCheckoutSession.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(StripeCheckoutSessionFromJSON(body), text);
    }

    return config;
}

/**
* Retrieves a single StripeCheckoutSession for a specific uid.
* Retrieve a single StripeCheckoutSession
*/
export function getStripeCheckoutSession<T>(requestParameters: GetStripeCheckoutSessionApiRequest, requestConfig?: runtime.TypedQueryConfig<T, StripeCheckoutSession>): QueryConfig<T> {
    return getStripeCheckoutSessionRaw(requestParameters, requestConfig);
}

/**
 * Retrieves a list of StripeCheckoutSessions.
 * Retrieve a list of StripeCheckoutSessions
 */
function getStripeCheckoutSessionListRaw<T>(requestParameters: GetStripeCheckoutSessionListApiRequest, requestConfig: runtime.TypedQueryConfig<T, Array<StripeCheckoutSession>> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.page !== undefined) {
        queryParameters['page'] = requestParameters.page;
    }


    if (requestParameters.size !== undefined) {
        queryParameters['size'] = requestParameters.size;
    }


    if (requestParameters.sort) {
        queryParameters['sort'] = requestParameters.sort;
    }


    if (requestParameters.example !== undefined) {
        queryParameters['example'] = requestParameters.example;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(StripeCheckoutSessionFromJSON), text);
    }

    return config;
}

/**
* Retrieves a list of StripeCheckoutSessions.
* Retrieve a list of StripeCheckoutSessions
*/
export function getStripeCheckoutSessionList<T>(requestParameters: GetStripeCheckoutSessionListApiRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<StripeCheckoutSession>>): QueryConfig<T> {
    return getStripeCheckoutSessionListRaw(requestParameters, requestConfig);
}

/**
 * Updates an existing StripeCheckoutSession.
 * Partially update an existing StripeCheckoutSession
 */
function patchStripeCheckoutSessionByIdRaw<T>(requestParameters: PatchStripeCheckoutSessionByIdApiRequest, requestConfig: runtime.TypedQueryConfig<T, StripeCheckoutSession> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling patchStripeCheckoutSessionById.');
    }

    if (requestParameters.stripeCheckoutSession === null || requestParameters.stripeCheckoutSession === undefined) {
        throw new runtime.RequiredError('stripeCheckoutSession','Required parameter requestParameters.stripeCheckoutSession was null or undefined when calling patchStripeCheckoutSessionById.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/merge-patch+json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PATCH',
            headers: headerParameters,
        },
        body: queryParameters || StripeCheckoutSessionToJSON(requestParameters.stripeCheckoutSession),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(StripeCheckoutSessionFromJSON(body), text);
    }

    return config;
}

/**
* Updates an existing StripeCheckoutSession.
* Partially update an existing StripeCheckoutSession
*/
export function patchStripeCheckoutSessionById<T>(requestParameters: PatchStripeCheckoutSessionByIdApiRequest, requestConfig?: runtime.TypedQueryConfig<T, StripeCheckoutSession>): QueryConfig<T> {
    return patchStripeCheckoutSessionByIdRaw(requestParameters, requestConfig);
}

/**
 * Creates a new StripeCheckoutSession.
 * Create a new StripeCheckoutSession
 */
function postStripeCheckoutSessionRaw<T>(requestParameters: PostStripeCheckoutSessionApiRequest, requestConfig: runtime.TypedQueryConfig<T, StripeCheckoutSession> = {}): QueryConfig<T> {
    if (requestParameters.stripeCheckoutSession === null || requestParameters.stripeCheckoutSession === undefined) {
        throw new runtime.RequiredError('stripeCheckoutSession','Required parameter requestParameters.stripeCheckoutSession was null or undefined when calling postStripeCheckoutSession.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || StripeCheckoutSessionToJSON(requestParameters.stripeCheckoutSession),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(StripeCheckoutSessionFromJSON(body), text);
    }

    return config;
}

/**
* Creates a new StripeCheckoutSession.
* Create a new StripeCheckoutSession
*/
export function postStripeCheckoutSession<T>(requestParameters: PostStripeCheckoutSessionApiRequest, requestConfig?: runtime.TypedQueryConfig<T, StripeCheckoutSession>): QueryConfig<T> {
    return postStripeCheckoutSessionRaw(requestParameters, requestConfig);
}

/**
 * Updates an existing StripeCheckoutSession.
 * Update an existing StripeCheckoutSession
 */
function updateStripeCheckoutSessionRaw<T>(requestParameters: UpdateStripeCheckoutSessionApiRequest, requestConfig: runtime.TypedQueryConfig<T, StripeCheckoutSession> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateStripeCheckoutSession.');
    }

    if (requestParameters.stripeCheckoutSession === null || requestParameters.stripeCheckoutSession === undefined) {
        throw new runtime.RequiredError('stripeCheckoutSession','Required parameter requestParameters.stripeCheckoutSession was null or undefined when calling updateStripeCheckoutSession.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/StripeCheckoutSession/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters || StripeCheckoutSessionToJSON(requestParameters.stripeCheckoutSession),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(StripeCheckoutSessionFromJSON(body), text);
    }

    return config;
}

/**
* Updates an existing StripeCheckoutSession.
* Update an existing StripeCheckoutSession
*/
export function updateStripeCheckoutSession<T>(requestParameters: UpdateStripeCheckoutSessionApiRequest, requestConfig?: runtime.TypedQueryConfig<T, StripeCheckoutSession>): QueryConfig<T> {
    return updateStripeCheckoutSessionRaw(requestParameters, requestConfig);
}

