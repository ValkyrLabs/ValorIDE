// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  ProjectObjectLink,
  ProjectObjectLinkObjectTypeEnum,
  ProjectObjectLinkRelationEnum,
  ProjectObjectLinkStatusEnum,
} from '@thorapi/model';

import { useAddProjectObjectLinkMutation } from '../../services/ProjectObjectLinkService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Typed link between a Project and another RBAC-visible Valhalla object. Use this for achieved artifacts, todos, dependencies, references, and generated outputs when a direct typed relationship is not enough. 
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const ObjectTypeValidation = () => {
  return [
    'Application',
    'Workflow',
    'Task',
    'Goal',
    'Deployment',
    'Mcp',
    'MemoryEntry',
    'OasOpenAPISpec',
    'Workbook',
    'ContentData',
    'Customer',
    'Product',
    'IntegrationAccount',
    'External',
  ];
};
const RelationValidation = () => {
  return [
    'achieved',
    'todo',
    'dependency',
    'reference',
    'generated-artifact',
    'evidence',
  ];
};
const StatusValidation = () => {
  return [
    'planned',
    'active',
    'blocked',
    'completed',
    'archived',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        label: Yup.string(),
      objectType: Yup.mixed()
        .oneOf(ObjectTypeValidation(), "Invalid value for objectType")
        ,
        objectId: Yup.string(),
      relation: Yup.mixed()
        .oneOf(RelationValidation(), "Invalid value for relation")
        ,
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        ,
        url: Yup.string(),
        notes: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const ProjectObjectLinkForm: React.FC = () => {
  const [addProjectObjectLink, addProjectObjectLinkResult] = useAddProjectObjectLinkMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<ProjectObjectLink> = {
          label: '',
        objectType: undefined,
          objectId: '',
        relation: undefined,
        status: undefined,
          url: '',
          notes: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new ProjectObjectLink:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<ProjectObjectLink>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("ProjectObjectLink form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addProjectObjectLink(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `ProjectObjectLink created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create ProjectObjectLink:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as ProjectObjectLink}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addProjectObjectLinkResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New ProjectObjectLink
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="label" className="nice-form-control">
                      <b>
                        Label:
                        {touched.label &&
                         !errors.label && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="label"
                            value={values?.label}
                            placeholder="Label"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="label"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="objectType" className="nice-form-control">
                      <b>
                        Object Type:
                        {touched.objectType &&
                         !errors.objectType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="objectType"
                          value={values.objectType || ''}
                          className={
                            errors.objectType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('objectType', true);
                            setFieldValue('objectType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Object Type" />
                          <ObjectTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="objectType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="objectId" className="nice-form-control">
                      <b>
                        Object Id:
                        {touched.objectId &&
                         !errors.objectId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="objectId"
                            value={values?.objectId}
                            placeholder="Object Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="objectId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="relation" className="nice-form-control">
                      <b>
                        Relation:
                        {touched.relation &&
                         !errors.relation && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="relation"
                          value={values.relation || ''}
                          className={
                            errors.relation
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('relation', true);
                            setFieldValue('relation', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Relation" />
                          <RelationLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="relation"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="url" className="nice-form-control">
                      <b>
                        Url:
                        {touched.url &&
                         !errors.url && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="url"
                            value={values?.url}
                            placeholder="Url"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="url"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="notes" className="nice-form-control">
                      <b>
                        Notes:
                        {touched.notes &&
                         !errors.notes && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="notes"
                            value={values?.notes}
                            placeholder="Notes"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="notes"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New ProjectObjectLink
                  </CoolButton>

                  {(addProjectObjectLinkResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addProjectObjectLinkResult as any).error ? (addProjectObjectLinkResult as any).error.data : (addProjectObjectLinkResult as any).error)}
                    </Alert>
                  )}

                  {(addProjectObjectLinkResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addProjectObjectLinkResult: {JSON.stringify(addProjectObjectLinkResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.ProjectObjectLink"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase objecttypelookup
uppercase OBJECTTYPELOOKUP
snakecase object_type_lookup
pascalcase ObjectTypeLookup
camelcase objectTypeLookup
kebabcase object-type-lookup
*/

const ObjectTypeLookup = () => {
  return (
    <>
      <option value='Application' label="Application" />
      <option value='Workflow' label="Workflow" />
      <option value='Task' label="Task" />
      <option value='Goal' label="Goal" />
      <option value='Deployment' label="Deployment" />
      <option value='Mcp' label="Mcp" />
      <option value='MemoryEntry' label="Memory Entry" />
      <option value='OasOpenAPISpec' label="Oas Open Api Spec" />
      <option value='Workbook' label="Workbook" />
      <option value='ContentData' label="Content Data" />
      <option value='Customer' label="Customer" />
      <option value='Product' label="Product" />
      <option value='IntegrationAccount' label="Integration Account" />
      <option value='External' label="External" />
    </>
  );
};

/*
lowercase relationlookup
uppercase RELATIONLOOKUP
snakecase relation_lookup
pascalcase RelationLookup
camelcase relationLookup
kebabcase relation-lookup
*/

const RelationLookup = () => {
  return (
    <>
      <option value='achieved' label="Achieved" />
      <option value='todo' label="Todo" />
      <option value='dependency' label="Dependency" />
      <option value='reference' label="Reference" />
      <option value='generated-artifact' label="Generated Artifact" />
      <option value='evidence' label="Evidence" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='planned' label="Planned" />
      <option value='active' label="Active" />
      <option value='blocked' label="Blocked" />
      <option value='completed' label="Completed" />
      <option value='archived' label="Archived" />
    </>
  );
};



/* Export the generated form */
export default ProjectObjectLinkForm;

