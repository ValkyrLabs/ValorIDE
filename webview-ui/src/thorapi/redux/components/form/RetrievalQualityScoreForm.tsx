// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  RetrievalQualityScore,
} from '@thorapi/model';

import { useAddRetrievalQualityScoreMutation } from '../../services/RetrievalQualityScoreService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
RetrievalQualityScore
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        overallScore: asNumber(Yup.number().typeError("overallScore must be a number")),
        topSimilarityScore: asNumber(Yup.number().typeError("topSimilarityScore must be a number")),
        meanSimilarityScore: asNumber(Yup.number().typeError("meanSimilarityScore must be a number")),
        minSimilarityScore: asNumber(Yup.number().typeError("minSimilarityScore must be a number")),
        resultCount: asNumber(Yup.number().integer().typeError("resultCount must be a number")),
        sourceDiversityScore: asNumber(Yup.number().typeError("sourceDiversityScore must be a number")),
        freshnessScore: asNumber(Yup.number().typeError("freshnessScore must be a number")),
        schemaCoverageScore: asNumber(Yup.number().typeError("schemaCoverageScore must be a number")),
        authorityScore: asNumber(Yup.number().typeError("authorityScore must be a number")),
        contradictionScore: asNumber(Yup.number().typeError("contradictionScore must be a number")),
        completenessScore: asNumber(Yup.number().typeError("completenessScore must be a number")),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const RetrievalQualityScoreForm: React.FC = () => {
  const [addRetrievalQualityScore, addRetrievalQualityScoreResult] = useAddRetrievalQualityScoreMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<RetrievalQualityScore> = {
          overallScore: 0,
          topSimilarityScore: 0,
          meanSimilarityScore: 0,
          minSimilarityScore: 0,
          resultCount: 0,
          sourceDiversityScore: 0,
          freshnessScore: 0,
          schemaCoverageScore: 0,
          authorityScore: 0,
          contradictionScore: 0,
          completenessScore: 0,
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new RetrievalQualityScore:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<RetrievalQualityScore>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("RetrievalQualityScore form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addRetrievalQualityScore(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `RetrievalQualityScore created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create RetrievalQualityScore:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as RetrievalQualityScore}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addRetrievalQualityScoreResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New RetrievalQualityScore
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="overallScore" className="nice-form-control">
                      <b>
                        Overall Score:
                        {touched.overallScore &&
                         !errors.overallScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="overallScore"
                            type="number"
                            step="any"
                            value={values.overallScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('overallScore', true);
                              const v = e.target.value;
                              setFieldValue('overallScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.overallScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="overallScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="topSimilarityScore" className="nice-form-control">
                      <b>
                        Top Similarity Score:
                        {touched.topSimilarityScore &&
                         !errors.topSimilarityScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="topSimilarityScore"
                            type="number"
                            step="any"
                            value={values.topSimilarityScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('topSimilarityScore', true);
                              const v = e.target.value;
                              setFieldValue('topSimilarityScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.topSimilarityScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="topSimilarityScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="meanSimilarityScore" className="nice-form-control">
                      <b>
                        Mean Similarity Score:
                        {touched.meanSimilarityScore &&
                         !errors.meanSimilarityScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="meanSimilarityScore"
                            type="number"
                            step="any"
                            value={values.meanSimilarityScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('meanSimilarityScore', true);
                              const v = e.target.value;
                              setFieldValue('meanSimilarityScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.meanSimilarityScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="meanSimilarityScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="minSimilarityScore" className="nice-form-control">
                      <b>
                        Min Similarity Score:
                        {touched.minSimilarityScore &&
                         !errors.minSimilarityScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="minSimilarityScore"
                            type="number"
                            step="any"
                            value={values.minSimilarityScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('minSimilarityScore', true);
                              const v = e.target.value;
                              setFieldValue('minSimilarityScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.minSimilarityScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="minSimilarityScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="resultCount" className="nice-form-control">
                      <b>
                        Result Count:
                        {touched.resultCount &&
                         !errors.resultCount && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="resultCount"
                            type="number"
                            value={values.resultCount || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('resultCount', true);
                              const v = e.target.value;
                              setFieldValue('resultCount', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.resultCount
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="resultCount"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceDiversityScore" className="nice-form-control">
                      <b>
                        Source Diversity Score:
                        {touched.sourceDiversityScore &&
                         !errors.sourceDiversityScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="sourceDiversityScore"
                            type="number"
                            step="any"
                            value={values.sourceDiversityScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('sourceDiversityScore', true);
                              const v = e.target.value;
                              setFieldValue('sourceDiversityScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.sourceDiversityScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="sourceDiversityScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="freshnessScore" className="nice-form-control">
                      <b>
                        Freshness Score:
                        {touched.freshnessScore &&
                         !errors.freshnessScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="freshnessScore"
                            type="number"
                            step="any"
                            value={values.freshnessScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('freshnessScore', true);
                              const v = e.target.value;
                              setFieldValue('freshnessScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.freshnessScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="freshnessScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="schemaCoverageScore" className="nice-form-control">
                      <b>
                        Schema Coverage Score:
                        {touched.schemaCoverageScore &&
                         !errors.schemaCoverageScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="schemaCoverageScore"
                            type="number"
                            step="any"
                            value={values.schemaCoverageScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('schemaCoverageScore', true);
                              const v = e.target.value;
                              setFieldValue('schemaCoverageScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.schemaCoverageScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="schemaCoverageScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="authorityScore" className="nice-form-control">
                      <b>
                        Authority Score:
                        {touched.authorityScore &&
                         !errors.authorityScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="authorityScore"
                            type="number"
                            step="any"
                            value={values.authorityScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('authorityScore', true);
                              const v = e.target.value;
                              setFieldValue('authorityScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.authorityScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="authorityScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="contradictionScore" className="nice-form-control">
                      <b>
                        Contradiction Score:
                        {touched.contradictionScore &&
                         !errors.contradictionScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="contradictionScore"
                            type="number"
                            step="any"
                            value={values.contradictionScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('contradictionScore', true);
                              const v = e.target.value;
                              setFieldValue('contradictionScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.contradictionScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="contradictionScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="completenessScore" className="nice-form-control">
                      <b>
                        Completeness Score:
                        {touched.completenessScore &&
                         !errors.completenessScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="completenessScore"
                            type="number"
                            step="any"
                            value={values.completenessScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('completenessScore', true);
                              const v = e.target.value;
                              setFieldValue('completenessScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.completenessScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="completenessScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New RetrievalQualityScore
                  </CoolButton>

                  {(addRetrievalQualityScoreResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addRetrievalQualityScoreResult as any).error ? (addRetrievalQualityScoreResult as any).error.data : (addRetrievalQualityScoreResult as any).error)}
                    </Alert>
                  )}

                  {(addRetrievalQualityScoreResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addRetrievalQualityScoreResult: {JSON.stringify(addRetrievalQualityScoreResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.RetrievalQualityScore"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};



/* Export the generated form */
export default RetrievalQualityScoreForm;

