// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  TenantRuntimeBinding,
  TenantRuntimeBindingRuntimeKindEnum,
  TenantRuntimeBindingAuthModeEnum,
  TenantRuntimeBindingRbacModeEnum,
  TenantRuntimeBindingStatusEnum,
} from '@thorapi/model';

import { useAddTenantRuntimeBindingMutation } from '../../services/TenantRuntimeBindingService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Binding from an application deployment to exactly one runtime and data-plane placement.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const RuntimeKindValidation = () => {
  return [
    'valkyr_hosted',
    'lightsail',
    'fargate',
    'external',
    'local_dev',
  ];
};
const AuthModeValidation = () => {
  return [
    'valkyr_rbac',
    'tenant_rbac',
    'service_token',
    'delegated_oidc',
  ];
};
const RbacModeValidation = () => {
  return [
    'valkyr_managed',
    'tenant_managed',
    'hybrid',
  ];
};
const StatusValidation = () => {
  return [
    'requested',
    'binding',
    'active',
    'validating',
    'suspended',
    'failed',
    'retired',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
      runtimeKind: Yup.mixed()
        .oneOf(RuntimeKindValidation(), "Invalid value for runtimeKind")
        .required("runtimeKind is required."),
      authMode: Yup.mixed()
        .oneOf(AuthModeValidation(), "Invalid value for authMode")
        .required("authMode is required."),
      rbacMode: Yup.mixed()
        .oneOf(RbacModeValidation(), "Invalid value for rbacMode")
        .required("rbacMode is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        runtimeUrl: Yup.string(),
        boundAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("boundAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const TenantRuntimeBindingForm: React.FC = () => {
  const [addTenantRuntimeBinding, addTenantRuntimeBindingResult] = useAddTenantRuntimeBindingMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<TenantRuntimeBinding> = {
        runtimeKind: undefined,
        authMode: undefined,
        rbacMode: undefined,
        status: undefined,
          runtimeUrl: '',
          boundAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new TenantRuntimeBinding:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<TenantRuntimeBinding>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("TenantRuntimeBinding form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addTenantRuntimeBinding(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `TenantRuntimeBinding created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create TenantRuntimeBinding:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as TenantRuntimeBinding}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addTenantRuntimeBindingResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New TenantRuntimeBinding
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="runtimeKind" className="nice-form-control">
                      <b>
                        Runtime Kind:
                        {touched.runtimeKind &&
                         !errors.runtimeKind && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="runtimeKind"
                          value={values.runtimeKind || ''}
                          className={
                            errors.runtimeKind
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('runtimeKind', true);
                            setFieldValue('runtimeKind', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Runtime Kind" />
                          <RuntimeKindLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="runtimeKind"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="authMode" className="nice-form-control">
                      <b>
                        Auth Mode:
                        {touched.authMode &&
                         !errors.authMode && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="authMode"
                          value={values.authMode || ''}
                          className={
                            errors.authMode
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('authMode', true);
                            setFieldValue('authMode', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Auth Mode" />
                          <AuthModeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="authMode"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="rbacMode" className="nice-form-control">
                      <b>
                        Rbac Mode:
                        {touched.rbacMode &&
                         !errors.rbacMode && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="rbacMode"
                          value={values.rbacMode || ''}
                          className={
                            errors.rbacMode
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('rbacMode', true);
                            setFieldValue('rbacMode', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Rbac Mode" />
                          <RbacModeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="rbacMode"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="runtimeUrl" className="nice-form-control">
                      <b>
                        Runtime Url:
                        {touched.runtimeUrl &&
                         !errors.runtimeUrl && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="runtimeUrl"
                            value={values?.runtimeUrl}
                            placeholder="Runtime Url"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="runtimeUrl"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="boundAt" className="nice-form-control">
                      <b>
                        Bound At:
                        {touched.boundAt &&
                         !errors.boundAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="boundAt"
                            type="datetime-local"
                            value={values.boundAt ? 
                              new Date(values.boundAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('boundAt', true);
                              const v = e.target.value;
                              setFieldValue('boundAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.boundAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="boundAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New TenantRuntimeBinding
                  </CoolButton>

                  {(addTenantRuntimeBindingResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addTenantRuntimeBindingResult as any).error ? (addTenantRuntimeBindingResult as any).error.data : (addTenantRuntimeBindingResult as any).error)}
                    </Alert>
                  )}

                  {(addTenantRuntimeBindingResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addTenantRuntimeBindingResult: {JSON.stringify(addTenantRuntimeBindingResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.TenantRuntimeBinding"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase runtimekindlookup
uppercase RUNTIMEKINDLOOKUP
snakecase runtime_kind_lookup
pascalcase RuntimeKindLookup
camelcase runtimeKindLookup
kebabcase runtime-kind-lookup
*/

const RuntimeKindLookup = () => {
  return (
    <>
      <option value='valkyr_hosted' label="Valkyr Hosted" />
      <option value='lightsail' label="Lightsail" />
      <option value='fargate' label="Fargate" />
      <option value='external' label="External" />
      <option value='local_dev' label="Local Dev" />
    </>
  );
};

/*
lowercase authmodelookup
uppercase AUTHMODELOOKUP
snakecase auth_mode_lookup
pascalcase AuthModeLookup
camelcase authModeLookup
kebabcase auth-mode-lookup
*/

const AuthModeLookup = () => {
  return (
    <>
      <option value='valkyr_rbac' label="Valkyr Rbac" />
      <option value='tenant_rbac' label="Tenant Rbac" />
      <option value='service_token' label="Service Token" />
      <option value='delegated_oidc' label="Delegated Oidc" />
    </>
  );
};

/*
lowercase rbacmodelookup
uppercase RBACMODELOOKUP
snakecase rbac_mode_lookup
pascalcase RbacModeLookup
camelcase rbacModeLookup
kebabcase rbac-mode-lookup
*/

const RbacModeLookup = () => {
  return (
    <>
      <option value='valkyr_managed' label="Valkyr Managed" />
      <option value='tenant_managed' label="Tenant Managed" />
      <option value='hybrid' label="Hybrid" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='requested' label="Requested" />
      <option value='binding' label="Binding" />
      <option value='active' label="Active" />
      <option value='validating' label="Validating" />
      <option value='suspended' label="Suspended" />
      <option value='failed' label="Failed" />
      <option value='retired' label="Retired" />
    </>
  );
};



/* Export the generated form */
export default TenantRuntimeBindingForm;

