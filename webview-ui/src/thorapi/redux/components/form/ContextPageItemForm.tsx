// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  ContextPageItem,
  ContextPageItemSourceTypeEnum,
} from '@thorapi/model';

import { useAddContextPageItemMutation } from '../../services/ContextPageItemService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
One compact included item inside a ContextPage.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const SourceTypeValidation = () => {
  return [
    'memory',
    'semantic_index',
    'retrieval_receipt',
    'file',
    'model',
    'schema',
    'receipt',
    'procedure',
    'workflow',
    'generated_artifact',
    'tenant_app',
    'external_source',
    'other',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        itemRef: Yup.string().required("itemRef is required."),
      sourceType: Yup.mixed()
        .oneOf(SourceTypeValidation(), "Invalid value for sourceType")
        .required("sourceType is required."),
        sourceId: Yup.string().required("sourceId is required."),
        summary: Yup.string(),
        relevanceScore: asNumber(Yup.number().typeError("relevanceScore must be a number")),
        confidence: asNumber(Yup.number().typeError("confidence must be a number")),
        retained: Yup.boolean(),
        hydrated: Yup.boolean(),
        policyDecision: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const ContextPageItemForm: React.FC = () => {
  const [addContextPageItem, addContextPageItemResult] = useAddContextPageItemMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<ContextPageItem> = {
          itemRef: '',
        sourceType: undefined,
          sourceId: '',
          summary: '',
          relevanceScore: 0,
          confidence: 0,
          retained: false,
          hydrated: false,
          policyDecision: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new ContextPageItem:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<ContextPageItem>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("ContextPageItem form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addContextPageItem(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `ContextPageItem created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create ContextPageItem:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as ContextPageItem}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addContextPageItemResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New ContextPageItem
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="itemRef" className="nice-form-control">
                      <b>
                        Item Ref:
                        {touched.itemRef &&
                         !errors.itemRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="itemRef"
                            value={values?.itemRef}
                            placeholder="Item Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="itemRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceType" className="nice-form-control">
                      <b>
                        Source Type:
                        {touched.sourceType &&
                         !errors.sourceType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="sourceType"
                          value={values.sourceType || ''}
                          className={
                            errors.sourceType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('sourceType', true);
                            setFieldValue('sourceType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Source Type" />
                          <SourceTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="sourceType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceId" className="nice-form-control">
                      <b>
                        Source Id:
                        {touched.sourceId &&
                         !errors.sourceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="sourceId"
                            value={values?.sourceId}
                            placeholder="Source Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="sourceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="summary" className="nice-form-control">
                      <b>
                        Summary:
                        {touched.summary &&
                         !errors.summary && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="summary"
                            value={values?.summary}
                            placeholder="Summary"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="summary"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="relevanceScore" className="nice-form-control">
                      <b>
                        Relevance Score:
                        {touched.relevanceScore &&
                         !errors.relevanceScore && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="relevanceScore"
                            type="number"
                            step="any"
                            value={values.relevanceScore || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('relevanceScore', true);
                              const v = e.target.value;
                              setFieldValue('relevanceScore', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.relevanceScore
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="relevanceScore"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="confidence" className="nice-form-control">
                      <b>
                        Confidence:
                        {touched.confidence &&
                         !errors.confidence && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="confidence"
                            type="number"
                            step="any"
                            value={values.confidence || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('confidence', true);
                              const v = e.target.value;
                              setFieldValue('confidence', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.confidence
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="confidence"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="retained" className="nice-form-control">
                      <b>
                        Retained:
                        {touched.retained &&
                         !errors.retained && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="retained"
                            name="retained"
                            checked={values.retained || false}
                            onChange={(e) => {
                              setFieldTouched('retained', true);
                              setFieldValue('retained', e.target.checked);
                            }}
                            isInvalid={!!errors.retained}
                            className={errors.retained ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="retained"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="hydrated" className="nice-form-control">
                      <b>
                        Hydrated:
                        {touched.hydrated &&
                         !errors.hydrated && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="hydrated"
                            name="hydrated"
                            checked={values.hydrated || false}
                            onChange={(e) => {
                              setFieldTouched('hydrated', true);
                              setFieldValue('hydrated', e.target.checked);
                            }}
                            isInvalid={!!errors.hydrated}
                            className={errors.hydrated ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="hydrated"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="policyDecision" className="nice-form-control">
                      <b>
                        Policy Decision:
                        {touched.policyDecision &&
                         !errors.policyDecision && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="policyDecision"
                            value={values?.policyDecision}
                            placeholder="Policy Decision"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="policyDecision"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New ContextPageItem
                  </CoolButton>

                  {(addContextPageItemResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addContextPageItemResult as any).error ? (addContextPageItemResult as any).error.data : (addContextPageItemResult as any).error)}
                    </Alert>
                  )}

                  {(addContextPageItemResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addContextPageItemResult: {JSON.stringify(addContextPageItemResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.ContextPageItem"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase sourcetypelookup
uppercase SOURCETYPELOOKUP
snakecase source_type_lookup
pascalcase SourceTypeLookup
camelcase sourceTypeLookup
kebabcase source-type-lookup
*/

const SourceTypeLookup = () => {
  return (
    <>
      <option value='memory' label="Memory" />
      <option value='semantic_index' label="Semantic Index" />
      <option value='retrieval_receipt' label="Retrieval Receipt" />
      <option value='file' label="File" />
      <option value='model' label="Model" />
      <option value='schema' label="Schema" />
      <option value='receipt' label="Receipt" />
      <option value='procedure' label="Procedure" />
      <option value='workflow' label="Workflow" />
      <option value='generated_artifact' label="Generated Artifact" />
      <option value='tenant_app' label="Tenant App" />
      <option value='external_source' label="External Source" />
      <option value='other' label="Other" />
    </>
  );
};



/* Export the generated form */
export default ContextPageItemForm;

