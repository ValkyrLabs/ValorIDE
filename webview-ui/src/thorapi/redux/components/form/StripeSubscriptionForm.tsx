// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  StripeSubscription,
  StripeSubscriptionStatusEnum,
} from '@thorapi/model';

import { useAddStripeSubscriptionMutation } from '../../services/StripeSubscriptionService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Tenant/account scoped Stripe subscription state used for entitlement and recurring credit audit.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const StatusValidation = () => {
  return [
    'incomplete',
    'trialing',
    'active',
    'past_due',
    'canceled',
    'unpaid',
    'paused',
    'deleted',
    'unknown',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        subscriptionRef: Yup.string().required("subscriptionRef is required."),
        tenantId: Yup.string().required("tenantId is required."),
        accountId: Yup.string().required("accountId is required."),
        customerId: Yup.string().required("customerId is required."),
        stripeSubscriptionId: Yup.string().required("stripeSubscriptionId is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        idempotencyKey: Yup.string().required("idempotencyKey is required."),
        organizationId: Yup.string(),
        stripeCustomerId: Yup.string(),
        sku: Yup.string(),
        currentPeriodStart: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("currentPeriodStart must be a valid date"),
        currentPeriodEnd: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("currentPeriodEnd must be a valid date"),
        cancelAtPeriodEnd: Yup.boolean(),
        lastInvoiceId: Yup.string(),
        lastPaymentIntentId: Yup.string(),
        lastStripeEventId: Yup.string(),
        entitlementReceiptRef: Yup.string(),
        lastCreditReceiptRef: Yup.string(),
        metadataJson: Yup.string(),
        createdAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("createdAt must be a valid date"),
        updatedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("updatedAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const StripeSubscriptionForm: React.FC = () => {
  const [addStripeSubscription, addStripeSubscriptionResult] = useAddStripeSubscriptionMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<StripeSubscription> = {
          subscriptionRef: '',
          tenantId: '',
          accountId: '',
          customerId: '',
          stripeSubscriptionId: '',
        status: undefined,
          idempotencyKey: '',
          organizationId: '',
          stripeCustomerId: '',
          sku: '',
          currentPeriodStart: new Date(),
          currentPeriodEnd: new Date(),
          cancelAtPeriodEnd: false,
          lastInvoiceId: '',
          lastPaymentIntentId: '',
          lastStripeEventId: '',
          entitlementReceiptRef: '',
          lastCreditReceiptRef: '',
          metadataJson: '',
          createdAt: new Date(),
          updatedAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new StripeSubscription:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<StripeSubscription>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("StripeSubscription form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addStripeSubscription(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `StripeSubscription created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create StripeSubscription:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as StripeSubscription}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addStripeSubscriptionResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New StripeSubscription
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="subscriptionRef" className="nice-form-control">
                      <b>
                        Subscription Ref:
                        {touched.subscriptionRef &&
                         !errors.subscriptionRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="subscriptionRef"
                            value={values?.subscriptionRef}
                            placeholder="Subscription Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="subscriptionRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="accountId" className="nice-form-control">
                      <b>
                        Account Id:
                        {touched.accountId &&
                         !errors.accountId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="accountId"
                            value={values?.accountId}
                            placeholder="Account Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="accountId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="customerId" className="nice-form-control">
                      <b>
                        Customer Id:
                        {touched.customerId &&
                         !errors.customerId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="customerId"
                            value={values?.customerId}
                            placeholder="Customer Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="customerId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stripeSubscriptionId" className="nice-form-control">
                      <b>
                        Stripe Subscription Id:
                        {touched.stripeSubscriptionId &&
                         !errors.stripeSubscriptionId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stripeSubscriptionId"
                            value={values?.stripeSubscriptionId}
                            placeholder="Stripe Subscription Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stripeSubscriptionId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="idempotencyKey" className="nice-form-control">
                      <b>
                        Idempotency Key:
                        {touched.idempotencyKey &&
                         !errors.idempotencyKey && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="idempotencyKey"
                            value={values?.idempotencyKey}
                            placeholder="Idempotency Key"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="idempotencyKey"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="organizationId" className="nice-form-control">
                      <b>
                        Organization Id:
                        {touched.organizationId &&
                         !errors.organizationId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="organizationId"
                            value={values?.organizationId}
                            placeholder="Organization Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="organizationId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stripeCustomerId" className="nice-form-control">
                      <b>
                        Stripe Customer Id:
                        {touched.stripeCustomerId &&
                         !errors.stripeCustomerId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stripeCustomerId"
                            value={values?.stripeCustomerId}
                            placeholder="Stripe Customer Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stripeCustomerId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sku" className="nice-form-control">
                      <b>
                        Sku:
                        {touched.sku &&
                         !errors.sku && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="sku"
                            value={values?.sku}
                            placeholder="Sku"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="sku"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentPeriodStart" className="nice-form-control">
                      <b>
                        Current Period Start:
                        {touched.currentPeriodStart &&
                         !errors.currentPeriodStart && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="currentPeriodStart"
                            type="datetime-local"
                            value={values.currentPeriodStart ? 
                              new Date(values.currentPeriodStart).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('currentPeriodStart', true);
                              const v = e.target.value;
                              setFieldValue('currentPeriodStart', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.currentPeriodStart
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="currentPeriodStart"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentPeriodEnd" className="nice-form-control">
                      <b>
                        Current Period End:
                        {touched.currentPeriodEnd &&
                         !errors.currentPeriodEnd && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="currentPeriodEnd"
                            type="datetime-local"
                            value={values.currentPeriodEnd ? 
                              new Date(values.currentPeriodEnd).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('currentPeriodEnd', true);
                              const v = e.target.value;
                              setFieldValue('currentPeriodEnd', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.currentPeriodEnd
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="currentPeriodEnd"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="cancelAtPeriodEnd" className="nice-form-control">
                      <b>
                        Cancel At Period End:
                        {touched.cancelAtPeriodEnd &&
                         !errors.cancelAtPeriodEnd && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="cancelAtPeriodEnd"
                            name="cancelAtPeriodEnd"
                            checked={values.cancelAtPeriodEnd || false}
                            onChange={(e) => {
                              setFieldTouched('cancelAtPeriodEnd', true);
                              setFieldValue('cancelAtPeriodEnd', e.target.checked);
                            }}
                            isInvalid={!!errors.cancelAtPeriodEnd}
                            className={errors.cancelAtPeriodEnd ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="cancelAtPeriodEnd"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastInvoiceId" className="nice-form-control">
                      <b>
                        Last Invoice Id:
                        {touched.lastInvoiceId &&
                         !errors.lastInvoiceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="lastInvoiceId"
                            value={values?.lastInvoiceId}
                            placeholder="Last Invoice Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="lastInvoiceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastPaymentIntentId" className="nice-form-control">
                      <b>
                        Last Payment Intent Id:
                        {touched.lastPaymentIntentId &&
                         !errors.lastPaymentIntentId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="lastPaymentIntentId"
                            value={values?.lastPaymentIntentId}
                            placeholder="Last Payment Intent Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="lastPaymentIntentId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastStripeEventId" className="nice-form-control">
                      <b>
                        Last Stripe Event Id:
                        {touched.lastStripeEventId &&
                         !errors.lastStripeEventId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="lastStripeEventId"
                            value={values?.lastStripeEventId}
                            placeholder="Last Stripe Event Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="lastStripeEventId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="entitlementReceiptRef" className="nice-form-control">
                      <b>
                        Entitlement Receipt Ref:
                        {touched.entitlementReceiptRef &&
                         !errors.entitlementReceiptRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="entitlementReceiptRef"
                            value={values?.entitlementReceiptRef}
                            placeholder="Entitlement Receipt Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="entitlementReceiptRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lastCreditReceiptRef" className="nice-form-control">
                      <b>
                        Last Credit Receipt Ref:
                        {touched.lastCreditReceiptRef &&
                         !errors.lastCreditReceiptRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="lastCreditReceiptRef"
                            value={values?.lastCreditReceiptRef}
                            placeholder="Last Credit Receipt Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="lastCreditReceiptRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="metadataJson" className="nice-form-control">
                      <b>
                        Metadata Json:
                        {touched.metadataJson &&
                         !errors.metadataJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="metadataJson"
                            value={values?.metadataJson}
                            placeholder="Metadata Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="metadataJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="createdAt" className="nice-form-control">
                      <b>
                        Created At:
                        {touched.createdAt &&
                         !errors.createdAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="createdAt"
                            type="datetime-local"
                            value={values.createdAt ? 
                              new Date(values.createdAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('createdAt', true);
                              const v = e.target.value;
                              setFieldValue('createdAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.createdAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="createdAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="updatedAt" className="nice-form-control">
                      <b>
                        Updated At:
                        {touched.updatedAt &&
                         !errors.updatedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="updatedAt"
                            type="datetime-local"
                            value={values.updatedAt ? 
                              new Date(values.updatedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('updatedAt', true);
                              const v = e.target.value;
                              setFieldValue('updatedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.updatedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="updatedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New StripeSubscription
                  </CoolButton>

                  {(addStripeSubscriptionResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addStripeSubscriptionResult as any).error ? (addStripeSubscriptionResult as any).error.data : (addStripeSubscriptionResult as any).error)}
                    </Alert>
                  )}

                  {(addStripeSubscriptionResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addStripeSubscriptionResult: {JSON.stringify(addStripeSubscriptionResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.StripeSubscription"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='incomplete' label="Incomplete" />
      <option value='trialing' label="Trialing" />
      <option value='active' label="Active" />
      <option value='past_due' label="Past Due" />
      <option value='canceled' label="Canceled" />
      <option value='unpaid' label="Unpaid" />
      <option value='paused' label="Paused" />
      <option value='deleted' label="Deleted" />
      <option value='unknown' label="Unknown" />
    </>
  );
};



/* Export the generated form */
export default StripeSubscriptionForm;

