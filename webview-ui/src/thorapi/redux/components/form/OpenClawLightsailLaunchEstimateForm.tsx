// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  OpenClawLightsailLaunchEstimate,
} from '@thorapi/model';

import { useAddOpenClawLightsailLaunchEstimateMutation } from '../../services/OpenClawLightsailLaunchEstimateService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Prepaid credit estimate for an OpenClaw Lightsail launch.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        size: Yup.string(),
        cpu: asNumber(Yup.number().integer().typeError("cpu must be a number")),
        memoryMb: asNumber(Yup.number().integer().typeError("memoryMb must be a number")),
        runtimeMinutes: asNumber(Yup.number().integer().typeError("runtimeMinutes must be a number")),
        schemaObjectCount: asNumber(Yup.number().integer().typeError("schemaObjectCount must be a number")),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const OpenClawLightsailLaunchEstimateForm: React.FC = () => {
  const [addOpenClawLightsailLaunchEstimate, addOpenClawLightsailLaunchEstimateResult] = useAddOpenClawLightsailLaunchEstimateMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<OpenClawLightsailLaunchEstimate> = {
          size: '',
          cpu: 0,
          memoryMb: 0,
          runtimeMinutes: 0,
          schemaObjectCount: 0,
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new OpenClawLightsailLaunchEstimate:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<OpenClawLightsailLaunchEstimate>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("OpenClawLightsailLaunchEstimate form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addOpenClawLightsailLaunchEstimate(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `OpenClawLightsailLaunchEstimate created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create OpenClawLightsailLaunchEstimate:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as OpenClawLightsailLaunchEstimate}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addOpenClawLightsailLaunchEstimateResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New OpenClawLightsailLaunchEstimate
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="size" className="nice-form-control">
                      <b>
                        Size:
                        {touched.size &&
                         !errors.size && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="size"
                            value={values?.size}
                            placeholder="Size"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="size"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="cpu" className="nice-form-control">
                      <b>
                        Cpu:
                        {touched.cpu &&
                         !errors.cpu && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="cpu"
                            type="number"
                            value={values.cpu || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('cpu', true);
                              const v = e.target.value;
                              setFieldValue('cpu', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.cpu
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="cpu"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="memoryMb" className="nice-form-control">
                      <b>
                        Memory Mb:
                        {touched.memoryMb &&
                         !errors.memoryMb && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="memoryMb"
                            type="number"
                            value={values.memoryMb || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('memoryMb', true);
                              const v = e.target.value;
                              setFieldValue('memoryMb', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.memoryMb
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="memoryMb"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="runtimeMinutes" className="nice-form-control">
                      <b>
                        Runtime Minutes:
                        {touched.runtimeMinutes &&
                         !errors.runtimeMinutes && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="runtimeMinutes"
                            type="number"
                            value={values.runtimeMinutes || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('runtimeMinutes', true);
                              const v = e.target.value;
                              setFieldValue('runtimeMinutes', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.runtimeMinutes
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="runtimeMinutes"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="schemaObjectCount" className="nice-form-control">
                      <b>
                        Schema Object Count:
                        {touched.schemaObjectCount &&
                         !errors.schemaObjectCount && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="schemaObjectCount"
                            type="number"
                            value={values.schemaObjectCount || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('schemaObjectCount', true);
                              const v = e.target.value;
                              setFieldValue('schemaObjectCount', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.schemaObjectCount
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="schemaObjectCount"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="totalCredits" className="nice-form-control">
                      <b>
                        Total Credits:
                        {touched.totalCredits &&
                         !errors.totalCredits && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>










                      <ErrorMessage
                        className="error"
                        name="totalCredits"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New OpenClawLightsailLaunchEstimate
                  </CoolButton>

                  {(addOpenClawLightsailLaunchEstimateResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addOpenClawLightsailLaunchEstimateResult as any).error ? (addOpenClawLightsailLaunchEstimateResult as any).error.data : (addOpenClawLightsailLaunchEstimateResult as any).error)}
                    </Alert>
                  )}

                  {(addOpenClawLightsailLaunchEstimateResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addOpenClawLightsailLaunchEstimateResult: {JSON.stringify(addOpenClawLightsailLaunchEstimateResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.OpenClawLightsailLaunchEstimate"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};



/* Export the generated form */
export default OpenClawLightsailLaunchEstimateForm;

