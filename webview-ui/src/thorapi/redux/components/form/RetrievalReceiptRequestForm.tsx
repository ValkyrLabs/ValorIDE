// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  RetrievalReceiptRequest,
} from '@thorapi/model';

import { useAddRetrievalReceiptRequestMutation } from '../../services/RetrievalReceiptRequestService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Request for a GrayMatter memory lookup with an auditable retrieval receipt.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        query: Yup.string().required("query is required."),
        agentId: Yup.string(),
        workflowId: Yup.string(),
        tenantId: Yup.string(),
        topK: asNumber(Yup.number().integer().typeError("topK must be a number")),
        includeItems: Yup.boolean(),
        includeText: Yup.boolean(),
        includeEvaluator: Yup.boolean(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const RetrievalReceiptRequestForm: React.FC = () => {
  const [addRetrievalReceiptRequest, addRetrievalReceiptRequestResult] = useAddRetrievalReceiptRequestMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<RetrievalReceiptRequest> = {
          query: '',
          agentId: '',
          workflowId: '',
          tenantId: '',
          topK: 0,
          includeItems: false,
          includeText: false,
          includeEvaluator: false,
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new RetrievalReceiptRequest:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<RetrievalReceiptRequest>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("RetrievalReceiptRequest form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addRetrievalReceiptRequest(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `RetrievalReceiptRequest created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create RetrievalReceiptRequest:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as RetrievalReceiptRequest}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addRetrievalReceiptRequestResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New RetrievalReceiptRequest
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="query" className="nice-form-control">
                      <b>
                        Query:
                        {touched.query &&
                         !errors.query && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="query"
                            value={values?.query}
                            placeholder="Query"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="query"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="agentId" className="nice-form-control">
                      <b>
                        Agent Id:
                        {touched.agentId &&
                         !errors.agentId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="agentId"
                            value={values?.agentId}
                            placeholder="Agent Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="agentId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="workflowId" className="nice-form-control">
                      <b>
                        Workflow Id:
                        {touched.workflowId &&
                         !errors.workflowId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="workflowId"
                            value={values?.workflowId}
                            placeholder="Workflow Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="workflowId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="topK" className="nice-form-control">
                      <b>
                        Top K:
                        {touched.topK &&
                         !errors.topK && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="topK"
                            type="number"
                            value={values.topK || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('topK', true);
                              const v = e.target.value;
                              setFieldValue('topK', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.topK
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="topK"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="includeItems" className="nice-form-control">
                      <b>
                        Include Items:
                        {touched.includeItems &&
                         !errors.includeItems && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="includeItems"
                            name="includeItems"
                            checked={values.includeItems || false}
                            onChange={(e) => {
                              setFieldTouched('includeItems', true);
                              setFieldValue('includeItems', e.target.checked);
                            }}
                            isInvalid={!!errors.includeItems}
                            className={errors.includeItems ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="includeItems"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="includeText" className="nice-form-control">
                      <b>
                        Include Text:
                        {touched.includeText &&
                         !errors.includeText && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="includeText"
                            name="includeText"
                            checked={values.includeText || false}
                            onChange={(e) => {
                              setFieldTouched('includeText', true);
                              setFieldValue('includeText', e.target.checked);
                            }}
                            isInvalid={!!errors.includeText}
                            className={errors.includeText ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="includeText"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="includeEvaluator" className="nice-form-control">
                      <b>
                        Include Evaluator:
                        {touched.includeEvaluator &&
                         !errors.includeEvaluator && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="includeEvaluator"
                            name="includeEvaluator"
                            checked={values.includeEvaluator || false}
                            onChange={(e) => {
                              setFieldTouched('includeEvaluator', true);
                              setFieldValue('includeEvaluator', e.target.checked);
                            }}
                            isInvalid={!!errors.includeEvaluator}
                            className={errors.includeEvaluator ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="includeEvaluator"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New RetrievalReceiptRequest
                  </CoolButton>

                  {(addRetrievalReceiptRequestResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addRetrievalReceiptRequestResult as any).error ? (addRetrievalReceiptRequestResult as any).error.data : (addRetrievalReceiptRequestResult as any).error)}
                    </Alert>
                  )}

                  {(addRetrievalReceiptRequestResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addRetrievalReceiptRequestResult: {JSON.stringify(addRetrievalReceiptRequestResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.RetrievalReceiptRequest"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};



/* Export the generated form */
export default RetrievalReceiptRequestForm;

