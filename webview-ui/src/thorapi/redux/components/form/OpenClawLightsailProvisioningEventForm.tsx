// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  OpenClawLightsailProvisioningEvent,
} from '@thorapi/model';

import { useAddOpenClawLightsailProvisioningEventMutation } from '../../services/OpenClawLightsailProvisioningEventService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Realtime provisioning event for one-click OpenClaw Lightsail launches.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        launchId: Yup.string(),
        stage: Yup.string(),
        status: Yup.string(),
        message: Yup.string(),
        progress: asNumber(Yup.number().integer().typeError("progress must be a number")),
        currentBalance: asNumber(Yup.number().integer().typeError("currentBalance must be a number")),
        occurredAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("occurredAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const OpenClawLightsailProvisioningEventForm: React.FC = () => {
  const [addOpenClawLightsailProvisioningEvent, addOpenClawLightsailProvisioningEventResult] = useAddOpenClawLightsailProvisioningEventMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<OpenClawLightsailProvisioningEvent> = {
          launchId: '',
          stage: '',
          status: '',
          message: '',
          progress: 0,
          currentBalance: 0,
          occurredAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new OpenClawLightsailProvisioningEvent:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<OpenClawLightsailProvisioningEvent>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("OpenClawLightsailProvisioningEvent form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addOpenClawLightsailProvisioningEvent(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `OpenClawLightsailProvisioningEvent created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create OpenClawLightsailProvisioningEvent:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as OpenClawLightsailProvisioningEvent}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addOpenClawLightsailProvisioningEventResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New OpenClawLightsailProvisioningEvent
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="launchId" className="nice-form-control">
                      <b>
                        Launch Id:
                        {touched.launchId &&
                         !errors.launchId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="launchId"
                            value={values?.launchId}
                            placeholder="Launch Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="launchId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stage" className="nice-form-control">
                      <b>
                        Stage:
                        {touched.stage &&
                         !errors.stage && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stage"
                            value={values?.stage}
                            placeholder="Stage"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stage"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="status"
                            value={values?.status}
                            placeholder="Status"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="message" className="nice-form-control">
                      <b>
                        Message:
                        {touched.message &&
                         !errors.message && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="message"
                            value={values?.message}
                            placeholder="Message"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="message"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="progress" className="nice-form-control">
                      <b>
                        Progress:
                        {touched.progress &&
                         !errors.progress && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="progress"
                            type="number"
                            value={values.progress || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('progress', true);
                              const v = e.target.value;
                              setFieldValue('progress', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.progress
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="progress"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentBalance" className="nice-form-control">
                      <b>
                        Current Balance:
                        {touched.currentBalance &&
                         !errors.currentBalance && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>







                          {/* LONG FIELD */}
                          <Field
                            name="currentBalance"
                            type="number"
                            value={values.currentBalance || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('currentBalance', true);
                              const v = e.target.value;
                              setFieldValue('currentBalance', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.currentBalance
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />



                      <ErrorMessage
                        className="error"
                        name="currentBalance"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="occurredAt" className="nice-form-control">
                      <b>
                        Occurred At:
                        {touched.occurredAt &&
                         !errors.occurredAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="occurredAt"
                            type="datetime-local"
                            value={values.occurredAt ? 
                              new Date(values.occurredAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('occurredAt', true);
                              const v = e.target.value;
                              setFieldValue('occurredAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.occurredAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="occurredAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New OpenClawLightsailProvisioningEvent
                  </CoolButton>

                  {(addOpenClawLightsailProvisioningEventResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addOpenClawLightsailProvisioningEventResult as any).error ? (addOpenClawLightsailProvisioningEventResult as any).error.data : (addOpenClawLightsailProvisioningEventResult as any).error)}
                    </Alert>
                  )}

                  {(addOpenClawLightsailProvisioningEventResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addOpenClawLightsailProvisioningEventResult: {JSON.stringify(addOpenClawLightsailProvisioningEventResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.OpenClawLightsailProvisioningEvent"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};



/* Export the generated form */
export default OpenClawLightsailProvisioningEventForm;

