// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  TenantSchemaAccessAudit,
  TenantSchemaAccessAuditActionEnum,
  TenantSchemaAccessAuditResultEnum,
} from '@thorapi/model';

import { useAddTenantSchemaAccessAuditMutation } from '../../services/TenantSchemaAccessAuditService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Mandatory audit row for every Valkyr admin access to customer schema data.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const ActionValidation = () => {
  return [
    'read',
    'search',
    'export',
    'update',
    'delete',
    'validate',
    'impersonation_denied',
  ];
};
const ResultValidation = () => {
  return [
    'allowed',
    'denied',
    'failed',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        adminPrincipalId: Yup.string().required("adminPrincipalId is required."),
      action: Yup.mixed()
        .oneOf(ActionValidation(), "Invalid value for action")
        .required("action is required."),
        resourceType: Yup.string().required("resourceType is required."),
        reason: Yup.string().required("reason is required."),
        requestId: Yup.string().required("requestId is required."),
      result: Yup.mixed()
        .oneOf(ResultValidation(), "Invalid value for result")
        .required("result is required."),
        occurredAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).required("occurredAt is required.").typeError("occurredAt must be a valid date"),
        resourceId: Yup.string(),
        sourceIp: Yup.string(),
        userAgent: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const TenantSchemaAccessAuditForm: React.FC = () => {
  const [addTenantSchemaAccessAudit, addTenantSchemaAccessAuditResult] = useAddTenantSchemaAccessAuditMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<TenantSchemaAccessAudit> = {
          adminPrincipalId: '',
        action: undefined,
          resourceType: '',
          reason: '',
          requestId: '',
        result: undefined,
          occurredAt: new Date(),
          resourceId: '',
          sourceIp: '',
          userAgent: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new TenantSchemaAccessAudit:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<TenantSchemaAccessAudit>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("TenantSchemaAccessAudit form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addTenantSchemaAccessAudit(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `TenantSchemaAccessAudit created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create TenantSchemaAccessAudit:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as TenantSchemaAccessAudit}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addTenantSchemaAccessAuditResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New TenantSchemaAccessAudit
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="adminPrincipalId" className="nice-form-control">
                      <b>
                        Admin Principal Id:
                        {touched.adminPrincipalId &&
                         !errors.adminPrincipalId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="adminPrincipalId"
                            value={values?.adminPrincipalId}
                            placeholder="Admin Principal Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="adminPrincipalId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="action" className="nice-form-control">
                      <b>
                        Action:
                        {touched.action &&
                         !errors.action && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="action"
                          value={values.action || ''}
                          className={
                            errors.action
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('action', true);
                            setFieldValue('action', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Action" />
                          <ActionLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="action"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="resourceType" className="nice-form-control">
                      <b>
                        Resource Type:
                        {touched.resourceType &&
                         !errors.resourceType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="resourceType"
                            value={values?.resourceType}
                            placeholder="Resource Type"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="resourceType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="reason" className="nice-form-control">
                      <b>
                        Reason:
                        {touched.reason &&
                         !errors.reason && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="reason"
                            value={values?.reason}
                            placeholder="Reason"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="reason"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="requestId" className="nice-form-control">
                      <b>
                        Request Id:
                        {touched.requestId &&
                         !errors.requestId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="requestId"
                            value={values?.requestId}
                            placeholder="Request Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="requestId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="result" className="nice-form-control">
                      <b>
                        Result:
                        {touched.result &&
                         !errors.result && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="result"
                          value={values.result || ''}
                          className={
                            errors.result
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('result', true);
                            setFieldValue('result', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Result" />
                          <ResultLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="result"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="occurredAt" className="nice-form-control">
                      <b>
                        Occurred At:
                        {touched.occurredAt &&
                         !errors.occurredAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="occurredAt"
                            type="datetime-local"
                            value={values.occurredAt ? 
                              new Date(values.occurredAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('occurredAt', true);
                              const v = e.target.value;
                              setFieldValue('occurredAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.occurredAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="occurredAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="resourceId" className="nice-form-control">
                      <b>
                        Resource Id:
                        {touched.resourceId &&
                         !errors.resourceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="resourceId"
                            value={values?.resourceId}
                            placeholder="Resource Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="resourceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceIp" className="nice-form-control">
                      <b>
                        Source Ip:
                        {touched.sourceIp &&
                         !errors.sourceIp && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="sourceIp"
                            value={values?.sourceIp}
                            placeholder="Source Ip"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="sourceIp"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="userAgent" className="nice-form-control">
                      <b>
                        User Agent:
                        {touched.userAgent &&
                         !errors.userAgent && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="userAgent"
                            value={values?.userAgent}
                            placeholder="User Agent"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="userAgent"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New TenantSchemaAccessAudit
                  </CoolButton>

                  {(addTenantSchemaAccessAuditResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addTenantSchemaAccessAuditResult as any).error ? (addTenantSchemaAccessAuditResult as any).error.data : (addTenantSchemaAccessAuditResult as any).error)}
                    </Alert>
                  )}

                  {(addTenantSchemaAccessAuditResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addTenantSchemaAccessAuditResult: {JSON.stringify(addTenantSchemaAccessAuditResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.TenantSchemaAccessAudit"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase actionlookup
uppercase ACTIONLOOKUP
snakecase action_lookup
pascalcase ActionLookup
camelcase actionLookup
kebabcase action-lookup
*/

const ActionLookup = () => {
  return (
    <>
      <option value='read' label="Read" />
      <option value='search' label="Search" />
      <option value='export' label="Export" />
      <option value='update' label="Update" />
      <option value='delete' label="Delete" />
      <option value='validate' label="Validate" />
      <option value='impersonation_denied' label="Impersonation Denied" />
    </>
  );
};

/*
lowercase resultlookup
uppercase RESULTLOOKUP
snakecase result_lookup
pascalcase ResultLookup
camelcase resultLookup
kebabcase result-lookup
*/

const ResultLookup = () => {
  return (
    <>
      <option value='allowed' label="Allowed" />
      <option value='denied' label="Denied" />
      <option value='failed' label="Failed" />
    </>
  );
};



/* Export the generated form */
export default TenantSchemaAccessAuditForm;

