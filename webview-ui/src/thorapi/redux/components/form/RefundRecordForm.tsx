// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  RefundRecord,
  RefundRecordStatusEnum,
} from '@thorapi/model';

import { useAddRefundRecordMutation } from '../../services/RefundRecordService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Credit reversal ledger leg created from Stripe refunds, chargebacks, or administrative refund policy.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const StatusValidation = () => {
  return [
    'pending',
    'succeeded',
    'failed',
    'canceled',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        refundRef: Yup.string().required("refundRef is required."),
        customerId: Yup.string().required("customerId is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        idempotencyKey: Yup.string().required("idempotencyKey is required."),
        stripePaymentIntentId: Yup.string(),
        stripeRefundId: Yup.string(),
        stripeEventId: Yup.string(),
        amountCents: asNumber(Yup.number().integer().typeError("amountCents must be a number")),
        reason: Yup.string(),
        currentBalance: asNumber(Yup.number().integer().typeError("currentBalance must be a number")),
        receiptJson: Yup.string(),
        createdAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("createdAt must be a valid date"),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const RefundRecordForm: React.FC = () => {
  const [addRefundRecord, addRefundRecordResult] = useAddRefundRecordMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<RefundRecord> = {
          refundRef: '',
          customerId: '',
        status: undefined,
          idempotencyKey: '',
          stripePaymentIntentId: '',
          stripeRefundId: '',
          stripeEventId: '',
          amountCents: 0,
          reason: '',
          currentBalance: 0,
          receiptJson: '',
          createdAt: new Date(),
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new RefundRecord:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<RefundRecord>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("RefundRecord form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addRefundRecord(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `RefundRecord created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create RefundRecord:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as RefundRecord}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addRefundRecordResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New RefundRecord
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="refundRef" className="nice-form-control">
                      <b>
                        Refund Ref:
                        {touched.refundRef &&
                         !errors.refundRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="refundRef"
                            value={values?.refundRef}
                            placeholder="Refund Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="refundRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="customerId" className="nice-form-control">
                      <b>
                        Customer Id:
                        {touched.customerId &&
                         !errors.customerId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="customerId"
                            value={values?.customerId}
                            placeholder="Customer Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="customerId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="creditsReversed" className="nice-form-control">
                      <b>
                        Credits Reversed:
                        {touched.creditsReversed &&
                         !errors.creditsReversed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>










                      <ErrorMessage
                        className="error"
                        name="creditsReversed"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="idempotencyKey" className="nice-form-control">
                      <b>
                        Idempotency Key:
                        {touched.idempotencyKey &&
                         !errors.idempotencyKey && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="idempotencyKey"
                            value={values?.idempotencyKey}
                            placeholder="Idempotency Key"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="idempotencyKey"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stripePaymentIntentId" className="nice-form-control">
                      <b>
                        Stripe Payment Intent Id:
                        {touched.stripePaymentIntentId &&
                         !errors.stripePaymentIntentId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stripePaymentIntentId"
                            value={values?.stripePaymentIntentId}
                            placeholder="Stripe Payment Intent Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stripePaymentIntentId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stripeRefundId" className="nice-form-control">
                      <b>
                        Stripe Refund Id:
                        {touched.stripeRefundId &&
                         !errors.stripeRefundId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stripeRefundId"
                            value={values?.stripeRefundId}
                            placeholder="Stripe Refund Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stripeRefundId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="stripeEventId" className="nice-form-control">
                      <b>
                        Stripe Event Id:
                        {touched.stripeEventId &&
                         !errors.stripeEventId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="stripeEventId"
                            value={values?.stripeEventId}
                            placeholder="Stripe Event Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="stripeEventId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="amountCents" className="nice-form-control">
                      <b>
                        Amount Cents:
                        {touched.amountCents &&
                         !errors.amountCents && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="amountCents"
                            type="number"
                            value={values.amountCents || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('amountCents', true);
                              const v = e.target.value;
                              setFieldValue('amountCents', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.amountCents
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="amountCents"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="reason" className="nice-form-control">
                      <b>
                        Reason:
                        {touched.reason &&
                         !errors.reason && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="reason"
                            value={values?.reason}
                            placeholder="Reason"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="reason"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentBalance" className="nice-form-control">
                      <b>
                        Current Balance:
                        {touched.currentBalance &&
                         !errors.currentBalance && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>







                          {/* LONG FIELD */}
                          <Field
                            name="currentBalance"
                            type="number"
                            value={values.currentBalance || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('currentBalance', true);
                              const v = e.target.value;
                              setFieldValue('currentBalance', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.currentBalance
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />



                      <ErrorMessage
                        className="error"
                        name="currentBalance"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="receiptJson" className="nice-form-control">
                      <b>
                        Receipt Json:
                        {touched.receiptJson &&
                         !errors.receiptJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="receiptJson"
                            value={values?.receiptJson}
                            placeholder="Receipt Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="receiptJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="createdAt" className="nice-form-control">
                      <b>
                        Created At:
                        {touched.createdAt &&
                         !errors.createdAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="createdAt"
                            type="datetime-local"
                            value={values.createdAt ? 
                              new Date(values.createdAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('createdAt', true);
                              const v = e.target.value;
                              setFieldValue('createdAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.createdAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="createdAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New RefundRecord
                  </CoolButton>

                  {(addRefundRecordResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addRefundRecordResult as any).error ? (addRefundRecordResult as any).error.data : (addRefundRecordResult as any).error)}
                    </Alert>
                  )}

                  {(addRefundRecordResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addRefundRecordResult: {JSON.stringify(addRefundRecordResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.RefundRecord"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='pending' label="Pending" />
      <option value='succeeded' label="Succeeded" />
      <option value='failed' label="Failed" />
      <option value='canceled' label="Canceled" />
    </>
  );
};



/* Export the generated form */
export default RefundRecordForm;

