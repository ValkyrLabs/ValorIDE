// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  SchemaEvolutionLedger,
  SchemaEvolutionLedgerCategoryEnum,
  SchemaEvolutionLedgerSourceKindEnum,
  SchemaEvolutionLedgerStatusEnum,
} from '@thorapi/model';

import { useAddSchemaEvolutionLedgerMutation } from '../../services/SchemaEvolutionLedgerService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
ThorAPI-generated ledger row for automatic, idempotent schema reconciliation derived from OpenAPI/entity contracts.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const CategoryValidation = () => {
  return [
    'string_widening',
    'enum_constraint_expansion',
    'nullable_column_addition',
    'data_backfill',
    'manual_review',
  ];
};
const SourceKindValidation = () => {
  return [
    'thorapi_openapi',
    'generated_entity_metadata',
    'tenant_bundle',
  ];
};
const StatusValidation = () => {
  return [
    'planned',
    'applying',
    'applied',
    'skipped',
    'failed',
    'manual_review_required',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        schemaName: Yup.string().required("schemaName is required."),
      category: Yup.mixed()
        .oneOf(CategoryValidation(), "Invalid value for category")
        .required("category is required."),
        statementHash: Yup.string().required("statementHash is required."),
        statement: Yup.string().required("statement is required."),
      sourceKind: Yup.mixed()
        .oneOf(SourceKindValidation(), "Invalid value for sourceKind")
        .required("sourceKind is required."),
        migrationIdentity: Yup.string().required("migrationIdentity is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        safeAutoApply: Yup.boolean().required("safeAutoApply is required."),
        autoApplied: Yup.boolean().required("autoApplied is required."),
        approvalRequired: Yup.boolean().required("approvalRequired is required."),
        plannedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).required("plannedAt is required.").typeError("plannedAt must be a valid date"),
        tableName: Yup.string(),
        columnName: Yup.string(),
        sourceChecksum: Yup.string(),
        lockToken: Yup.string(),
        appliedAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("appliedAt must be a valid date"),
        errorMessage: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const SchemaEvolutionLedgerForm: React.FC = () => {
  const [addSchemaEvolutionLedger, addSchemaEvolutionLedgerResult] = useAddSchemaEvolutionLedgerMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<SchemaEvolutionLedger> = {
          schemaName: '',
        category: undefined,
          statementHash: '',
          statement: '',
        sourceKind: undefined,
          migrationIdentity: '',
        status: undefined,
          safeAutoApply: false,
          autoApplied: false,
          approvalRequired: false,
          plannedAt: new Date(),
          tableName: '',
          columnName: '',
          sourceChecksum: '',
          lockToken: '',
          appliedAt: new Date(),
          errorMessage: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new SchemaEvolutionLedger:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<SchemaEvolutionLedger>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("SchemaEvolutionLedger form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addSchemaEvolutionLedger(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `SchemaEvolutionLedger created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create SchemaEvolutionLedger:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as SchemaEvolutionLedger}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addSchemaEvolutionLedgerResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New SchemaEvolutionLedger
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="schemaName" className="nice-form-control">
                      <b>
                        Schema Name:
                        {touched.schemaName &&
                         !errors.schemaName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="schemaName"
                            value={values?.schemaName}
                            placeholder="Schema Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="schemaName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="category" className="nice-form-control">
                      <b>
                        Category:
                        {touched.category &&
                         !errors.category && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="category"
                          value={values.category || ''}
                          className={
                            errors.category
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('category', true);
                            setFieldValue('category', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Category" />
                          <CategoryLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="category"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="statementHash" className="nice-form-control">
                      <b>
                        Statement Hash:
                        {touched.statementHash &&
                         !errors.statementHash && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="statementHash"
                            value={values?.statementHash}
                            placeholder="Statement Hash"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="statementHash"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="statement" className="nice-form-control">
                      <b>
                        Statement:
                        {touched.statement &&
                         !errors.statement && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="statement"
                            value={values?.statement}
                            placeholder="Statement"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="statement"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceKind" className="nice-form-control">
                      <b>
                        Source Kind:
                        {touched.sourceKind &&
                         !errors.sourceKind && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="sourceKind"
                          value={values.sourceKind || ''}
                          className={
                            errors.sourceKind
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('sourceKind', true);
                            setFieldValue('sourceKind', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Source Kind" />
                          <SourceKindLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="sourceKind"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="migrationIdentity" className="nice-form-control">
                      <b>
                        Migration Identity:
                        {touched.migrationIdentity &&
                         !errors.migrationIdentity && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="migrationIdentity"
                            value={values?.migrationIdentity}
                            placeholder="Migration Identity"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="migrationIdentity"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="safeAutoApply" className="nice-form-control">
                      <b>
                        Safe Auto Apply:
                        {touched.safeAutoApply &&
                         !errors.safeAutoApply && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="safeAutoApply"
                            name="safeAutoApply"
                            checked={values.safeAutoApply || false}
                            onChange={(e) => {
                              setFieldTouched('safeAutoApply', true);
                              setFieldValue('safeAutoApply', e.target.checked);
                            }}
                            isInvalid={!!errors.safeAutoApply}
                            className={errors.safeAutoApply ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="safeAutoApply"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="autoApplied" className="nice-form-control">
                      <b>
                        Auto Applied:
                        {touched.autoApplied &&
                         !errors.autoApplied && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="autoApplied"
                            name="autoApplied"
                            checked={values.autoApplied || false}
                            onChange={(e) => {
                              setFieldTouched('autoApplied', true);
                              setFieldValue('autoApplied', e.target.checked);
                            }}
                            isInvalid={!!errors.autoApplied}
                            className={errors.autoApplied ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="autoApplied"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="approvalRequired" className="nice-form-control">
                      <b>
                        Approval Required:
                        {touched.approvalRequired &&
                         !errors.approvalRequired && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="approvalRequired"
                            name="approvalRequired"
                            checked={values.approvalRequired || false}
                            onChange={(e) => {
                              setFieldTouched('approvalRequired', true);
                              setFieldValue('approvalRequired', e.target.checked);
                            }}
                            isInvalid={!!errors.approvalRequired}
                            className={errors.approvalRequired ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="approvalRequired"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="plannedAt" className="nice-form-control">
                      <b>
                        Planned At:
                        {touched.plannedAt &&
                         !errors.plannedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="plannedAt"
                            type="datetime-local"
                            value={values.plannedAt ? 
                              new Date(values.plannedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('plannedAt', true);
                              const v = e.target.value;
                              setFieldValue('plannedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.plannedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="plannedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tableName" className="nice-form-control">
                      <b>
                        Table Name:
                        {touched.tableName &&
                         !errors.tableName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tableName"
                            value={values?.tableName}
                            placeholder="Table Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tableName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="columnName" className="nice-form-control">
                      <b>
                        Column Name:
                        {touched.columnName &&
                         !errors.columnName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="columnName"
                            value={values?.columnName}
                            placeholder="Column Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="columnName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="sourceChecksum" className="nice-form-control">
                      <b>
                        Source Checksum:
                        {touched.sourceChecksum &&
                         !errors.sourceChecksum && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="sourceChecksum"
                            value={values?.sourceChecksum}
                            placeholder="Source Checksum"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="sourceChecksum"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="lockToken" className="nice-form-control">
                      <b>
                        Lock Token:
                        {touched.lockToken &&
                         !errors.lockToken && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="lockToken"
                            value={values?.lockToken}
                            placeholder="Lock Token"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="lockToken"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="appliedAt" className="nice-form-control">
                      <b>
                        Applied At:
                        {touched.appliedAt &&
                         !errors.appliedAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="appliedAt"
                            type="datetime-local"
                            value={values.appliedAt ? 
                              new Date(values.appliedAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('appliedAt', true);
                              const v = e.target.value;
                              setFieldValue('appliedAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.appliedAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="appliedAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="errorMessage" className="nice-form-control">
                      <b>
                        Error Message:
                        {touched.errorMessage &&
                         !errors.errorMessage && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="errorMessage"
                            value={values?.errorMessage}
                            placeholder="Error Message"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="errorMessage"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New SchemaEvolutionLedger
                  </CoolButton>

                  {(addSchemaEvolutionLedgerResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addSchemaEvolutionLedgerResult as any).error ? (addSchemaEvolutionLedgerResult as any).error.data : (addSchemaEvolutionLedgerResult as any).error)}
                    </Alert>
                  )}

                  {(addSchemaEvolutionLedgerResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addSchemaEvolutionLedgerResult: {JSON.stringify(addSchemaEvolutionLedgerResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.SchemaEvolutionLedger"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase categorylookup
uppercase CATEGORYLOOKUP
snakecase category_lookup
pascalcase CategoryLookup
camelcase categoryLookup
kebabcase category-lookup
*/

const CategoryLookup = () => {
  return (
    <>
      <option value='string_widening' label="String Widening" />
      <option value='enum_constraint_expansion' label="Enum Constraint Expansion" />
      <option value='nullable_column_addition' label="Nullable Column Addition" />
      <option value='data_backfill' label="Data Backfill" />
      <option value='manual_review' label="Manual Review" />
    </>
  );
};

/*
lowercase sourcekindlookup
uppercase SOURCEKINDLOOKUP
snakecase source_kind_lookup
pascalcase SourceKindLookup
camelcase sourceKindLookup
kebabcase source-kind-lookup
*/

const SourceKindLookup = () => {
  return (
    <>
      <option value='thorapi_openapi' label="Thorapi Openapi" />
      <option value='generated_entity_metadata' label="Generated Entity Metadata" />
      <option value='tenant_bundle' label="Tenant Bundle" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='planned' label="Planned" />
      <option value='applying' label="Applying" />
      <option value='applied' label="Applied" />
      <option value='skipped' label="Skipped" />
      <option value='failed' label="Failed" />
      <option value='manual_review_required' label="Manual Review Required" />
    </>
  );
};



/* Export the generated form */
export default SchemaEvolutionLedgerForm;

