// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  WorkforceTransitionRecord,
  WorkforceTransitionRecordCertificationStatusEnum,
} from '@thorapi/model';

import { useAddWorkforceTransitionRecordMutation } from '../../services/WorkforceTransitionRecordService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Durable transition ledger row recording role progression during AI adoption.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const CertificationStatusValidation = () => {
  return [
    'NOT_STARTED',
    'IN_PROGRESS',
    'CERTIFIED',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        organizationId: Yup.string(),
        workflowId: Yup.string(),
        userId: Yup.string(),
        priorRole: Yup.string(),
        currentRole: Yup.string(),
        skillsGained: Yup.string(),
      certificationStatus: Yup.mixed()
        .oneOf(CertificationStatusValidation(), "Invalid value for certificationStatus")
        ,
        wageDelta: asNumber(Yup.number().typeError("wageDelta must be a number")),
        transitionDate: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("transitionDate must be a valid date"),
        notes: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const WorkforceTransitionRecordForm: React.FC = () => {
  const [addWorkforceTransitionRecord, addWorkforceTransitionRecordResult] = useAddWorkforceTransitionRecordMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<WorkforceTransitionRecord> = {
          organizationId: '',
          workflowId: '',
          userId: '',
          priorRole: '',
          currentRole: '',
          skillsGained: '',
        certificationStatus: undefined,
          wageDelta: 0,
          transitionDate: new Date(),
          notes: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new WorkforceTransitionRecord:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<WorkforceTransitionRecord>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("WorkforceTransitionRecord form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addWorkforceTransitionRecord(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `WorkforceTransitionRecord created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create WorkforceTransitionRecord:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as WorkforceTransitionRecord}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addWorkforceTransitionRecordResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New WorkforceTransitionRecord
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="organizationId" className="nice-form-control">
                      <b>
                        Organization Id:
                        {touched.organizationId &&
                         !errors.organizationId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="organizationId"
                            value={values?.organizationId}
                            placeholder="Organization Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="organizationId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="workflowId" className="nice-form-control">
                      <b>
                        Workflow Id:
                        {touched.workflowId &&
                         !errors.workflowId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="workflowId"
                            value={values?.workflowId}
                            placeholder="Workflow Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="workflowId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="userId" className="nice-form-control">
                      <b>
                        User Id:
                        {touched.userId &&
                         !errors.userId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="userId"
                            value={values?.userId}
                            placeholder="User Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="userId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="priorRole" className="nice-form-control">
                      <b>
                        Prior Role:
                        {touched.priorRole &&
                         !errors.priorRole && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="priorRole"
                            value={values?.priorRole}
                            placeholder="Prior Role"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="priorRole"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currentRole" className="nice-form-control">
                      <b>
                        Current Role:
                        {touched.currentRole &&
                         !errors.currentRole && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="currentRole"
                            value={values?.currentRole}
                            placeholder="Current Role"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="currentRole"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="skillsGained" className="nice-form-control">
                      <b>
                        Skills Gained:
                        {touched.skillsGained &&
                         !errors.skillsGained && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="skillsGained"
                            value={values?.skillsGained}
                            placeholder="Skills Gained"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="skillsGained"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="certificationStatus" className="nice-form-control">
                      <b>
                        Certification Status:
                        {touched.certificationStatus &&
                         !errors.certificationStatus && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="certificationStatus"
                          value={values.certificationStatus || ''}
                          className={
                            errors.certificationStatus
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('certificationStatus', true);
                            setFieldValue('certificationStatus', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Certification Status" />
                          <CertificationStatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="certificationStatus"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="wageDelta" className="nice-form-control">
                      <b>
                        Wage Delta:
                        {touched.wageDelta &&
                         !errors.wageDelta && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="wageDelta"
                            type="number"
                            step="any"
                            value={values.wageDelta || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('wageDelta', true);
                              const v = e.target.value;
                              setFieldValue('wageDelta', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.wageDelta
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="wageDelta"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="transitionDate" className="nice-form-control">
                      <b>
                        Transition Date:
                        {touched.transitionDate &&
                         !errors.transitionDate && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="transitionDate"
                            type="datetime-local"
                            value={values.transitionDate ? 
                              new Date(values.transitionDate).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('transitionDate', true);
                              const v = e.target.value;
                              setFieldValue('transitionDate', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.transitionDate
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="transitionDate"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="notes" className="nice-form-control">
                      <b>
                        Notes:
                        {touched.notes &&
                         !errors.notes && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="notes"
                            value={values?.notes}
                            placeholder="Notes"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="notes"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New WorkforceTransitionRecord
                  </CoolButton>

                  {(addWorkforceTransitionRecordResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addWorkforceTransitionRecordResult as any).error ? (addWorkforceTransitionRecordResult as any).error.data : (addWorkforceTransitionRecordResult as any).error)}
                    </Alert>
                  )}

                  {(addWorkforceTransitionRecordResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addWorkforceTransitionRecordResult: {JSON.stringify(addWorkforceTransitionRecordResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.WorkforceTransitionRecord"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase certificationstatuslookup
uppercase CERTIFICATIONSTATUSLOOKUP
snakecase certification_status_lookup
pascalcase CertificationStatusLookup
camelcase certificationStatusLookup
kebabcase certification-status-lookup
*/

const CertificationStatusLookup = () => {
  return (
    <>
      <option value='NOT_STARTED' label="NOT _ STARTED" />
      <option value='IN_PROGRESS' label="IN _ PROGRESS" />
      <option value='CERTIFIED' label="CERTIFIED" />
    </>
  );
};



/* Export the generated form */
export default WorkforceTransitionRecordForm;

