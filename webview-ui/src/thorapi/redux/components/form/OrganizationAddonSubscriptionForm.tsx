// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  OrganizationAddonSubscription,
  OrganizationAddonSubscriptionPlanTierEnum,
  OrganizationAddonSubscriptionStatusEnum,
} from '@thorapi/model';

import { useAddOrganizationAddonSubscriptionMutation } from '../../services/OrganizationAddonSubscriptionService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Tracks additional organization subscriptions beyond plan allowances.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const PlanTierValidation = () => {
  return [
    'FREE',
    'SOLO',
    'TEAM',
    'PRO',
    'RESELLER',
    'ENTERPRISE',
  ];
};
const StatusValidation = () => {
  return [
    'ACTIVE',
    'CANCELLED',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
      planTier: Yup.mixed()
        .oneOf(PlanTierValidation(), "Invalid value for planTier")
        ,
        monthlyPriceCredits: asNumber(Yup.number().typeError("monthlyPriceCredits must be a number")),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        ,
        cancelledAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("cancelledAt must be a valid date"),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const OrganizationAddonSubscriptionForm: React.FC = () => {
  const [addOrganizationAddonSubscription, addOrganizationAddonSubscriptionResult] = useAddOrganizationAddonSubscriptionMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<OrganizationAddonSubscription> = {
        planTier: undefined,
          monthlyPriceCredits: 0,
        status: undefined,
          cancelledAt: new Date(),
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new OrganizationAddonSubscription:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<OrganizationAddonSubscription>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("OrganizationAddonSubscription form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addOrganizationAddonSubscription(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `OrganizationAddonSubscription created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create OrganizationAddonSubscription:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as OrganizationAddonSubscription}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addOrganizationAddonSubscriptionResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New OrganizationAddonSubscription
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="planTier" className="nice-form-control">
                      <b>
                        Plan Tier:
                        {touched.planTier &&
                         !errors.planTier && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="planTier"
                          value={values.planTier || ''}
                          className={
                            errors.planTier
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('planTier', true);
                            setFieldValue('planTier', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Plan Tier" />
                          <PlanTierLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="planTier"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="monthlyPriceCredits" className="nice-form-control">
                      <b>
                        Monthly Price Credits:
                        {touched.monthlyPriceCredits &&
                         !errors.monthlyPriceCredits && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="monthlyPriceCredits"
                            type="number"
                            step="any"
                            value={values.monthlyPriceCredits || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('monthlyPriceCredits', true);
                              const v = e.target.value;
                              setFieldValue('monthlyPriceCredits', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.monthlyPriceCredits
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="monthlyPriceCredits"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="cancelledAt" className="nice-form-control">
                      <b>
                        Cancelled At:
                        {touched.cancelledAt &&
                         !errors.cancelledAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="cancelledAt"
                            type="datetime-local"
                            value={values.cancelledAt ? 
                              new Date(values.cancelledAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('cancelledAt', true);
                              const v = e.target.value;
                              setFieldValue('cancelledAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.cancelledAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="cancelledAt"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New OrganizationAddonSubscription
                  </CoolButton>

                  {(addOrganizationAddonSubscriptionResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addOrganizationAddonSubscriptionResult as any).error ? (addOrganizationAddonSubscriptionResult as any).error.data : (addOrganizationAddonSubscriptionResult as any).error)}
                    </Alert>
                  )}

                  {(addOrganizationAddonSubscriptionResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addOrganizationAddonSubscriptionResult: {JSON.stringify(addOrganizationAddonSubscriptionResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.OrganizationAddonSubscription"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase plantierlookup
uppercase PLANTIERLOOKUP
snakecase plan_tier_lookup
pascalcase PlanTierLookup
camelcase planTierLookup
kebabcase plan-tier-lookup
*/

const PlanTierLookup = () => {
  return (
    <>
      <option value='FREE' label="FREE" />
      <option value='SOLO' label="SOLO" />
      <option value='TEAM' label="TEAM" />
      <option value='PRO' label="PRO" />
      <option value='RESELLER' label="RESELLER" />
      <option value='ENTERPRISE' label="ENTERPRISE" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='ACTIVE' label="Active" />
      <option value='CANCELLED' label="Cancelled" />
    </>
  );
};



/* Export the generated form */
export default OrganizationAddonSubscriptionForm;

