// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  GeneratedArtifact,
  GeneratedArtifactArtifactTypeEnum,
  GeneratedArtifactStatusEnum,
} from '@thorapi/model';

import { useAddGeneratedArtifactMutation } from '../../services/GeneratedArtifactService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
One generated artifact produced by ThorAPI for a tenant app generation run.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const ArtifactTypeValidation = () => {
  return [
    'backend',
    'frontend',
    'openapi_spec',
    'package_zip',
    'build_output',
    'test_output',
    'manifest',
    'other',
  ];
};
const StatusValidation = () => {
  return [
    'created',
    'packaged',
    'validated',
    'failed',
    'superseded',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        artifactRef: Yup.string().required("artifactRef is required."),
        artifactSetRef: Yup.string().required("artifactSetRef is required."),
        generationRunRef: Yup.string().required("generationRunRef is required."),
        tenantId: Yup.string().required("tenantId is required."),
        traceId: Yup.string().required("traceId is required."),
      artifactType: Yup.mixed()
        .oneOf(ArtifactTypeValidation(), "Invalid value for artifactType")
        .required("artifactType is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        accountId: Yup.string(),
        applicationId: Yup.string(),
        fileName: Yup.string(),
        filePath: Yup.string(),
        checksum: Yup.string(),
        byteSize: asNumber(Yup.number().integer().typeError("byteSize must be a number")),
        createdAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("createdAt must be a valid date"),
        metadataJson: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const GeneratedArtifactForm: React.FC = () => {
  const [addGeneratedArtifact, addGeneratedArtifactResult] = useAddGeneratedArtifactMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<GeneratedArtifact> = {
          artifactRef: '',
          artifactSetRef: '',
          generationRunRef: '',
          tenantId: '',
          traceId: '',
        artifactType: undefined,
        status: undefined,
          accountId: '',
          applicationId: '',
          fileName: '',
          filePath: '',
          checksum: '',
          byteSize: 0,
          createdAt: new Date(),
          metadataJson: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new GeneratedArtifact:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<GeneratedArtifact>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("GeneratedArtifact form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addGeneratedArtifact(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `GeneratedArtifact created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create GeneratedArtifact:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as GeneratedArtifact}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addGeneratedArtifactResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New GeneratedArtifact
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="artifactRef" className="nice-form-control">
                      <b>
                        Artifact Ref:
                        {touched.artifactRef &&
                         !errors.artifactRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="artifactRef"
                            value={values?.artifactRef}
                            placeholder="Artifact Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="artifactRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="artifactSetRef" className="nice-form-control">
                      <b>
                        Artifact Set Ref:
                        {touched.artifactSetRef &&
                         !errors.artifactSetRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="artifactSetRef"
                            value={values?.artifactSetRef}
                            placeholder="Artifact Set Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="artifactSetRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="generationRunRef" className="nice-form-control">
                      <b>
                        Generation Run Ref:
                        {touched.generationRunRef &&
                         !errors.generationRunRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="generationRunRef"
                            value={values?.generationRunRef}
                            placeholder="Generation Run Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="generationRunRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="tenantId" className="nice-form-control">
                      <b>
                        Tenant Id:
                        {touched.tenantId &&
                         !errors.tenantId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="tenantId"
                            value={values?.tenantId}
                            placeholder="Tenant Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="tenantId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="traceId" className="nice-form-control">
                      <b>
                        Trace Id:
                        {touched.traceId &&
                         !errors.traceId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="traceId"
                            value={values?.traceId}
                            placeholder="Trace Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="traceId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="artifactType" className="nice-form-control">
                      <b>
                        Artifact Type:
                        {touched.artifactType &&
                         !errors.artifactType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="artifactType"
                          value={values.artifactType || ''}
                          className={
                            errors.artifactType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('artifactType', true);
                            setFieldValue('artifactType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Artifact Type" />
                          <ArtifactTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="artifactType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="accountId" className="nice-form-control">
                      <b>
                        Account Id:
                        {touched.accountId &&
                         !errors.accountId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="accountId"
                            value={values?.accountId}
                            placeholder="Account Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="accountId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="applicationId" className="nice-form-control">
                      <b>
                        Application Id:
                        {touched.applicationId &&
                         !errors.applicationId && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="applicationId"
                            value={values?.applicationId}
                            placeholder="Application Id"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="applicationId"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="fileName" className="nice-form-control">
                      <b>
                        File Name:
                        {touched.fileName &&
                         !errors.fileName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="fileName"
                            value={values?.fileName}
                            placeholder="File Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="fileName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="filePath" className="nice-form-control">
                      <b>
                        File Path:
                        {touched.filePath &&
                         !errors.filePath && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="filePath"
                            value={values?.filePath}
                            placeholder="File Path"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="filePath"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="checksum" className="nice-form-control">
                      <b>
                        Checksum:
                        {touched.checksum &&
                         !errors.checksum && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="checksum"
                            value={values?.checksum}
                            placeholder="Checksum"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="checksum"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="byteSize" className="nice-form-control">
                      <b>
                        Byte Size:
                        {touched.byteSize &&
                         !errors.byteSize && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>







                          {/* LONG FIELD */}
                          <Field
                            name="byteSize"
                            type="number"
                            value={values.byteSize || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('byteSize', true);
                              const v = e.target.value;
                              setFieldValue('byteSize', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.byteSize
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />



                      <ErrorMessage
                        className="error"
                        name="byteSize"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="createdAt" className="nice-form-control">
                      <b>
                        Created At:
                        {touched.createdAt &&
                         !errors.createdAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="createdAt"
                            type="datetime-local"
                            value={values.createdAt ? 
                              new Date(values.createdAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('createdAt', true);
                              const v = e.target.value;
                              setFieldValue('createdAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.createdAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="createdAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="metadataJson" className="nice-form-control">
                      <b>
                        Metadata Json:
                        {touched.metadataJson &&
                         !errors.metadataJson && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="metadataJson"
                            value={values?.metadataJson}
                            placeholder="Metadata Json"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="metadataJson"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New GeneratedArtifact
                  </CoolButton>

                  {(addGeneratedArtifactResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addGeneratedArtifactResult as any).error ? (addGeneratedArtifactResult as any).error.data : (addGeneratedArtifactResult as any).error)}
                    </Alert>
                  )}

                  {(addGeneratedArtifactResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addGeneratedArtifactResult: {JSON.stringify(addGeneratedArtifactResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.GeneratedArtifact"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase artifacttypelookup
uppercase ARTIFACTTYPELOOKUP
snakecase artifact_type_lookup
pascalcase ArtifactTypeLookup
camelcase artifactTypeLookup
kebabcase artifact-type-lookup
*/

const ArtifactTypeLookup = () => {
  return (
    <>
      <option value='backend' label="Backend" />
      <option value='frontend' label="Frontend" />
      <option value='openapi_spec' label="Openapi Spec" />
      <option value='package_zip' label="Package Zip" />
      <option value='build_output' label="Build Output" />
      <option value='test_output' label="Test Output" />
      <option value='manifest' label="Manifest" />
      <option value='other' label="Other" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='created' label="Created" />
      <option value='packaged' label="Packaged" />
      <option value='validated' label="Validated" />
      <option value='failed' label="Failed" />
      <option value='superseded' label="Superseded" />
    </>
  );
};



/* Export the generated form */
export default GeneratedArtifactForm;

