// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { ErrorMessage, Field, Formik, FormikHelpers, FormikValues } from 'formik';
import React, { useState } from 'react';
import {
  Form as BSForm,
  Accordion,
  Col,
  Row,
  Spinner,
  Alert
} from 'react-bootstrap';
import LoadingSpinner from '@valkyr/component-library/LoadingSpinner';
import { FaCheckCircle, FaCogs, FaRegPlusSquare } from 'react-icons/fa';
import CoolButton from '@valkyr/component-library/CoolButton';
import * as Yup from 'yup';
import { SmartField } from '@valkyr/component-library/ForeignKey/SmartField';

import { PermissionDialog } from '@valkyr/component-library/PermissionDialog';
import { AclGrantRequest, PermissionType } from '@valkyr/component-library/PermissionDialog/types';


import {
  CreditPricingMatrix,
  CreditPricingMatrixUnitTypeEnum,
  CreditPricingMatrixBillingScopeEnum,
  CreditPricingMatrixStatusEnum,
} from '@thorapi/model';

import { useAddCreditPricingMatrixMutation } from '../../services/CreditPricingMatrixService';

/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelForm.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################

Description:
Data-driven per-unit credit pricing lookup for metered billing.
*/

/* -----------------------------------------------------
   ENUM VALIDATION ARRAYS (Yup oneOf checks), if any
-------------------------------------------------------- */
const UnitTypeValidation = () => {
  return [
    'cpu_hour',
    'memory_gb_hour',
    'generated_thorapi_component',
    'valoride_sign_in',
    'lightsail_instance_hour',
    'schema_object',
    'api_call',
    'llm_token_1k',
    'storage_gb_month',
    'workflow_run',
    'custom',
  ];
};
const BillingScopeValidation = () => {
  return [
    'global',
    'plan',
    'customer',
    'organization',
    'deployment',
  ];
};
const StatusValidation = () => {
  return [
    'active',
    'draft',
    'retired',
  ];
};

/* -----------------------------------------------------
   YUP VALIDATION SCHEMA (skip read-only fields)
-------------------------------------------------------- */
const asNumber = (schema: Yup.NumberSchema) =>
  schema.transform((val, orig) => (orig === '' || orig === null ? undefined : val));

const validationSchema = Yup.object().shape({
        unitKey: Yup.string().required("unitKey is required."),
      unitType: Yup.mixed()
        .oneOf(UnitTypeValidation(), "Invalid value for unitType")
        .required("unitType is required."),
        creditCost: asNumber(Yup.number().typeError("creditCost must be a number")).required("creditCost is required."),
      billingScope: Yup.mixed()
        .oneOf(BillingScopeValidation(), "Invalid value for billingScope")
        .required("billingScope is required."),
      status: Yup.mixed()
        .oneOf(StatusValidation(), "Invalid value for status")
        .required("status is required."),
        scopeRef: Yup.string(),
        meterName: Yup.string(),
        provider: Yup.string(),
        region: Yup.string(),
        currency: Yup.string(),
        effectiveAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("effectiveAt must be a valid date"),
        expiresAt: Yup.date()
          .transform((value, originalValue) => {
            if (!originalValue) {
              return value;
            }
            const parsed = new Date(originalValue);
            return Number.isNaN(parsed.getTime()) ? value : parsed;
          }).typeError("expiresAt must be a valid date"),
        priority: asNumber(Yup.number().integer().typeError("priority must be a number")),
        notes: Yup.string(),
        trashed: Yup.boolean(),
});

/* -----------------------------------------------------
   COMPONENT
-------------------------------------------------------- */
const CreditPricingMatrixForm: React.FC = () => {
  const [addCreditPricingMatrix, addCreditPricingMatrixResult] = useAddCreditPricingMatrixMutation();
  const [successMessage, setSuccessMessage] = useState<string | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  // Permission Management State
  const [showPermissionDialog, setShowPermissionDialog] = useState(false);
  const [createdObjectId, setCreatedObjectId] = useState<string | null>(null);

  // Mock current user - in real implementation, this would come from auth context
  const currentUser = {
    username: 'current_user',
    permissions: {
      isOwner: true,
      isAdmin: true,
      canGrantPermissions: true,
      permissions: [PermissionType.READ, PermissionType.WRITE, PermissionType.CREATE, PermissionType.DELETE, PermissionType.ADMINISTRATION],
    },
  };

  /* -----------------------------------------------------
     INITIAL VALUES - only NON read-only fields
  -------------------------------------------------------- */
  const initialValues: Partial<CreditPricingMatrix> = {
          unitKey: '',
        unitType: undefined,
          creditCost: 0,
        billingScope: undefined,
        status: undefined,
          scopeRef: '',
          meterName: '',
          provider: '',
          region: '',
          currency: '',
          effectiveAt: new Date(),
          expiresAt: new Date(),
          priority: 0,
          notes: '',
          trashed: false,
  };

  // Permission Management Handlers
  const handleManagePermissions = (objectId: string) => {
    setCreatedObjectId(objectId);
    setShowPermissionDialog(true);
  };

  const handlePermissionDialogClose = () => {
    setShowPermissionDialog(false);
    setCreatedObjectId(null);
  };

  const handlePermissionsSave = (grants: AclGrantRequest[]) => {
    console.log('Permissions saved for new CreditPricingMatrix:', grants);
  };

  /* SUBMIT HANDLER */
  const handleSubmit = async (values: FormikValues, { setSubmitting }: FormikHelpers<CreditPricingMatrix>) => {
    try {
      setSuccessMessage(null);
      setErrorMessage(null);
      console.log("CreditPricingMatrix form values:", values);

      // NOTE: depending on your generated endpoint, you may need { body: values }
      const result = await addCreditPricingMatrix(values as any).unwrap();

      if (result && result.id && currentUser.permissions.canGrantPermissions) {
        const shouldSetPermissions = window.confirm(
          `CreditPricingMatrix created successfully! Would you like to set permissions for this object?`
        );
        if (shouldSetPermissions) {
          handleManagePermissions(result.id);
        }
      }
      setSuccessMessage("Saved successfully.");
    } catch (error) {
      console.error('Failed to create CreditPricingMatrix:', error);
      setErrorMessage('Failed to save. Please try again.');
    }
    setSubmitting(false);
  };

  return (
    <div>
      <Formik
        validateOnBlur={true}
        initialValues={initialValues as CreditPricingMatrix}
        validationSchema={validationSchema}
        onSubmit={handleSubmit}
      >
        {({
          isSubmitting,
          isValid,
          errors,
          values,
          setFieldValue,
          touched,
          setFieldTouched,
          handleSubmit
        }) => {
          const isSaving = isSubmitting || addCreditPricingMatrixResult.isLoading;
          return (
          <form onSubmit={handleSubmit} className="form">
            <Accordion defaultActiveKey="1">
              
              {/* Editable Fields (NON read-only) */}
              <Accordion.Item eventKey="1">
                <Accordion.Header>
                  <FaRegPlusSquare size={28} /> &nbsp; Add New CreditPricingMatrix
                </Accordion.Header>
                <Accordion.Body>
                    <label htmlFor="unitKey" className="nice-form-control">
                      <b>
                        Unit Key:
                        {touched.unitKey &&
                         !errors.unitKey && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="unitKey"
                            value={values?.unitKey}
                            placeholder="Unit Key"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="unitKey"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="unitType" className="nice-form-control">
                      <b>
                        Unit Type:
                        {touched.unitType &&
                         !errors.unitType && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="unitType"
                          value={values.unitType || ''}
                          className={
                            errors.unitType
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('unitType', true);
                            setFieldValue('unitType', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Unit Type" />
                          <UnitTypeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="unitType"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="creditCost" className="nice-form-control">
                      <b>
                        Credit Cost:
                        {touched.creditCost &&
                         !errors.creditCost && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>






                          {/* DOUBLE FIELD */}
                          <Field
                            name="creditCost"
                            type="number"
                            step="any"
                            value={values.creditCost || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('creditCost', true);
                              const v = e.target.value;
                              setFieldValue('creditCost', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.creditCost
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />




                      <ErrorMessage
                        className="error"
                        name="creditCost"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="billingScope" className="nice-form-control">
                      <b>
                        Billing Scope:
                        {touched.billingScope &&
                         !errors.billingScope && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="billingScope"
                          value={values.billingScope || ''}
                          className={
                            errors.billingScope
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('billingScope', true);
                            setFieldValue('billingScope', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Billing Scope" />
                          <BillingScopeLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="billingScope"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="status" className="nice-form-control">
                      <b>
                        Status:
                        {touched.status &&
                         !errors.status && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>

                        {/* ENUM DROPDOWN */}
                        <BSForm.Select
                          name="status"
                          value={values.status || ''}
                          className={
                            errors.status
                              ? 'form-control field-error'
                              : 'nice-form-control form-control'
                          }
                          onChange={(e) => {
                            setFieldTouched('status', true);
                            setFieldValue('status', e.target.value || undefined);
                          }}
                        >
                          <option value="" label="Select Status" />
                          <StatusLookup />
                        </BSForm.Select>


                      <ErrorMessage
                        className="error"
                        name="status"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="scopeRef" className="nice-form-control">
                      <b>
                        Scope Ref:
                        {touched.scopeRef &&
                         !errors.scopeRef && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="scopeRef"
                            value={values?.scopeRef}
                            placeholder="Scope Ref"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="scopeRef"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="meterName" className="nice-form-control">
                      <b>
                        Meter Name:
                        {touched.meterName &&
                         !errors.meterName && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="meterName"
                            value={values?.meterName}
                            placeholder="Meter Name"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="meterName"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="provider" className="nice-form-control">
                      <b>
                        Provider:
                        {touched.provider &&
                         !errors.provider && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="provider"
                            value={values?.provider}
                            placeholder="Provider"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="provider"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="region" className="nice-form-control">
                      <b>
                        Region:
                        {touched.region &&
                         !errors.region && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="region"
                            value={values?.region}
                            placeholder="Region"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="region"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="currency" className="nice-form-control">
                      <b>
                        Currency:
                        {touched.currency &&
                         !errors.currency && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="currency"
                            value={values?.currency}
                            placeholder="Currency"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="currency"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="effectiveAt" className="nice-form-control">
                      <b>
                        Effective At:
                        {touched.effectiveAt &&
                         !errors.effectiveAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="effectiveAt"
                            type="datetime-local"
                            value={values.effectiveAt ? 
                              new Date(values.effectiveAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('effectiveAt', true);
                              const v = e.target.value;
                              setFieldValue('effectiveAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.effectiveAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="effectiveAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="expiresAt" className="nice-form-control">
                      <b>
                        Expires At:
                        {touched.expiresAt &&
                         !errors.expiresAt && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>









                          {/* DATETIME FIELD */}
                          <Field
                            name="expiresAt"
                            type="datetime-local"
                            value={values.expiresAt ? 
                              new Date(values.expiresAt).toISOString().slice(0, 16) : 
                              ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('expiresAt', true);
                              const v = e.target.value;
                              setFieldValue('expiresAt', v ? new Date(v).toISOString() : '');
                            }}
                            className={
                              errors.expiresAt
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />

                      <ErrorMessage
                        className="error"
                        name="expiresAt"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="priority" className="nice-form-control">
                      <b>
                        Priority:
                        {touched.priority &&
                         !errors.priority && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>




                          {/* INTEGER FIELD */}
                          <Field
                            name="priority"
                            type="number"
                            value={values.priority || ''}
                            onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                              setFieldTouched('priority', true);
                              const v = e.target.value;
                              setFieldValue('priority', v === '' ? undefined : Number(v));
                            }}
                            className={
                              errors.priority
                                ? 'form-control field-error'
                                : 'nice-form-control form-control'
                            }
                          />






                      <ErrorMessage
                        className="error"
                        name="priority"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="notes" className="nice-form-control">
                      <b>
                        Notes:
                        {touched.notes &&
                         !errors.notes && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>



                          {/* SMART FIELD (UUID-aware picker for *Id), fallback text */}
                          <SmartField
                            name="notes"
                            value={values?.notes}
                            placeholder="Notes"
                            setFieldValue={setFieldValue}
                            setFieldTouched={setFieldTouched}
                          />







                      <ErrorMessage
                        className="error"
                        name="notes"
                        component="span"
                      />
                    </label>
                    <br />
                    <label htmlFor="trashed" className="nice-form-control">
                      <b>
                        Trashed:
                        {touched.trashed &&
                         !errors.trashed && (
                          <span className="okCheck"><FaCheckCircle /> looks good!</span>
                        )}
                      </b>


                          {/* CHECKBOX FIELD */}
                          <BSForm.Check
                            id="trashed"
                            name="trashed"
                            checked={values.trashed || false}
                            onChange={(e) => {
                              setFieldTouched('trashed', true);
                              setFieldValue('trashed', e.target.checked);
                            }}
                            isInvalid={!!errors.trashed}
                            className={errors.trashed ? 'error' : ''}
                          />








                      <ErrorMessage
                        className="error"
                        name="trashed"
                        component="span"
                      />
                    </label>
                    <br />

                  {/* SUBMIT BUTTON */}
                  <CoolButton
                    variant={isValid ? (isSaving ? 'disabled' : 'success') : 'warning'}
                    type="submit"
                    disabled={!isValid || isSaving}
                  >
                    {isSaving && (<span style={ { float: 'left', minHeight: 0 } }><LoadingSpinner label="" size={18} /></span>)}
                    <FaCheckCircle size={28} /> Create New CreditPricingMatrix
                  </CoolButton>

                  {(addCreditPricingMatrixResult.isError || errorMessage) && (
                    <Alert variant="danger" className="mt-3">
                      {errorMessage ||
                        JSON.stringify('data' in (addCreditPricingMatrixResult as any).error ? (addCreditPricingMatrixResult as any).error.data : (addCreditPricingMatrixResult as any).error)}
                    </Alert>
                  )}

                  {(addCreditPricingMatrixResult.isSuccess || successMessage) && (
                    <Alert variant="success" className="mt-3">
                      {successMessage || 'Saved successfully.'}
                    </Alert>
                  )}
                </Accordion.Body>
              </Accordion.Item>

            {/* Debug/Dev Accordion */}
              <Accordion.Item eventKey="0">
                <Accordion.Header>
                  <FaCogs size={28} /> &nbsp;Server Messages
                </Accordion.Header>
                <Accordion.Body>
                  errors: {JSON.stringify(errors)}
                  <br />
                  addCreditPricingMatrixResult: {JSON.stringify(addCreditPricingMatrixResult)}
                </Accordion.Body>
              </Accordion.Item>

            </Accordion>
          </form>
          );
        }}
      </Formik>

      {/* Permission Management Dialog */}
      {createdObjectId && (
        <PermissionDialog
          objectType="com.valkyrlabs.model.CreditPricingMatrix"
          objectId={createdObjectId}
          isVisible={showPermissionDialog}
          onClose={handlePermissionDialogClose}
          onSave={handlePermissionsSave}
          currentUser={currentUser}
        />
      )}
    </div>
  );
};

/*
lowercase unittypelookup
uppercase UNITTYPELOOKUP
snakecase unit_type_lookup
pascalcase UnitTypeLookup
camelcase unitTypeLookup
kebabcase unit-type-lookup
*/

const UnitTypeLookup = () => {
  return (
    <>
      <option value='cpu_hour' label="Cpu Hour" />
      <option value='memory_gb_hour' label="Memory Gb Hour" />
      <option value='generated_thorapi_component' label="Generated Thorapi Component" />
      <option value='valoride_sign_in' label="Valoride Sign In" />
      <option value='lightsail_instance_hour' label="Lightsail Instance Hour" />
      <option value='schema_object' label="Schema Object" />
      <option value='api_call' label="Api Call" />
      <option value='llm_token_1k' label="Llm Token 1 k" />
      <option value='storage_gb_month' label="Storage Gb Month" />
      <option value='workflow_run' label="Workflow Run" />
      <option value='custom' label="Custom" />
    </>
  );
};

/*
lowercase billingscopelookup
uppercase BILLINGSCOPELOOKUP
snakecase billing_scope_lookup
pascalcase BillingScopeLookup
camelcase billingScopeLookup
kebabcase billing-scope-lookup
*/

const BillingScopeLookup = () => {
  return (
    <>
      <option value='global' label="Global" />
      <option value='plan' label="Plan" />
      <option value='customer' label="Customer" />
      <option value='organization' label="Organization" />
      <option value='deployment' label="Deployment" />
    </>
  );
};

/*
lowercase statuslookup
uppercase STATUSLOOKUP
snakecase status_lookup
pascalcase StatusLookup
camelcase statusLookup
kebabcase status-lookup
*/

const StatusLookup = () => {
  return (
    <>
      <option value='active' label="Active" />
      <option value='draft' label="Draft" />
      <option value='retired' label="Retired" />
    </>
  );
};



/* Export the generated form */
export default CreditPricingMatrixForm;

