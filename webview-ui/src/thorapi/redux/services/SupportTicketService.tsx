// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SupportTicket } from '@thorapi/model/SupportTicket'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SupportTicketResponse = SupportTicket[]
type SupportTicketPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SupportTicket>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SupportTicketListQueryArg = {
  example?: Partial<SupportTicket>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSupportTicketList = (result: unknown): SupportTicketResponse => {
  if (Array.isArray(result)) {
    return result as SupportTicketResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SupportTicketResponse) : []
}

export const SupportTicketService = createApi({
  reducerPath: 'SupportTicket', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SupportTicket'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSupportTicketsPaged: build.query<SupportTicketResponse, SupportTicketPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SupportTicket?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSupportTicketList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SupportTicket' as const, id })),
          { type: 'SupportTicket', id: `PAGE_${page}` },
          { type: 'SupportTicket', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSupportTickets: build.query<SupportTicketResponse, SupportTicketListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SupportTicket?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SupportTicket`;
      },
      providesTags: (result) => {
        const rows = toSupportTicketList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SupportTicket' as const, id })),
          { type: 'SupportTicket', id: 'LIST' },
          { type: 'SupportTicket', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSupportTicket: build.mutation<SupportTicket, Partial<SupportTicket>>({
      query: (body) => ({
        url: `SupportTicket`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SupportTicket', id: 'LIST' },
        { type: 'SupportTicket', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSupportTicket: build.query<SupportTicket, string>({
      query: (id) => `SupportTicket/${id}`,
      providesTags: (result, error, id) => [{ type: 'SupportTicket', id }],
    }),

    // 5) Update
    updateSupportTicket: build.mutation<SupportTicket, Pick<SupportTicket, 'id'> & Partial<SupportTicket>>({
      query: ({ id, ...patch }) => ({
        url: `SupportTicket/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SupportTicket, 'id'>) => [
        { type: 'SupportTicket', id },
        { type: 'SupportTicket', id: 'LIST' },
        { type: 'SupportTicket', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSupportTicket: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SupportTicket/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SupportTicket', id },
        { type: 'SupportTicket', id: 'LIST' },
        { type: 'SupportTicket', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSupportTicketCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SupportTicket/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SupportTicket', id },
        { type: 'SupportTicket', id: 'LIST' },
        { type: 'SupportTicket', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSupportTicketsPagedQuery`
export const {
  useGetSupportTicketsPagedQuery,     // immediate fetch
  useLazyGetSupportTicketsPagedQuery, // lazy fetch
  useGetSupportTicketQuery,
  useGetSupportTicketsQuery,
  useAddSupportTicketMutation,
  useUpdateSupportTicketMutation,
  useDeleteSupportTicketMutation,
  useDeleteSupportTicketCascadeMutation,
} = SupportTicketService
