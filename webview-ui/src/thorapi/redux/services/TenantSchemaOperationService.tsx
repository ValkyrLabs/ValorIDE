// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { TenantSchemaOperation } from '@thorapi/model/TenantSchemaOperation'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type TenantSchemaOperationResponse = TenantSchemaOperation[]
type TenantSchemaOperationPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<TenantSchemaOperation>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type TenantSchemaOperationListQueryArg = {
  example?: Partial<TenantSchemaOperation>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toTenantSchemaOperationList = (result: unknown): TenantSchemaOperationResponse => {
  if (Array.isArray(result)) {
    return result as TenantSchemaOperationResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as TenantSchemaOperationResponse) : []
}

export const TenantSchemaOperationService = createApi({
  reducerPath: 'TenantSchemaOperation', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['TenantSchemaOperation'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getTenantSchemaOperationsPaged: build.query<TenantSchemaOperationResponse, TenantSchemaOperationPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `TenantSchemaOperation?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toTenantSchemaOperationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaOperation' as const, id })),
          { type: 'TenantSchemaOperation', id: `PAGE_${page}` },
          { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getTenantSchemaOperations: build.query<TenantSchemaOperationResponse, TenantSchemaOperationListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `TenantSchemaOperation?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `TenantSchemaOperation`;
      },
      providesTags: (result) => {
        const rows = toTenantSchemaOperationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'TenantSchemaOperation' as const, id })),
          { type: 'TenantSchemaOperation', id: 'LIST' },
          { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addTenantSchemaOperation: build.mutation<TenantSchemaOperation, Partial<TenantSchemaOperation>>({
      query: (body) => ({
        url: `TenantSchemaOperation`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'TenantSchemaOperation', id: 'LIST' },
        { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getTenantSchemaOperation: build.query<TenantSchemaOperation, string>({
      query: (id) => `TenantSchemaOperation/${id}`,
      providesTags: (result, error, id) => [{ type: 'TenantSchemaOperation', id }],
    }),

    // 5) Update
    updateTenantSchemaOperation: build.mutation<TenantSchemaOperation, Pick<TenantSchemaOperation, 'id'> & Partial<TenantSchemaOperation>>({
      query: ({ id, ...patch }) => ({
        url: `TenantSchemaOperation/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<TenantSchemaOperation, 'id'>) => [
        { type: 'TenantSchemaOperation', id },
        { type: 'TenantSchemaOperation', id: 'LIST' },
        { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteTenantSchemaOperation: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `TenantSchemaOperation/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'TenantSchemaOperation', id },
        { type: 'TenantSchemaOperation', id: 'LIST' },
        { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteTenantSchemaOperationCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `TenantSchemaOperation/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'TenantSchemaOperation', id },
        { type: 'TenantSchemaOperation', id: 'LIST' },
        { type: 'TenantSchemaOperation', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetTenantSchemaOperationsPagedQuery`
export const {
  useGetTenantSchemaOperationsPagedQuery,     // immediate fetch
  useLazyGetTenantSchemaOperationsPagedQuery, // lazy fetch
  useGetTenantSchemaOperationQuery,
  useGetTenantSchemaOperationsQuery,
  useAddTenantSchemaOperationMutation,
  useUpdateTenantSchemaOperationMutation,
  useDeleteTenantSchemaOperationMutation,
  useDeleteTenantSchemaOperationCascadeMutation,
} = TenantSchemaOperationService
