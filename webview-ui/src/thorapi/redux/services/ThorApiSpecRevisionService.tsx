// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ThorApiSpecRevision } from '@thorapi/model/ThorApiSpecRevision'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ThorApiSpecRevisionResponse = ThorApiSpecRevision[]
type ThorApiSpecRevisionPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ThorApiSpecRevision>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ThorApiSpecRevisionListQueryArg = {
  example?: Partial<ThorApiSpecRevision>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toThorApiSpecRevisionList = (result: unknown): ThorApiSpecRevisionResponse => {
  if (Array.isArray(result)) {
    return result as ThorApiSpecRevisionResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ThorApiSpecRevisionResponse) : []
}

export const ThorApiSpecRevisionService = createApi({
  reducerPath: 'ThorApiSpecRevision', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ThorApiSpecRevision'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getThorApiSpecRevisionsPaged: build.query<ThorApiSpecRevisionResponse, ThorApiSpecRevisionPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ThorApiSpecRevision?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toThorApiSpecRevisionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ThorApiSpecRevision' as const, id })),
          { type: 'ThorApiSpecRevision', id: `PAGE_${page}` },
          { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getThorApiSpecRevisions: build.query<ThorApiSpecRevisionResponse, ThorApiSpecRevisionListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ThorApiSpecRevision?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ThorApiSpecRevision`;
      },
      providesTags: (result) => {
        const rows = toThorApiSpecRevisionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ThorApiSpecRevision' as const, id })),
          { type: 'ThorApiSpecRevision', id: 'LIST' },
          { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addThorApiSpecRevision: build.mutation<ThorApiSpecRevision, Partial<ThorApiSpecRevision>>({
      query: (body) => ({
        url: `ThorApiSpecRevision`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ThorApiSpecRevision', id: 'LIST' },
        { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getThorApiSpecRevision: build.query<ThorApiSpecRevision, string>({
      query: (id) => `ThorApiSpecRevision/${id}`,
      providesTags: (result, error, id) => [{ type: 'ThorApiSpecRevision', id }],
    }),

    // 5) Update
    updateThorApiSpecRevision: build.mutation<ThorApiSpecRevision, Pick<ThorApiSpecRevision, 'id'> & Partial<ThorApiSpecRevision>>({
      query: ({ id, ...patch }) => ({
        url: `ThorApiSpecRevision/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ThorApiSpecRevision, 'id'>) => [
        { type: 'ThorApiSpecRevision', id },
        { type: 'ThorApiSpecRevision', id: 'LIST' },
        { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteThorApiSpecRevision: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ThorApiSpecRevision/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ThorApiSpecRevision', id },
        { type: 'ThorApiSpecRevision', id: 'LIST' },
        { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteThorApiSpecRevisionCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ThorApiSpecRevision/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ThorApiSpecRevision', id },
        { type: 'ThorApiSpecRevision', id: 'LIST' },
        { type: 'ThorApiSpecRevision', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetThorApiSpecRevisionsPagedQuery`
export const {
  useGetThorApiSpecRevisionsPagedQuery,     // immediate fetch
  useLazyGetThorApiSpecRevisionsPagedQuery, // lazy fetch
  useGetThorApiSpecRevisionQuery,
  useGetThorApiSpecRevisionsQuery,
  useAddThorApiSpecRevisionMutation,
  useUpdateThorApiSpecRevisionMutation,
  useDeleteThorApiSpecRevisionMutation,
  useDeleteThorApiSpecRevisionCascadeMutation,
} = ThorApiSpecRevisionService
