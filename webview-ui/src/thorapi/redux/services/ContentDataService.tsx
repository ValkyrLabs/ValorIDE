// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContentData } from '@thorapi/model/ContentData'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContentDataResponse = ContentData[]
type ContentDataPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContentData>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContentDataListQueryArg = {
  example?: Partial<ContentData>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContentDataList = (result: unknown): ContentDataResponse => {
  if (Array.isArray(result)) {
    return result as ContentDataResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContentDataResponse) : []
}

export const ContentDataService = createApi({
  reducerPath: 'ContentData', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContentData'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContentDatasPaged: build.query<ContentDataResponse, ContentDataPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContentData?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContentDataList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContentData' as const, id })),
          { type: 'ContentData', id: `PAGE_${page}` },
          { type: 'ContentData', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContentDatas: build.query<ContentDataResponse, ContentDataListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContentData?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContentData`;
      },
      providesTags: (result) => {
        const rows = toContentDataList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContentData' as const, id })),
          { type: 'ContentData', id: 'LIST' },
          { type: 'ContentData', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContentData: build.mutation<ContentData, Partial<ContentData>>({
      query: (body) => ({
        url: `ContentData`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContentData', id: 'LIST' },
        { type: 'ContentData', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContentData: build.query<ContentData, string>({
      query: (id) => `ContentData/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContentData', id }],
    }),

    // 5) Update
    updateContentData: build.mutation<ContentData, Pick<ContentData, 'id'> & Partial<ContentData>>({
      query: ({ id, ...patch }) => ({
        url: `ContentData/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContentData, 'id'>) => [
        { type: 'ContentData', id },
        { type: 'ContentData', id: 'LIST' },
        { type: 'ContentData', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContentData: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContentData/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContentData', id },
        { type: 'ContentData', id: 'LIST' },
        { type: 'ContentData', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContentDataCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContentData/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContentData', id },
        { type: 'ContentData', id: 'LIST' },
        { type: 'ContentData', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContentDatasPagedQuery`
export const {
  useGetContentDatasPagedQuery,     // immediate fetch
  useLazyGetContentDatasPagedQuery, // lazy fetch
  useGetContentDataQuery,
  useGetContentDatasQuery,
  useAddContentDataMutation,
  useUpdateContentDataMutation,
  useDeleteContentDataMutation,
  useDeleteContentDataCascadeMutation,
} = ContentDataService
