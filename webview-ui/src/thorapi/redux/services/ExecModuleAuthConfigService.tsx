// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ExecModuleAuthConfig } from '@thorapi/model/ExecModuleAuthConfig'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ExecModuleAuthConfigResponse = ExecModuleAuthConfig[]
type ExecModuleAuthConfigPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ExecModuleAuthConfig>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ExecModuleAuthConfigListQueryArg = {
  example?: Partial<ExecModuleAuthConfig>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toExecModuleAuthConfigList = (result: unknown): ExecModuleAuthConfigResponse => {
  if (Array.isArray(result)) {
    return result as ExecModuleAuthConfigResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ExecModuleAuthConfigResponse) : []
}

export const ExecModuleAuthConfigService = createApi({
  reducerPath: 'ExecModuleAuthConfig', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ExecModuleAuthConfig'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getExecModuleAuthConfigsPaged: build.query<ExecModuleAuthConfigResponse, ExecModuleAuthConfigPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ExecModuleAuthConfig?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toExecModuleAuthConfigList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ExecModuleAuthConfig' as const, id })),
          { type: 'ExecModuleAuthConfig', id: `PAGE_${page}` },
          { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getExecModuleAuthConfigs: build.query<ExecModuleAuthConfigResponse, ExecModuleAuthConfigListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ExecModuleAuthConfig?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ExecModuleAuthConfig`;
      },
      providesTags: (result) => {
        const rows = toExecModuleAuthConfigList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ExecModuleAuthConfig' as const, id })),
          { type: 'ExecModuleAuthConfig', id: 'LIST' },
          { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addExecModuleAuthConfig: build.mutation<ExecModuleAuthConfig, Partial<ExecModuleAuthConfig>>({
      query: (body) => ({
        url: `ExecModuleAuthConfig`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ExecModuleAuthConfig', id: 'LIST' },
        { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getExecModuleAuthConfig: build.query<ExecModuleAuthConfig, string>({
      query: (id) => `ExecModuleAuthConfig/${id}`,
      providesTags: (result, error, id) => [{ type: 'ExecModuleAuthConfig', id }],
    }),

    // 5) Update
    updateExecModuleAuthConfig: build.mutation<ExecModuleAuthConfig, Pick<ExecModuleAuthConfig, 'id'> & Partial<ExecModuleAuthConfig>>({
      query: ({ id, ...patch }) => ({
        url: `ExecModuleAuthConfig/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ExecModuleAuthConfig, 'id'>) => [
        { type: 'ExecModuleAuthConfig', id },
        { type: 'ExecModuleAuthConfig', id: 'LIST' },
        { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteExecModuleAuthConfig: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ExecModuleAuthConfig/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ExecModuleAuthConfig', id },
        { type: 'ExecModuleAuthConfig', id: 'LIST' },
        { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteExecModuleAuthConfigCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ExecModuleAuthConfig/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ExecModuleAuthConfig', id },
        { type: 'ExecModuleAuthConfig', id: 'LIST' },
        { type: 'ExecModuleAuthConfig', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetExecModuleAuthConfigsPagedQuery`
export const {
  useGetExecModuleAuthConfigsPagedQuery,     // immediate fetch
  useLazyGetExecModuleAuthConfigsPagedQuery, // lazy fetch
  useGetExecModuleAuthConfigQuery,
  useGetExecModuleAuthConfigsQuery,
  useAddExecModuleAuthConfigMutation,
  useUpdateExecModuleAuthConfigMutation,
  useDeleteExecModuleAuthConfigMutation,
  useDeleteExecModuleAuthConfigCascadeMutation,
} = ExecModuleAuthConfigService
