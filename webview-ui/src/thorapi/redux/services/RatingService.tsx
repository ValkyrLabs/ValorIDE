// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Rating } from '@thorapi/model/Rating'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RatingResponse = Rating[]
type RatingPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Rating>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RatingListQueryArg = {
  example?: Partial<Rating>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRatingList = (result: unknown): RatingResponse => {
  if (Array.isArray(result)) {
    return result as RatingResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RatingResponse) : []
}

export const RatingService = createApi({
  reducerPath: 'Rating', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Rating'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRatingsPaged: build.query<RatingResponse, RatingPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Rating?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRatingList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Rating' as const, id })),
          { type: 'Rating', id: `PAGE_${page}` },
          { type: 'Rating', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRatings: build.query<RatingResponse, RatingListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Rating?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Rating`;
      },
      providesTags: (result) => {
        const rows = toRatingList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Rating' as const, id })),
          { type: 'Rating', id: 'LIST' },
          { type: 'Rating', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRating: build.mutation<Rating, Partial<Rating>>({
      query: (body) => ({
        url: `Rating`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Rating', id: 'LIST' },
        { type: 'Rating', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRating: build.query<Rating, string>({
      query: (id) => `Rating/${id}`,
      providesTags: (result, error, id) => [{ type: 'Rating', id }],
    }),

    // 5) Update
    updateRating: build.mutation<Rating, Pick<Rating, 'id'> & Partial<Rating>>({
      query: ({ id, ...patch }) => ({
        url: `Rating/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Rating, 'id'>) => [
        { type: 'Rating', id },
        { type: 'Rating', id: 'LIST' },
        { type: 'Rating', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRating: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Rating/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Rating', id },
        { type: 'Rating', id: 'LIST' },
        { type: 'Rating', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRatingCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Rating/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Rating', id },
        { type: 'Rating', id: 'LIST' },
        { type: 'Rating', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRatingsPagedQuery`
export const {
  useGetRatingsPagedQuery,     // immediate fetch
  useLazyGetRatingsPagedQuery, // lazy fetch
  useGetRatingQuery,
  useGetRatingsQuery,
  useAddRatingMutation,
  useUpdateRatingMutation,
  useDeleteRatingMutation,
  useDeleteRatingCascadeMutation,
} = RatingService
