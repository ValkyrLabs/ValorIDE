// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { GeneratedArtifact } from '@thorapi/model/GeneratedArtifact'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type GeneratedArtifactResponse = GeneratedArtifact[]
type GeneratedArtifactPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<GeneratedArtifact>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type GeneratedArtifactListQueryArg = {
  example?: Partial<GeneratedArtifact>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toGeneratedArtifactList = (result: unknown): GeneratedArtifactResponse => {
  if (Array.isArray(result)) {
    return result as GeneratedArtifactResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as GeneratedArtifactResponse) : []
}

export const GeneratedArtifactService = createApi({
  reducerPath: 'GeneratedArtifact', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['GeneratedArtifact'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getGeneratedArtifactsPaged: build.query<GeneratedArtifactResponse, GeneratedArtifactPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `GeneratedArtifact?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toGeneratedArtifactList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'GeneratedArtifact' as const, id })),
          { type: 'GeneratedArtifact', id: `PAGE_${page}` },
          { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getGeneratedArtifacts: build.query<GeneratedArtifactResponse, GeneratedArtifactListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `GeneratedArtifact?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `GeneratedArtifact`;
      },
      providesTags: (result) => {
        const rows = toGeneratedArtifactList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'GeneratedArtifact' as const, id })),
          { type: 'GeneratedArtifact', id: 'LIST' },
          { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addGeneratedArtifact: build.mutation<GeneratedArtifact, Partial<GeneratedArtifact>>({
      query: (body) => ({
        url: `GeneratedArtifact`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'GeneratedArtifact', id: 'LIST' },
        { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getGeneratedArtifact: build.query<GeneratedArtifact, string>({
      query: (id) => `GeneratedArtifact/${id}`,
      providesTags: (result, error, id) => [{ type: 'GeneratedArtifact', id }],
    }),

    // 5) Update
    updateGeneratedArtifact: build.mutation<GeneratedArtifact, Pick<GeneratedArtifact, 'id'> & Partial<GeneratedArtifact>>({
      query: ({ id, ...patch }) => ({
        url: `GeneratedArtifact/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<GeneratedArtifact, 'id'>) => [
        { type: 'GeneratedArtifact', id },
        { type: 'GeneratedArtifact', id: 'LIST' },
        { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteGeneratedArtifact: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `GeneratedArtifact/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'GeneratedArtifact', id },
        { type: 'GeneratedArtifact', id: 'LIST' },
        { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteGeneratedArtifactCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `GeneratedArtifact/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'GeneratedArtifact', id },
        { type: 'GeneratedArtifact', id: 'LIST' },
        { type: 'GeneratedArtifact', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetGeneratedArtifactsPagedQuery`
export const {
  useGetGeneratedArtifactsPagedQuery,     // immediate fetch
  useLazyGetGeneratedArtifactsPagedQuery, // lazy fetch
  useGetGeneratedArtifactQuery,
  useGetGeneratedArtifactsQuery,
  useAddGeneratedArtifactMutation,
  useUpdateGeneratedArtifactMutation,
  useDeleteGeneratedArtifactMutation,
  useDeleteGeneratedArtifactCascadeMutation,
} = GeneratedArtifactService
