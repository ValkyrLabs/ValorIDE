// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { AppGenerationRequest } from '@thorapi/model/AppGenerationRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type AppGenerationRequestResponse = AppGenerationRequest[]
type AppGenerationRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<AppGenerationRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type AppGenerationRequestListQueryArg = {
  example?: Partial<AppGenerationRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toAppGenerationRequestList = (result: unknown): AppGenerationRequestResponse => {
  if (Array.isArray(result)) {
    return result as AppGenerationRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as AppGenerationRequestResponse) : []
}

export const AppGenerationRequestService = createApi({
  reducerPath: 'AppGenerationRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['AppGenerationRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAppGenerationRequestsPaged: build.query<AppGenerationRequestResponse, AppGenerationRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `AppGenerationRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAppGenerationRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppGenerationRequest' as const, id })),
          { type: 'AppGenerationRequest', id: `PAGE_${page}` },
          { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAppGenerationRequests: build.query<AppGenerationRequestResponse, AppGenerationRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `AppGenerationRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `AppGenerationRequest`;
      },
      providesTags: (result) => {
        const rows = toAppGenerationRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppGenerationRequest' as const, id })),
          { type: 'AppGenerationRequest', id: 'LIST' },
          { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addAppGenerationRequest: build.mutation<AppGenerationRequest, Partial<AppGenerationRequest>>({
      query: (body) => ({
        url: `AppGenerationRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'AppGenerationRequest', id: 'LIST' },
        { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getAppGenerationRequest: build.query<AppGenerationRequest, string>({
      query: (id) => `AppGenerationRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'AppGenerationRequest', id }],
    }),

    // 5) Update
    updateAppGenerationRequest: build.mutation<AppGenerationRequest, Pick<AppGenerationRequest, 'id'> & Partial<AppGenerationRequest>>({
      query: ({ id, ...patch }) => ({
        url: `AppGenerationRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<AppGenerationRequest, 'id'>) => [
        { type: 'AppGenerationRequest', id },
        { type: 'AppGenerationRequest', id: 'LIST' },
        { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteAppGenerationRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `AppGenerationRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'AppGenerationRequest', id },
        { type: 'AppGenerationRequest', id: 'LIST' },
        { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAppGenerationRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `AppGenerationRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'AppGenerationRequest', id },
        { type: 'AppGenerationRequest', id: 'LIST' },
        { type: 'AppGenerationRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetAppGenerationRequestsPagedQuery`
export const {
  useGetAppGenerationRequestsPagedQuery,     // immediate fetch
  useLazyGetAppGenerationRequestsPagedQuery, // lazy fetch
  useGetAppGenerationRequestQuery,
  useGetAppGenerationRequestsQuery,
  useAddAppGenerationRequestMutation,
  useUpdateAppGenerationRequestMutation,
  useDeleteAppGenerationRequestMutation,
  useDeleteAppGenerationRequestCascadeMutation,
} = AppGenerationRequestService
