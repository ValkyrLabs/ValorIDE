// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ThorApiGenerationRun } from '@thorapi/model/ThorApiGenerationRun'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ThorApiGenerationRunResponse = ThorApiGenerationRun[]
type ThorApiGenerationRunPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ThorApiGenerationRun>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ThorApiGenerationRunListQueryArg = {
  example?: Partial<ThorApiGenerationRun>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toThorApiGenerationRunList = (result: unknown): ThorApiGenerationRunResponse => {
  if (Array.isArray(result)) {
    return result as ThorApiGenerationRunResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ThorApiGenerationRunResponse) : []
}

export const ThorApiGenerationRunService = createApi({
  reducerPath: 'ThorApiGenerationRun', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ThorApiGenerationRun'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getThorApiGenerationRunsPaged: build.query<ThorApiGenerationRunResponse, ThorApiGenerationRunPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ThorApiGenerationRun?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toThorApiGenerationRunList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ThorApiGenerationRun' as const, id })),
          { type: 'ThorApiGenerationRun', id: `PAGE_${page}` },
          { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getThorApiGenerationRuns: build.query<ThorApiGenerationRunResponse, ThorApiGenerationRunListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ThorApiGenerationRun?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ThorApiGenerationRun`;
      },
      providesTags: (result) => {
        const rows = toThorApiGenerationRunList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ThorApiGenerationRun' as const, id })),
          { type: 'ThorApiGenerationRun', id: 'LIST' },
          { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addThorApiGenerationRun: build.mutation<ThorApiGenerationRun, Partial<ThorApiGenerationRun>>({
      query: (body) => ({
        url: `ThorApiGenerationRun`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ThorApiGenerationRun', id: 'LIST' },
        { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getThorApiGenerationRun: build.query<ThorApiGenerationRun, string>({
      query: (id) => `ThorApiGenerationRun/${id}`,
      providesTags: (result, error, id) => [{ type: 'ThorApiGenerationRun', id }],
    }),

    // 5) Update
    updateThorApiGenerationRun: build.mutation<ThorApiGenerationRun, Pick<ThorApiGenerationRun, 'id'> & Partial<ThorApiGenerationRun>>({
      query: ({ id, ...patch }) => ({
        url: `ThorApiGenerationRun/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ThorApiGenerationRun, 'id'>) => [
        { type: 'ThorApiGenerationRun', id },
        { type: 'ThorApiGenerationRun', id: 'LIST' },
        { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteThorApiGenerationRun: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ThorApiGenerationRun/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ThorApiGenerationRun', id },
        { type: 'ThorApiGenerationRun', id: 'LIST' },
        { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteThorApiGenerationRunCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ThorApiGenerationRun/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ThorApiGenerationRun', id },
        { type: 'ThorApiGenerationRun', id: 'LIST' },
        { type: 'ThorApiGenerationRun', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetThorApiGenerationRunsPagedQuery`
export const {
  useGetThorApiGenerationRunsPagedQuery,     // immediate fetch
  useLazyGetThorApiGenerationRunsPagedQuery, // lazy fetch
  useGetThorApiGenerationRunQuery,
  useGetThorApiGenerationRunsQuery,
  useAddThorApiGenerationRunMutation,
  useUpdateThorApiGenerationRunMutation,
  useDeleteThorApiGenerationRunMutation,
  useDeleteThorApiGenerationRunCascadeMutation,
} = ThorApiGenerationRunService
