// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { McpMarketplaceCatalog } from '@thorapi/model/McpMarketplaceCatalog'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type McpMarketplaceCatalogResponse = McpMarketplaceCatalog[]
type McpMarketplaceCatalogPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<McpMarketplaceCatalog>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type McpMarketplaceCatalogListQueryArg = {
  example?: Partial<McpMarketplaceCatalog>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toMcpMarketplaceCatalogList = (result: unknown): McpMarketplaceCatalogResponse => {
  if (Array.isArray(result)) {
    return result as McpMarketplaceCatalogResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as McpMarketplaceCatalogResponse) : []
}

export const McpMarketplaceCatalogService = createApi({
  reducerPath: 'McpMarketplaceCatalog', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['McpMarketplaceCatalog'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getMcpMarketplaceCatalogsPaged: build.query<McpMarketplaceCatalogResponse, McpMarketplaceCatalogPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `McpMarketplaceCatalog?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toMcpMarketplaceCatalogList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpMarketplaceCatalog' as const, id })),
          { type: 'McpMarketplaceCatalog', id: `PAGE_${page}` },
          { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getMcpMarketplaceCatalogs: build.query<McpMarketplaceCatalogResponse, McpMarketplaceCatalogListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `McpMarketplaceCatalog?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `McpMarketplaceCatalog`;
      },
      providesTags: (result) => {
        const rows = toMcpMarketplaceCatalogList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpMarketplaceCatalog' as const, id })),
          { type: 'McpMarketplaceCatalog', id: 'LIST' },
          { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addMcpMarketplaceCatalog: build.mutation<McpMarketplaceCatalog, Partial<McpMarketplaceCatalog>>({
      query: (body) => ({
        url: `McpMarketplaceCatalog`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'McpMarketplaceCatalog', id: 'LIST' },
        { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getMcpMarketplaceCatalog: build.query<McpMarketplaceCatalog, string>({
      query: (id) => `McpMarketplaceCatalog/${id}`,
      providesTags: (result, error, id) => [{ type: 'McpMarketplaceCatalog', id }],
    }),

    // 5) Update
    updateMcpMarketplaceCatalog: build.mutation<McpMarketplaceCatalog, Pick<McpMarketplaceCatalog, 'id'> & Partial<McpMarketplaceCatalog>>({
      query: ({ id, ...patch }) => ({
        url: `McpMarketplaceCatalog/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<McpMarketplaceCatalog, 'id'>) => [
        { type: 'McpMarketplaceCatalog', id },
        { type: 'McpMarketplaceCatalog', id: 'LIST' },
        { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteMcpMarketplaceCatalog: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `McpMarketplaceCatalog/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'McpMarketplaceCatalog', id },
        { type: 'McpMarketplaceCatalog', id: 'LIST' },
        { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteMcpMarketplaceCatalogCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `McpMarketplaceCatalog/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'McpMarketplaceCatalog', id },
        { type: 'McpMarketplaceCatalog', id: 'LIST' },
        { type: 'McpMarketplaceCatalog', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetMcpMarketplaceCatalogsPagedQuery`
export const {
  useGetMcpMarketplaceCatalogsPagedQuery,     // immediate fetch
  useLazyGetMcpMarketplaceCatalogsPagedQuery, // lazy fetch
  useGetMcpMarketplaceCatalogQuery,
  useGetMcpMarketplaceCatalogsQuery,
  useAddMcpMarketplaceCatalogMutation,
  useUpdateMcpMarketplaceCatalogMutation,
  useDeleteMcpMarketplaceCatalogMutation,
  useDeleteMcpMarketplaceCatalogCascadeMutation,
} = McpMarketplaceCatalogService
