// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { McpServiceContractsResponse } from '@thorapi/model/McpServiceContractsResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type McpServiceContractsResponseResponse = McpServiceContractsResponse[]
type McpServiceContractsResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<McpServiceContractsResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type McpServiceContractsResponseListQueryArg = {
  example?: Partial<McpServiceContractsResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toMcpServiceContractsResponseList = (result: unknown): McpServiceContractsResponseResponse => {
  if (Array.isArray(result)) {
    return result as McpServiceContractsResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as McpServiceContractsResponseResponse) : []
}

export const McpServiceContractsResponseService = createApi({
  reducerPath: 'McpServiceContractsResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['McpServiceContractsResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getMcpServiceContractsResponsesPaged: build.query<McpServiceContractsResponseResponse, McpServiceContractsResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `McpServiceContractsResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toMcpServiceContractsResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpServiceContractsResponse' as const, id })),
          { type: 'McpServiceContractsResponse', id: `PAGE_${page}` },
          { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getMcpServiceContractsResponses: build.query<McpServiceContractsResponseResponse, McpServiceContractsResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `McpServiceContractsResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `McpServiceContractsResponse`;
      },
      providesTags: (result) => {
        const rows = toMcpServiceContractsResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpServiceContractsResponse' as const, id })),
          { type: 'McpServiceContractsResponse', id: 'LIST' },
          { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addMcpServiceContractsResponse: build.mutation<McpServiceContractsResponse, Partial<McpServiceContractsResponse>>({
      query: (body) => ({
        url: `McpServiceContractsResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'McpServiceContractsResponse', id: 'LIST' },
        { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getMcpServiceContractsResponse: build.query<McpServiceContractsResponse, string>({
      query: (id) => `McpServiceContractsResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'McpServiceContractsResponse', id }],
    }),

    // 5) Update
    updateMcpServiceContractsResponse: build.mutation<McpServiceContractsResponse, Pick<McpServiceContractsResponse, 'id'> & Partial<McpServiceContractsResponse>>({
      query: ({ id, ...patch }) => ({
        url: `McpServiceContractsResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<McpServiceContractsResponse, 'id'>) => [
        { type: 'McpServiceContractsResponse', id },
        { type: 'McpServiceContractsResponse', id: 'LIST' },
        { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteMcpServiceContractsResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `McpServiceContractsResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'McpServiceContractsResponse', id },
        { type: 'McpServiceContractsResponse', id: 'LIST' },
        { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteMcpServiceContractsResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `McpServiceContractsResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'McpServiceContractsResponse', id },
        { type: 'McpServiceContractsResponse', id: 'LIST' },
        { type: 'McpServiceContractsResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetMcpServiceContractsResponsesPagedQuery`
export const {
  useGetMcpServiceContractsResponsesPagedQuery,     // immediate fetch
  useLazyGetMcpServiceContractsResponsesPagedQuery, // lazy fetch
  useGetMcpServiceContractsResponseQuery,
  useGetMcpServiceContractsResponsesQuery,
  useAddMcpServiceContractsResponseMutation,
  useUpdateMcpServiceContractsResponseMutation,
  useDeleteMcpServiceContractsResponseMutation,
  useDeleteMcpServiceContractsResponseCascadeMutation,
} = McpServiceContractsResponseService
