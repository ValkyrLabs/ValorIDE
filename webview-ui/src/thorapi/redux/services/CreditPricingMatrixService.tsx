// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { CreditPricingMatrix } from '@thorapi/model/CreditPricingMatrix'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type CreditPricingMatrixResponse = CreditPricingMatrix[]
type CreditPricingMatrixPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<CreditPricingMatrix>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type CreditPricingMatrixListQueryArg = {
  example?: Partial<CreditPricingMatrix>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toCreditPricingMatrixList = (result: unknown): CreditPricingMatrixResponse => {
  if (Array.isArray(result)) {
    return result as CreditPricingMatrixResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as CreditPricingMatrixResponse) : []
}

export const CreditPricingMatrixService = createApi({
  reducerPath: 'CreditPricingMatrix', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['CreditPricingMatrix'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getCreditPricingMatrixsPaged: build.query<CreditPricingMatrixResponse, CreditPricingMatrixPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `CreditPricingMatrix?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toCreditPricingMatrixList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditPricingMatrix' as const, id })),
          { type: 'CreditPricingMatrix', id: `PAGE_${page}` },
          { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getCreditPricingMatrixs: build.query<CreditPricingMatrixResponse, CreditPricingMatrixListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `CreditPricingMatrix?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `CreditPricingMatrix`;
      },
      providesTags: (result) => {
        const rows = toCreditPricingMatrixList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditPricingMatrix' as const, id })),
          { type: 'CreditPricingMatrix', id: 'LIST' },
          { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addCreditPricingMatrix: build.mutation<CreditPricingMatrix, Partial<CreditPricingMatrix>>({
      query: (body) => ({
        url: `CreditPricingMatrix`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'CreditPricingMatrix', id: 'LIST' },
        { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getCreditPricingMatrix: build.query<CreditPricingMatrix, string>({
      query: (id) => `CreditPricingMatrix/${id}`,
      providesTags: (result, error, id) => [{ type: 'CreditPricingMatrix', id }],
    }),

    // 5) Update
    updateCreditPricingMatrix: build.mutation<CreditPricingMatrix, Pick<CreditPricingMatrix, 'id'> & Partial<CreditPricingMatrix>>({
      query: ({ id, ...patch }) => ({
        url: `CreditPricingMatrix/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<CreditPricingMatrix, 'id'>) => [
        { type: 'CreditPricingMatrix', id },
        { type: 'CreditPricingMatrix', id: 'LIST' },
        { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteCreditPricingMatrix: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `CreditPricingMatrix/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'CreditPricingMatrix', id },
        { type: 'CreditPricingMatrix', id: 'LIST' },
        { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteCreditPricingMatrixCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `CreditPricingMatrix/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'CreditPricingMatrix', id },
        { type: 'CreditPricingMatrix', id: 'LIST' },
        { type: 'CreditPricingMatrix', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetCreditPricingMatrixsPagedQuery`
export const {
  useGetCreditPricingMatrixsPagedQuery,     // immediate fetch
  useLazyGetCreditPricingMatrixsPagedQuery, // lazy fetch
  useGetCreditPricingMatrixQuery,
  useGetCreditPricingMatrixsQuery,
  useAddCreditPricingMatrixMutation,
  useUpdateCreditPricingMatrixMutation,
  useDeleteCreditPricingMatrixMutation,
  useDeleteCreditPricingMatrixCascadeMutation,
} = CreditPricingMatrixService
