// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { PublishMcpToolRequest } from '@thorapi/model/PublishMcpToolRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type PublishMcpToolRequestResponse = PublishMcpToolRequest[]
type PublishMcpToolRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<PublishMcpToolRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type PublishMcpToolRequestListQueryArg = {
  example?: Partial<PublishMcpToolRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toPublishMcpToolRequestList = (result: unknown): PublishMcpToolRequestResponse => {
  if (Array.isArray(result)) {
    return result as PublishMcpToolRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as PublishMcpToolRequestResponse) : []
}

export const PublishMcpToolRequestService = createApi({
  reducerPath: 'PublishMcpToolRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['PublishMcpToolRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getPublishMcpToolRequestsPaged: build.query<PublishMcpToolRequestResponse, PublishMcpToolRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `PublishMcpToolRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toPublishMcpToolRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'PublishMcpToolRequest' as const, id })),
          { type: 'PublishMcpToolRequest', id: `PAGE_${page}` },
          { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getPublishMcpToolRequests: build.query<PublishMcpToolRequestResponse, PublishMcpToolRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `PublishMcpToolRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `PublishMcpToolRequest`;
      },
      providesTags: (result) => {
        const rows = toPublishMcpToolRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'PublishMcpToolRequest' as const, id })),
          { type: 'PublishMcpToolRequest', id: 'LIST' },
          { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addPublishMcpToolRequest: build.mutation<PublishMcpToolRequest, Partial<PublishMcpToolRequest>>({
      query: (body) => ({
        url: `PublishMcpToolRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'PublishMcpToolRequest', id: 'LIST' },
        { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getPublishMcpToolRequest: build.query<PublishMcpToolRequest, string>({
      query: (id) => `PublishMcpToolRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'PublishMcpToolRequest', id }],
    }),

    // 5) Update
    updatePublishMcpToolRequest: build.mutation<PublishMcpToolRequest, Pick<PublishMcpToolRequest, 'id'> & Partial<PublishMcpToolRequest>>({
      query: ({ id, ...patch }) => ({
        url: `PublishMcpToolRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<PublishMcpToolRequest, 'id'>) => [
        { type: 'PublishMcpToolRequest', id },
        { type: 'PublishMcpToolRequest', id: 'LIST' },
        { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deletePublishMcpToolRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `PublishMcpToolRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'PublishMcpToolRequest', id },
        { type: 'PublishMcpToolRequest', id: 'LIST' },
        { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deletePublishMcpToolRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `PublishMcpToolRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'PublishMcpToolRequest', id },
        { type: 'PublishMcpToolRequest', id: 'LIST' },
        { type: 'PublishMcpToolRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetPublishMcpToolRequestsPagedQuery`
export const {
  useGetPublishMcpToolRequestsPagedQuery,     // immediate fetch
  useLazyGetPublishMcpToolRequestsPagedQuery, // lazy fetch
  useGetPublishMcpToolRequestQuery,
  useGetPublishMcpToolRequestsQuery,
  useAddPublishMcpToolRequestMutation,
  useUpdatePublishMcpToolRequestMutation,
  useDeletePublishMcpToolRequestMutation,
  useDeletePublishMcpToolRequestCascadeMutation,
} = PublishMcpToolRequestService
