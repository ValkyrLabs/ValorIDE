// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ProjectObjectLink } from '@thorapi/model/ProjectObjectLink'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ProjectObjectLinkResponse = ProjectObjectLink[]
type ProjectObjectLinkPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ProjectObjectLink>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ProjectObjectLinkListQueryArg = {
  example?: Partial<ProjectObjectLink>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toProjectObjectLinkList = (result: unknown): ProjectObjectLinkResponse => {
  if (Array.isArray(result)) {
    return result as ProjectObjectLinkResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ProjectObjectLinkResponse) : []
}

export const ProjectObjectLinkService = createApi({
  reducerPath: 'ProjectObjectLink', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ProjectObjectLink'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getProjectObjectLinksPaged: build.query<ProjectObjectLinkResponse, ProjectObjectLinkPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ProjectObjectLink?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toProjectObjectLinkList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ProjectObjectLink' as const, id })),
          { type: 'ProjectObjectLink', id: `PAGE_${page}` },
          { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getProjectObjectLinks: build.query<ProjectObjectLinkResponse, ProjectObjectLinkListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ProjectObjectLink?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ProjectObjectLink`;
      },
      providesTags: (result) => {
        const rows = toProjectObjectLinkList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ProjectObjectLink' as const, id })),
          { type: 'ProjectObjectLink', id: 'LIST' },
          { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addProjectObjectLink: build.mutation<ProjectObjectLink, Partial<ProjectObjectLink>>({
      query: (body) => ({
        url: `ProjectObjectLink`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ProjectObjectLink', id: 'LIST' },
        { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getProjectObjectLink: build.query<ProjectObjectLink, string>({
      query: (id) => `ProjectObjectLink/${id}`,
      providesTags: (result, error, id) => [{ type: 'ProjectObjectLink', id }],
    }),

    // 5) Update
    updateProjectObjectLink: build.mutation<ProjectObjectLink, Pick<ProjectObjectLink, 'id'> & Partial<ProjectObjectLink>>({
      query: ({ id, ...patch }) => ({
        url: `ProjectObjectLink/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ProjectObjectLink, 'id'>) => [
        { type: 'ProjectObjectLink', id },
        { type: 'ProjectObjectLink', id: 'LIST' },
        { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteProjectObjectLink: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ProjectObjectLink/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ProjectObjectLink', id },
        { type: 'ProjectObjectLink', id: 'LIST' },
        { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteProjectObjectLinkCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ProjectObjectLink/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ProjectObjectLink', id },
        { type: 'ProjectObjectLink', id: 'LIST' },
        { type: 'ProjectObjectLink', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetProjectObjectLinksPagedQuery`
export const {
  useGetProjectObjectLinksPagedQuery,     // immediate fetch
  useLazyGetProjectObjectLinksPagedQuery, // lazy fetch
  useGetProjectObjectLinkQuery,
  useGetProjectObjectLinksQuery,
  useAddProjectObjectLinkMutation,
  useUpdateProjectObjectLinkMutation,
  useDeleteProjectObjectLinkMutation,
  useDeleteProjectObjectLinkCascadeMutation,
} = ProjectObjectLinkService
