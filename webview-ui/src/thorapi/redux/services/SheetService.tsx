// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Sheet } from '@thorapi/model/Sheet'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SheetResponse = Sheet[]
type SheetPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Sheet>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SheetListQueryArg = {
  example?: Partial<Sheet>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSheetList = (result: unknown): SheetResponse => {
  if (Array.isArray(result)) {
    return result as SheetResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SheetResponse) : []
}

export const SheetService = createApi({
  reducerPath: 'Sheet', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Sheet'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSheetsPaged: build.query<SheetResponse, SheetPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Sheet?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSheetList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Sheet' as const, id })),
          { type: 'Sheet', id: `PAGE_${page}` },
          { type: 'Sheet', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSheets: build.query<SheetResponse, SheetListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Sheet?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Sheet`;
      },
      providesTags: (result) => {
        const rows = toSheetList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Sheet' as const, id })),
          { type: 'Sheet', id: 'LIST' },
          { type: 'Sheet', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSheet: build.mutation<Sheet, Partial<Sheet>>({
      query: (body) => ({
        url: `Sheet`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Sheet', id: 'LIST' },
        { type: 'Sheet', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSheet: build.query<Sheet, string>({
      query: (id) => `Sheet/${id}`,
      providesTags: (result, error, id) => [{ type: 'Sheet', id }],
    }),

    // 5) Update
    updateSheet: build.mutation<Sheet, Pick<Sheet, 'id'> & Partial<Sheet>>({
      query: ({ id, ...patch }) => ({
        url: `Sheet/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Sheet, 'id'>) => [
        { type: 'Sheet', id },
        { type: 'Sheet', id: 'LIST' },
        { type: 'Sheet', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSheet: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Sheet/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Sheet', id },
        { type: 'Sheet', id: 'LIST' },
        { type: 'Sheet', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSheetCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Sheet/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Sheet', id },
        { type: 'Sheet', id: 'LIST' },
        { type: 'Sheet', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSheetsPagedQuery`
export const {
  useGetSheetsPagedQuery,     // immediate fetch
  useLazyGetSheetsPagedQuery, // lazy fetch
  useGetSheetQuery,
  useGetSheetsQuery,
  useAddSheetMutation,
  useUpdateSheetMutation,
  useDeleteSheetMutation,
  useDeleteSheetCascadeMutation,
} = SheetService
