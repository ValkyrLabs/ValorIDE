// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { AppGenerationTraceResponse } from '@thorapi/model/AppGenerationTraceResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type AppGenerationTraceResponseResponse = AppGenerationTraceResponse[]
type AppGenerationTraceResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<AppGenerationTraceResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type AppGenerationTraceResponseListQueryArg = {
  example?: Partial<AppGenerationTraceResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toAppGenerationTraceResponseList = (result: unknown): AppGenerationTraceResponseResponse => {
  if (Array.isArray(result)) {
    return result as AppGenerationTraceResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as AppGenerationTraceResponseResponse) : []
}

export const AppGenerationTraceResponseService = createApi({
  reducerPath: 'AppGenerationTraceResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['AppGenerationTraceResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAppGenerationTraceResponsesPaged: build.query<AppGenerationTraceResponseResponse, AppGenerationTraceResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `AppGenerationTraceResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAppGenerationTraceResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppGenerationTraceResponse' as const, id })),
          { type: 'AppGenerationTraceResponse', id: `PAGE_${page}` },
          { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAppGenerationTraceResponses: build.query<AppGenerationTraceResponseResponse, AppGenerationTraceResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `AppGenerationTraceResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `AppGenerationTraceResponse`;
      },
      providesTags: (result) => {
        const rows = toAppGenerationTraceResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppGenerationTraceResponse' as const, id })),
          { type: 'AppGenerationTraceResponse', id: 'LIST' },
          { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addAppGenerationTraceResponse: build.mutation<AppGenerationTraceResponse, Partial<AppGenerationTraceResponse>>({
      query: (body) => ({
        url: `AppGenerationTraceResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'AppGenerationTraceResponse', id: 'LIST' },
        { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getAppGenerationTraceResponse: build.query<AppGenerationTraceResponse, string>({
      query: (id) => `AppGenerationTraceResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'AppGenerationTraceResponse', id }],
    }),

    // 5) Update
    updateAppGenerationTraceResponse: build.mutation<AppGenerationTraceResponse, Pick<AppGenerationTraceResponse, 'id'> & Partial<AppGenerationTraceResponse>>({
      query: ({ id, ...patch }) => ({
        url: `AppGenerationTraceResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<AppGenerationTraceResponse, 'id'>) => [
        { type: 'AppGenerationTraceResponse', id },
        { type: 'AppGenerationTraceResponse', id: 'LIST' },
        { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteAppGenerationTraceResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `AppGenerationTraceResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'AppGenerationTraceResponse', id },
        { type: 'AppGenerationTraceResponse', id: 'LIST' },
        { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAppGenerationTraceResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `AppGenerationTraceResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'AppGenerationTraceResponse', id },
        { type: 'AppGenerationTraceResponse', id: 'LIST' },
        { type: 'AppGenerationTraceResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetAppGenerationTraceResponsesPagedQuery`
export const {
  useGetAppGenerationTraceResponsesPagedQuery,     // immediate fetch
  useLazyGetAppGenerationTraceResponsesPagedQuery, // lazy fetch
  useGetAppGenerationTraceResponseQuery,
  useGetAppGenerationTraceResponsesQuery,
  useAddAppGenerationTraceResponseMutation,
  useUpdateAppGenerationTraceResponseMutation,
  useDeleteAppGenerationTraceResponseMutation,
  useDeleteAppGenerationTraceResponseCascadeMutation,
} = AppGenerationTraceResponseService
