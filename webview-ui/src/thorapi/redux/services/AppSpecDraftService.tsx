// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { AppSpecDraft } from '@thorapi/model/AppSpecDraft'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type AppSpecDraftResponse = AppSpecDraft[]
type AppSpecDraftPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<AppSpecDraft>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type AppSpecDraftListQueryArg = {
  example?: Partial<AppSpecDraft>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toAppSpecDraftList = (result: unknown): AppSpecDraftResponse => {
  if (Array.isArray(result)) {
    return result as AppSpecDraftResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as AppSpecDraftResponse) : []
}

export const AppSpecDraftService = createApi({
  reducerPath: 'AppSpecDraft', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['AppSpecDraft'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAppSpecDraftsPaged: build.query<AppSpecDraftResponse, AppSpecDraftPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `AppSpecDraft?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAppSpecDraftList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppSpecDraft' as const, id })),
          { type: 'AppSpecDraft', id: `PAGE_${page}` },
          { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAppSpecDrafts: build.query<AppSpecDraftResponse, AppSpecDraftListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `AppSpecDraft?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `AppSpecDraft`;
      },
      providesTags: (result) => {
        const rows = toAppSpecDraftList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppSpecDraft' as const, id })),
          { type: 'AppSpecDraft', id: 'LIST' },
          { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addAppSpecDraft: build.mutation<AppSpecDraft, Partial<AppSpecDraft>>({
      query: (body) => ({
        url: `AppSpecDraft`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'AppSpecDraft', id: 'LIST' },
        { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getAppSpecDraft: build.query<AppSpecDraft, string>({
      query: (id) => `AppSpecDraft/${id}`,
      providesTags: (result, error, id) => [{ type: 'AppSpecDraft', id }],
    }),

    // 5) Update
    updateAppSpecDraft: build.mutation<AppSpecDraft, Pick<AppSpecDraft, 'id'> & Partial<AppSpecDraft>>({
      query: ({ id, ...patch }) => ({
        url: `AppSpecDraft/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<AppSpecDraft, 'id'>) => [
        { type: 'AppSpecDraft', id },
        { type: 'AppSpecDraft', id: 'LIST' },
        { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteAppSpecDraft: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `AppSpecDraft/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'AppSpecDraft', id },
        { type: 'AppSpecDraft', id: 'LIST' },
        { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAppSpecDraftCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `AppSpecDraft/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'AppSpecDraft', id },
        { type: 'AppSpecDraft', id: 'LIST' },
        { type: 'AppSpecDraft', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetAppSpecDraftsPagedQuery`
export const {
  useGetAppSpecDraftsPagedQuery,     // immediate fetch
  useLazyGetAppSpecDraftsPagedQuery, // lazy fetch
  useGetAppSpecDraftQuery,
  useGetAppSpecDraftsQuery,
  useAddAppSpecDraftMutation,
  useUpdateAppSpecDraftMutation,
  useDeleteAppSpecDraftMutation,
  useDeleteAppSpecDraftCascadeMutation,
} = AppSpecDraftService
