// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalRetryRequest } from '@thorapi/model/RetrievalRetryRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalRetryRequestResponse = RetrievalRetryRequest[]
type RetrievalRetryRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalRetryRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalRetryRequestListQueryArg = {
  example?: Partial<RetrievalRetryRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalRetryRequestList = (result: unknown): RetrievalRetryRequestResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalRetryRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalRetryRequestResponse) : []
}

export const RetrievalRetryRequestService = createApi({
  reducerPath: 'RetrievalRetryRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalRetryRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalRetryRequestsPaged: build.query<RetrievalRetryRequestResponse, RetrievalRetryRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalRetryRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalRetryRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalRetryRequest' as const, id })),
          { type: 'RetrievalRetryRequest', id: `PAGE_${page}` },
          { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalRetryRequests: build.query<RetrievalRetryRequestResponse, RetrievalRetryRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalRetryRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalRetryRequest`;
      },
      providesTags: (result) => {
        const rows = toRetrievalRetryRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalRetryRequest' as const, id })),
          { type: 'RetrievalRetryRequest', id: 'LIST' },
          { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalRetryRequest: build.mutation<RetrievalRetryRequest, Partial<RetrievalRetryRequest>>({
      query: (body) => ({
        url: `RetrievalRetryRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalRetryRequest', id: 'LIST' },
        { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalRetryRequest: build.query<RetrievalRetryRequest, string>({
      query: (id) => `RetrievalRetryRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalRetryRequest', id }],
    }),

    // 5) Update
    updateRetrievalRetryRequest: build.mutation<RetrievalRetryRequest, Pick<RetrievalRetryRequest, 'id'> & Partial<RetrievalRetryRequest>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalRetryRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalRetryRequest, 'id'>) => [
        { type: 'RetrievalRetryRequest', id },
        { type: 'RetrievalRetryRequest', id: 'LIST' },
        { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalRetryRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalRetryRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalRetryRequest', id },
        { type: 'RetrievalRetryRequest', id: 'LIST' },
        { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalRetryRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalRetryRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalRetryRequest', id },
        { type: 'RetrievalRetryRequest', id: 'LIST' },
        { type: 'RetrievalRetryRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalRetryRequestsPagedQuery`
export const {
  useGetRetrievalRetryRequestsPagedQuery,     // immediate fetch
  useLazyGetRetrievalRetryRequestsPagedQuery, // lazy fetch
  useGetRetrievalRetryRequestQuery,
  useGetRetrievalRetryRequestsQuery,
  useAddRetrievalRetryRequestMutation,
  useUpdateRetrievalRetryRequestMutation,
  useDeleteRetrievalRetryRequestMutation,
  useDeleteRetrievalRetryRequestCascadeMutation,
} = RetrievalRetryRequestService
