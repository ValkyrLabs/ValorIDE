// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextCompressionReceipt } from '@thorapi/model/ContextCompressionReceipt'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextCompressionReceiptResponse = ContextCompressionReceipt[]
type ContextCompressionReceiptPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextCompressionReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextCompressionReceiptListQueryArg = {
  example?: Partial<ContextCompressionReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextCompressionReceiptList = (result: unknown): ContextCompressionReceiptResponse => {
  if (Array.isArray(result)) {
    return result as ContextCompressionReceiptResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextCompressionReceiptResponse) : []
}

export const ContextCompressionReceiptService = createApi({
  reducerPath: 'ContextCompressionReceipt', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextCompressionReceipt'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextCompressionReceiptsPaged: build.query<ContextCompressionReceiptResponse, ContextCompressionReceiptPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextCompressionReceipt?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextCompressionReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextCompressionReceipt' as const, id })),
          { type: 'ContextCompressionReceipt', id: `PAGE_${page}` },
          { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextCompressionReceipts: build.query<ContextCompressionReceiptResponse, ContextCompressionReceiptListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextCompressionReceipt?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextCompressionReceipt`;
      },
      providesTags: (result) => {
        const rows = toContextCompressionReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextCompressionReceipt' as const, id })),
          { type: 'ContextCompressionReceipt', id: 'LIST' },
          { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextCompressionReceipt: build.mutation<ContextCompressionReceipt, Partial<ContextCompressionReceipt>>({
      query: (body) => ({
        url: `ContextCompressionReceipt`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextCompressionReceipt', id: 'LIST' },
        { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextCompressionReceipt: build.query<ContextCompressionReceipt, string>({
      query: (id) => `ContextCompressionReceipt/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextCompressionReceipt', id }],
    }),

    // 5) Update
    updateContextCompressionReceipt: build.mutation<ContextCompressionReceipt, Pick<ContextCompressionReceipt, 'id'> & Partial<ContextCompressionReceipt>>({
      query: ({ id, ...patch }) => ({
        url: `ContextCompressionReceipt/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextCompressionReceipt, 'id'>) => [
        { type: 'ContextCompressionReceipt', id },
        { type: 'ContextCompressionReceipt', id: 'LIST' },
        { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextCompressionReceipt: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextCompressionReceipt/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextCompressionReceipt', id },
        { type: 'ContextCompressionReceipt', id: 'LIST' },
        { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextCompressionReceiptCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextCompressionReceipt/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextCompressionReceipt', id },
        { type: 'ContextCompressionReceipt', id: 'LIST' },
        { type: 'ContextCompressionReceipt', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextCompressionReceiptsPagedQuery`
export const {
  useGetContextCompressionReceiptsPagedQuery,     // immediate fetch
  useLazyGetContextCompressionReceiptsPagedQuery, // lazy fetch
  useGetContextCompressionReceiptQuery,
  useGetContextCompressionReceiptsQuery,
  useAddContextCompressionReceiptMutation,
  useUpdateContextCompressionReceiptMutation,
  useDeleteContextCompressionReceiptMutation,
  useDeleteContextCompressionReceiptCascadeMutation,
} = ContextCompressionReceiptService
