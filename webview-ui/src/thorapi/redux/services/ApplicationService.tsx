// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Application } from '@thorapi/model/Application'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ApplicationResponse = Application[]
type ApplicationPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Application>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ApplicationListQueryArg = {
  example?: Partial<Application>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toApplicationList = (result: unknown): ApplicationResponse => {
  if (Array.isArray(result)) {
    return result as ApplicationResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ApplicationResponse) : []
}

export const ApplicationService = createApi({
  reducerPath: 'Application', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Application'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getApplicationsPaged: build.query<ApplicationResponse, ApplicationPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Application?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toApplicationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Application' as const, id })),
          { type: 'Application', id: `PAGE_${page}` },
          { type: 'Application', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getApplications: build.query<ApplicationResponse, ApplicationListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Application?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Application`;
      },
      providesTags: (result) => {
        const rows = toApplicationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Application' as const, id })),
          { type: 'Application', id: 'LIST' },
          { type: 'Application', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addApplication: build.mutation<Application, Partial<Application>>({
      query: (body) => ({
        url: `Application`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Application', id: 'LIST' },
        { type: 'Application', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getApplication: build.query<Application, string>({
      query: (id) => `Application/${id}`,
      providesTags: (result, error, id) => [{ type: 'Application', id }],
    }),

    // 5) Update
    updateApplication: build.mutation<Application, Pick<Application, 'id'> & Partial<Application>>({
      query: ({ id, ...patch }) => ({
        url: `Application/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Application, 'id'>) => [
        { type: 'Application', id },
        { type: 'Application', id: 'LIST' },
        { type: 'Application', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteApplication: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Application/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Application', id },
        { type: 'Application', id: 'LIST' },
        { type: 'Application', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteApplicationCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Application/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Application', id },
        { type: 'Application', id: 'LIST' },
        { type: 'Application', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetApplicationsPagedQuery`
export const {
  useGetApplicationsPagedQuery,     // immediate fetch
  useLazyGetApplicationsPagedQuery, // lazy fetch
  useGetApplicationQuery,
  useGetApplicationsQuery,
  useAddApplicationMutation,
  useUpdateApplicationMutation,
  useDeleteApplicationMutation,
  useDeleteApplicationCascadeMutation,
} = ApplicationService
