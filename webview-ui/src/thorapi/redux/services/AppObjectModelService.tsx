// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { AppObjectModel } from '@thorapi/model/AppObjectModel'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type AppObjectModelResponse = AppObjectModel[]
type AppObjectModelPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<AppObjectModel>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type AppObjectModelListQueryArg = {
  example?: Partial<AppObjectModel>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toAppObjectModelList = (result: unknown): AppObjectModelResponse => {
  if (Array.isArray(result)) {
    return result as AppObjectModelResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as AppObjectModelResponse) : []
}

export const AppObjectModelService = createApi({
  reducerPath: 'AppObjectModel', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['AppObjectModel'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAppObjectModelsPaged: build.query<AppObjectModelResponse, AppObjectModelPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `AppObjectModel?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAppObjectModelList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppObjectModel' as const, id })),
          { type: 'AppObjectModel', id: `PAGE_${page}` },
          { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAppObjectModels: build.query<AppObjectModelResponse, AppObjectModelListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `AppObjectModel?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `AppObjectModel`;
      },
      providesTags: (result) => {
        const rows = toAppObjectModelList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'AppObjectModel' as const, id })),
          { type: 'AppObjectModel', id: 'LIST' },
          { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addAppObjectModel: build.mutation<AppObjectModel, Partial<AppObjectModel>>({
      query: (body) => ({
        url: `AppObjectModel`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'AppObjectModel', id: 'LIST' },
        { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getAppObjectModel: build.query<AppObjectModel, string>({
      query: (id) => `AppObjectModel/${id}`,
      providesTags: (result, error, id) => [{ type: 'AppObjectModel', id }],
    }),

    // 5) Update
    updateAppObjectModel: build.mutation<AppObjectModel, Pick<AppObjectModel, 'id'> & Partial<AppObjectModel>>({
      query: ({ id, ...patch }) => ({
        url: `AppObjectModel/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<AppObjectModel, 'id'>) => [
        { type: 'AppObjectModel', id },
        { type: 'AppObjectModel', id: 'LIST' },
        { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteAppObjectModel: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `AppObjectModel/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'AppObjectModel', id },
        { type: 'AppObjectModel', id: 'LIST' },
        { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAppObjectModelCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `AppObjectModel/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'AppObjectModel', id },
        { type: 'AppObjectModel', id: 'LIST' },
        { type: 'AppObjectModel', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetAppObjectModelsPagedQuery`
export const {
  useGetAppObjectModelsPagedQuery,     // immediate fetch
  useLazyGetAppObjectModelsPagedQuery, // lazy fetch
  useGetAppObjectModelQuery,
  useGetAppObjectModelsQuery,
  useAddAppObjectModelMutation,
  useUpdateAppObjectModelMutation,
  useDeleteAppObjectModelMutation,
  useDeleteAppObjectModelCascadeMutation,
} = AppObjectModelService
