// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalReceiptItem } from '@thorapi/model/RetrievalReceiptItem'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalReceiptItemResponse = RetrievalReceiptItem[]
type RetrievalReceiptItemPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalReceiptItem>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalReceiptItemListQueryArg = {
  example?: Partial<RetrievalReceiptItem>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalReceiptItemList = (result: unknown): RetrievalReceiptItemResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalReceiptItemResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalReceiptItemResponse) : []
}

export const RetrievalReceiptItemService = createApi({
  reducerPath: 'RetrievalReceiptItem', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalReceiptItem'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalReceiptItemsPaged: build.query<RetrievalReceiptItemResponse, RetrievalReceiptItemPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalReceiptItem?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalReceiptItemList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptItem' as const, id })),
          { type: 'RetrievalReceiptItem', id: `PAGE_${page}` },
          { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalReceiptItems: build.query<RetrievalReceiptItemResponse, RetrievalReceiptItemListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalReceiptItem?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalReceiptItem`;
      },
      providesTags: (result) => {
        const rows = toRetrievalReceiptItemList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptItem' as const, id })),
          { type: 'RetrievalReceiptItem', id: 'LIST' },
          { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalReceiptItem: build.mutation<RetrievalReceiptItem, Partial<RetrievalReceiptItem>>({
      query: (body) => ({
        url: `RetrievalReceiptItem`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalReceiptItem', id: 'LIST' },
        { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalReceiptItem: build.query<RetrievalReceiptItem, string>({
      query: (id) => `RetrievalReceiptItem/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalReceiptItem', id }],
    }),

    // 5) Update
    updateRetrievalReceiptItem: build.mutation<RetrievalReceiptItem, Pick<RetrievalReceiptItem, 'id'> & Partial<RetrievalReceiptItem>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalReceiptItem/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalReceiptItem, 'id'>) => [
        { type: 'RetrievalReceiptItem', id },
        { type: 'RetrievalReceiptItem', id: 'LIST' },
        { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalReceiptItem: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalReceiptItem/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalReceiptItem', id },
        { type: 'RetrievalReceiptItem', id: 'LIST' },
        { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalReceiptItemCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalReceiptItem/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalReceiptItem', id },
        { type: 'RetrievalReceiptItem', id: 'LIST' },
        { type: 'RetrievalReceiptItem', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalReceiptItemsPagedQuery`
export const {
  useGetRetrievalReceiptItemsPagedQuery,     // immediate fetch
  useLazyGetRetrievalReceiptItemsPagedQuery, // lazy fetch
  useGetRetrievalReceiptItemQuery,
  useGetRetrievalReceiptItemsQuery,
  useAddRetrievalReceiptItemMutation,
  useUpdateRetrievalReceiptItemMutation,
  useDeleteRetrievalReceiptItemMutation,
  useDeleteRetrievalReceiptItemCascadeMutation,
} = RetrievalReceiptItemService
