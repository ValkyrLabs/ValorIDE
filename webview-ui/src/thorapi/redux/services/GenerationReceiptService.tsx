// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { GenerationReceipt } from '@thorapi/model/GenerationReceipt'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type GenerationReceiptResponse = GenerationReceipt[]
type GenerationReceiptPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<GenerationReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type GenerationReceiptListQueryArg = {
  example?: Partial<GenerationReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toGenerationReceiptList = (result: unknown): GenerationReceiptResponse => {
  if (Array.isArray(result)) {
    return result as GenerationReceiptResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as GenerationReceiptResponse) : []
}

export const GenerationReceiptService = createApi({
  reducerPath: 'GenerationReceipt', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['GenerationReceipt'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getGenerationReceiptsPaged: build.query<GenerationReceiptResponse, GenerationReceiptPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `GenerationReceipt?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toGenerationReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'GenerationReceipt' as const, id })),
          { type: 'GenerationReceipt', id: `PAGE_${page}` },
          { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getGenerationReceipts: build.query<GenerationReceiptResponse, GenerationReceiptListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `GenerationReceipt?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `GenerationReceipt`;
      },
      providesTags: (result) => {
        const rows = toGenerationReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'GenerationReceipt' as const, id })),
          { type: 'GenerationReceipt', id: 'LIST' },
          { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addGenerationReceipt: build.mutation<GenerationReceipt, Partial<GenerationReceipt>>({
      query: (body) => ({
        url: `GenerationReceipt`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'GenerationReceipt', id: 'LIST' },
        { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getGenerationReceipt: build.query<GenerationReceipt, string>({
      query: (id) => `GenerationReceipt/${id}`,
      providesTags: (result, error, id) => [{ type: 'GenerationReceipt', id }],
    }),

    // 5) Update
    updateGenerationReceipt: build.mutation<GenerationReceipt, Pick<GenerationReceipt, 'id'> & Partial<GenerationReceipt>>({
      query: ({ id, ...patch }) => ({
        url: `GenerationReceipt/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<GenerationReceipt, 'id'>) => [
        { type: 'GenerationReceipt', id },
        { type: 'GenerationReceipt', id: 'LIST' },
        { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteGenerationReceipt: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `GenerationReceipt/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'GenerationReceipt', id },
        { type: 'GenerationReceipt', id: 'LIST' },
        { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteGenerationReceiptCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `GenerationReceipt/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'GenerationReceipt', id },
        { type: 'GenerationReceipt', id: 'LIST' },
        { type: 'GenerationReceipt', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetGenerationReceiptsPagedQuery`
export const {
  useGetGenerationReceiptsPagedQuery,     // immediate fetch
  useLazyGetGenerationReceiptsPagedQuery, // lazy fetch
  useGetGenerationReceiptQuery,
  useGetGenerationReceiptsQuery,
  useAddGenerationReceiptMutation,
  useUpdateGenerationReceiptMutation,
  useDeleteGenerationReceiptMutation,
  useDeleteGenerationReceiptCascadeMutation,
} = GenerationReceiptService
