// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Authority } from '@thorapi/model/Authority'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type AuthorityResponse = Authority[]
type AuthorityPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Authority>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type AuthorityListQueryArg = {
  example?: Partial<Authority>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toAuthorityList = (result: unknown): AuthorityResponse => {
  if (Array.isArray(result)) {
    return result as AuthorityResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as AuthorityResponse) : []
}

export const AuthorityService = createApi({
  reducerPath: 'Authority', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Authority'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getAuthoritysPaged: build.query<AuthorityResponse, AuthorityPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Authority?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toAuthorityList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Authority' as const, id })),
          { type: 'Authority', id: `PAGE_${page}` },
          { type: 'Authority', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getAuthoritys: build.query<AuthorityResponse, AuthorityListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Authority?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Authority`;
      },
      providesTags: (result) => {
        const rows = toAuthorityList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Authority' as const, id })),
          { type: 'Authority', id: 'LIST' },
          { type: 'Authority', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addAuthority: build.mutation<Authority, Partial<Authority>>({
      query: (body) => ({
        url: `Authority`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Authority', id: 'LIST' },
        { type: 'Authority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getAuthority: build.query<Authority, string>({
      query: (id) => `Authority/${id}`,
      providesTags: (result, error, id) => [{ type: 'Authority', id }],
    }),

    // 5) Update
    updateAuthority: build.mutation<Authority, Pick<Authority, 'id'> & Partial<Authority>>({
      query: ({ id, ...patch }) => ({
        url: `Authority/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Authority, 'id'>) => [
        { type: 'Authority', id },
        { type: 'Authority', id: 'LIST' },
        { type: 'Authority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteAuthority: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Authority/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Authority', id },
        { type: 'Authority', id: 'LIST' },
        { type: 'Authority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteAuthorityCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Authority/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Authority', id },
        { type: 'Authority', id: 'LIST' },
        { type: 'Authority', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetAuthoritysPagedQuery`
export const {
  useGetAuthoritysPagedQuery,     // immediate fetch
  useLazyGetAuthoritysPagedQuery, // lazy fetch
  useGetAuthorityQuery,
  useGetAuthoritysQuery,
  useAddAuthorityMutation,
  useUpdateAuthorityMutation,
  useDeleteAuthorityMutation,
  useDeleteAuthorityCascadeMutation,
} = AuthorityService
