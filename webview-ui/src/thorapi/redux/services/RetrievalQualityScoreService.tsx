// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalQualityScore } from '@thorapi/model/RetrievalQualityScore'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalQualityScoreResponse = RetrievalQualityScore[]
type RetrievalQualityScorePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalQualityScore>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalQualityScoreListQueryArg = {
  example?: Partial<RetrievalQualityScore>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalQualityScoreList = (result: unknown): RetrievalQualityScoreResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalQualityScoreResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalQualityScoreResponse) : []
}

export const RetrievalQualityScoreService = createApi({
  reducerPath: 'RetrievalQualityScore', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalQualityScore'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalQualityScoresPaged: build.query<RetrievalQualityScoreResponse, RetrievalQualityScorePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalQualityScore?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalQualityScoreList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalQualityScore' as const, id })),
          { type: 'RetrievalQualityScore', id: `PAGE_${page}` },
          { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalQualityScores: build.query<RetrievalQualityScoreResponse, RetrievalQualityScoreListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalQualityScore?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalQualityScore`;
      },
      providesTags: (result) => {
        const rows = toRetrievalQualityScoreList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalQualityScore' as const, id })),
          { type: 'RetrievalQualityScore', id: 'LIST' },
          { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalQualityScore: build.mutation<RetrievalQualityScore, Partial<RetrievalQualityScore>>({
      query: (body) => ({
        url: `RetrievalQualityScore`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalQualityScore', id: 'LIST' },
        { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalQualityScore: build.query<RetrievalQualityScore, string>({
      query: (id) => `RetrievalQualityScore/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalQualityScore', id }],
    }),

    // 5) Update
    updateRetrievalQualityScore: build.mutation<RetrievalQualityScore, Pick<RetrievalQualityScore, 'id'> & Partial<RetrievalQualityScore>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalQualityScore/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalQualityScore, 'id'>) => [
        { type: 'RetrievalQualityScore', id },
        { type: 'RetrievalQualityScore', id: 'LIST' },
        { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalQualityScore: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalQualityScore/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalQualityScore', id },
        { type: 'RetrievalQualityScore', id: 'LIST' },
        { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalQualityScoreCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalQualityScore/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalQualityScore', id },
        { type: 'RetrievalQualityScore', id: 'LIST' },
        { type: 'RetrievalQualityScore', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalQualityScoresPagedQuery`
export const {
  useGetRetrievalQualityScoresPagedQuery,     // immediate fetch
  useLazyGetRetrievalQualityScoresPagedQuery, // lazy fetch
  useGetRetrievalQualityScoreQuery,
  useGetRetrievalQualityScoresQuery,
  useAddRetrievalQualityScoreMutation,
  useUpdateRetrievalQualityScoreMutation,
  useDeleteRetrievalQualityScoreMutation,
  useDeleteRetrievalQualityScoreCascadeMutation,
} = RetrievalQualityScoreService
