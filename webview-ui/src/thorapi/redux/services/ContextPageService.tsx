// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPage } from '@thorapi/model/ContextPage'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageResponse = ContextPage[]
type ContextPagePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageListQueryArg = {
  example?: Partial<ContextPage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageList = (result: unknown): ContextPageResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageResponse) : []
}

export const ContextPageService = createApi({
  reducerPath: 'ContextPage', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPage'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPagesPaged: build.query<ContextPageResponse, ContextPagePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPage?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPage' as const, id })),
          { type: 'ContextPage', id: `PAGE_${page}` },
          { type: 'ContextPage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPages: build.query<ContextPageResponse, ContextPageListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPage?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPage`;
      },
      providesTags: (result) => {
        const rows = toContextPageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPage' as const, id })),
          { type: 'ContextPage', id: 'LIST' },
          { type: 'ContextPage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPage: build.mutation<ContextPage, Partial<ContextPage>>({
      query: (body) => ({
        url: `ContextPage`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPage', id: 'LIST' },
        { type: 'ContextPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPage: build.query<ContextPage, string>({
      query: (id) => `ContextPage/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPage', id }],
    }),

    // 5) Update
    updateContextPage: build.mutation<ContextPage, Pick<ContextPage, 'id'> & Partial<ContextPage>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPage/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPage, 'id'>) => [
        { type: 'ContextPage', id },
        { type: 'ContextPage', id: 'LIST' },
        { type: 'ContextPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPage: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPage/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPage', id },
        { type: 'ContextPage', id: 'LIST' },
        { type: 'ContextPage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPage/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPage', id },
        { type: 'ContextPage', id: 'LIST' },
        { type: 'ContextPage', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPagesPagedQuery`
export const {
  useGetContextPagesPagedQuery,     // immediate fetch
  useLazyGetContextPagesPagedQuery, // lazy fetch
  useGetContextPageQuery,
  useGetContextPagesQuery,
  useAddContextPageMutation,
  useUpdateContextPageMutation,
  useDeleteContextPageMutation,
  useDeleteContextPageCascadeMutation,
} = ContextPageService
