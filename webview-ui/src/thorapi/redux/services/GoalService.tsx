// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { Goal } from '@thorapi/model/Goal'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type GoalResponse = Goal[]
type GoalPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<Goal>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type GoalListQueryArg = {
  example?: Partial<Goal>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toGoalList = (result: unknown): GoalResponse => {
  if (Array.isArray(result)) {
    return result as GoalResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as GoalResponse) : []
}

export const GoalService = createApi({
  reducerPath: 'Goal', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['Goal'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getGoalsPaged: build.query<GoalResponse, GoalPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `Goal?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toGoalList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Goal' as const, id })),
          { type: 'Goal', id: `PAGE_${page}` },
          { type: 'Goal', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getGoals: build.query<GoalResponse, GoalListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `Goal?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `Goal`;
      },
      providesTags: (result) => {
        const rows = toGoalList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'Goal' as const, id })),
          { type: 'Goal', id: 'LIST' },
          { type: 'Goal', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addGoal: build.mutation<Goal, Partial<Goal>>({
      query: (body) => ({
        url: `Goal`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'Goal', id: 'LIST' },
        { type: 'Goal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getGoal: build.query<Goal, string>({
      query: (id) => `Goal/${id}`,
      providesTags: (result, error, id) => [{ type: 'Goal', id }],
    }),

    // 5) Update
    updateGoal: build.mutation<Goal, Pick<Goal, 'id'> & Partial<Goal>>({
      query: ({ id, ...patch }) => ({
        url: `Goal/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<Goal, 'id'>) => [
        { type: 'Goal', id },
        { type: 'Goal', id: 'LIST' },
        { type: 'Goal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteGoal: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `Goal/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'Goal', id },
        { type: 'Goal', id: 'LIST' },
        { type: 'Goal', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteGoalCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `Goal/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'Goal', id },
        { type: 'Goal', id: 'LIST' },
        { type: 'Goal', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetGoalsPagedQuery`
export const {
  useGetGoalsPagedQuery,     // immediate fetch
  useLazyGetGoalsPagedQuery, // lazy fetch
  useGetGoalQuery,
  useGetGoalsQuery,
  useAddGoalMutation,
  useUpdateGoalMutation,
  useDeleteGoalMutation,
  useDeleteGoalCascadeMutation,
} = GoalService
