// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalEvaluatorResult } from '@thorapi/model/RetrievalEvaluatorResult'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalEvaluatorResultResponse = RetrievalEvaluatorResult[]
type RetrievalEvaluatorResultPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalEvaluatorResult>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalEvaluatorResultListQueryArg = {
  example?: Partial<RetrievalEvaluatorResult>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalEvaluatorResultList = (result: unknown): RetrievalEvaluatorResultResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalEvaluatorResultResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalEvaluatorResultResponse) : []
}

export const RetrievalEvaluatorResultService = createApi({
  reducerPath: 'RetrievalEvaluatorResult', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalEvaluatorResult'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalEvaluatorResultsPaged: build.query<RetrievalEvaluatorResultResponse, RetrievalEvaluatorResultPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalEvaluatorResult?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalEvaluatorResultList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalEvaluatorResult' as const, id })),
          { type: 'RetrievalEvaluatorResult', id: `PAGE_${page}` },
          { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalEvaluatorResults: build.query<RetrievalEvaluatorResultResponse, RetrievalEvaluatorResultListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalEvaluatorResult?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalEvaluatorResult`;
      },
      providesTags: (result) => {
        const rows = toRetrievalEvaluatorResultList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalEvaluatorResult' as const, id })),
          { type: 'RetrievalEvaluatorResult', id: 'LIST' },
          { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalEvaluatorResult: build.mutation<RetrievalEvaluatorResult, Partial<RetrievalEvaluatorResult>>({
      query: (body) => ({
        url: `RetrievalEvaluatorResult`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalEvaluatorResult', id: 'LIST' },
        { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalEvaluatorResult: build.query<RetrievalEvaluatorResult, string>({
      query: (id) => `RetrievalEvaluatorResult/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalEvaluatorResult', id }],
    }),

    // 5) Update
    updateRetrievalEvaluatorResult: build.mutation<RetrievalEvaluatorResult, Pick<RetrievalEvaluatorResult, 'id'> & Partial<RetrievalEvaluatorResult>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalEvaluatorResult/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalEvaluatorResult, 'id'>) => [
        { type: 'RetrievalEvaluatorResult', id },
        { type: 'RetrievalEvaluatorResult', id: 'LIST' },
        { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalEvaluatorResult: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalEvaluatorResult/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalEvaluatorResult', id },
        { type: 'RetrievalEvaluatorResult', id: 'LIST' },
        { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalEvaluatorResultCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalEvaluatorResult/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalEvaluatorResult', id },
        { type: 'RetrievalEvaluatorResult', id: 'LIST' },
        { type: 'RetrievalEvaluatorResult', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalEvaluatorResultsPagedQuery`
export const {
  useGetRetrievalEvaluatorResultsPagedQuery,     // immediate fetch
  useLazyGetRetrievalEvaluatorResultsPagedQuery, // lazy fetch
  useGetRetrievalEvaluatorResultQuery,
  useGetRetrievalEvaluatorResultsQuery,
  useAddRetrievalEvaluatorResultMutation,
  useUpdateRetrievalEvaluatorResultMutation,
  useDeleteRetrievalEvaluatorResultMutation,
  useDeleteRetrievalEvaluatorResultCascadeMutation,
} = RetrievalEvaluatorResultService
