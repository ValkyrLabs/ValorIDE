// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { StripeSubscription } from '@thorapi/model/StripeSubscription'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type StripeSubscriptionResponse = StripeSubscription[]
type StripeSubscriptionPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<StripeSubscription>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type StripeSubscriptionListQueryArg = {
  example?: Partial<StripeSubscription>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toStripeSubscriptionList = (result: unknown): StripeSubscriptionResponse => {
  if (Array.isArray(result)) {
    return result as StripeSubscriptionResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as StripeSubscriptionResponse) : []
}

export const StripeSubscriptionService = createApi({
  reducerPath: 'StripeSubscription', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['StripeSubscription'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getStripeSubscriptionsPaged: build.query<StripeSubscriptionResponse, StripeSubscriptionPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `StripeSubscription?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toStripeSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeSubscription' as const, id })),
          { type: 'StripeSubscription', id: `PAGE_${page}` },
          { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getStripeSubscriptions: build.query<StripeSubscriptionResponse, StripeSubscriptionListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `StripeSubscription?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `StripeSubscription`;
      },
      providesTags: (result) => {
        const rows = toStripeSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StripeSubscription' as const, id })),
          { type: 'StripeSubscription', id: 'LIST' },
          { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addStripeSubscription: build.mutation<StripeSubscription, Partial<StripeSubscription>>({
      query: (body) => ({
        url: `StripeSubscription`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'StripeSubscription', id: 'LIST' },
        { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getStripeSubscription: build.query<StripeSubscription, string>({
      query: (id) => `StripeSubscription/${id}`,
      providesTags: (result, error, id) => [{ type: 'StripeSubscription', id }],
    }),

    // 5) Update
    updateStripeSubscription: build.mutation<StripeSubscription, Pick<StripeSubscription, 'id'> & Partial<StripeSubscription>>({
      query: ({ id, ...patch }) => ({
        url: `StripeSubscription/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<StripeSubscription, 'id'>) => [
        { type: 'StripeSubscription', id },
        { type: 'StripeSubscription', id: 'LIST' },
        { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteStripeSubscription: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `StripeSubscription/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'StripeSubscription', id },
        { type: 'StripeSubscription', id: 'LIST' },
        { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteStripeSubscriptionCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `StripeSubscription/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'StripeSubscription', id },
        { type: 'StripeSubscription', id: 'LIST' },
        { type: 'StripeSubscription', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetStripeSubscriptionsPagedQuery`
export const {
  useGetStripeSubscriptionsPagedQuery,     // immediate fetch
  useLazyGetStripeSubscriptionsPagedQuery, // lazy fetch
  useGetStripeSubscriptionQuery,
  useGetStripeSubscriptionsQuery,
  useAddStripeSubscriptionMutation,
  useUpdateStripeSubscriptionMutation,
  useDeleteStripeSubscriptionMutation,
  useDeleteStripeSubscriptionCascadeMutation,
} = StripeSubscriptionService
