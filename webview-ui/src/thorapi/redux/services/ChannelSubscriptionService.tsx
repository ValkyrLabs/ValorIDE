// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ChannelSubscription } from '@thorapi/model/ChannelSubscription'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ChannelSubscriptionResponse = ChannelSubscription[]
type ChannelSubscriptionPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ChannelSubscription>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ChannelSubscriptionListQueryArg = {
  example?: Partial<ChannelSubscription>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toChannelSubscriptionList = (result: unknown): ChannelSubscriptionResponse => {
  if (Array.isArray(result)) {
    return result as ChannelSubscriptionResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ChannelSubscriptionResponse) : []
}

export const ChannelSubscriptionService = createApi({
  reducerPath: 'ChannelSubscription', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ChannelSubscription'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getChannelSubscriptionsPaged: build.query<ChannelSubscriptionResponse, ChannelSubscriptionPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ChannelSubscription?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toChannelSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ChannelSubscription' as const, id })),
          { type: 'ChannelSubscription', id: `PAGE_${page}` },
          { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getChannelSubscriptions: build.query<ChannelSubscriptionResponse, ChannelSubscriptionListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ChannelSubscription?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ChannelSubscription`;
      },
      providesTags: (result) => {
        const rows = toChannelSubscriptionList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ChannelSubscription' as const, id })),
          { type: 'ChannelSubscription', id: 'LIST' },
          { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addChannelSubscription: build.mutation<ChannelSubscription, Partial<ChannelSubscription>>({
      query: (body) => ({
        url: `ChannelSubscription`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ChannelSubscription', id: 'LIST' },
        { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getChannelSubscription: build.query<ChannelSubscription, string>({
      query: (id) => `ChannelSubscription/${id}`,
      providesTags: (result, error, id) => [{ type: 'ChannelSubscription', id }],
    }),

    // 5) Update
    updateChannelSubscription: build.mutation<ChannelSubscription, Pick<ChannelSubscription, 'id'> & Partial<ChannelSubscription>>({
      query: ({ id, ...patch }) => ({
        url: `ChannelSubscription/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ChannelSubscription, 'id'>) => [
        { type: 'ChannelSubscription', id },
        { type: 'ChannelSubscription', id: 'LIST' },
        { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteChannelSubscription: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ChannelSubscription/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ChannelSubscription', id },
        { type: 'ChannelSubscription', id: 'LIST' },
        { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteChannelSubscriptionCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ChannelSubscription/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ChannelSubscription', id },
        { type: 'ChannelSubscription', id: 'LIST' },
        { type: 'ChannelSubscription', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetChannelSubscriptionsPagedQuery`
export const {
  useGetChannelSubscriptionsPagedQuery,     // immediate fetch
  useLazyGetChannelSubscriptionsPagedQuery, // lazy fetch
  useGetChannelSubscriptionQuery,
  useGetChannelSubscriptionsQuery,
  useAddChannelSubscriptionMutation,
  useUpdateChannelSubscriptionMutation,
  useDeleteChannelSubscriptionMutation,
  useDeleteChannelSubscriptionCascadeMutation,
} = ChannelSubscriptionService
