// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { SkillOptRouteReceipt } from '@thorapi/model/SkillOptRouteReceipt'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type SkillOptRouteReceiptResponse = SkillOptRouteReceipt[]
type SkillOptRouteReceiptPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<SkillOptRouteReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type SkillOptRouteReceiptListQueryArg = {
  example?: Partial<SkillOptRouteReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toSkillOptRouteReceiptList = (result: unknown): SkillOptRouteReceiptResponse => {
  if (Array.isArray(result)) {
    return result as SkillOptRouteReceiptResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as SkillOptRouteReceiptResponse) : []
}

export const SkillOptRouteReceiptService = createApi({
  reducerPath: 'SkillOptRouteReceipt', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['SkillOptRouteReceipt'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getSkillOptRouteReceiptsPaged: build.query<SkillOptRouteReceiptResponse, SkillOptRouteReceiptPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `SkillOptRouteReceipt?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toSkillOptRouteReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteReceipt' as const, id })),
          { type: 'SkillOptRouteReceipt', id: `PAGE_${page}` },
          { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getSkillOptRouteReceipts: build.query<SkillOptRouteReceiptResponse, SkillOptRouteReceiptListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `SkillOptRouteReceipt?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `SkillOptRouteReceipt`;
      },
      providesTags: (result) => {
        const rows = toSkillOptRouteReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'SkillOptRouteReceipt' as const, id })),
          { type: 'SkillOptRouteReceipt', id: 'LIST' },
          { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addSkillOptRouteReceipt: build.mutation<SkillOptRouteReceipt, Partial<SkillOptRouteReceipt>>({
      query: (body) => ({
        url: `SkillOptRouteReceipt`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'SkillOptRouteReceipt', id: 'LIST' },
        { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getSkillOptRouteReceipt: build.query<SkillOptRouteReceipt, string>({
      query: (id) => `SkillOptRouteReceipt/${id}`,
      providesTags: (result, error, id) => [{ type: 'SkillOptRouteReceipt', id }],
    }),

    // 5) Update
    updateSkillOptRouteReceipt: build.mutation<SkillOptRouteReceipt, Pick<SkillOptRouteReceipt, 'id'> & Partial<SkillOptRouteReceipt>>({
      query: ({ id, ...patch }) => ({
        url: `SkillOptRouteReceipt/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<SkillOptRouteReceipt, 'id'>) => [
        { type: 'SkillOptRouteReceipt', id },
        { type: 'SkillOptRouteReceipt', id: 'LIST' },
        { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteSkillOptRouteReceipt: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `SkillOptRouteReceipt/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'SkillOptRouteReceipt', id },
        { type: 'SkillOptRouteReceipt', id: 'LIST' },
        { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteSkillOptRouteReceiptCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `SkillOptRouteReceipt/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'SkillOptRouteReceipt', id },
        { type: 'SkillOptRouteReceipt', id: 'LIST' },
        { type: 'SkillOptRouteReceipt', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetSkillOptRouteReceiptsPagedQuery`
export const {
  useGetSkillOptRouteReceiptsPagedQuery,     // immediate fetch
  useLazyGetSkillOptRouteReceiptsPagedQuery, // lazy fetch
  useGetSkillOptRouteReceiptQuery,
  useGetSkillOptRouteReceiptsQuery,
  useAddSkillOptRouteReceiptMutation,
  useUpdateSkillOptRouteReceiptMutation,
  useDeleteSkillOptRouteReceiptMutation,
  useDeleteSkillOptRouteReceiptCascadeMutation,
} = SkillOptRouteReceiptService
