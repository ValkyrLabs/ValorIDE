// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageHydrateRequest } from '@thorapi/model/ContextPageHydrateRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageHydrateRequestResponse = ContextPageHydrateRequest[]
type ContextPageHydrateRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageHydrateRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageHydrateRequestListQueryArg = {
  example?: Partial<ContextPageHydrateRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageHydrateRequestList = (result: unknown): ContextPageHydrateRequestResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageHydrateRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageHydrateRequestResponse) : []
}

export const ContextPageHydrateRequestService = createApi({
  reducerPath: 'ContextPageHydrateRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageHydrateRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageHydrateRequestsPaged: build.query<ContextPageHydrateRequestResponse, ContextPageHydrateRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageHydrateRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageHydrateRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageHydrateRequest' as const, id })),
          { type: 'ContextPageHydrateRequest', id: `PAGE_${page}` },
          { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageHydrateRequests: build.query<ContextPageHydrateRequestResponse, ContextPageHydrateRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageHydrateRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageHydrateRequest`;
      },
      providesTags: (result) => {
        const rows = toContextPageHydrateRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageHydrateRequest' as const, id })),
          { type: 'ContextPageHydrateRequest', id: 'LIST' },
          { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageHydrateRequest: build.mutation<ContextPageHydrateRequest, Partial<ContextPageHydrateRequest>>({
      query: (body) => ({
        url: `ContextPageHydrateRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageHydrateRequest', id: 'LIST' },
        { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageHydrateRequest: build.query<ContextPageHydrateRequest, string>({
      query: (id) => `ContextPageHydrateRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageHydrateRequest', id }],
    }),

    // 5) Update
    updateContextPageHydrateRequest: build.mutation<ContextPageHydrateRequest, Pick<ContextPageHydrateRequest, 'id'> & Partial<ContextPageHydrateRequest>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageHydrateRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageHydrateRequest, 'id'>) => [
        { type: 'ContextPageHydrateRequest', id },
        { type: 'ContextPageHydrateRequest', id: 'LIST' },
        { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageHydrateRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageHydrateRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageHydrateRequest', id },
        { type: 'ContextPageHydrateRequest', id: 'LIST' },
        { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageHydrateRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageHydrateRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageHydrateRequest', id },
        { type: 'ContextPageHydrateRequest', id: 'LIST' },
        { type: 'ContextPageHydrateRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageHydrateRequestsPagedQuery`
export const {
  useGetContextPageHydrateRequestsPagedQuery,     // immediate fetch
  useLazyGetContextPageHydrateRequestsPagedQuery, // lazy fetch
  useGetContextPageHydrateRequestQuery,
  useGetContextPageHydrateRequestsQuery,
  useAddContextPageHydrateRequestMutation,
  useUpdateContextPageHydrateRequestMutation,
  useDeleteContextPageHydrateRequestMutation,
  useDeleteContextPageHydrateRequestCascadeMutation,
} = ContextPageHydrateRequestService
