// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageTraverseRequest } from '@thorapi/model/ContextPageTraverseRequest'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageTraverseRequestResponse = ContextPageTraverseRequest[]
type ContextPageTraverseRequestPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageTraverseRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageTraverseRequestListQueryArg = {
  example?: Partial<ContextPageTraverseRequest>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageTraverseRequestList = (result: unknown): ContextPageTraverseRequestResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageTraverseRequestResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageTraverseRequestResponse) : []
}

export const ContextPageTraverseRequestService = createApi({
  reducerPath: 'ContextPageTraverseRequest', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageTraverseRequest'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageTraverseRequestsPaged: build.query<ContextPageTraverseRequestResponse, ContextPageTraverseRequestPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageTraverseRequest?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageTraverseRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageTraverseRequest' as const, id })),
          { type: 'ContextPageTraverseRequest', id: `PAGE_${page}` },
          { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageTraverseRequests: build.query<ContextPageTraverseRequestResponse, ContextPageTraverseRequestListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageTraverseRequest?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageTraverseRequest`;
      },
      providesTags: (result) => {
        const rows = toContextPageTraverseRequestList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageTraverseRequest' as const, id })),
          { type: 'ContextPageTraverseRequest', id: 'LIST' },
          { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageTraverseRequest: build.mutation<ContextPageTraverseRequest, Partial<ContextPageTraverseRequest>>({
      query: (body) => ({
        url: `ContextPageTraverseRequest`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageTraverseRequest', id: 'LIST' },
        { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageTraverseRequest: build.query<ContextPageTraverseRequest, string>({
      query: (id) => `ContextPageTraverseRequest/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageTraverseRequest', id }],
    }),

    // 5) Update
    updateContextPageTraverseRequest: build.mutation<ContextPageTraverseRequest, Pick<ContextPageTraverseRequest, 'id'> & Partial<ContextPageTraverseRequest>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageTraverseRequest/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageTraverseRequest, 'id'>) => [
        { type: 'ContextPageTraverseRequest', id },
        { type: 'ContextPageTraverseRequest', id: 'LIST' },
        { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageTraverseRequest: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageTraverseRequest/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageTraverseRequest', id },
        { type: 'ContextPageTraverseRequest', id: 'LIST' },
        { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageTraverseRequestCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageTraverseRequest/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageTraverseRequest', id },
        { type: 'ContextPageTraverseRequest', id: 'LIST' },
        { type: 'ContextPageTraverseRequest', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageTraverseRequestsPagedQuery`
export const {
  useGetContextPageTraverseRequestsPagedQuery,     // immediate fetch
  useLazyGetContextPageTraverseRequestsPagedQuery, // lazy fetch
  useGetContextPageTraverseRequestQuery,
  useGetContextPageTraverseRequestsQuery,
  useAddContextPageTraverseRequestMutation,
  useUpdateContextPageTraverseRequestMutation,
  useDeleteContextPageTraverseRequestMutation,
  useDeleteContextPageTraverseRequestCascadeMutation,
} = ContextPageTraverseRequestService
