// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { JobMetadata } from '@thorapi/model/JobMetadata'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type JobMetadataResponse = JobMetadata[]
type JobMetadataPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<JobMetadata>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type JobMetadataListQueryArg = {
  example?: Partial<JobMetadata>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toJobMetadataList = (result: unknown): JobMetadataResponse => {
  if (Array.isArray(result)) {
    return result as JobMetadataResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as JobMetadataResponse) : []
}

export const JobMetadataService = createApi({
  reducerPath: 'JobMetadata', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['JobMetadata'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getJobMetadatasPaged: build.query<JobMetadataResponse, JobMetadataPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `JobMetadata?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toJobMetadataList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'JobMetadata' as const, id })),
          { type: 'JobMetadata', id: `PAGE_${page}` },
          { type: 'JobMetadata', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getJobMetadatas: build.query<JobMetadataResponse, JobMetadataListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `JobMetadata?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `JobMetadata`;
      },
      providesTags: (result) => {
        const rows = toJobMetadataList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'JobMetadata' as const, id })),
          { type: 'JobMetadata', id: 'LIST' },
          { type: 'JobMetadata', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addJobMetadata: build.mutation<JobMetadata, Partial<JobMetadata>>({
      query: (body) => ({
        url: `JobMetadata`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'JobMetadata', id: 'LIST' },
        { type: 'JobMetadata', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getJobMetadata: build.query<JobMetadata, string>({
      query: (id) => `JobMetadata/${id}`,
      providesTags: (result, error, id) => [{ type: 'JobMetadata', id }],
    }),

    // 5) Update
    updateJobMetadata: build.mutation<JobMetadata, Pick<JobMetadata, 'id'> & Partial<JobMetadata>>({
      query: ({ id, ...patch }) => ({
        url: `JobMetadata/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<JobMetadata, 'id'>) => [
        { type: 'JobMetadata', id },
        { type: 'JobMetadata', id: 'LIST' },
        { type: 'JobMetadata', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteJobMetadata: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `JobMetadata/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'JobMetadata', id },
        { type: 'JobMetadata', id: 'LIST' },
        { type: 'JobMetadata', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteJobMetadataCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `JobMetadata/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'JobMetadata', id },
        { type: 'JobMetadata', id: 'LIST' },
        { type: 'JobMetadata', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetJobMetadatasPagedQuery`
export const {
  useGetJobMetadatasPagedQuery,     // immediate fetch
  useLazyGetJobMetadatasPagedQuery, // lazy fetch
  useGetJobMetadataQuery,
  useGetJobMetadatasQuery,
  useAddJobMetadataMutation,
  useUpdateJobMetadataMutation,
  useDeleteJobMetadataMutation,
  useDeleteJobMetadataCascadeMutation,
} = JobMetadataService
