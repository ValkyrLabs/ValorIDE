// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { CreditReservation } from '@thorapi/model/CreditReservation'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type CreditReservationResponse = CreditReservation[]
type CreditReservationPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<CreditReservation>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type CreditReservationListQueryArg = {
  example?: Partial<CreditReservation>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toCreditReservationList = (result: unknown): CreditReservationResponse => {
  if (Array.isArray(result)) {
    return result as CreditReservationResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as CreditReservationResponse) : []
}

export const CreditReservationService = createApi({
  reducerPath: 'CreditReservation', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['CreditReservation'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getCreditReservationsPaged: build.query<CreditReservationResponse, CreditReservationPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `CreditReservation?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toCreditReservationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditReservation' as const, id })),
          { type: 'CreditReservation', id: `PAGE_${page}` },
          { type: 'CreditReservation', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getCreditReservations: build.query<CreditReservationResponse, CreditReservationListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `CreditReservation?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `CreditReservation`;
      },
      providesTags: (result) => {
        const rows = toCreditReservationList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditReservation' as const, id })),
          { type: 'CreditReservation', id: 'LIST' },
          { type: 'CreditReservation', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addCreditReservation: build.mutation<CreditReservation, Partial<CreditReservation>>({
      query: (body) => ({
        url: `CreditReservation`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'CreditReservation', id: 'LIST' },
        { type: 'CreditReservation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getCreditReservation: build.query<CreditReservation, string>({
      query: (id) => `CreditReservation/${id}`,
      providesTags: (result, error, id) => [{ type: 'CreditReservation', id }],
    }),

    // 5) Update
    updateCreditReservation: build.mutation<CreditReservation, Pick<CreditReservation, 'id'> & Partial<CreditReservation>>({
      query: ({ id, ...patch }) => ({
        url: `CreditReservation/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<CreditReservation, 'id'>) => [
        { type: 'CreditReservation', id },
        { type: 'CreditReservation', id: 'LIST' },
        { type: 'CreditReservation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteCreditReservation: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `CreditReservation/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'CreditReservation', id },
        { type: 'CreditReservation', id: 'LIST' },
        { type: 'CreditReservation', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteCreditReservationCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `CreditReservation/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'CreditReservation', id },
        { type: 'CreditReservation', id: 'LIST' },
        { type: 'CreditReservation', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetCreditReservationsPagedQuery`
export const {
  useGetCreditReservationsPagedQuery,     // immediate fetch
  useLazyGetCreditReservationsPagedQuery, // lazy fetch
  useGetCreditReservationQuery,
  useGetCreditReservationsQuery,
  useAddCreditReservationMutation,
  useUpdateCreditReservationMutation,
  useDeleteCreditReservationMutation,
  useDeleteCreditReservationCascadeMutation,
} = CreditReservationService
