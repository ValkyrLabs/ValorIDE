// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { FileListResponse } from '@thorapi/model/FileListResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type FileListResponseResponse = FileListResponse[]
type FileListResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<FileListResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type FileListResponseListQueryArg = {
  example?: Partial<FileListResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toFileListResponseList = (result: unknown): FileListResponseResponse => {
  if (Array.isArray(result)) {
    return result as FileListResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as FileListResponseResponse) : []
}

export const FileListResponseService = createApi({
  reducerPath: 'FileListResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['FileListResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getFileListResponsesPaged: build.query<FileListResponseResponse, FileListResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `FileListResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toFileListResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'FileListResponse' as const, id })),
          { type: 'FileListResponse', id: `PAGE_${page}` },
          { type: 'FileListResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getFileListResponses: build.query<FileListResponseResponse, FileListResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `FileListResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `FileListResponse`;
      },
      providesTags: (result) => {
        const rows = toFileListResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'FileListResponse' as const, id })),
          { type: 'FileListResponse', id: 'LIST' },
          { type: 'FileListResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addFileListResponse: build.mutation<FileListResponse, Partial<FileListResponse>>({
      query: (body) => ({
        url: `FileListResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'FileListResponse', id: 'LIST' },
        { type: 'FileListResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getFileListResponse: build.query<FileListResponse, string>({
      query: (id) => `FileListResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'FileListResponse', id }],
    }),

    // 5) Update
    updateFileListResponse: build.mutation<FileListResponse, Pick<FileListResponse, 'id'> & Partial<FileListResponse>>({
      query: ({ id, ...patch }) => ({
        url: `FileListResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<FileListResponse, 'id'>) => [
        { type: 'FileListResponse', id },
        { type: 'FileListResponse', id: 'LIST' },
        { type: 'FileListResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteFileListResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `FileListResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'FileListResponse', id },
        { type: 'FileListResponse', id: 'LIST' },
        { type: 'FileListResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteFileListResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `FileListResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'FileListResponse', id },
        { type: 'FileListResponse', id: 'LIST' },
        { type: 'FileListResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetFileListResponsesPagedQuery`
export const {
  useGetFileListResponsesPagedQuery,     // immediate fetch
  useLazyGetFileListResponsesPagedQuery, // lazy fetch
  useGetFileListResponseQuery,
  useGetFileListResponsesQuery,
  useAddFileListResponseMutation,
  useUpdateFileListResponseMutation,
  useDeleteFileListResponseMutation,
  useDeleteFileListResponseCascadeMutation,
} = FileListResponseService
