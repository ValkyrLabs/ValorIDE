// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { McpResponsePart } from '@thorapi/model/McpResponsePart'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type McpResponsePartResponse = McpResponsePart[]
type McpResponsePartPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<McpResponsePart>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type McpResponsePartListQueryArg = {
  example?: Partial<McpResponsePart>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toMcpResponsePartList = (result: unknown): McpResponsePartResponse => {
  if (Array.isArray(result)) {
    return result as McpResponsePartResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as McpResponsePartResponse) : []
}

export const McpResponsePartService = createApi({
  reducerPath: 'McpResponsePart', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['McpResponsePart'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getMcpResponsePartsPaged: build.query<McpResponsePartResponse, McpResponsePartPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `McpResponsePart?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toMcpResponsePartList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpResponsePart' as const, id })),
          { type: 'McpResponsePart', id: `PAGE_${page}` },
          { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getMcpResponseParts: build.query<McpResponsePartResponse, McpResponsePartListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `McpResponsePart?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `McpResponsePart`;
      },
      providesTags: (result) => {
        const rows = toMcpResponsePartList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpResponsePart' as const, id })),
          { type: 'McpResponsePart', id: 'LIST' },
          { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addMcpResponsePart: build.mutation<McpResponsePart, Partial<McpResponsePart>>({
      query: (body) => ({
        url: `McpResponsePart`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'McpResponsePart', id: 'LIST' },
        { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getMcpResponsePart: build.query<McpResponsePart, string>({
      query: (id) => `McpResponsePart/${id}`,
      providesTags: (result, error, id) => [{ type: 'McpResponsePart', id }],
    }),

    // 5) Update
    updateMcpResponsePart: build.mutation<McpResponsePart, Pick<McpResponsePart, 'id'> & Partial<McpResponsePart>>({
      query: ({ id, ...patch }) => ({
        url: `McpResponsePart/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<McpResponsePart, 'id'>) => [
        { type: 'McpResponsePart', id },
        { type: 'McpResponsePart', id: 'LIST' },
        { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteMcpResponsePart: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `McpResponsePart/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'McpResponsePart', id },
        { type: 'McpResponsePart', id: 'LIST' },
        { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteMcpResponsePartCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `McpResponsePart/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'McpResponsePart', id },
        { type: 'McpResponsePart', id: 'LIST' },
        { type: 'McpResponsePart', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetMcpResponsePartsPagedQuery`
export const {
  useGetMcpResponsePartsPagedQuery,     // immediate fetch
  useLazyGetMcpResponsePartsPagedQuery, // lazy fetch
  useGetMcpResponsePartQuery,
  useGetMcpResponsePartsQuery,
  useAddMcpResponsePartMutation,
  useUpdateMcpResponsePartMutation,
  useDeleteMcpResponsePartMutation,
  useDeleteMcpResponsePartCascadeMutation,
} = McpResponsePartService
