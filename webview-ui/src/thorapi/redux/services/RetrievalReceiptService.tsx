// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalReceipt } from '@thorapi/model/RetrievalReceipt'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalReceiptResponse = RetrievalReceipt[]
type RetrievalReceiptPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalReceiptListQueryArg = {
  example?: Partial<RetrievalReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalReceiptList = (result: unknown): RetrievalReceiptResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalReceiptResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalReceiptResponse) : []
}

export const RetrievalReceiptService = createApi({
  reducerPath: 'RetrievalReceipt', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalReceipt'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalReceiptsPaged: build.query<RetrievalReceiptResponse, RetrievalReceiptPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalReceipt?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceipt' as const, id })),
          { type: 'RetrievalReceipt', id: `PAGE_${page}` },
          { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalReceipts: build.query<RetrievalReceiptResponse, RetrievalReceiptListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalReceipt?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalReceipt`;
      },
      providesTags: (result) => {
        const rows = toRetrievalReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceipt' as const, id })),
          { type: 'RetrievalReceipt', id: 'LIST' },
          { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalReceipt: build.mutation<RetrievalReceipt, Partial<RetrievalReceipt>>({
      query: (body) => ({
        url: `RetrievalReceipt`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalReceipt', id: 'LIST' },
        { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalReceipt: build.query<RetrievalReceipt, string>({
      query: (id) => `RetrievalReceipt/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalReceipt', id }],
    }),

    // 5) Update
    updateRetrievalReceipt: build.mutation<RetrievalReceipt, Pick<RetrievalReceipt, 'id'> & Partial<RetrievalReceipt>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalReceipt/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalReceipt, 'id'>) => [
        { type: 'RetrievalReceipt', id },
        { type: 'RetrievalReceipt', id: 'LIST' },
        { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalReceipt: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalReceipt/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalReceipt', id },
        { type: 'RetrievalReceipt', id: 'LIST' },
        { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalReceiptCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalReceipt/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalReceipt', id },
        { type: 'RetrievalReceipt', id: 'LIST' },
        { type: 'RetrievalReceipt', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalReceiptsPagedQuery`
export const {
  useGetRetrievalReceiptsPagedQuery,     // immediate fetch
  useLazyGetRetrievalReceiptsPagedQuery, // lazy fetch
  useGetRetrievalReceiptQuery,
  useGetRetrievalReceiptsQuery,
  useAddRetrievalReceiptMutation,
  useUpdateRetrievalReceiptMutation,
  useDeleteRetrievalReceiptMutation,
  useDeleteRetrievalReceiptCascadeMutation,
} = RetrievalReceiptService
