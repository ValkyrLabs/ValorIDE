// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageResponse } from '@thorapi/model/ContextPageResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageResponseResponse = ContextPageResponse[]
type ContextPageResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageResponseListQueryArg = {
  example?: Partial<ContextPageResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageResponseList = (result: unknown): ContextPageResponseResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageResponseResponse) : []
}

export const ContextPageResponseService = createApi({
  reducerPath: 'ContextPageResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageResponsesPaged: build.query<ContextPageResponseResponse, ContextPageResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageResponse' as const, id })),
          { type: 'ContextPageResponse', id: `PAGE_${page}` },
          { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageResponses: build.query<ContextPageResponseResponse, ContextPageResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageResponse`;
      },
      providesTags: (result) => {
        const rows = toContextPageResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageResponse' as const, id })),
          { type: 'ContextPageResponse', id: 'LIST' },
          { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageResponse: build.mutation<ContextPageResponse, Partial<ContextPageResponse>>({
      query: (body) => ({
        url: `ContextPageResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageResponse', id: 'LIST' },
        { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageResponse: build.query<ContextPageResponse, string>({
      query: (id) => `ContextPageResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageResponse', id }],
    }),

    // 5) Update
    updateContextPageResponse: build.mutation<ContextPageResponse, Pick<ContextPageResponse, 'id'> & Partial<ContextPageResponse>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageResponse, 'id'>) => [
        { type: 'ContextPageResponse', id },
        { type: 'ContextPageResponse', id: 'LIST' },
        { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageResponse', id },
        { type: 'ContextPageResponse', id: 'LIST' },
        { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageResponse', id },
        { type: 'ContextPageResponse', id: 'LIST' },
        { type: 'ContextPageResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageResponsesPagedQuery`
export const {
  useGetContextPageResponsesPagedQuery,     // immediate fetch
  useLazyGetContextPageResponsesPagedQuery, // lazy fetch
  useGetContextPageResponseQuery,
  useGetContextPageResponsesQuery,
  useAddContextPageResponseMutation,
  useUpdateContextPageResponseMutation,
  useDeleteContextPageResponseMutation,
  useDeleteContextPageResponseCascadeMutation,
} = ContextPageResponseService
