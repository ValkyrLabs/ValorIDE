// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalReceiptResponse } from '@thorapi/model/RetrievalReceiptResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalReceiptResponseResponse = RetrievalReceiptResponse[]
type RetrievalReceiptResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalReceiptResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalReceiptResponseListQueryArg = {
  example?: Partial<RetrievalReceiptResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalReceiptResponseList = (result: unknown): RetrievalReceiptResponseResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalReceiptResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalReceiptResponseResponse) : []
}

export const RetrievalReceiptResponseService = createApi({
  reducerPath: 'RetrievalReceiptResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalReceiptResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalReceiptResponsesPaged: build.query<RetrievalReceiptResponseResponse, RetrievalReceiptResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalReceiptResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalReceiptResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptResponse' as const, id })),
          { type: 'RetrievalReceiptResponse', id: `PAGE_${page}` },
          { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalReceiptResponses: build.query<RetrievalReceiptResponseResponse, RetrievalReceiptResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalReceiptResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalReceiptResponse`;
      },
      providesTags: (result) => {
        const rows = toRetrievalReceiptResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalReceiptResponse' as const, id })),
          { type: 'RetrievalReceiptResponse', id: 'LIST' },
          { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalReceiptResponse: build.mutation<RetrievalReceiptResponse, Partial<RetrievalReceiptResponse>>({
      query: (body) => ({
        url: `RetrievalReceiptResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalReceiptResponse', id: 'LIST' },
        { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalReceiptResponse: build.query<RetrievalReceiptResponse, string>({
      query: (id) => `RetrievalReceiptResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalReceiptResponse', id }],
    }),

    // 5) Update
    updateRetrievalReceiptResponse: build.mutation<RetrievalReceiptResponse, Pick<RetrievalReceiptResponse, 'id'> & Partial<RetrievalReceiptResponse>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalReceiptResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalReceiptResponse, 'id'>) => [
        { type: 'RetrievalReceiptResponse', id },
        { type: 'RetrievalReceiptResponse', id: 'LIST' },
        { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalReceiptResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalReceiptResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalReceiptResponse', id },
        { type: 'RetrievalReceiptResponse', id: 'LIST' },
        { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalReceiptResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalReceiptResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalReceiptResponse', id },
        { type: 'RetrievalReceiptResponse', id: 'LIST' },
        { type: 'RetrievalReceiptResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalReceiptResponsesPagedQuery`
export const {
  useGetRetrievalReceiptResponsesPagedQuery,     // immediate fetch
  useLazyGetRetrievalReceiptResponsesPagedQuery, // lazy fetch
  useGetRetrievalReceiptResponseQuery,
  useGetRetrievalReceiptResponsesQuery,
  useAddRetrievalReceiptResponseMutation,
  useUpdateRetrievalReceiptResponseMutation,
  useDeleteRetrievalReceiptResponseMutation,
  useDeleteRetrievalReceiptResponseCascadeMutation,
} = RetrievalReceiptResponseService
