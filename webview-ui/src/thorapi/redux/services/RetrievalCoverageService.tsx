// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { RetrievalCoverage } from '@thorapi/model/RetrievalCoverage'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type RetrievalCoverageResponse = RetrievalCoverage[]
type RetrievalCoveragePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<RetrievalCoverage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type RetrievalCoverageListQueryArg = {
  example?: Partial<RetrievalCoverage>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toRetrievalCoverageList = (result: unknown): RetrievalCoverageResponse => {
  if (Array.isArray(result)) {
    return result as RetrievalCoverageResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as RetrievalCoverageResponse) : []
}

export const RetrievalCoverageService = createApi({
  reducerPath: 'RetrievalCoverage', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['RetrievalCoverage'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getRetrievalCoveragesPaged: build.query<RetrievalCoverageResponse, RetrievalCoveragePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `RetrievalCoverage?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toRetrievalCoverageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalCoverage' as const, id })),
          { type: 'RetrievalCoverage', id: `PAGE_${page}` },
          { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getRetrievalCoverages: build.query<RetrievalCoverageResponse, RetrievalCoverageListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `RetrievalCoverage?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `RetrievalCoverage`;
      },
      providesTags: (result) => {
        const rows = toRetrievalCoverageList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'RetrievalCoverage' as const, id })),
          { type: 'RetrievalCoverage', id: 'LIST' },
          { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addRetrievalCoverage: build.mutation<RetrievalCoverage, Partial<RetrievalCoverage>>({
      query: (body) => ({
        url: `RetrievalCoverage`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'RetrievalCoverage', id: 'LIST' },
        { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getRetrievalCoverage: build.query<RetrievalCoverage, string>({
      query: (id) => `RetrievalCoverage/${id}`,
      providesTags: (result, error, id) => [{ type: 'RetrievalCoverage', id }],
    }),

    // 5) Update
    updateRetrievalCoverage: build.mutation<RetrievalCoverage, Pick<RetrievalCoverage, 'id'> & Partial<RetrievalCoverage>>({
      query: ({ id, ...patch }) => ({
        url: `RetrievalCoverage/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<RetrievalCoverage, 'id'>) => [
        { type: 'RetrievalCoverage', id },
        { type: 'RetrievalCoverage', id: 'LIST' },
        { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteRetrievalCoverage: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `RetrievalCoverage/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'RetrievalCoverage', id },
        { type: 'RetrievalCoverage', id: 'LIST' },
        { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteRetrievalCoverageCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `RetrievalCoverage/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'RetrievalCoverage', id },
        { type: 'RetrievalCoverage', id: 'LIST' },
        { type: 'RetrievalCoverage', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetRetrievalCoveragesPagedQuery`
export const {
  useGetRetrievalCoveragesPagedQuery,     // immediate fetch
  useLazyGetRetrievalCoveragesPagedQuery, // lazy fetch
  useGetRetrievalCoverageQuery,
  useGetRetrievalCoveragesQuery,
  useAddRetrievalCoverageMutation,
  useUpdateRetrievalCoverageMutation,
  useDeleteRetrievalCoverageMutation,
  useDeleteRetrievalCoverageCascadeMutation,
} = RetrievalCoverageService
