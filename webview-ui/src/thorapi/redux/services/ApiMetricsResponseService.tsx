// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ApiMetricsResponse } from '@thorapi/model/ApiMetricsResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ApiMetricsResponseResponse = ApiMetricsResponse[]
type ApiMetricsResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ApiMetricsResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ApiMetricsResponseListQueryArg = {
  example?: Partial<ApiMetricsResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toApiMetricsResponseList = (result: unknown): ApiMetricsResponseResponse => {
  if (Array.isArray(result)) {
    return result as ApiMetricsResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ApiMetricsResponseResponse) : []
}

export const ApiMetricsResponseService = createApi({
  reducerPath: 'ApiMetricsResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ApiMetricsResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getApiMetricsResponsesPaged: build.query<ApiMetricsResponseResponse, ApiMetricsResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ApiMetricsResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toApiMetricsResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ApiMetricsResponse' as const, id })),
          { type: 'ApiMetricsResponse', id: `PAGE_${page}` },
          { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getApiMetricsResponses: build.query<ApiMetricsResponseResponse, ApiMetricsResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ApiMetricsResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ApiMetricsResponse`;
      },
      providesTags: (result) => {
        const rows = toApiMetricsResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ApiMetricsResponse' as const, id })),
          { type: 'ApiMetricsResponse', id: 'LIST' },
          { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addApiMetricsResponse: build.mutation<ApiMetricsResponse, Partial<ApiMetricsResponse>>({
      query: (body) => ({
        url: `ApiMetricsResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ApiMetricsResponse', id: 'LIST' },
        { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getApiMetricsResponse: build.query<ApiMetricsResponse, string>({
      query: (id) => `ApiMetricsResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'ApiMetricsResponse', id }],
    }),

    // 5) Update
    updateApiMetricsResponse: build.mutation<ApiMetricsResponse, Pick<ApiMetricsResponse, 'id'> & Partial<ApiMetricsResponse>>({
      query: ({ id, ...patch }) => ({
        url: `ApiMetricsResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ApiMetricsResponse, 'id'>) => [
        { type: 'ApiMetricsResponse', id },
        { type: 'ApiMetricsResponse', id: 'LIST' },
        { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteApiMetricsResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ApiMetricsResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ApiMetricsResponse', id },
        { type: 'ApiMetricsResponse', id: 'LIST' },
        { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteApiMetricsResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ApiMetricsResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ApiMetricsResponse', id },
        { type: 'ApiMetricsResponse', id: 'LIST' },
        { type: 'ApiMetricsResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetApiMetricsResponsesPagedQuery`
export const {
  useGetApiMetricsResponsesPagedQuery,     // immediate fetch
  useLazyGetApiMetricsResponsesPagedQuery, // lazy fetch
  useGetApiMetricsResponseQuery,
  useGetApiMetricsResponsesQuery,
  useAddApiMetricsResponseMutation,
  useUpdateApiMetricsResponseMutation,
  useDeleteApiMetricsResponseMutation,
  useDeleteApiMetricsResponseCascadeMutation,
} = ApiMetricsResponseService
