// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { McpToolCallResponse } from '@thorapi/model/McpToolCallResponse'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type McpToolCallResponseResponse = McpToolCallResponse[]
type McpToolCallResponsePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<McpToolCallResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type McpToolCallResponseListQueryArg = {
  example?: Partial<McpToolCallResponse>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toMcpToolCallResponseList = (result: unknown): McpToolCallResponseResponse => {
  if (Array.isArray(result)) {
    return result as McpToolCallResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as McpToolCallResponseResponse) : []
}

export const McpToolCallResponseService = createApi({
  reducerPath: 'McpToolCallResponse', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['McpToolCallResponse'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getMcpToolCallResponsesPaged: build.query<McpToolCallResponseResponse, McpToolCallResponsePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `McpToolCallResponse?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toMcpToolCallResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpToolCallResponse' as const, id })),
          { type: 'McpToolCallResponse', id: `PAGE_${page}` },
          { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getMcpToolCallResponses: build.query<McpToolCallResponseResponse, McpToolCallResponseListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `McpToolCallResponse?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `McpToolCallResponse`;
      },
      providesTags: (result) => {
        const rows = toMcpToolCallResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'McpToolCallResponse' as const, id })),
          { type: 'McpToolCallResponse', id: 'LIST' },
          { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addMcpToolCallResponse: build.mutation<McpToolCallResponse, Partial<McpToolCallResponse>>({
      query: (body) => ({
        url: `McpToolCallResponse`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'McpToolCallResponse', id: 'LIST' },
        { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getMcpToolCallResponse: build.query<McpToolCallResponse, string>({
      query: (id) => `McpToolCallResponse/${id}`,
      providesTags: (result, error, id) => [{ type: 'McpToolCallResponse', id }],
    }),

    // 5) Update
    updateMcpToolCallResponse: build.mutation<McpToolCallResponse, Pick<McpToolCallResponse, 'id'> & Partial<McpToolCallResponse>>({
      query: ({ id, ...patch }) => ({
        url: `McpToolCallResponse/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<McpToolCallResponse, 'id'>) => [
        { type: 'McpToolCallResponse', id },
        { type: 'McpToolCallResponse', id: 'LIST' },
        { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteMcpToolCallResponse: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `McpToolCallResponse/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'McpToolCallResponse', id },
        { type: 'McpToolCallResponse', id: 'LIST' },
        { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteMcpToolCallResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `McpToolCallResponse/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'McpToolCallResponse', id },
        { type: 'McpToolCallResponse', id: 'LIST' },
        { type: 'McpToolCallResponse', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetMcpToolCallResponsesPagedQuery`
export const {
  useGetMcpToolCallResponsesPagedQuery,     // immediate fetch
  useLazyGetMcpToolCallResponsesPagedQuery, // lazy fetch
  useGetMcpToolCallResponseQuery,
  useGetMcpToolCallResponsesQuery,
  useAddMcpToolCallResponseMutation,
  useUpdateMcpToolCallResponseMutation,
  useDeleteMcpToolCallResponseMutation,
  useDeleteMcpToolCallResponseCascadeMutation,
} = McpToolCallResponseService
