// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { LlmCommandResult } from '@thorapi/model/LlmCommandResult'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type LlmCommandResultResponse = LlmCommandResult[]
type LlmCommandResultPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<LlmCommandResult>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type LlmCommandResultListQueryArg = {
  example?: Partial<LlmCommandResult>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toLlmCommandResultList = (result: unknown): LlmCommandResultResponse => {
  if (Array.isArray(result)) {
    return result as LlmCommandResultResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as LlmCommandResultResponse) : []
}

export const LlmCommandResultService = createApi({
  reducerPath: 'LlmCommandResult', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['LlmCommandResult'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getLlmCommandResultsPaged: build.query<LlmCommandResultResponse, LlmCommandResultPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `LlmCommandResult?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toLlmCommandResultList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'LlmCommandResult' as const, id })),
          { type: 'LlmCommandResult', id: `PAGE_${page}` },
          { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getLlmCommandResults: build.query<LlmCommandResultResponse, LlmCommandResultListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `LlmCommandResult?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `LlmCommandResult`;
      },
      providesTags: (result) => {
        const rows = toLlmCommandResultList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'LlmCommandResult' as const, id })),
          { type: 'LlmCommandResult', id: 'LIST' },
          { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addLlmCommandResult: build.mutation<LlmCommandResult, Partial<LlmCommandResult>>({
      query: (body) => ({
        url: `LlmCommandResult`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'LlmCommandResult', id: 'LIST' },
        { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getLlmCommandResult: build.query<LlmCommandResult, string>({
      query: (id) => `LlmCommandResult/${id}`,
      providesTags: (result, error, id) => [{ type: 'LlmCommandResult', id }],
    }),

    // 5) Update
    updateLlmCommandResult: build.mutation<LlmCommandResult, Pick<LlmCommandResult, 'id'> & Partial<LlmCommandResult>>({
      query: ({ id, ...patch }) => ({
        url: `LlmCommandResult/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<LlmCommandResult, 'id'>) => [
        { type: 'LlmCommandResult', id },
        { type: 'LlmCommandResult', id: 'LIST' },
        { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteLlmCommandResult: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `LlmCommandResult/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'LlmCommandResult', id },
        { type: 'LlmCommandResult', id: 'LIST' },
        { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteLlmCommandResultCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `LlmCommandResult/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'LlmCommandResult', id },
        { type: 'LlmCommandResult', id: 'LIST' },
        { type: 'LlmCommandResult', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetLlmCommandResultsPagedQuery`
export const {
  useGetLlmCommandResultsPagedQuery,     // immediate fetch
  useLazyGetLlmCommandResultsPagedQuery, // lazy fetch
  useGetLlmCommandResultQuery,
  useGetLlmCommandResultsQuery,
  useAddLlmCommandResultMutation,
  useUpdateLlmCommandResultMutation,
  useDeleteLlmCommandResultMutation,
  useDeleteLlmCommandResultCascadeMutation,
} = LlmCommandResultService
