// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { OpenClawLightsailLaunchEstimate } from '@thorapi/model/OpenClawLightsailLaunchEstimate'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type OpenClawLightsailLaunchEstimateResponse = OpenClawLightsailLaunchEstimate[]
type OpenClawLightsailLaunchEstimatePagedQueryArg = {
  page: number
  size?: number
  example?: Partial<OpenClawLightsailLaunchEstimate>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type OpenClawLightsailLaunchEstimateListQueryArg = {
  example?: Partial<OpenClawLightsailLaunchEstimate>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toOpenClawLightsailLaunchEstimateList = (result: unknown): OpenClawLightsailLaunchEstimateResponse => {
  if (Array.isArray(result)) {
    return result as OpenClawLightsailLaunchEstimateResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as OpenClawLightsailLaunchEstimateResponse) : []
}

export const OpenClawLightsailLaunchEstimateService = createApi({
  reducerPath: 'OpenClawLightsailLaunchEstimate', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['OpenClawLightsailLaunchEstimate'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getOpenClawLightsailLaunchEstimatesPaged: build.query<OpenClawLightsailLaunchEstimateResponse, OpenClawLightsailLaunchEstimatePagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `OpenClawLightsailLaunchEstimate?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toOpenClawLightsailLaunchEstimateList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchEstimate' as const, id })),
          { type: 'OpenClawLightsailLaunchEstimate', id: `PAGE_${page}` },
          { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getOpenClawLightsailLaunchEstimates: build.query<OpenClawLightsailLaunchEstimateResponse, OpenClawLightsailLaunchEstimateListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `OpenClawLightsailLaunchEstimate?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `OpenClawLightsailLaunchEstimate`;
      },
      providesTags: (result) => {
        const rows = toOpenClawLightsailLaunchEstimateList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'OpenClawLightsailLaunchEstimate' as const, id })),
          { type: 'OpenClawLightsailLaunchEstimate', id: 'LIST' },
          { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addOpenClawLightsailLaunchEstimate: build.mutation<OpenClawLightsailLaunchEstimate, Partial<OpenClawLightsailLaunchEstimate>>({
      query: (body) => ({
        url: `OpenClawLightsailLaunchEstimate`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'OpenClawLightsailLaunchEstimate', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getOpenClawLightsailLaunchEstimate: build.query<OpenClawLightsailLaunchEstimate, string>({
      query: (id) => `OpenClawLightsailLaunchEstimate/${id}`,
      providesTags: (result, error, id) => [{ type: 'OpenClawLightsailLaunchEstimate', id }],
    }),

    // 5) Update
    updateOpenClawLightsailLaunchEstimate: build.mutation<OpenClawLightsailLaunchEstimate, Pick<OpenClawLightsailLaunchEstimate, 'id'> & Partial<OpenClawLightsailLaunchEstimate>>({
      query: ({ id, ...patch }) => ({
        url: `OpenClawLightsailLaunchEstimate/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<OpenClawLightsailLaunchEstimate, 'id'>) => [
        { type: 'OpenClawLightsailLaunchEstimate', id },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteOpenClawLightsailLaunchEstimate: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `OpenClawLightsailLaunchEstimate/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'OpenClawLightsailLaunchEstimate', id },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteOpenClawLightsailLaunchEstimateCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `OpenClawLightsailLaunchEstimate/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'OpenClawLightsailLaunchEstimate', id },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'LIST' },
        { type: 'OpenClawLightsailLaunchEstimate', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetOpenClawLightsailLaunchEstimatesPagedQuery`
export const {
  useGetOpenClawLightsailLaunchEstimatesPagedQuery,     // immediate fetch
  useLazyGetOpenClawLightsailLaunchEstimatesPagedQuery, // lazy fetch
  useGetOpenClawLightsailLaunchEstimateQuery,
  useGetOpenClawLightsailLaunchEstimatesQuery,
  useAddOpenClawLightsailLaunchEstimateMutation,
  useUpdateOpenClawLightsailLaunchEstimateMutation,
  useDeleteOpenClawLightsailLaunchEstimateMutation,
  useDeleteOpenClawLightsailLaunchEstimateCascadeMutation,
} = OpenClawLightsailLaunchEstimateService
