// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { CreditDebitReceipt } from '@thorapi/model/CreditDebitReceipt'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type CreditDebitReceiptResponse = CreditDebitReceipt[]
type CreditDebitReceiptPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<CreditDebitReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type CreditDebitReceiptListQueryArg = {
  example?: Partial<CreditDebitReceipt>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toCreditDebitReceiptList = (result: unknown): CreditDebitReceiptResponse => {
  if (Array.isArray(result)) {
    return result as CreditDebitReceiptResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as CreditDebitReceiptResponse) : []
}

export const CreditDebitReceiptService = createApi({
  reducerPath: 'CreditDebitReceipt', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['CreditDebitReceipt'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getCreditDebitReceiptsPaged: build.query<CreditDebitReceiptResponse, CreditDebitReceiptPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `CreditDebitReceipt?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toCreditDebitReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditDebitReceipt' as const, id })),
          { type: 'CreditDebitReceipt', id: `PAGE_${page}` },
          { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getCreditDebitReceipts: build.query<CreditDebitReceiptResponse, CreditDebitReceiptListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `CreditDebitReceipt?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `CreditDebitReceipt`;
      },
      providesTags: (result) => {
        const rows = toCreditDebitReceiptList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'CreditDebitReceipt' as const, id })),
          { type: 'CreditDebitReceipt', id: 'LIST' },
          { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addCreditDebitReceipt: build.mutation<CreditDebitReceipt, Partial<CreditDebitReceipt>>({
      query: (body) => ({
        url: `CreditDebitReceipt`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'CreditDebitReceipt', id: 'LIST' },
        { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getCreditDebitReceipt: build.query<CreditDebitReceipt, string>({
      query: (id) => `CreditDebitReceipt/${id}`,
      providesTags: (result, error, id) => [{ type: 'CreditDebitReceipt', id }],
    }),

    // 5) Update
    updateCreditDebitReceipt: build.mutation<CreditDebitReceipt, Pick<CreditDebitReceipt, 'id'> & Partial<CreditDebitReceipt>>({
      query: ({ id, ...patch }) => ({
        url: `CreditDebitReceipt/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<CreditDebitReceipt, 'id'>) => [
        { type: 'CreditDebitReceipt', id },
        { type: 'CreditDebitReceipt', id: 'LIST' },
        { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteCreditDebitReceipt: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `CreditDebitReceipt/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'CreditDebitReceipt', id },
        { type: 'CreditDebitReceipt', id: 'LIST' },
        { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteCreditDebitReceiptCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `CreditDebitReceipt/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'CreditDebitReceipt', id },
        { type: 'CreditDebitReceipt', id: 'LIST' },
        { type: 'CreditDebitReceipt', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetCreditDebitReceiptsPagedQuery`
export const {
  useGetCreditDebitReceiptsPagedQuery,     // immediate fetch
  useLazyGetCreditDebitReceiptsPagedQuery, // lazy fetch
  useGetCreditDebitReceiptQuery,
  useGetCreditDebitReceiptsQuery,
  useAddCreditDebitReceiptMutation,
  useUpdateCreditDebitReceiptMutation,
  useDeleteCreditDebitReceiptMutation,
  useDeleteCreditDebitReceiptCascadeMutation,
} = CreditDebitReceiptService
