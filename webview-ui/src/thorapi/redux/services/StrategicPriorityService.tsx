// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { StrategicPriority } from '@thorapi/model/StrategicPriority'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type StrategicPriorityResponse = StrategicPriority[]
type StrategicPriorityPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<StrategicPriority>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type StrategicPriorityListQueryArg = {
  example?: Partial<StrategicPriority>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toStrategicPriorityList = (result: unknown): StrategicPriorityResponse => {
  if (Array.isArray(result)) {
    return result as StrategicPriorityResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as StrategicPriorityResponse) : []
}

export const StrategicPriorityService = createApi({
  reducerPath: 'StrategicPriority', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['StrategicPriority'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getStrategicPrioritysPaged: build.query<StrategicPriorityResponse, StrategicPriorityPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `StrategicPriority?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toStrategicPriorityList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StrategicPriority' as const, id })),
          { type: 'StrategicPriority', id: `PAGE_${page}` },
          { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getStrategicPrioritys: build.query<StrategicPriorityResponse, StrategicPriorityListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `StrategicPriority?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `StrategicPriority`;
      },
      providesTags: (result) => {
        const rows = toStrategicPriorityList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'StrategicPriority' as const, id })),
          { type: 'StrategicPriority', id: 'LIST' },
          { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addStrategicPriority: build.mutation<StrategicPriority, Partial<StrategicPriority>>({
      query: (body) => ({
        url: `StrategicPriority`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'StrategicPriority', id: 'LIST' },
        { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getStrategicPriority: build.query<StrategicPriority, string>({
      query: (id) => `StrategicPriority/${id}`,
      providesTags: (result, error, id) => [{ type: 'StrategicPriority', id }],
    }),

    // 5) Update
    updateStrategicPriority: build.mutation<StrategicPriority, Pick<StrategicPriority, 'id'> & Partial<StrategicPriority>>({
      query: ({ id, ...patch }) => ({
        url: `StrategicPriority/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<StrategicPriority, 'id'>) => [
        { type: 'StrategicPriority', id },
        { type: 'StrategicPriority', id: 'LIST' },
        { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteStrategicPriority: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `StrategicPriority/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'StrategicPriority', id },
        { type: 'StrategicPriority', id: 'LIST' },
        { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteStrategicPriorityCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `StrategicPriority/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'StrategicPriority', id },
        { type: 'StrategicPriority', id: 'LIST' },
        { type: 'StrategicPriority', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetStrategicPrioritysPagedQuery`
export const {
  useGetStrategicPrioritysPagedQuery,     // immediate fetch
  useLazyGetStrategicPrioritysPagedQuery, // lazy fetch
  useGetStrategicPriorityQuery,
  useGetStrategicPrioritysQuery,
  useAddStrategicPriorityMutation,
  useUpdateStrategicPriorityMutation,
  useDeleteStrategicPriorityMutation,
  useDeleteStrategicPriorityCascadeMutation,
} = StrategicPriorityService
