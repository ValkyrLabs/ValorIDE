// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ScanBuildArtifacts200Response } from '@thorapi/model/ScanBuildArtifacts200Response'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ScanBuildArtifacts200ResponseResponse = ScanBuildArtifacts200Response[]

const toScanBuildArtifacts200ResponseList = (result: unknown): ScanBuildArtifacts200ResponseResponse => {
  if (Array.isArray(result)) {
    return result as ScanBuildArtifacts200ResponseResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ScanBuildArtifacts200ResponseResponse) : []
}

export const ScanBuildArtifacts200ResponseService = createApi({
  reducerPath: 'ScanBuildArtifacts200Response', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ScanBuildArtifacts200Response'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getScanBuildArtifacts200ResponsesPaged: build.query<ScanBuildArtifacts200ResponseResponse, { page: number; size?: number; example?: Partial<ScanBuildArtifacts200Response> }>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ScanBuildArtifacts200Response?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toScanBuildArtifacts200ResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ScanBuildArtifacts200Response' as const, id })),
          { type: 'ScanBuildArtifacts200Response', id: `PAGE_${page}` },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getScanBuildArtifacts200Responses: build.query<ScanBuildArtifacts200ResponseResponse, { example?: Partial<ScanBuildArtifacts200Response> } | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ScanBuildArtifacts200Response?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ScanBuildArtifacts200Response`;
      },
      providesTags: (result) => {
        const rows = toScanBuildArtifacts200ResponseList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ScanBuildArtifacts200Response' as const, id })),
          { type: 'ScanBuildArtifacts200Response', id: 'LIST' },
        ]
      },
    }),

    // 3) Create
    addScanBuildArtifacts200Response: build.mutation<ScanBuildArtifacts200Response, Partial<ScanBuildArtifacts200Response>>({
      query: (body) => ({
        url: `ScanBuildArtifacts200Response`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [{ type: 'ScanBuildArtifacts200Response', id: 'LIST' }],
    }),

    // 4) Get single by ID
    getScanBuildArtifacts200Response: build.query<ScanBuildArtifacts200Response, string>({
      query: (id) => `ScanBuildArtifacts200Response/${id}`,
      providesTags: (result, error, id) => [{ type: 'ScanBuildArtifacts200Response', id }],
    }),

    // 5) Update
    updateScanBuildArtifacts200Response: build.mutation<void, Pick<ScanBuildArtifacts200Response, 'id'> & Partial<ScanBuildArtifacts200Response>>({
      query: ({ id, ...patch }) => ({
        url: `ScanBuildArtifacts200Response/${id}`,
        method: 'PUT',
        body: patch,
      }),
      async onQueryStarted({ id, ...patch }, { dispatch, queryFulfilled }) {
        if (id) {
          const patchResult = dispatch(
            ScanBuildArtifacts200ResponseService.util.updateQueryData('getScanBuildArtifacts200Response', id, (draft) => {
              Object.assign(draft, patch)
            })
          )
          try {
            await queryFulfilled
          } catch {
            patchResult.undo()
          }
        }
      },
      invalidatesTags: (result, error, { id }: Pick<ScanBuildArtifacts200Response, 'id'>) => [
        { type: 'ScanBuildArtifacts200Response', id },
        { type: 'ScanBuildArtifacts200Response', id: 'LIST' },
      ],
    }),

    // 6) Delete
    deleteScanBuildArtifacts200Response: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ScanBuildArtifacts200Response/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [{ type: 'ScanBuildArtifacts200Response', id }],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteScanBuildArtifacts200ResponseCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ScanBuildArtifacts200Response/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [{ type: 'ScanBuildArtifacts200Response', id }, { type: 'ScanBuildArtifacts200Response', id: 'LIST' }],
    }),
  }),
})

// Notice we now also export `useLazyGetScanBuildArtifacts200ResponsesPagedQuery`
export const {
  useGetScanBuildArtifacts200ResponsesPagedQuery,     // immediate fetch
  useLazyGetScanBuildArtifacts200ResponsesPagedQuery, // lazy fetch
  useGetScanBuildArtifacts200ResponseQuery,
  useGetScanBuildArtifacts200ResponsesQuery,
  useAddScanBuildArtifacts200ResponseMutation,
  useUpdateScanBuildArtifacts200ResponseMutation,
  useDeleteScanBuildArtifacts200ResponseMutation,
  useDeleteScanBuildArtifacts200ResponseCascadeMutation,
} = ScanBuildArtifacts200ResponseService
