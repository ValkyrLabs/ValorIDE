// tslint:disable
/**
############################## DO NOT EDIT: GENERATED FILE ##############################

Generated by Valkyr Labs ThorAPI: https://valkyrlabs.com/thorapi
Powered by Swagger Codegen: http://swagger.io

Generated Details:
**GENERATOR VERSION:** 7.5.0
**GENERATOR CLASS:** org.openapitools.codegen.languages.TypeScriptReduxQueryClientCodegen

Template file: typescript-redux-query/modelService.mustache

############################## DO NOT EDIT: GENERATED FILE ##############################
*/
import { createApi } from '@reduxjs/toolkit/query/react'
import { ContextPageItem } from '@thorapi/model/ContextPageItem'
import customBaseQuery from '../customBaseQuery'; // Import the custom base query

type ContextPageItemResponse = ContextPageItem[]
type ContextPageItemPagedQueryArg = {
  page: number
  size?: number
  example?: Partial<ContextPageItem>
  /**
   * Cache discriminator only. Do not send this to ThorAPI; callers pass the
   * authenticated principal id/username so RBAC-filtered pages cannot be
   * reused across login boundaries by RTK Query.
   */
  authSessionKey?: string
}

type ContextPageItemListQueryArg = {
  example?: Partial<ContextPageItem>
  /**
   * Cache discriminator only. Do not send this to ThorAPI.
   */
  authSessionKey?: string
}

const toContextPageItemList = (result: unknown): ContextPageItemResponse => {
  if (Array.isArray(result)) {
    return result as ContextPageItemResponse
  }

  const candidate = (result as any)?.content ?? (result as any)?.items ?? (result as any)?.results ?? (result as any)?.data
  return Array.isArray(candidate) ? (candidate as ContextPageItemResponse) : []
}

export const ContextPageItemService = createApi({
  reducerPath: 'ContextPageItem', // This should remain unique
  baseQuery: customBaseQuery,
  tagTypes: ['ContextPageItem'],
  endpoints: (build) => ({
    // 1) Paged Query Endpoint
    // Standardized pagination: page (0-based), size (page size)
    getContextPageItemsPaged: build.query<ContextPageItemResponse, ContextPageItemPagedQueryArg>({
      query: ({ page, size = 20, example }) => {
        const q: string[] = [`page=${page}`, `size=${size}`];
        if (example) q.push(`example=${encodeURIComponent(JSON.stringify(example))}`);
        return `ContextPageItem?${q.join('&')}`;
      },
      providesTags: (result, error, { page }) => {
        const rows = toContextPageItemList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageItem' as const, id })),
          { type: 'ContextPageItem', id: `PAGE_${page}` },
          { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 2) Simple "get all" Query (optional)
    getContextPageItems: build.query<ContextPageItemResponse, ContextPageItemListQueryArg | void>({
      query: (arg) => {
        if (arg && (arg as any).example) {
          const ex = (arg as any).example;
          return `ContextPageItem?example=${encodeURIComponent(JSON.stringify(ex))}`;
        }
        return `ContextPageItem`;
      },
      providesTags: (result) => {
        const rows = toContextPageItemList(result)
        return [
          ...rows
            .filter((row) => row?.id != null)
            .map(({ id }) => ({ type: 'ContextPageItem' as const, id })),
          { type: 'ContextPageItem', id: 'LIST' },
          { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
        ]
      },
    }),

    // 3) Create
    addContextPageItem: build.mutation<ContextPageItem, Partial<ContextPageItem>>({
      query: (body) => ({
        url: `ContextPageItem`,
        method: 'POST',
        body,
      }),
      invalidatesTags: [
        { type: 'ContextPageItem', id: 'LIST' },
        { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 4) Get single by ID
    getContextPageItem: build.query<ContextPageItem, string>({
      query: (id) => `ContextPageItem/${id}`,
      providesTags: (result, error, id) => [{ type: 'ContextPageItem', id }],
    }),

    // 5) Update
    updateContextPageItem: build.mutation<ContextPageItem, Pick<ContextPageItem, 'id'> & Partial<ContextPageItem>>({
      query: ({ id, ...patch }) => ({
        url: `ContextPageItem/${id}`,
        method: 'PUT',
        body: patch,
      }),
      invalidatesTags: (result, error, { id }: Pick<ContextPageItem, 'id'>) => [
        { type: 'ContextPageItem', id },
        { type: 'ContextPageItem', id: 'LIST' },
        { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 6) Delete
    deleteContextPageItem: build.mutation<{ success: boolean; id: string }, number>({
      query(id) {
        return {
          url: `ContextPageItem/${id}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, id) => [
        { type: 'ContextPageItem', id },
        { type: 'ContextPageItem', id: 'LIST' },
        { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
      ],
    }),

    // 7) Cascade / soft-delete (marks trashed, cascades children)
    deleteContextPageItemCascade: build.mutation<{ success: boolean; id: string }, { id: string; cascade?: boolean; trash?: boolean }>({
      query({ id, cascade = true, trash = true }) {
        const params = [`cascade=${cascade}`, `trash=${trash}`].join('&');
        return {
          url: `ContextPageItem/${id}?${params}`,
          method: 'DELETE',
        }
      },
      invalidatesTags: (result, error, { id }) => [
        { type: 'ContextPageItem', id },
        { type: 'ContextPageItem', id: 'LIST' },
        { type: 'ContextPageItem', id: 'PARTIAL-LIST' },
      ],
    }),
  }),
})

// Notice we now also export `useLazyGetContextPageItemsPagedQuery`
export const {
  useGetContextPageItemsPagedQuery,     // immediate fetch
  useLazyGetContextPageItemsPagedQuery, // lazy fetch
  useGetContextPageItemQuery,
  useGetContextPageItemsQuery,
  useAddContextPageItemMutation,
  useUpdateContextPageItemMutation,
  useDeleteContextPageItemMutation,
  useDeleteContextPageItemCascadeMutation,
} = ContextPageItemService
